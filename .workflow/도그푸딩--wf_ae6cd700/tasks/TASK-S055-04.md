---
schema: workflow-labs/task@1
id: TASK-S055-04
title: 작업 상세에서 정의 수정을 요청하고 처리 결과를 확인한다
status: todo
source_spec_id: SPEC-055
source_decision_id: DECISION-22C3CC2F
depends_on: [TASK-149, TASK-S055-02]
scope_files: [src/features/projects/application/useProjectWorkspace.test.ts, src/features/projects/application/useProjectWorkspace.ts, src/features/projects/components/ActivityView.test.tsx, src/features/projects/components/ActivityView.tsx, src/features/projects/components/BlockedTaskPanel.css, src/features/projects/components/BlockedTaskPanel.test.tsx, src/features/projects/components/BlockedTaskPanel.tsx, src/features/projects/components/DevelopmentBoard.test.tsx, src/features/projects/components/DevelopmentBoard.tsx, src/features/projects/components/WorkspaceShell.test.tsx, src/features/projects/components/WorkspaceShell.tsx, src/features/projects/domain/types.ts, src/features/projects/infrastructure/tauriProjectGateway.ts]
updated_at: 2026-08-08T08:50:00Z
history:
  - { at: 2026-08-08T08:50:00Z, kind: created }
---

# 작업 상세에서 정의 수정을 요청하고 처리 결과를 확인한다

## 결정권자 요약

### 제안

작업 상세에서 정의 수정을 요청하는 조작을 제공하고, 요청과 처리 결과를 같은 자리에서 읽게 한다.

### 현재

막힌 작업을 열면 사유와 재개 조작만 보인다. 막힌 이유가 정의 오류일 때 사용자가 할 수 있는 일은 재개뿐
이고, 재개해도 같은 정의가 남아 같은 자리에서 다시 막힌다. 아직 시작하지 않은 작업의 정의가 잘못된 것을
발견해도 남길 곳이 없다.

### 변경 후

막힌 작업과 아직 선점되지 않은 개발 준비 작업에 정의 수정 요청 조작이 생긴다. 요청 전에 현재 범위와
선행과 갱신 시각과 기존 요청을 함께 보여 주고, 이유를 적어 두 번 확인해야 요청이 남는다. 요청이 처리
되면 처리 결과와 사전 검사 근거가 같은 화면에서 읽히고, 상태는 사용자가 재개할 때까지 그대로다.

### 사용자 결과

잘못 분해된 작업을 발견한 자리에서 바로 이유와 함께 되돌려 보낼 수 있고, 무엇이 고쳐졌는지 확인한 뒤에
재개 여부를 판단할 수 있다.

### 영향 범위

- 변경: 작업 상세의 정의 수정 요청 조작과 요청 목록 표시, 활동 화면의 요청과 수정 이벤트 이름
- 유지: 사용자 품질 확인 도장과 일괄 확인, 막힌 사유 표시와 재개 조작, 상태 기계와 결정 기록의 뜻

### 비용과 위험

요청 조작이 재개 조작 옆에 함께 놓이므로 두 조작을 혼동할 여지가 있다. 두 조작의 이름과 결과를 화면에서
분명히 갈라야 한다.

### 결정 요청

품질 확인에서는 두 번 확인해야 요청이 한 번만 나가는지, 요청과 수정이 재개와 다른 이름으로 읽히는지,
그리고 수정만으로 상태가 바뀌지 않는지를 화면에서 직접 확인한다.

## 목적

SPEC-055의 R2 화면 부분과 R9와 R12를 구현한다. 사용자가 정의 수정을 요청하고 처리 결과를 확인하는
흐름을 작업 상세와 활동 화면에 연결한다. 요청을 저장하는 명령은 앞 작업이 제공하며 이 작업은 그것을
호출한다.

## 범위 사전 검사

- 막힌 작업의 사유 패널과 재개 조작은 TASK-145와 TASK-149가 만든다. 이 작업은 그 패널을 확장하므로
  같은 컴포넌트와 그 검사 파일이 범위에 있고 TASK-149를 선행으로 둔다.
- 요청 저장 명령의 요청과 응답 형태는 앞 작업이 확정한다. 화면은 그 값을 그대로 전달하고 받은 결과를
  표시하기만 한다.
- 화면이 값을 받는 경로는 게이트웨이와 작업 공간 훅과 껍데기 컴포넌트를 차례로 지난다. 새 조작 하나가
  그 세 층을 모두 지나므로 함께 범위에 있다.
- 최상위 조립 파일은 범위에 넣지 않았다. 그 파일은 게이트웨이와 작업 공간 훅을 통째로 넘기고 개별
  조작을 열거하지 않으므로 새 조작이 늘어도 바뀌지 않는다.
- 전역 스타일 파일도 범위에 넣지 않았다. 새 조작의 표시는 사유 패널 전용 스타일 파일 안에서 끝난다.
  전역 스타일 파일은 다른 미완료 작업이 함께 쓰고 있어 이유 없이 겹치지 않게 한다.
- 활동 화면의 이벤트 이름은 화면 쪽에서 정한다. 기록되는 값 자체는 앞 작업들이 정한 그대로 두고 표시
  이름만 구분한다.
- 이 승인의 다른 세 작업과 파일이 겹치지 않는다. 뒤에 오는 두 화면 작업과는 일부 파일이 겹치지만 그
  작업들은 다른 승인의 선행 사슬에 묶여 훨씬 뒤에 온다.

## 변경 범위

### 요청 조작

- 현재 상태가 막힘이거나, 개발 준비이면서 활성 선점이 없을 때만 요청 영역을 보여 준다.
- 요청 전에 작업의 현재 상태와 선언 범위와 선행 관계와 갱신 시각과 막힌 사유와 기존 요청 목록을 함께
  보여 준다.
- 이유 입력은 필수다. 빈 값은 호출 전에 막고 길이 제한과 현재 글자 수를 보여 준다.
- 첫 확인은 대상과 갱신 시각과 입력한 이유를 확인 상태로 고정하고, 두 번째 확인만 명령을 한 번 호출한다.
  진행 중에는 중복 클릭과 입력 변경을 막는다.
- 오래된 문서와 허용되지 않는 상태와 활성 선점과 마이그레이션 잠금과 중복 요청은 성공으로 표시하지
  않는다. 입력을 유지한 채 사유와 최신 문서를 다시 읽는 동작을 제공한다.

### 처리 결과 표시

- 작업에 연결된 요청은 처리 완료로, 연결되지 않은 요청은 미처리로 구분해 보여 준다. 화면이 처리 여부를
  다른 근거로 추측하지 않는다.
- 처리된 요청이 있으면 그 요청의 이유와 처리 시점을 함께 보여 준다. 수정 내용 자체는 작업 본문과 보고서
  에 있으므로 화면이 다시 요약하지 않는다.
- 작업 본문에 범위 사전 검사 절이 있으면 그 절을 읽을 수 있게 한다. 절이 없으면 없는 대로 두고 문장을
  지어내지 않는다.
- 수정이 처리돼도 상태는 그대로다. 막힌 작업은 막힌 채로 남고 재개 조작은 지금과 같은 자리에 있다.
  개발 준비 작업은 개발 준비로 남는다.

### 활동 화면

- 정의 수정 요청과 아키텍트 수정 처리를 각각 고유한 이름으로 보여 준다. 기획 수정 요청과 품질 확인 반려
  와 사용자 재개의 기존 이름은 바꾸지 않는다.
- 같은 작업이 같은 분류로 다시 막히면 이전 요청과 수정 기록을 함께 확인할 수 있게 한다.
- 이 정보는 이미 읽은 작업 사실과 앱 소유 기록에서만 만든다. 새 요약 호출이나 원격 호출을 만들지 않는다.

## 손대지 않는 것

- 사용자 품질 확인 도장과 레인 일괄 확인 경로
- 막힌 사유 절의 표시 규칙과 재개 조작의 동작
- 상태 기계와 이력 값의 뜻
- 요청 기록의 저장 판정과 형식
- 아키텍트 자격 판정
- 최상위 조립 파일과 전역 스타일 파일

## 완료 조건

1. 막힘 상태와, 선점이 없는 개발 준비 상태의 작업에서만 요청 영역이 보인다. 진행 중과 품질 확인 대기와
   완료 상태에서는 보이지 않는다.
2. 요청 영역이 현재 상태와 선언 범위와 선행 관계와 갱신 시각과 기존 요청 목록을 함께 보여 준다.
3. 빈 이유와 길이 초과는 호출 전에 거절되고 첫 확인만으로는 명령 호출이 일어나지 않는다.
4. 두 번째 확인이 같은 대상과 갱신 시각과 이유로 명령을 한 번만 호출한다. 연타와 응답 지연에서도 호출은
   한 번이다.
5. 오래된 문서와 활성 선점과 마이그레이션 잠금과 허용되지 않는 상태와 중복 요청 응답에서 성공 표시가
   없고 입력이 유지되며 최신 문서를 다시 읽을 수 있다.
6. 작업이 연결한 요청은 처리 완료로, 연결되지 않은 요청은 미처리로 표시된다. 화면이 다른 근거로 처리
   여부를 만들지 않는다.
7. 작업 본문에 범위 사전 검사 절이 있으면 읽을 수 있고, 없으면 대체 문장을 만들지 않는다.
8. 수정이 처리된 뒤에도 막힌 작업은 막힘으로, 개발 준비 작업은 개발 준비로 남는다. 화면이 상태를 자동
   으로 바꾸지 않는다.
9. 정의 수정 요청과 아키텍트 수정이 활동 화면에서 서로 다른 이름으로 한 번씩 보이고, 기획 수정 요청과
   품질 확인 반려와 사용자 재개의 기존 이름이 바뀌지 않는다.
10. 같은 작업이 같은 분류로 다시 막힌 픽스처에서 이전 요청과 수정 기록을 함께 확인할 수 있다.
11. 좁은 화면과 넓은 화면에서 요청 영역의 읽기 순서와 초점 순서가 같고 긴 이유와 식별자가 잘리지 않는다.
12. 키보드만으로 이유 입력과 첫 확인과 최종 확인과 오류 뒤 재조회에 순서대로 도달한다.
13. 기존 품질 확인 대기 표시와 일괄 확인과 재개 조작과 막힌 사유 표시 검사가 변경 전과 같이 통과한다.
14. 요청 기록이 없는 기존 프로젝트에서 작업 상세와 활동 화면이 변경 전과 같이 보인다.

## 검증 절차

1. `npx vitest run src/features/projects/components/BlockedTaskPanel.test.tsx src/features/projects/components/DevelopmentBoard.test.tsx`를 실행한다.
2. 다섯 가지 상태와 선점 있음 픽스처로 요청 영역의 노출 여부를 표로 검사한다.
3. 빈 값과 길이 초과와 첫 확인에서 호출이 없는지, 연타와 응답 지연에서 한 번인지 확인한다.
4. 오래된 갱신 시각과 활성 선점과 마이그레이션 잠금과 중복 요청 응답을 주입해 입력 보존과 재조회 동작을
   확인한다.
5. 연결 필드가 있는 작업과 없는 작업으로 처리 완료와 미처리 표시를 대조한다.
6. 범위 사전 검사 절이 있는 작업과 없는 작업으로 표시와 폴백을 확인한다.
7. 수정 처리 응답 뒤 상태가 그대로인지, 재개 조작이 여전히 사용자 조작으로만 동작하는지 확인한다.
8. `npx vitest run src/features/projects/components/ActivityView.test.tsx`로 새 두 이름과 기존 이름을
   대조한다.
9. 좁은 화면과 넓은 화면과 키보드 전용 이동을 확인한다.
10. 저장소 루트에서 `npm run check`를 실행한다.

## 범위와 선행

TASK-149가 같은 사유 패널에 재개 조작을 먼저 만들고 이 작업이 그 옆에 요청 조작을 더하므로 그 작업이
선행한다. 요청을 저장하는 명령은 이 승인의 두 번째 작업이 제공하므로 그 작업도 선행한다.

이 승인의 규칙 문언 작업과 자격 판정 작업과는 파일이 겹치지 않는다. 다만 요청 저장 작업을 선행으로
두었고 그 작업이 다시 규칙 문언 작업을 선행으로 두므로, 순서상으로는 규칙 문언 작업이 먼저 온다. 화면에
표시할 범위 사전 검사 절의 제목도 그 작업이 확정한다.

화면이 값을 받는 세 층은 다른 두 승인의 미완료 작업들과도 겹친다. 에이전트 화면 두 작업과 결정 보드
확인 작업이 그것이며, 그 작업들은 각자의 선행 사슬에 묶여 훨씬 뒤에 온다. 이 겹침에는 어느 쪽이 먼저여야
할 이유가 없어 선행 관계를 선언하지 않았다. 선언 범위가 겹치는 두 작업을 같은 시각에 시작하지 않게 하는
것이 이 경우의 안전장치다.

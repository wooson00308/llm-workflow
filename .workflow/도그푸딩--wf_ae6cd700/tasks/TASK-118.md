---
schema: workflow-labs/task@1
id: TASK-118
title: 계약이 결정권자 요약과 확인 동선을 요구하게 하고 규칙 자산 버전을 올린다
status: verified
source_spec_id: SPEC-038
source_decision_id: DECISION-BCACD11B
depends_on:
- TASK-111
scope_files:
- src-tauri/src/infrastructure/project_instructions.rs
- docs/file-contract.md
updated_at: 2026-08-14T09:08:07.880257+00:00
history:
- at: 2026-08-05T06:10:00Z
  kind: created
- at: 2026-08-05T08:45:00Z
  kind: in_progress
- at: 2026-08-05T08:55:00Z
  kind: qa_waiting
- at: 2026-08-05T09:12:37.042601+00:00
  kind: completed
- at: 2026-08-14T09:08:07.880257+00:00
  kind: migrated_verified
work_group_id: GROUP-DECISION-BCACD11B
work_group_revision: 1
---

# 계약이 결정권자 요약과 확인 동선을 요구하게 하고 규칙 자산 버전을 올린다

## 결정권자 요약

이 작업이 끝나면, 앞으로 에이전트가 쓰는 기획서·개발 작업·개발 보고서는 맨 앞에 결정권자 요약
절을 갖는다. 승인 도장과 QA 도장을 찍기 전에 읽을 문단이 문서마다 생긴다는 뜻이다. 개발 작업에는
확인 동선 절도 붙어서, 어느 화면에서 무엇을 조작하면 무엇이 보여야 정상인지를 개발자가 평문으로
적는다.

값은 화면을 고치지 않아도 바로 난다. 목록 카드와 검색 결과의 미리보기가 본문 앞 세 줄을 그대로
싣고 있고, QA 확인 화면에는 작업 문서 본문이 이미 펼쳐져 있기 때문이다.

이 작업이 바꾸는 것은 규칙 문서뿐이다. 이미 쓰인 문서 355건은 그대로 읽히고 그대로 보이며,
요약이 없다는 이유로 세션이 멈추거나 작업이 닫히는 일은 없다.

---

SPEC-038 전부다. R1~R9와 완료 조건 1~15를 이 작업 하나가 닫는다.

기획서의 산출물은 계약 문언 하나뿐이고, 그 문언이 사는 자리는 두 곳이다 — 앱 내장 계약 본문
(`project_instructions.rs`의 네 상수)과 그것을 서술하는 `docs/file-contract.md`. R7이 "셋 중 하나만
고치면 설치된 규칙과 문서가 갈라진다"고 적었으므로 한 세션이 둘을 함께 쓴다. 제외 범위가 표시·검사
장치·소급 기입을 모두 빼 놓았으므로 이 작업 밖에 남는 구현은 없다.

## 승인된 확인 필요가 이 작업의 설계다

DECISION-BCACD11B는 코멘트 없이 승인됐고, 기획서 "확인 필요" 머리글이 "승인 시 아래 제안대로
진행한다"이므로 네 항목 모두 제안대로다.

1. **확인 동선은 개발 작업 문서에 쓴다.** 보고서가 아니다. 사용자가 확인 도장을 찍는 화면에 작업
   문서 본문이 이미 펼쳐져 있고, 보고서는 앱이 열지 못한다. 보고서의 `## 사용자 QA 제안` 관례
   31건은 계약 대상이 아닌 자유 기술로 남고, 정본은 작업 문서다.
2. **요약 규칙을 검사하는 자동 장치를 만들지 않는다.** 이 변경분에 문서 본문을 읽는 코드가 한 줄도
   들어가지 않는다. 계약 문언으로만 존재한다.
3. **아이디어 문서는 대상이 아니다.** 사용자가 직접 쓰는 자리라 계약을 걸지 않는다.
4. **요약 절의 이름은 `## 결정권자 요약`이다.** 세 문서 종류에서 같은 문자열이다.

## 아키텍트가 고정하는 값

기획서가 "무엇을"까지 적고 남긴 자리다. 네 계약 본문과 파일 계약 문서가 같은 답을 내야 하므로
여기서 못박는다.

### 1. 두 절의 이름과 자리

| 절 | 문자열 | 어느 문서 | 누가 쓰나 |
| --- | --- | --- | --- |
| 요약 | `## 결정권자 요약` | 기획서·개발 작업·개발 보고서 | 그 문서를 만든 세션 |
| 확인 동선 | `## 확인 동선` | 개발 작업 | 개발자가 `qa_waiting` 전이 편집에서 |

- 요약 절은 본문 맨 앞, H1 제목 바로 다음이다. 다른 어떤 절보다 앞이다(R1).
- 확인 동선 절의 이름은 기획서가 정하지 않았으므로 아키텍트가 고정한다. `## 확인 동선`을 고른
  근거는 셋이다 — 사용자가 그 자리에서 할 일을 그대로 부르는 이름이고, `.workflow/` 아래 현행
  문서 355건에 같은 제목이 0건이라 충돌이 없으며, 요약 절과 마찬가지로 후속 기획서가 찾을 토큰이
  하나로 고정된다.
- 두 제목은 계약 본문이 영어여도 한국어 문자열 그대로 인용한다. 표시를 다루는 후속 기획서가 찾을
  토큰이 하나여야 한다는 R1의 요구가 번역을 허용하지 않는다.
- 확인 동선 절은 요약 절이 아니다. 요약은 문서를 만든 세션이 쓰고 확인 동선은 개발자가 나중에
  덧붙이므로, 한 절에 섞으면 저자가 갈리는 자리가 문서에서 보이지 않게 된다.

### 2. 공통 규칙의 새 절은 §8이다

공통 규칙에 §8 하나를 **맨 뒤에 덧붙인다.** 중간에 끼워 넣지 않는다.

근거는 상호참조다. `project_instructions.rs` 안에 `§4` 11건·`§5` 2건·`§6` 3건,
`docs/file-contract.md`에 `§4` 2건이 있다. 새 절을 §6이나 §7 자리에 넣으면 그 16건이 전부 밀리고,
하나라도 놓치면 계약이 없는 절을 가리킨다. 맨 뒤에 덧붙이면 기존 번호가 한 건도 움직이지 않는다.

R6이 요구한 구조는 그대로 성립한다 — §8이 세 역할에 공통인 정의(자리·이름·최소 내용·금지 조건·
분량 상한·갱신 의무)를 한 번만 적고, 세 역할 계약은 자기 문서의 의무만 적으며 §8을 가리킨다.
인수 의무를 §4에 한 번만 적고 역할 계약이 참조하는 지금 구조와 같다.

§5(전이 기록)는 이 작업에서 손대지 않는다. R4의 갱신 의무는 §8 안에 적는다. 전이 기록 절에 요약
의무를 섞으면 `history` 항목의 정의가 흐려지고, 기획서에는 `history`가 없어서 §5가 기획서 요약의
갱신 의무를 담을 자리도 아니다.

### 3. 분량 상한과 금지 조건

- **상한: 빈 줄을 제외하고 10줄 이하.** 세 문서 종류에 같은 값이다. 근거는 둘이다 — 기획서 평균이
  189줄이므로 10줄이면 두 번째 본문이 되지 않고, R2가 기획서에 요구하는 넷을 각각 두 줄 남짓으로
  말할 수 있다. 미리보기가 본문 앞 세 줄을 쓴다는 사실을 계약이 함께 적어, 첫 세 줄에 핵심이
  오도록 안내한다.
- **금지 조건은 이 목록으로 열거한다**(R3). 요약 절 안에 아래가 0건이다.
  - 백틱으로 감싼 토큰
  - 파일 경로 — 슬래시를 가진 이름과 확장자를 가진 파일 이름
  - `snake_case`·`camelCase` 식별자
  - `파일:줄번호` 표기
  - 함수·타입·필드 이름
- **문서 id는 허용한다.** `SPEC-`·`TASK-`·`IDEA-`·`DECISION-`으로 시작하는 값이다. 사용자가 앱
  화면에서 실제로 마주치는 이름이라 코드 식별자와 성격이 다르다. 다만 백틱 없이 평문으로 쓴다 —
  그래야 "백틱 토큰 0건"이 예외 없는 한 줄로 남는다.
- 이 조건은 요약 절에만 걸린다. 본문의 나머지 절과 확인 동선 절은 지금 어법 그대로다. 확인 동선은
  재현 조건에 경로나 명령이 필요할 수 있으므로 금지 목록을 걸지 않는다.

### 4. 확인 동선 절의 내용

- 최소 형태는 "어느 화면 → 어느 조작 → 무엇이 보이면 정상"이다.
- 화면이 없는 작업은 그 사실을 평문으로 말한다. 계약이 그 문장의 내용을 함께 적는다 — 자동 검사로
  닫혔고 확인 도장은 그 검사 수치를 신뢰한다는 뜻이라는 것. 빈 절을 두거나 화면이 있는 척하지
  않는다.
- 재현 조건이 있는 결함 수정은 그 조건을 동선에 적는다.
- 이 절은 사용자 QA가 필요한 작업, 즉 `qa_waiting`으로 넘어가는 작업에 걸린다.

### 5. 갱신 의무의 문언 (R4)

- 문서의 상태를 전이하는 세션은 같은 편집에서 요약도 현재 사실에 맞춘다.
- 이 의무는 에이전트 세션에만 걸린다. 앱이 기록하는 두 전이(`completed`·`revision_requested`)는
  본문을 건드리지 않으므로 계약이 앱에 의무를 지우지 않는다. 계약 문면에서 이것이 읽혀야 한다.
- 수정 요청으로 다시 쓰는 기획서는 새 문서이므로 요약도 새로 쓴다. 앞 문서의 요약을 복사해 오는
  것은 계약을 지킨 것이 아니다.

### 6. 역할별로 무엇을 적는가 (R2·R6)

| 계약 | 자기 문서 | 요구 |
| --- | --- | --- |
| `planner.md` | 기획서 | R2의 넷을 모두 — 무엇을 왜 바꾸는지, 사용자가 결정할 것이 무엇인지, 승인하면 무엇이 달라지는지, 승인하지 않으면 무엇이 그대로 남는지 |
| `architect.md` | 개발 작업 | 이 작업이 끝나면 사용자에게 무엇이 달라지는지. 코드 구조가 아니라 사용자가 겪을 변화 |
| `developer.md` | 개발 보고서 | 무엇을 했고 무엇이 확인됐는지, 사용자가 지금 무엇을 해야 하는지. 더해서 담당 작업 문서의 `## 확인 동선`을 `qa_waiting` 편집에서 쓴다 |

- 면제는 없다. 세 계약 모두 자기 문서의 의무를 적는다.
- 요약은 본문의 축약이 아니라 독자가 다른 층이라는 전제로 다시 쓴 문단이고, 본문에 없는 사실을
  새로 만들지 않는다. 이 문장은 §8에 한 번만 적는다.

### 7. 버전은 이렇게 움직인다

| 대상 | 지금 | 뒤 |
| --- | --- | --- |
| `WORKFLOW_RULES` 본문의 `rules_version`과 `WORKFLOW_RULES_VERSION` | 11 | 12 |
| `planner.md` 본문의 `rules_version` | 6 | 7 |
| `developer.md` 본문의 `rules_version` | 6 | 7 |
| `architect.md` 본문의 `rules_version` | 5 | 6 |
| `ROLE_RULES_VERSION` | 6 | 7 |

- 본문이 바뀌는 계약은 셋 다이므로 셋 다 하나씩 올린다. `ROLE_RULES_VERSION`은 세 계약의 최댓값
  규약 그대로 7이다.
- **`architect.md`를 7이 아니라 6으로 두는 것이 이 표의 핵심이다.**
  `a_role_contract_below_the_version_constant_is_not_rewritten_every_time`이 아키텍트 계약의 버전이
  상수보다 낮다는 것을 전제로 세워져 있고, 전제가 깨지면 그 검사는 아무것도 확인하지 않는다. 6 < 7은
  그 전제를 유지한다. TASK-111이 5 < 6으로 세운 자리를 그대로 한 칸씩 옮기는 것이다.
- 기존 검사가 버전 문자열을 여러 자리에서 단언한다(`rules_version: 11` 6건, `rules_version: 6` 7건,
  `rules_version: 5` 4건 남짓). 지우지 말고 새 값으로 고친다.
- 설치된 `.workflow/rules/` 아래 파일을 손으로 고치지 않는다. 앱이 갱신한다.

### 8. `docs/file-contract.md`에서 고쳐야 할 자리

- **기획서 절**(`:47`~`:71`의 예시) — 본문 예시에 `## 결정권자 요약`을 맨 앞 절로 넣는다. 지금
  예시는 `기획 내용`·`요구사항 명세`·`기대효과` 셋만 보인다.
- **개발 작업 절**(`:119`~`:136`의 예시와 `:135`의 "작업 범위와 완료 조건을 작성한다") — 요약 절과
  확인 동선 절을 본문 권장에 넣는다. 두 절의 저자가 다르다는 것도 함께 적는다.
- **에이전트 역할 계약 요약 절**(`:110`~`:115`) — 세 역할이 자기 문서에 지는 요약 의무를 한 줄씩
  잇는다.
- **개발 작업의 QA 절**(`:148`~`:153`) — 사용자가 QA 화면에서 읽을 확인 동선이 작업 문서 본문에
  있다는 것을 적는다. 보고서 쪽은 계약 대상이 아니라는 것도 함께.
- 세 자산(공통 규칙·역할 계약·`file-contract.md`)이 같은 규칙을 말해야 한다. 문구가 완전히 같을
  필요는 없고, 읽어서 다른 규칙으로 읽히면 안 된다.

### 9. 넘지 않는 선

- 문서 본문을 읽거나 검사하는 코드를 만들지 않는다(확인 필요 2).
- 프론트매터 계약을 바꾸지 않는다. 새 필드도 새 스키마 버전도 없다. 요약은 본문의 한 절이다.
- 조건 스크립트(`heartbeat_condition.rs`·`.workflow/rules/wf-eligible.sh`)와 앱의 세 역할 판정
  (`role_eligibility.rs`)을 손대지 않는다. 요약의 유무는 자격 판정에 들어가지 않는다.
- 미리보기·검색·문서 읽기·QA 기록 코드를 손대지 않는다. 미리보기가 요약을 보이게 되는 것은 규칙이
  아니라 입력이 바뀌어서다.
- 기존 문서에 요약을 소급 기입하지 않는다. `.workflow/` 아래 기존 문서의 본문을 고치지 않는다.
- 아이디어 문서에 의무를 걸지 않는다(확인 필요 3).
- 결정 문서에 어떤 요구도 하지 않는다. 앱 소유 상태다.
- 기존 자동 검사를 지우거나 비활성화하지 않는다. 버전 문자열 단언만 새 값으로 고친다.

## 완료 조건

1. 공통 규칙에 §8이 **맨 뒤에 덧붙어** 있고, 기존 §1~§7의 번호가 한 건도 움직이지 않았다. 그 절이
   요약 절의 자리(본문 맨 앞, 제목 다음)·이름(`## 결정권자 요약`)·최소 내용·금지 조건·분량 상한
   (빈 줄 제외 10줄)·갱신 의무를 한 번씩 정의한다. 검증: 계약 본문을 단언하는 테스트와
   `project_instructions.rs`·`docs/file-contract.md`의 `§` 참조가 그대로인지 확인. (R1·R2·R3·R6 /
   기획서 완료 조건 1·2·3)
2. 금지 조건이 판정 가능한 어법으로 열거된다 — 고정값 3의 다섯 항목이 모두 계약 본문에 있고, 문서
   id를 백틱 없는 평문으로 허용한다는 예외가 명시된다. 검증: 같은 테스트. (R3 / 기획서 완료 조건 4)
3. 갱신 의무가 "상태를 전이하는 세션이 같은 편집에서"로 적히고, 앱이 기록하는 두 전이는 대상이
   아니라는 것이 계약 문면에서 읽힌다. 수정 요청 재작업 기획서가 요약을 새로 쓴다는 문장도 있다.
   검증: 같은 테스트와 계약 문면 확인. (R4 / 기획서 완료 조건 5)
4. 세 역할 계약이 고정값 6의 표대로 자기 문서의 요약 의무를 적고, 공통 정의는 §8을 가리킨다. 어느
   계약도 §8의 정의를 자기 안에 다시 쓰지 않는다. 검증: 같은 테스트. (R2·R6 / 기획서 완료 조건 1)
5. `developer.md`가 `## 확인 동선`을 개발 작업 문서에 쓰는 의무로 적고, 최소 형태와 화면 없는
   작업의 표현이 함께 적힌다. 쓰는 시점이 `qa_waiting` 전이 편집이라는 것도 읽힌다. 검증: 같은
   테스트. (R5 / 기획서 완료 조건 6)
6. 계약이 요약 없는 문서의 유효성을 명시한다 — 요약 절이 없는 기존 문서가 그대로 읽히고 표시되고
   판정되며, 요약의 유무가 자격 판정에 들어가지 않는다. 검증: 같은 테스트. (R8)
7. `docs/file-contract.md`의 고정값 8에 있는 네 자리가 모두 갱신됐고, 공통 규칙·세 역할 계약과 같은
   규칙을 말한다. 검증: 문서 대조. (R7 / 기획서 완료 조건 7)
8. `WORKFLOW_RULES_VERSION`이 12, `ROLE_RULES_VERSION`이 7이고, 본문의 `rules_version`이 공통 규칙
   12·`planner.md` 7·`developer.md` 7·`architect.md` 6이다. 검증: `cargo test`. (R7 / 기획서 완료
   조건 8)
9. `a_role_contract_below_the_version_constant_is_not_rewritten_every_time`이 새 값에서 통과하고,
   그 전제(`architect.md`의 버전이 상수보다 낮다)가 유지된다. 검증: 같은 테스트.
10. 이 변경분에 문서 본문을 읽거나 검사하는 코드가 없다. 검증: 변경분 확인. (기획서 완료 조건 9,
    제외 범위)
11. 조건 스크립트와 앱의 세 역할 판정, 미리보기·검색·문서 읽기·QA 기록이 달라지지 않는다.
    프론트매터 계약과 스키마 버전도 그대로다. 검증: 기존 대조 테스트가 수정 없이 통과하고, 범위
    파일 둘 밖에 변경이 없다. (R8·R9 / 기획서 완료 조건 10·11)
12. `.workflow/` 아래 기존 문서의 본문 수정이 변경분에 없다. 검증: 변경분 확인. (기획서 완료
    조건 14)
13. 기존 자동 검사가 삭제되거나 비활성화되지 않는다. 버전 문자열을 단언하는 검사는 새 값으로
    고친다. 검증: 테스트 목록 변경분 확인. (기획서 완료 조건 13)
14. `npm run check`와 `cargo test --manifest-path src-tauri/Cargo.toml`이 통과한다. (기획서 완료
    조건 15)

## 검증 절차

1. `cargo test --manifest-path src-tauri/Cargo.toml` — 계약 본문 단언, 버전 문자열, 설치·갱신 계획
   검사.
2. `npm run check`.
3. `git diff --stat`으로 변경 파일이 범위 파일 둘뿐임을 확인하고, 결과를 보고서에 적는다. 완료 조건
   10·11·12의 근거가 그 확인이다.
4. `§` 참조 개수를 변경 전후로 세어 기존 번호가 밀리지 않았음을 확인한다. 완료 조건 1의 후반이다.
5. 갱신된 공통 규칙 §8과 세 역할 계약, `docs/file-contract.md`를 나란히 두고 요약 절의 이름·자리·
   상한·금지 조건이 같은 규칙으로 읽히는지 문장 단위로 대조한 결과를 보고서에 적는다. 완료 조건
   7의 근거다.
6. 앱을 띄워 요약 절이 없는 옛 기획서와 작업 문서가 그대로 열리는지 확인한다. 기획서 완료 조건
   10의 후반이다.

## 범위 파일

- `src-tauri/src/infrastructure/project_instructions.rs` — 공통 규칙 본문의 새 §8, 세 역할 계약
  본문, 두 버전 상수, 그 값과 문언을 단언하는 검사.
- `docs/file-contract.md` — 같은 규칙을 서술한 네 자리.

판정 구현(`heartbeat_condition.rs`·`role_eligibility.rs`·`fs_project_repository.rs`)과 프론트엔드는
이 작업이 만지지 않는다. 표시 쪽은 IDEA-61927447의 몫이다.

## 선행

- `TASK-111`(SPEC-035) — 같은 두 파일을 마지막으로 고친 작업이고, 이 작업의 버전 인상분이 그 위에
  얹힌다. 기획서 완료 조건 12가 이 순서를 요구한다.

`TASK-102`는 TASK-111이 이미 선행으로 두고 있고 `completed`이므로 여기서 다시 적지 않는다.

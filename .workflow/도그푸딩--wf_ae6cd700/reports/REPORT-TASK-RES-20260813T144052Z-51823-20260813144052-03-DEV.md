# TASK-RES-20260813T144052Z-51823-20260813144052-03 개발 보고서

## 결정권자 요약

앱이 프로젝트 설정을 읽을 때 실행 권한 동의 상태를 함께 받아 오고, 동의와 철회를 실행 환경에 보내는 통로 둘을 만들었다.
동의 상태는 동의함, 동의 필요, 지원하지 않음, 확인 불가 넷 중 하나이며, 어떤 실패도 동의함으로 바뀌지 않는다.
동의 확인이 실패해도 설정과 진단과 실행 가능 여부는 평소대로 조회된다. 실패는 동의 값 안의 상태로만 남는다.
동의와 철회 두 통로는 설정 저장과 같은 규칙을 따라, 호환되지 않는 실행 환경에는 요청 자체를 보내지 않는다.
이 작업만으로는 화면이 달라지지 않는다. 사용자가 만나는 동의 화면은 뒤따르는 작업이 붙인다.
앱 저장소 전체 검사가 738개에서 747개로 늘고 실패 없이 통과했다. 늘어난 9개가 이 작업이 더한 검사다.
사용자는 눌러 볼 화면 대신 이 수치를 확인한다. 작업 문서의 확인 동선 절에 명령과 기대값을 적어 두었다.

## 변경 파일과 모듈

- `src-tauri/src/infrastructure/agent_runtime_process.rs`: 동의 확인·동의·철회 세 명령의 고정 인자 상수와
  호출 함수를 더했다. 세 명령이 같은 모양의 동의 객체 하나로 답하므로 응답을 꺼내는 자리는 `consent_call`
  하나로 두었다. 동의 시각은 실행 환경이 스스로 읽으므로 요청에 싣지 않는다.
- `src-tauri/src/application/agent_runtime_config_service.rs`: 동의 값 `ProjectConsent`와 상태 `ConsentStatus`를
  정의하고 `PolicySnapshot`에 실었다. `read`가 설정·진단에 이어 동의를 읽으며, 실패는 전체 실패로 올리지 않고
  `consent_from_runtime`이 상태로 옮긴다. 동의와 철회 메서드는 호환 판정을 먼저 통과한다.
- `src-tauri/src/commands/agent_runtime.rs`: 화면이 부를 동의·철회 커맨드 둘을 더했다. 설정 저장 커맨드와 같은
  방식으로 호환 판정을 읽어 서비스에 넘긴다.
- `src-tauri/src/lib.rs`: 두 커맨드를 명령 등록 목록에 올렸다.
- `docs/agent-runtime.md`: "실행 권한 동의" 절을 더해 앱과 실행 환경의 분담, 조회에 실리는 값, 읽기 실패를
  동의로 추정하지 않는다는 규칙을 적었다.

## 검증 절차와 결과

- `cargo test --manifest-path src-tauri/Cargo.toml` 통과: 747 passed, 0 failed. 통합 검사 20 passed는 별도다.
- 같은 명령을 변경 전 파일로 되돌려 실행한 값은 738 passed, 0 failed이었다. 늘어난 9개가 이 작업의 검사이고,
  실패와 건너뜀은 늘지 않았다.
- 완료 조건 1·2·3·4는 흉내 낸 호출자로 각각 확인했다. 조건 2와 3은 거절 사유 코드만 다른 두 검사로 나눠,
  `unsupported_command`만 지원하지 않음이 되고 나머지는 사유를 실은 확인 불가가 되는 것을 대조했다.
- 완료 조건 5·6은 두 커맨드가 보내는 명령 인자와 요청 본문을 고정해 확인했다. 조건 8은 호환되지 않는 판정에서
  호출자에게 아무 요청도 가지 않은 것으로 확인했다.
- 완료 조건 7은 등록 원문에서 목록 구간을 잘라 두 커맨드 이름이 그 안에 있는지 확인했다. 등록 목록은 매크로
  인자여서 실행 중에 셀 자리가 없다.
- 화면이 읽을 필드 이름과 상태 문자열은 조회 응답을 직렬화해 대조하는 검사로 함께 고정했다.
- `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings` 통과: 경고 없음.
- `cargo fmt --manifest-path src-tauri/Cargo.toml --check` 통과: 출력 없음.
- `git status --short`로 이 작업이 건드린 파일이 선언된 범위 파일 다섯 개뿐임을 확인했다.

## 남은 리스크

- 실행 환경의 셸 표면에 `agent consent` 하위 명령이 아직 없다. 앱은 실행 환경을 셸 인자로 부르므로, 지금
  실제 실행 환경에 조회를 보내면 동의 상태는 확인 불가가 된다. 이 상태는 동의로 바뀌지 않으므로 관문이
  느슨해지지는 않지만, 실제 동의 기록을 읽으려면 아래 후속 작업의 첫 항목이 필요하다.
- 앱은 동의 상태를 나르기만 하고 이 값으로 실행을 막지 않는다. 시작 직전 판정은 실행 환경이 이미 갖고 있고,
  화면에서의 차단은 뒤따르는 작업의 완료 조건이다.
- 동의 값은 설정 지문에 넣지 않았다. 동의나 철회가 저장 충돌을 만들지 않는다는 뜻이며, 반대로 화면이 동의
  직후의 상태를 보려면 조회를 다시 불러야 한다.

## 후속 작업

- 실행 환경 저장소의 `src/heartbeat/cli.py`에 `agent consent read|grant|revoke` 하위 명령이 없다. 앱이
  보내는 인자는 기존 `agent provider diagnose`와 같은 모양으로 맞춰 두었으므로, 그 파일의 명령 대응표에
  세 줄을 더하면 두 저장소가 이어진다. 이 파일은 이 작업의 선언된 범위 밖이고 TASK-01의 보고서도 같은
  자리를 남겨 두었다.
- 앱 화면 쪽 타입에는 동의 값이 아직 없다. 조회 응답에 필드가 늘어나도 화면은 그대로 동작하며, 동의 화면을
  만드는 작업이 그 타입과 함께 붙이면 된다.
- 이 작업은 배정된 작업 문서만으로 진행했고, 기획서와 결정 문서는 실행 환경 계약의 동의 절을 대조하려고
  열었다. 작업 문서의 범위 사전 검사가 값이 지나는 자리를 파일과 함수 이름까지 적어 두어 판단에 부족한
  곳은 없었다.
- 실행 환경이 넘겨준 결과 접두사 RES-20260813T152318Z-94460-20260813152318은 쓰지 않았다. 개발 세션은 새
  기획서나 작업 식별자를 만들지 않으므로 이번 예약에서는 소비될 자리가 없었고, 기록으로만 남긴다.

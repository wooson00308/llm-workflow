# TASK-RES-20260813T150633Z-92948-20260813150633-02 개발 보고서

## 결정권자 요약

어떤 기능을 지금 확인할 수 있는지 정하는 판정을 개발 화면 파일에서 꺼내 두 화면이 함께 읽는 자리로
옮겼고, 제출 전 검토를 기기에 담아 두는 자리를 새로 만들었다. 판정 기준은 한 줄도 바꾸지 않았다.
개발 화면과 에이전트 화면의 검사 파일은 한 글자도 고치지 않은 채 131건이 그대로 통과했고, 저장소
전체 검사 528건과 타입 검사도 통과했다. 사용자는 개발 화면의 확인할 기능 카드와 에이전트 화면의
대기 건수가 이전과 같은 수를 보여 주는지만 확인하면 된다. 화면에 새로 생긴 것은 없다.

## 변경한 파일과 모듈

- `src/features/projects/domain/qaReview.ts`: 새 파일. 기능 묶음 판정, 확인 항목 정렬, 자동 근거 구분,
  지금 확인할 수 있는 작업 파일명 모으기가 여기로 모였다. `buildQaReviewGroups`가 지금 확인할 수 있는
  묶음과 아직 확인할 수 없는 묶음을 나눠 돌려주고, `actionableQaFileNames`는 이전 서명 그대로다.
- `src/features/projects/domain/qaReview.test.ts`: 새 파일. 완료 조건 2·3·4의 판정 검사 11건.
- `src/features/projects/infrastructure/browserQaReviewDraftStore.ts`: 새 파일. 워크플로 디렉터리와
  기능 묶음 키로 제출 전 검토를 담는 저장소이며 읽기·저장·지우기 세 동작을 갖는다.
- `src/features/projects/infrastructure/browserQaReviewDraftStore.test.ts`: 새 파일. 완료 조건 5·6의
  저장소 검사 10건.
- `src/features/projects/components/DevelopmentBoard.tsx`: 판정 함수 일곱 개를 걷어내고 새 모듈을
  읽는다. 자동 근거 항목을 목록에서 빼던 동작을 없앴고, 직접 확인 단계와 접어 둔 항목은 항목이 들고
  있는 값으로 화면에서 나눈다. `openTask`가 항목 대신 파일 이름을 받는다.
- `src/features/projects/components/agents/AgentRunDashboard.tsx`: 판정을 개발 화면이 아니라 새
  모듈에서 가져온다. 바뀐 것은 가져오는 자리 한 줄이다.

## 검증 단계와 결과

1. `npx vitest run src/features/projects/domain/qaReview.test.ts` → 11건 통과.
2. `npx vitest run src/features/projects/infrastructure/browserQaReviewDraftStore.test.ts` → 10건 통과.
3. `npx vitest run src/features/projects/components/DevelopmentBoard.test.tsx src/features/projects/components/agents/AgentRuntimeView.test.tsx`
   → 131건 통과. 두 검사 파일은 편집하지 않았다.
4. `npm run typecheck` → 오류 없음.
5. `npm test` → 27개 파일 528건 통과, 42건 건너뜀. 건너뛴 42건은 이 작업 이전과 같은 항목이다.

## 남은 위험

- 확인할 수 없는 묶음의 제목은 기획서 문서가 목록에 없으면 묶음 키를 그대로 쓴다. 옮기기 전 동작이
  그러했으므로 그대로 두었고, 확인할 수 있는 묶음만 단건 작업 제목으로 대신 부른다. 두 경로가 다른
  것은 의도이며 주석에 근거를 적었다.
- 제출 전 검토 저장소는 읽는 화면이 아직 없다. 저장 형식이 실제 검토 화면과 맞는지는 뒤따르는 작업이
  붙이는 시점에 처음 확인된다. 지금은 제출 계약의 항목 한 건과 같은 값을 담도록 맞춰 두었다.
- 개발 화면과 에이전트 화면이 같은 수를 보여 주는지는 자동 검사가 각 화면을 그려 확인했고, 두 화면을
  나란히 띄운 실제 실행은 확인하지 않았다.

## 후속 작업

- 이 판정 위에 확인 대기열과 검토 화면을 만드는 일은 뒤따르는 세 작업의 몫이다. 저장소를 읽고 쓰는
  자리도 그 작업들이 만든다.
- 역할 밖 발견: 없다. 작업 문서의 범위 사전 검사가 가리킨 파일과 실제 저장소 상태가 어긋나지 않았고,
  선언한 파일만으로 완료 조건을 모두 충족했다.
- 런타임 인계로 받은 결과 접두사 RES-20260813T164858Z-98141-20260813164858은 새 문서 식별자를 만들
  필요가 없어 쓰지 않았다. 이 작업은 기존 작업 문서 하나를 구현했을 뿐이다.
- 작업 문서만 읽고 구현했다. 기획서와 결정 문서는 열지 않았고, 그럴 필요도 없었다.

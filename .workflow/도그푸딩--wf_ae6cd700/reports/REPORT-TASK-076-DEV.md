# TASK-076 개발자 핸드오프

> 기록 경위: 하네스가 서브에이전트(dev-076=dev-063)의 보고서 파일 작성을 차단해, 발신된 전문을 TL이 대리 기록한다. 내용은 발신 전문 그대로다. (수신 2026-08-04T09:52Z, TL 세션)

- 대상: TASK-076 (조건 스크립트가 판정 사유를 ASCII 코드로 표준 출력 첫 줄에 낸다, SPEC-023 R4·R6)
- 상태: `qa_waiting` (lease-52770-20260804092101, 전 단계 exit 0)

## TASK-077 인계 — 사유 코드 목록 (완료 조건 10)

| 코드 | 종료 코드 | 뜻 | 사용자가 할 일 |
| --- | --- | --- | --- |
| `eligible` | 0 | 처리 대상 있음(정상 기동) | 없음 |
| `no-target` | 1 | 처리 대상 없음(정상 건너뜀) | 없음 |
| `migration-lock` | 1 | migration.lock으로 판정 중단 | 마이그레이션 완료 필요 |
| `usage` | 2 | 역할 인자 오류 | condition 문자열 수정 필요 |

설계 사실 셋: (1) no-target과 migration-lock은 종료 코드가 같으니(1) 화면이 뭉치면 이 작업의 값이 사라짐 — 할 일이 정반대. (2) eligible도 냄 — quota 스킵이 낡은 사유를 안 지우는 데몬 결함의 완화(매 실행이 자기 사유를 덮어씀). 완화지 해결 아님 — R3 우회는 여전히 필요. (3) 모르는 코드 폴백 필수 — 데몬 생성 사유(타임아웃·실행 실패)가 어휘 밖으로 같은 자리에 옴.

## 변경한 파일 (둘 + 작업 문서)

1. heartbeat_condition.rs — VERSION 4→5, sh에 verdict() + 종료 지점 7곳, ps1에 Write-Verdict + 7곳(ASCII만), test_support가 ConditionRun{code, stdout} 반환, Scenario 표에 reason 열(14행 전부)·REASON_CODES·신규 테스트 3·호출부 13곳 .code.
2. role_eligibility.rs — 한 줄(.code == 0). 근거: 사유용 헬퍼 분리는 test_support 독 주석("두 곳이 다른 명령으로 부르면 대조의 뜻이 사라진다")이 경고한 상태를 만듦. 판정 규칙 무변경, 대조 36곳 그대로.

프런트 0, 보호 상태 무변경, 커밋 없음.

## 표준 출력 충돌 해소 (실측 증명)

deps_of 출력은 전부 명령 치환(서브셸)이 받아 진짜 stdout에 안 나감. verdict()는 최상위 종료 지점에서만 → 사유가 첫 줄이자 유일한 줄(계약보다 강함). 실측: developer 경로 stdout `eligible\n` 9바이트(od -c), 줄 수 1. 표의 매 행 lines()==1 단언으로 회귀 고정. usage는 stderr(사람용) + stdout `usage`(데몬용) 분리.

## 사유 일치 3겹 보장 (완료 조건 11) + 변이 검증

(1) 시나리오 표 사유 열 — 실행 결과 대조, CI 3플랫폼. (2) both_implementations 확장 — REASON_CODES 정적 검사. (3) 표-어휘 묶음 검사. 변이 확인: migration-lock→no-target 치환 시 1·2번 동시 실패 + 기존 종료 코드 테스트는 통과 — 사유 열이 종료 코드로 못 잡는 것을 잡는 증거. 복구 후 최종 게이트.

## 게이트 수치

cargo 380/0/0, npm 17 files 406 passed + build, fmt 통과, clippy는 범위 밖 병행 이슈(heartbeat_process.rs:216) 1건 외 내 경고 0. 중간에 본 TS2741 6건은 TASK-072 병행 세션 원인(수렴 후 최종 통과 — mtime·lease 실측).

## 판정 불변 (완료 조건 7)

같은 시점에 구본문(v4)·신본문(v5) 나란히 실행 — 4개 인자 전부 종료 코드 동일 + 신본문 사유 정합. migration.lock 픽스처 별도 확인(락이 역할 해석보다 먼저라 잘못된 인자도 1 — 기존 성질 유지).

## 후속 / 리스크

- 설치본은 아직 v4 — 앱의 다음 설치가 v5로 덮음. QA에서 "사유 안 보임"이면 역할 잡 한 번 저장.
- TASK-077의 실물 값은 데몬 0.8 이후(이 기기는 이미 통로 열림) — R2 하위 호환 필요 근거.
- 어휘 확장 시 네 곳(두 본문·REASON_CODES·표) 동시 수정 — 하나만 고치면 테스트 실패하게 묶어둠.
- 사유 파라미터 미도입(200자·ASCII·일치 비용) — 필요 시 `no-target:18` 접미사 + 접두 비교 확장이 자연스러움.

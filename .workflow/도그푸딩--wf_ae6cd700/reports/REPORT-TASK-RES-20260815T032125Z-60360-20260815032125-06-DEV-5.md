# TASK-RES-20260815T032125Z-60360-20260815032125-06 개발 보고서 5

## 결정권자 요약

격리 작업 사본을 다루지 못하는 실행 환경에서 개발 역할 실행을 막는 구현은 이미 완성돼 있고, 이번
세션도 앞선 수치를 옮기지 않고 검사를 처음부터 다시 실행해 전부 통과하는 것을 확인했다. 공유 작업
공간에 남은 미커밋 변경 때문에 반영은 이번에도 시작하지 않았다. 이번에 새로 밝힌 것은 그 미커밋
변경이 생긴 원인이다. 같은 그룹의 앞선 작업이 반영되면서 앱이 관리하는 실행 보조 파일 두 개의
원본이 올라갔는데 저장소에 담긴 설치 사본은 옛 판 그대로 남았고, 앱은 켜질 때마다 설치 사본을 새
원본대로 다시 써 넣는다. 두 파일의 내용은 이미 커밋된 원본과 한 글자도 다르지 않다는 것을 바이트
단위로 확인했으므로, 사용자가 그대로 커밋해도 잃는 작업이 없다. 그 커밋 한 번이면 반영 경로가
열리고, 그전까지는 어떤 개발 세션도 같은 자리에서 멈춘다.

## 이어받은 것과 버린 것

앞선 세션이 남긴 통합 후보 커밋 `157fafd`(작업 사본 `lease-5124-20260815084700`)와 보고서 4를
이어받았다. 버린 것은 없다.

- 유지: `157fafd`의 네 파일 변경 전부. 완료 조건 1~13과 직접 대조했고 어긋나는 자리가 없다. 삭제되거나
  약해진 시험은 없다. 시험 모듈에서 바뀐 줄은 `plan`의 인자가 하나 늘어난 데 따른 호출 네 자리와
  import 두 줄뿐이고, 완료 조건 8이 보호하는 `start`·`cancel`·`retry`의 시험은 손대지 않았다.
- 유지: 기록된 기준 커밋 `f06fd6b`. 이번 확인 시점에도 공유 기준이 같은 커밋이라 후보를 다른 커밋 위로
  옮길 필요가 없었다.
- 새로 만든 것: 이번 lease의 작업 사본에 `157fafd`를 cherry-pick한 커밋 `e6d1a75`. 두 커밋의 트리가
  같다. 앞선 lease가 만료돼 그 작업 사본이 회수될 수 있으므로 같은 내용을 이번 가지에도 두었다.

## 변경한 파일과 모듈

`e6d1a75`가 담은 네 파일이며 이번 세션이 새로 고친 제품 파일은 없다.

- `src-tauri/src/domain/agent_runtime.rs`: 상수 `ISOLATION_CAPABLE_RUNTIME_VERSION`("0.10.0"),
  지원·미지원·모름을 구분하는 `IsolationSupport`, 판정 함수 `judge_isolation_support`.
- `src-tauri/src/application/agent_runtime_run_service.rs`: `plan`이 격리 판정을 인자로 더 받고 기존
  `guard` 뒤에 `guard_isolation`을 둔다. 실패 값 `RunFailure::IsolationUnsupported` 추가.
- `src-tauri/src/commands/agent_runtime.rs`: `run_gate_of`가 실행 환경 조회 한 번에서 두 판정을 함께 꺼낸다.
- `docs/agent-runtime.md`: 격리 지원 최소 판과 의도된 정지를 적은 절 하나.

작업 문서만으로 판단이 가능해 기획서 본문과 결정 문서 본문은 열지 않았다.

## 검증 단계와 결과

작업 사본 `lease-72950-20260815090559`의 커밋 `e6d1a75`에서 전부 다시 실행했고 결과는 보고서 4와 같다.
반영을 시작하지 않았으므로 모두 격리 상태의 값이다.

- `cargo test ... agent_runtime`: 105개 통과, 실패 0. e2e 대상 21개도 통과, 실패 0. 완료 조건 1~3을
  재현하는 시험이 여기 포함된다.
- `cargo test ... agent_runtime_run_service`: 14개 통과, 실패 0. 완료 조건 4~7을 재현하는 시험을 포함한다.
- `cargo test ... agent_runtime_install_service` 16개와 `... agent_runtime_status` 14개 통과, 실패 0.
  이 두 검사가 완료 조건 10을 확인한다.
- `cargo fmt --check`: 종료 코드 0. `cargo check`: 오류와 경고를 합쳐 0건. 위 명령은 모두
  `--manifest-path src-tauri/Cargo.toml`을 붙여 실행했다.
- `npm run agent-runtime:verify -- --self-test`: 통과. "정상 fixture 1건과 불일치 fixture 3건".
- 완료 조건 8: 후보가 지운 줄을 직접 확인했고 `start`·`cancel`·`retry` 관련 시험은 한 줄도 바뀌지 않았다.
- 완료 조건 13: `git diff --name-only f06fd6b HEAD -- src/`가 0건이다.

## 반영에서 멈춘 지점과 그 원인

격리 검사 결과와 반영 결과는 별개의 사실이다. 위 검사는 모두 통과했고 반영은 시작하지 않았다.

공유 기준은 `f06fd6b`로 후보의 기준과 같았으나 추적 파일 4개에 미커밋 변경이 있었다. 개발자 역할
계약은 이 상태에서 자동 반영을 시작하지 않고, 남의 미커밋 변경을 커밋하거나 되돌리지도 않는다.

이번 세션은 그 상태가 생긴 원인을 확인했다. 같은 그룹의 TASK-RES-20260815T032125Z-60360-20260815032125-03이
반영된 커밋 `f06fd6b`가 `src-tauri/src/infrastructure/heartbeat_condition.rs`에 담긴 조건 스크립트 원본을
판 20에서 21로 올렸는데, 저장소에 커밋된 설치 사본 `.workflow/rules/wf-eligible.sh`는 판 20 그대로다.
앱은 켜질 때마다 설치 사본을 원본대로 다시 써 넣으므로 그 차이가 미커밋 변경으로 나타난다. 이 정지는
사람이 편집하다 만 결과가 아니라 앞선 작업의 반영이 만들어 낸 결과이며, 기다림만으로는 풀리지 않는다.

두 파일이 이미 커밋된 원본과 같은 내용인지도 바이트 단위로 확인했다. `.workflow/rules/wf-eligible.sh`는
`heartbeat_condition.rs`의 스크립트 문자열과, `.workflow/rules/wf-reserve.sh`는
`src-tauri/src/infrastructure/reservation_helper.rs`의 스크립트 문자열과 각각 완전히 일치한다. 두 원본
모두 `f06fd6b`에 이미 커밋돼 있으므로 두 파일을 그대로 커밋해도 사라지는 사람의 작업은 없다.

나머지 2개는 이 워크플로의 작업 문서 `-03`과 `-06`이며, 역할 세션이 공유 저장소에 쓰는 제어 문서다.

통합 후보를 갱신해 둔다. 커밋 `e6d1a75`(가지
`wf-iso/TASK-RES-20260815T032125Z-60360-20260815032125-06/lease-72950-20260815090559`), 기준 `f06fd6b`,
변경 파일은 위 네 개다. 앞선 후보 `157fafd`, `f3134c0`, `fcc54c9`도 트리가 같아 어느 쪽에서 이어가도
결과가 같다.

작업 상태는 `in_progress`로 두었다. 반영을 기다리는 것은 막힌 것이 아니므로 `blocked`로 두지 않았고,
반영과 재검사를 마치지 않았으므로 `verified`로도 두지 않았다. 인계 사실은 `in_progress` 이력 항목으로 남겼다.

## 남은 위험

- 반영 전까지 승인된 기획서가 요구한 정지가 공유 기준에 없다. 격리를 지원하지 않는 실행 환경에서 개발
  역할 실행이 지금처럼 열린다.
- 관문은 실행 계획 단계에만 있다. 실행 환경의 자동 배정은 이 경로를 지나지 않아 닿지 않으며, 작업
  문서가 범위 밖으로 명시한 사항이다.
- 반영 시점에 공유 기준이 `f06fd6b`에서 전진해 있으면 그 커밋 위로 옮겨 격리 검사를 다시 실행해야 한다.
- 관리 스크립트 원본을 바꾸는 작업이 반영될 때마다 같은 정지가 되풀이된다. 설치 사본을 함께 커밋하지
  않는 한 다음 작업의 반영도 같은 자리에서 막힌다.

## 후속 작업

- 사용자 확인 필요: 두 관리 파일의 미커밋 변경을 커밋할지 추적에서 뺄지 정해야 한다. 위 확인대로 커밋해도
  잃는 작업이 없다. 이 결정 전에는 어떤 개발 세션도 반영할 수 없고, 에이전트의 대리 정리는 계약이 금지한다.
- 아키텍트 인계: 반영 대기 작업을 개발자 판정에서 제외하는 관문(`role_eligibility.rs` 414행,
  `heartbeat_condition.rs` 1167행)은 격리 기록의 `step`이 `integration_waiting`일 때만 동작하는데 그 값을
  쓰는 곳이 저장소에 없다. 기록은 앱 소유 상태라 개발 세션이 만들 수 없다. 이것이 이 작업이 다섯 번째로
  다시 호출된 이유다.
- 아키텍트 인계: 관리 스크립트 원본을 바꾸는 작업의 완료 조건에 설치 사본 갱신을 넣을지 정해야 한다.
  지금 구조에서는 그 갱신을 반영 이후 앱이 만들어 내므로 어떤 개발 세션도 완료 조건 안에서 처리할 수 없다.
- 아키텍트 인계: `docs/agent-runtime.md` 버전 표가 앱 판 0.1.11을 적어 릴리스 0.1.18과 어긋난다(다섯 번째 기록).
- 이번 예약의 결과 접두사는 RES-20260815T090608Z-72950-20260815090559이다.

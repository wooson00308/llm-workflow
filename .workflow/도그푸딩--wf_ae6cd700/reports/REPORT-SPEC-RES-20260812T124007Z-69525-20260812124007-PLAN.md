# SPEC-RES-20260812T124007Z-69525-20260812124007 기획 보고서

## 결정권자 요약

IDEA-79A384D4의 실행 기록 문제를 사용자 요약, 이상 신호, 개발자 진단 번들로 재구성했다.
반복되는 진행과 도구 이벤트는 집계하고 시작과 종료와 실패 상세와 활동 없음 신호만 줄로 남긴다.
실행별 결과 보고서를 앱 안에서 읽고, 실행 정보와 원본 이벤트와 진단과 오류를 한 번에 저장하거나 복사하게 했다.
도구 이벨트에는 이름만 더하고 인자와 결과 본문은 표시와 저장과 내보내기 모두에서 금지했다.
활성 실행의 마지막 이벤트 후 5분을 주의 신호 기준으로 정했으며, 새 활동이 오면 사라지고 실패로는 전환하지 않는다.
실행 배정과 취소와 재시도, 문서 상태와 사용자 판단 관문은 유지한다.
지금 요청하는 것은 SPEC-RES-20260812T124007Z-69525-20260812124007의 승인 여부다.

## 대상과 절차

- 예약 대상은 IDEA-79A384D4, lease는 `lease-69525-20260812124007`이다.
- `wf-claim renew IDEA-79A384D4 lease-69525-20260812124007 15`를 실행해 종료 코드 0을 확인했다. `acquire`는 실행하지 않았다.
- 지정된 결과 경로가 없고, 대상 아이디어를 참조하는 기획서가 없으며, 마이그레이션 잠금이 없음을 쓰기 전에 확인했다.
- 중단된 초안의 인수가 아니라 처음 처리하는 아이디어다. 지정된 식별자로 `draft` 골격을 먼저 만든 뒤 본문을 완성하고 `user_review`로 옮겼다.
- 사용자 정의 자정 규칙은 존재하지 않아 공통 규칙과 planner 역할 계약만 적용했다.

## 조사와 확인 사실

아이디어의 실측값은 시작 1건, 진행 8건, 도구 22건이다. 이 값은 아이디어가 제공한 2026-08-12 실측으로
기획서에 출처를 명시했다. 이 세션에서 앱 코드와 워크플로 문서를 읽어 다음을 직접 확인했다.

- 실행 상세는 `safeEvent`가 고른 문자열 필드를 이어 이벤트마다 한 줄로 나열한다.
- `RunLogPage`는 해석하지 않는 이벤트 배열과 cursor를 담고, 앱 상태 서비스는 런타임이 민감정보를 제거했다는 계약 아래 그대로 전달한다.
- `RunSummary`는 대상과 상태와 시간과 사유를 가지지만, 마지막 활동과 도구 집계와 보고서 참조는 가지지 않는다.
- 프로젝트 요약은 보고서 개수만 제공하고 보고서 목록과 본문을 제공하지 않는다.
- 큐 상태에 provider 진단과 최근 오류가 있지만 원본 이벤트와 함께 하나의 전달용 결과로 만드는 표면은 없다.
- TASK-146은 실행별 JSONL 이벤트와 cursor 기반 재연결, 프롬프트와 인증 값의 비보존을 이미 검수한 선행 계약이다. 이 기획은 그 경계를 유지한다.

## 기획 판단

- 이벤트 전체 내용을 복원하는 방향을 제외했다. 작업 지시와 인증 값 비보존은 결함이 아니라 보존해야 할 안전 경계다.
- 활동 없음 기준은 5분으로 확정했다. 오탐지의 비용을 줄이기 위해 실패나 자동 취소로 연결하지 않고, 새 이벤트가 오면 즉시 풀리는 주의 신호로 한정했다.
- 보고서 연결은 클라이언트가 파일명을 맞추는 구조를 금지했다. 병렬 실행에서 잘못된 보고서를 열지 않도록 백엔드가 실행 결과 참조를 확인하게 했다.
- 진단 번들은 파일 저장과 클립보드 복사가 같은 버전화된 내용을 쓰게 했다. 한 가지 생성 경로를 검증하면 두 전달 방식이 엇갈리지 않는다.
- 진단을 읽지 못해도 번들 전체를 거절하지 않고 누락 사유를 적게 했다. 장애 상황에서 읽을 수 있는 증거까지 버리는 것을 막는다.
- 도구 이름은 인자와 결과 본문과 분리했다. 알려진 이름은 안정적인 유형으로 묶고 알지 못하는 이름은 사용자 화면에 원문으로 표시하지 않게 했다.

## 검증

- 기획서 frontmatter의 schema, 식별자, `source_idea_id`, 생성과 갱신 시각, `status: user_review`를 확인했다.
- 구조화 요약의 하위 제목 일곱 개가 계약 순서로 각각 한 번씩 있고, 영향 범위의 변경과 유지 표식이 각각 한 번씩 있음을 확인했다.
- 요약 안에 역따옴표 토큰, 파일 경로, 확장자를 가진 파일명, 코드 식별자가 없음을 검색했다.
- 기획서와 이 보고서에 영문식 긴 대시가 없는지 검사했다.
- 앱 소스, 기존 기획서와 개발 작업은 읽기만 했다. 개발 작업, 제품 코드, 사용자 결정, 앱 소유 상태는 변경하지 않았다.

## 인계 사항

- 승인 후 아키텍트는 앱과 claude-heartbeat의 변경을 계약 순서에 맞게 분해해야 한다. 구형 런타임 호환성을 먼저 보존하는 순서가 필요하다.
- 버전화된 번들의 세부 컨테이너 형식, provider 도구 이름을 사용자 유형으로 묶는 표, 보고서 연결을 확인하는 방법은 아키텍트가 정한다.
- 보고서 열람은 개수 응답만 넓히는 일이 아니라 읽기 전용 문서 표면과 실행별 확인된 연결을 함께 제공해야 한다.
- 승인 전에는 아키텍처나 구현을 진행하지 않는다.

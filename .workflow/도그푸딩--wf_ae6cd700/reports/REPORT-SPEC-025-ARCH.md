# SPEC-025 아키텍트 핸드오프

> 기록 경위: 하네스가 서브에이전트(arch-b)의 보고서 파일 작성을 차단해, 발신된 전문을 TL이 대리 기록한다. 내용은 발신 전문 그대로다. (수신 2026-08-04T09:26Z, TL 세션)

- 역할: 프로젝트 아키텍트 (architect)
- 대상: DECISION-5B7E9F14 (SPEC-025 승인)
- 산출 작업: TASK-078, TASK-079 (2건, todo)
- 세션: 2026-08-04T09:09Z ~ 09:14Z / lease 취득→반납 exit 0

## 의존 지도

TASK-078(초안 저장소 신설 — 새 파일 둘) → TASK-079(두 입력창 배선 + 테스트, R1·R2·R3·R5·R6). SPEC-019 선례(TASK-061/062)와 같은 분할. 순환·댕글링 없음.

## 핵심 분해 결정

- 키 구조 유도(유의 1): 확인 1(통합 초안)→입력창 자리를 키에 안 넣음, 확인 2(워크플로 단위)→워크플로 디렉터리가 키. 저장 형태는 디렉터리 키 문자열 맵 하나. `<이름>--<wf_id>` 형태라 프로젝트 축 불요(유일성 성립) — 반례 발견 시 축 추가 탈출구 명시.
- 실패 삼킴은 선례 재사용하되 근거 문장 복사 금지(유의 2): 모양의 본보기는 jobValueMemoryStore.ts(같은 맵 형태). 선례 주석의 "표시 상태라 알릴 가치 없다"는 이 자산(사용자 저작물)에서 참이 아님 — 승인된 실제 근거(실패해도 화면 글·제출 정상, 글자마다 저장이라 오류 표시 반복이 더 나쁨)를 새로 적을 것.
- R3의 실질 = key 언마운트 동작(유의 3): 인박스는 key={workflow.directory} 재마운트, 오늘 화면은 key 없음 — 권장 방식(마운트 시 읽기·편집 시 저장)이면 오늘 화면에도 key를 걸어야 R3 성립. 완료 조건 8은 양쪽 화면에서 확인(한쪽만 보면 통과해버림).
- 가장 조용히 깨질 지점 명시: "첫 마운트에서 읽은 값을 되쓰지 않는다" — 깨지면 비활성 렌더가 빈 값을 저장해 사용자 글 소멸(완료 조건 12 붕괴). TASK-062 선례 연결.
- 아키텍트 위임 결정(R5): "초안이 보이되 제출만 막힌다" — textarea가 이미 disabled라 안전하고, 숨기면 R1 약속과 화면이 어긋남.
- 완료 조건 11 제외: 확인 3의 대안(알림) 승인 시 항목인데 제안(미알림)이 승인됨 — 범위 밖 기록.

## 후속 / 리스크

- SPEC-026·027 겹침은 현재 없음(대상 구간 분리)이나 같은 파일(IdeaInbox.tsx) — 그 둘의 분해자가 TASK-079 착지 여부를 보고 판단할 것 (인계).
- 두 입력창 테스트 전무 재확인 — TASK-079 완료 조건 17개 중 12개가 새 테스트(부피 쏠림). TASK-078은 가벼움.
- 초안 무기한 축적(확인 4 승인의 인지된 한계) — 실사용에서 거슬리면 별도 아이디어 자리.
- 반납 시점 관측: SPEC-024.yml을 다른 세션(하트비트 아키텍트)이 선점.

## 상태

보호 상태 무변경, 제품 코드 무변경(읽기만), git 커밋 없음. 작업 둘 todo + created history. lease 반납 완료.

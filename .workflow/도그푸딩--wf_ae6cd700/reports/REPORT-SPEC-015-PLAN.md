# SPEC-015 기획자 핸드오프

- 역할: 기획자 (planner)
- 대상: DECISION-2F71D20D (SPEC-014 수정 요청, `revision_requested`)
- 산출 기획서: SPEC-015 (`status: user_review`)
- 세션: 2026-08-03T05:16Z ~ 05:26Z

## 무엇을 했나

선점 시점(05:17Z)에 기획자 대상은 4건이었다. 미처리 아이디어 3건(IDEA-48EDAF2B: 01:33Z, IDEA-08303478: 02:05Z, IDEA-C95EABD2: 02:26Z)과 후속 기획서가 없는 `revision_requested` 결정 1건(DECISION-2F71D20D, 04:35Z)이다. 그중 수정 요청을 골랐다. 이미 사용자 검토 비용이 든 피드백이고, REPORT-SPEC-013-PLAN이 같은 근거로 같은 선택을 한 선례가 있다.

`.workflow/.runtime/leases/DECISION-2F71D20D.yml`을 배타적 생성으로 선점하고, 즉시 SPEC-015 스켈레톤(`status: draft`)을 만든 뒤 본문을 작성했다. 완료 후 `user_review`로 올리고 lease를 해제했다.

기존 lease 두 개(`SPEC-009.yml` 만료, `SPEC-013.yml` 미만료·아키텍트)는 건드리지 않았다.

## 결정 사항

- **SPEC-014는 보존하고 새 ID로 개선판을 냈다.** SPEC-013이 세운 선례를 그대로 따랐다. 프론트매터에 `source_spec_id: SPEC-014`, `source_decision_id: DECISION-2F71D20D`를 두고 원 아이디어 `source_idea_id: IDEA-54B29779`도 유지했다. 아이디어 채택 판정 키는 `source_idea_id` 하나이므로 두 기획서가 같은 아이디어를 참조해도 판정은 바뀌지 않는다.
- **확인 필요 1~4번은 질문에서 확정으로 옮겼다.** 결정문이 네 제안에 모두 동의했으므로 다시 묻지 않고 "확정된 결정" 절(D1~D4)과 R4·R5·R8·R11 본문에 반영했다.
- **확인 필요 5번은 결정문대로 뒤집었다(D5).** 선점 헬퍼의 Windows 지원을 포함 범위로 확정하고, 그 작업의 실행 순서를 TASK-039 뒤로 거는 요구를 R12로 분리했다. 실행 순서를 거는 방법(선행 선언 필드냐 다른 명시냐)은 결정문이 아키텍트 몫으로 지정했으므로 이 기획서가 정하지 않았다.
- **Windows 차단 해제는 헬퍼 작업에 묶지 않았다.** 결정문의 순서 제약은 "그 작업"(헬퍼의 PowerShell 구현과 판정 일치 고정)에 걸린 것이지 차단 해제에 걸린 것이 아니라고 읽었다. 그 결과 헬퍼의 PowerShell 구현이 들어오기 전까지 Windows에만 헬퍼 파일이 없는 기간이 생긴다. 이건 DECISION-73D4BC1B의 "헬퍼가 있으면 강제"를 각 플랫폼에서 그대로 적용한 결과라 플랫폼 안에서는 모순이 없고, 시한부 차이로 R10 마지막 줄에 명시했다. 이 읽기가 사용자 의도와 다르면 R12의 네 번째 줄만 뒤집으면 된다.
- **확인 필요 2번의 사실을 결정문대로 바로잡았고, 그 갱신에서 새 완료 조건이 나왔다.** `.github/workflows/ci.yml`의 `rust` 잡이 ubuntu-22.04·windows-latest·macos-latest 매트릭스이고 `fail-fast: false`다. `frontend` 잡만 ubuntu 단독이다. v0.1.6 핫픽스 2건은 `7de81af`(조건 테스트의 unix 전용 import를 cfg로 가둠)와 `7b6fc69`(조건 스크립트 경로를 POSIX 표기로 유지)로, 둘 다 조건 스크립트 모듈을 만졌다. 그래서 "Windows 러너에서 판정 대조가 실제로 실행·통과로 기록된다"를 완료 조건 10번으로 못박았다.
- **확인 필요를 하나만 남겼다.** CI Windows 러너는 `cargo test`만 돌린다. 실행 정책이 걸린 상태의 조건 실행, 앱 화면, 데몬의 slug 역변환 cwd는 그 러너가 검증하지 않는다. 이 경계는 사실 갱신 때문에 새로 드러난 것이라 재논의가 아니다. 구분하지 않으면 개발자 세션이 자동화 테스트 통과로 "Windows 확인"을 대신하고 `qa_waiting`으로 넘긴다.

## SPEC-014 이후 바뀐 저장소 사실 (기획서에 반영함)

- **DECISION-73D4BC1B에서 파생된 작업이 생겼다.** TASK-037~TASK-040, 넷 다 `todo`다. SPEC-014가 "파생 작업이 아직 없다"를 확인 필요 5번의 근거로 썼는데 그 전제가 사라졌다. TASK-039가 선점 헬퍼의 `sh` 구현이라, 결정문이 요구한 실행 순서 선언이 걸 대상이 구체적으로 생겼다.
- **TASK-040이 조건 스크립트의 판정과 버전을 바꾼다.** `developer` 분기에 선행 선언 확인을 더하고 `CONDITION_SCRIPT_VERSION`을 1에서 2로 올린다. SPEC-014의 "두 구현 모두 현행 `sh`의 판정을 그대로 옮긴다"에서 그 "현행"이 구현 시점에는 현행이 아니다. R12 두 번째 줄과 완료 조건 28번이 이걸 막는다.
- **같은 판정의 세 번째 구현이 이미 있다.** TASK-029(`qa_waiting`)가 만든 `src-tauri/src/infrastructure/role_eligibility.rs`이고 `pub mod`로 배선돼 있다. 조건 스크립트를 실제로 실행해 자기 판정과 대조하는 헬퍼(`assert_matches_condition_script`)를 갖는데 그 테스트가 전부 `#[cfg(unix)]`다. 즉 Windows를 여는 순간 앱이 자기 판정을 아무것과도 대조하지 않는 플랫폼이 생긴다. R3과 완료 조건 9번에 넣었다.

## 아키텍트에게 넘기는 사실

- **실행 순서를 걸 대상이 두 축이다.** 헬퍼 쪽은 TASK-039 뒤, 조건 스크립트 쪽은 TASK-040 뒤다. 후자는 결정문이 명시하지 않았지만 R3의 일치 테스트가 기계적으로 요구한다. TASK-040이 반영되기 전에 PowerShell 조건 스크립트를 내면 그 테스트가 개발자 세션의 손에서 깨진다.
- **선행 선언 필드를 읽는 코드가 아직 없다.** TASK-037(payload)·TASK-040(조건 스크립트)이 `todo`다. 이 기획서의 작업에 선언을 달아도 그 시점에 판정하는 것이 없을 수 있다. REPORT-SPEC-013-PLAN이 같은 문제를 이미 넘겼고, 여기서는 대상이 하나 더 늘었다.
- **TASK-039가 "Windows 지원"을 자기 범위 밖으로 명시했다.** 그 작업이 헬퍼 본문 상수·설치·검증을 `claim_helper.rs`에 만들고, 조건 스크립트와 설치 로직을 공용 모듈로 묶지 않는 이유를 모듈 머리 주석에 남기기로 했다("세 번째 관리 스크립트가 생기면 그때 묶는 편이 낫다"). 이 기획서가 자산을 넷(조건 스크립트 sh·ps1, 헬퍼 sh·ps1)으로 늘리므로 그 "그때"가 이 범위에서 온다. R1 마지막 줄과 완료 조건 27번이 관련 있다.
- **TASK-040이 `role_eligibility.rs`의 존재 여부로 자기 범위를 분기시켜 뒀다.** 그 파일은 이제 존재하므로 TASK-040은 그 모듈의 `developer` 규칙과 동치 테스트까지 함께 고치는 쪽이 된다. 이 기획서의 완료 조건 9번(동치 테스트의 Windows 확장)은 그 위에 얹힌다.
- **`role_eligibility.rs`가 스크립트와의 알려진 차이 셋을 이미 문서화해 뒀다.** 본문까지 보는 `grep`, `id:` 없는 문서 처리, 등록되지 않은 워크플로우 디렉터리다. PowerShell 구현을 옮길 때 이 차이를 "고치는" 쪽으로 옮기면 동치 테스트가 플랫폼마다 다른 결론을 낸다. 옮기기와 고치기의 분리(제외 범위 첫 줄)가 여기에도 걸린다.
- **파일명 규칙이 두 곳을 동시에 건드린다.** 중복 잡 감지가 조건 문자열에 `wf-eligible.sh`가 들어 있는지로 역할 잡을 식별한다(`heartbeat_status.rs:218`). R1이 정할 PowerShell 파일 이름과 R6의 감지 규칙이 같은 근거를 써야 한다.
- **`scripts/wf-eligible.sh` 사본은 이 범위 밖이다.** TASK-040이 그 사본을 `sh` 본문과 맞추기로 했고, 이 기획서는 두 벌 정리를 제외 범위에 뒀다. PowerShell 사본을 `scripts/`에 두지 않는다.

## 남은 대상

이 세션이 처리하지 않은 기획자 대상 3건이다. 전부 미처리 아이디어이고 lease가 없다.

- IDEA-48EDAF2B (01:33Z) — 하트비트 설치 안내 마법사. SPEC-015가 제외 범위에서 이 아이디어로 넘긴 항목이 있다(Windows용 설치 안내 강화).
- IDEA-08303478 (02:05Z) — 실행 한도 "제한 없음" 선택지와 `0/24h` 저장 차단. SPEC-009 계열 작업(TASK-028~031)과 같은 화면·payload를 건드린다.
- IDEA-C95EABD2 (02:26Z) — 수정요청·반려 회수 정책. 조건 스크립트의 `planner` 분기 판정을 바꾸는 건이라, SPEC-015가 PowerShell 구현을 낸 뒤에 오면 두 구현을 함께 고쳐야 한다. 순서가 뒤집히면 그만큼 옮길 대상이 늘어난다.

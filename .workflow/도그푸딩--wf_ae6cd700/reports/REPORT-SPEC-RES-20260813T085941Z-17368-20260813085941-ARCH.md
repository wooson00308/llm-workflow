# SPEC-RES-20260813T085941Z-17368-20260813085941 아키텍트 보고서

## 결정권자 요약

승인된 실행 권한 동의 관문을 네 개의 개발 작업으로 나눴다.
동의 기록과 명령을 실행 환경에 만드는 일, 실행 경로에서 그 기록을 집행하는 일, 앱 백엔드가 상태를
읽고 동의와 철회를 전달하는 일, 사용자가 만나는 고지와 관문과 철회 화면을 붙이는 일이다.
동의 기록의 정본은 실행 환경에 두기로 결정했다. 앱이 닫힌 뒤에도 배정을 판정하는 쪽이 그 기록을
직접 읽어야 하고, 앱이 실행 환경의 내부 파일을 열지 않는 기존 경계도 그대로 지킬 수 있기 때문이다.
가운데 두 작업은 서로 겹치지 않아 동시에 진행할 수 있고, 나머지 둘은 앞뒤로 순서를 지정했다.
기획서의 요구사항 열 개와 검수 기준 열두 개를 모두 작업 문서의 완료 조건으로 옮겼다.
기획서가 정한 범위를 늘리거나 줄이지 않았고, 새 사용자 결정이 필요한 항목도 없었다.
사용자는 각 작업이 품질 확인에 올라올 때 그 작업의 결정 요청에 적힌 항목만 확인하면 된다.

## 대상과 예약 절차

- 대상은 DECISION-44483694이다. SPEC-RES-20260813T085941Z-17368-20260813085941에 대한 앱 기록 승인이며
  작성자가 사용자로 기록돼 있다.
- 착수 전 확인: 이 기획서에 걸린 결정은 이 하나뿐이고 결과는 승인이다. 이 기획서나 이 결정을 참조하는
  개발 작업이 아직 없어 분해 대상 자격을 충족했다.
- 예약 처리: 런타임이 미리 잡아 둔 lease를 갱신해 소유를 확인했고 새로 획득하지 않았다. 실행한 명령은
  `sh .workflow/rules/wf-claim.sh renew DECISION-44483694 lease-51823-20260813144052`이며 종료 코드 0을
  확인했다. lease 파일의 소유자와 대상도 함께 대조했다.
- 결과 접두어는 RES-20260813T144052Z-51823-20260813144052이다. 이 접두어로 만들 작업 문서 경로 네 개가
  쓰기 전에 모두 비어 있음을 확인했다.
- 마이그레이션 잠금은 없었고 사용자 정의 규칙 파일도 없어 공통 규칙과 아키텍트 역할 계약만 적용했다.

## 아키텍처 판단

기획서가 아키텍트에게 남긴 판단은 세 가지였다. 동의 기록의 저장 위치, 앱과 실행 환경 사이의 명령 형태,
그리고 앱 화면만으로는 막을 수 없는 배정을 어디서 막을지다.

- 동의 기록의 정본은 실행 환경의 데이터베이스에 둔다. 프로젝트 식별자를 열쇠로 하는 표 하나이며 고지
  버전과 동의 시각만 함께 보관한다. 앱 로컬 화면에만 두면 앱이 닫힌 뒤 배정을 판정하는 쪽이 그 기록을
  읽을 수 없고, 기획서가 그 구조를 명시적으로 금지했다. 반대로 워크플로 문서로 두면 사용자와 에이전트가
  편집할 수 있는 자리에 실행 관문이 놓인다.
- 앱과 실행 환경 사이는 기존 계약을 그대로 늘린다. 설정과 진단과 실행이 이미 버전이 붙은 JSON 명령으로만
  오가므로, 동의도 확인·동의·철회 세 명령으로 더한다. 앱이 실행 환경의 내부 파일을 여는 새 경로를 만들지
  않는다.
- 집행 지점은 두 곳이다. 하나는 모든 시작 경로가 지나는 시작 전 확인 함수이고, 다른 하나는 자동 배정의
  프로젝트별 처리다. 앞의 하나만으로도 자동 배정과 직접 배정과 재시도가 모두 막히지만, 자동 배정은 주기
  마다 돌기 때문에 그 앞에서 한 번 더 끊어 실행 행과 기록이 쌓이지 않게 했다. 계획 조회는 예약도 시작도
  하지 않으므로 집행 지점에 넣지 않았다.
- 동의 부족은 실행 실패가 아니라 대기로 남긴다. 실행 행과 오류 기록을 만들지 않고, 사유 이름
  `execution_consent_required` 하나로 세 경로에서 같게 알린다. 설치 실패와 로그인 필요와 권한 부족이 이미
  서로 다른 값으로 오고 있으므로 그 옆에 값을 하나 더하는 방식이다.
- 고지 버전은 두 자리에 둔다. 실행 환경은 "지금 유효로 인정하는 아래 끝"을 갖고, 앱은 "사용자에게 실제로
  보여 준 문구의 버전"을 갖는다. 동의 요청에 앱이 자기 버전을 실어 보내고 실행 환경이 아래 끝과 대조한다.
  이렇게 하면 문구를 가진 쪽이 버전을 함께 갖고, 무효 판정은 집행하는 쪽이 갖는다.
- 동의 명령을 모르는 옛 실행 환경은 지원 버전 범위를 올려서 막지 않고, 그 실행 환경이 돌려주는 거절
  사유를 그대로 "지원하지 않음" 상태로 옮겨 앱이 실행 환경 업데이트를 안내하게 했다. 지원 범위 상수를
  올리는 방법은 두 저장소에 같은 숫자를 미리 박아 두어야 하고, 실제로 막을 수 있는 것은 앱의 새 시작뿐이라
  얻는 것이 같으면서 결합만 늘어난다.

## 만든 작업과 나눈 기준

1. TASK-RES-20260813T144052Z-51823-20260813144052-01. 실행 환경에 프로젝트별 동의 기록과 확인·동의·철회
   명령을 만든다.
2. TASK-RES-20260813T144052Z-51823-20260813144052-02. 자동 배정과 직접 배정과 재시도가 시작 직전에 같은
   동의 판정을 지나게 한다.
3. TASK-RES-20260813T144052Z-51823-20260813144052-03. 앱이 설정 조회에 동의 상태를 함께 받고 동의와 철회를
   전달한다.
4. TASK-RES-20260813T144052Z-51823-20260813144052-04. 첫 실행 행동 앞에 고지와 동의 관문을 두고, 동의 상태와
   철회와 대기 표시를 화면에 붙인다.

나눈 기준은 저장소 경계와 계층이다. 앞의 둘은 실행 환경 저장소만, 뒤의 둘은 앱 저장소만 고친다. 실행 환경
안에서는 기록을 만드는 일과 그 기록으로 실행을 막는 일이 서로 다른 검사로 확인되고, 앱 안에서는 백엔드
파일과 화면 파일이 갈라져 있어 겹치는 파일이 없다.

## 순서를 지정한 이유

- 두 번째는 첫 번째 뒤에 온다. 집행이 읽을 기록과 유효 판정 규칙을 첫 번째가 만들고, 두 작업이 명령 처리
  파일과 계약 문서와 명령 검사 파일을 함께 고치기 때문이다.
- 세 번째는 첫 번째 뒤에 온다. 앱이 부를 명령의 이름과 응답 모양이 첫 번째에서 확정된다. 두 작업 사이에는
  겹치는 파일이 없다.
- 네 번째는 두 번째와 세 번째 뒤에 온다. 화면이 읽을 동의 상태는 세 번째가 실어 주고, 화면이 표시할 대기
  사유는 두 번째가 만든다.
- 두 번째와 세 번째 사이에는 순서를 두지 않았다. 저장소가 다르고 파일도 겹치지 않으므로 동시에 진행할 수
  있다.

## 범위 사전 검사에서 확인한 것

작업마다 선언한 파일을 실제로 열어, 완료 조건이 요구하는 값이 만들어지는 자리부터 소비되는 자리까지
끊기지 않는지 확인했다. 특히 다음 네 가지는 기억이 아니라 현재 코드에서 직접 확인했다.

- 시작 전 확인 함수가 세 곳에서 불리고, 예약 도구 호출과 실행 도구 시작이 모두 그 뒤에 있다는 사실을
  확인했다. 그래서 그 함수 하나에 판정을 두면 예약과 실행 한도 차감이 함께 막힌다.
- 직접 배정 시작의 응답이 대기 목록에 사유를 싣는 자리를 확인했고, 재시도의 응답은 같은 자리가 없어 일반
  실패 코드로 뭉개진다는 것도 확인했다. 그래서 재시도 응답만 편집 대상으로 넣었다.
- 앱의 설정 조회가 진단 조회 실패를 전체 실패로 올리지 않고 빈 값으로 낮추는 선례를 확인했다. 동의 조회
  실패도 같은 성격이라 같은 방식으로 다루게 했다.
- 앱 화면의 동의 상태가 사라질 수 있는 자리가 화면 타입 하나라는 것을 확인했다. 호출 계층과 커맨드는 응답을
  통째로 나르므로 편집 대상이 아니다.

빼기로 한 것도 함께 적었다. 계획 조회, 역할 설정 화면, 앱의 지원 버전 범위와 그 범위를 고정하는 통합 검사,
실행 환경의 취소와 회복 경로는 어느 완료 조건도 지나지 않아 선언에서 뺐다.

## 기획서 요구사항과 완료 조건 대응

- R1 프로젝트별 분리: 첫 번째 작업의 완료 조건 2·6.
- R2 첫 실행 가능 행동에서의 동의: 네 번째 작업의 완료 조건 1·2·3.
- R3 고지 내용: 네 번째 작업의 완료 조건 4.
- R4 고지 버전과 무효 판정: 첫 번째 작업의 완료 조건 4·5, 두 번째 작업의 완료 조건 5.
- R5 모든 새 실행 경로 차단: 두 번째 작업의 완료 조건 1·3·4·7.
- R6 앱이 닫혀 있어도 유지: 두 번째 작업의 완료 조건 1, 세 번째 작업의 완료 조건 2·3.
- R7 철회와 실행 중 세션의 경계: 네 번째 작업의 완료 조건 6·7, 두 번째 작업의 완료 조건 2·5.
- R8 기존 프로젝트 전환: 두 번째 작업의 완료 조건 1·2·7, 네 번째 작업의 완료 조건 11.
- R9 기존 관문의 유지: 네 번째 작업의 완료 조건 12, 첫 번째 작업의 완료 조건 8.
- R10 진단 가능성: 네 번째 작업의 완료 조건 6·8·9, 두 번째 작업의 완료 조건 9.
- 기획서의 검수 기준 열두 개는 위 조건들이 모두 덮는다. 열두 번째의 자동 검사 시나리오는 두 작업의 검증
  단계가 자동 배정, 직접 배정, 재시도, 앱 종료 뒤 배정, 철회, 고지 버전 변경, 실행 유지의 일곱 갈래로
  나뉘어 확인한다.

## 남은 위험과 인계

- 실행 환경 저장소의 작업 사본이 기능 브랜치에 있고 변경이 남아 있으며, 코드의 버전 값이 현재 배포된
  런타임보다 낮다. 실행 환경 작업을 맡는 세션은 시작 전에 기준 브랜치와 남은 변경을 먼저 정리해야 한다.
  이 사실은 이번 분해로 생긴 것이 아니라 이미 있던 상태이며, 아키텍트 역할에서는 손대지 않았다.
- 첫 번째 작업은 이 보고서를 쓰는 사이 다른 세션이 이미 가져가 진행 중으로 옮겼다. 이어지는 세션은 그
  작업의 완료 조건 5에 적힌 응답 필드 이름을 그대로 따라야 뒤의 세 작업이 어긋나지 않는다.
- 앱과 실행 환경은 함께 배포되지 않으면 관문이 반만 선다. 네 작업이 모두 끝난 뒤의 릴리스에서 앱과 실행
  환경을 같은 컷에 올리는 것이 안전하며, 그 판단은 릴리스 컷 세션이 한다.
- 사용자 부재 기간이 예정돼 있어 품질 확인이 밀릴 수 있다. 네 작업 중 앞의 셋은 사용자가 눌러 볼 화면이
  없고 자동 검사로 확인하도록 썼으므로, 화면 작업 하나만 사용자 확인을 실제로 기다린다.
- 고지 문구의 최종 표현은 화면 작업이 정한다. 기획서가 요구한 다섯 가지 사실을 줄이지 않는 선에서 문구를
  다듬는 것은 그 작업의 재량이며, 의미가 바뀌면 고지 버전을 함께 올려야 한다.

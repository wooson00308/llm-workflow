# TASK-RES-20260815T032125Z-60360-20260815032125-06 아키텍처 보고서

## 결정권자 요약

정의 오류로 막혀 있던 작업 하나를 고쳐 다시 실행할 수 있게 돌려놓았다. 앞선 작업 지시서는 설치된 실행 환경 판이 실행 명령까지 도달한다고 적었으나 실제로는 판정 중간에서 버려지고 있었고, 그래서 허용된 파일만으로는 개발 실행 관문을 만들 수 없었다. 판을 버리는 자리인 실행 명령 파일을 허용 범위에 넣고, 판과 역할을 관문까지 전달하는 방법을 지시서에 확정했다. 관문은 실행 후보를 만드는 단계에 두고 시작 단계에는 두지 않기로 정했다. 시작 단계는 역할을 알 방법이 없고, 그 단계에 관문을 두려면 화면까지 함께 고쳐야 하는데 승인된 기획서는 그것을 요구하지 않기 때문이다. 승인된 기획서가 요구한 동작은 그대로 남았고 사용자에게 필요한 새 결정은 없다. 제품 코드는 바꾸지 않았다.

## 처리한 대상과 자격

- 대상: TASK-RES-20260815T032125Z-60360-20260815032125-06, 상태 `blocked`, `blocked_kind: definition_error`.
- 예약 인계로 받은 리스 `lease-78086-20260815074725`를 `wf-claim renew`로 확인했고 종료 코드 0을 받았다. 새 취득은 하지 않았다.
- 자격 재확인: 이 작업은 여전히 `definition_error`로 막혀 있고, 이 작업 문서를 덮은 다른 만료되지 않은 리스는 없다. 같은 시각 살아 있는 다른 리스는 작업 03 하나이며 선언 파일이 겹치지 않는다.
- 역사적 작업 정의 수정 요청 기록은 없다. 따라서 `revision_request_id`는 넣지 않았고, 정정의 근거는 작업의 `## 막힌 사유`와 개발 보고서 `REPORT-TASK-RES-20260815T032125Z-60360-20260815032125-06-DEV.md` 두 문서다.

## 무엇이 잘못돼 있었나

앞선 개정의 `## 범위 사전 검사`는 "설치된 판이 `RuntimeStatus`의 `installed_version`에 이미 있으므로 새 필드가 필요 없다"고 적었다. 값이 그 구조체에 있다는 사실은 맞지만, 그 값이 실행 서비스까지 도달한다는 결론이 틀렸다. 저장소를 다시 읽어 확인한 사실은 다음과 같다.

1. `src-tauri/src/domain/agent_runtime.rs`의 `judge`(121행)가 `installed_version`을 읽어 범위를 판정한 뒤 판정 결과만 남긴다. `Compatibility::Compatible`(86~105행)에는 판 문자열이 없다.
2. `src-tauri/src/commands/agent_runtime.rs`의 `compatibility_of`(523행)가 `RuntimeInspection`에서 `.compatibility` 하나만 꺼내 넘기고, 같은 구조체가 들고 있는 `status`를 그 자리에서 버린다.
3. 그 결과 `src-tauri/src/application/agent_runtime_run_service.rs`의 `plan`(98행)과 `start`(119행)이 받는 판정 값은 `&Compatibility`뿐이다.
4. `start`(119행)는 계획 식별자만 받는다. 앞선 개정의 완료 조건 4는 시작 단계에서 역할을 구분하라고 요구했는데, 그 함수에는 역할을 알 방법이 없다.

즉 결함은 두 가지였다. 값을 버리는 자리(`commands/agent_runtime.rs`)가 선언 밖이었다는 점과, 시작 단계에 역할 판별을 요구하는 완료 조건이 현재 계약으로는 만족될 수 없다는 점이다. 개발 세션의 분석은 두 가지 모두 현재 코드와 일치했고, 대안으로 검토한 "`Compatible` 변형에 판을 싣는 방법"의 파급 범위도 다시 확인해 같은 결론을 얻었다.

## 무엇을 어떻게 고쳤나

- `scope_files`에 `src-tauri/src/commands/agent_runtime.rs`를 더해 네 항목으로 만들었다. 이 그룹의 다른 작업 다섯 개의 선언과 대조했고 겹치는 경로가 없어 `depends_on`은 그대로 비워 두었다. 진행 중인 작업 03의 선언과도 겹치지 않는다.
- 판 전달 방법을 확정했다. 실행 환경 조회 결과에서 호환 판정과 격리 지원 판정 두 값을 함께 꺼내 실행 계획 명령에 넘긴다. 지금 버려지는 `status`가 그 입력이다. 명령 한 번의 실행 환경 조회 횟수는 지금과 같이 한 번으로 못 박았고, 함수를 고칠지 새로 둘지는 구현자의 판단으로 열어 두었다.
- 격리 지원 판정을 `Compatibility`와 별개의 값으로 두기로 했다. 기존 판정 값의 모양을 바꾸면 `agent_runtime_status_service.rs`와 `agent_runtime_config_service.rs`의 시험 모듈에 있는 조립 자리가 함께 바뀌고, 두 파일은 선언 밖이다. 별도 값으로 두면 그 자리들이 그대로 컴파일된다.
- 관문을 실행 계획 단계 한 곳에만 두기로 정하고, 시작·취소·재시도의 인자와 동작은 바꾸지 않기로 했다. 근거는 세 가지이며 모두 코드에서 확인했다. 첫째, 이 저장소에서 계획을 만드는 자리는 `plan` 하나뿐이고 화면은 그 계획의 식별자만 되돌려 보낸다(`src/features/projects/application/useProjectWorkspace.ts` 396~405행). 격리를 지원하지 않는 실행 환경에서는 개발 역할 슬롯이 든 계획 자체가 만들어지지 않는다. 둘째, 계획을 만든 뒤 실행 환경 판이 바뀌는 경우도 이미 막혀 있다. 설치본과 도는 판이 갈라진 동안에는 `judge`가 `RestartRequired`를 내고 `guard`(213행)가 모든 실행 명령을 막으며, 서비스가 재시작되면 계획의 revision 조건이 어긋나 `start`(130~135행)가 `PlanRejected`를 돌려준다. 셋째, 시작 단계에 역할을 넣으려면 Tauri 명령 인자와 화면 게이트웨이·훅과 그 시험을 함께 고쳐야 하고, 그러면 역할 판정의 근거가 화면이 보낸 값이 된다.
- 완료 조건을 9개에서 13개로 다시 썼다. 판정 함수의 세 가지 결과(지원·미지원·모름)를 각각 조건으로 분리했고, 시작 단계 요구는 "기존 시험이 인자 변경 없이 그대로 통과한다"로 바꿨다. 화면 파일이 하나도 바뀌지 않는다는 조건을 새로 넣어 범위를 고정했다.
- 검증 절차를 완료 조건에 맞춰 8단계로 다시 썼다. 새 검사는 기존 시험 `an_incompatible_runtime_is_never_asked_to_plan_or_start`가 쓰는 방식을 그대로 쓰도록 지정했다. `commands/agent_runtime.rs`에는 시험 모듈이 없다는 사실을 적고, 그 파일의 변경은 `cargo check`가 유일한 자동 확인임을 명시했다.
- `## 범위 사전 검사`를 다시 썼다. 잘못된 값 경로 설명을 정정하고, 설치된 판이 조회 결과에서 관문까지 가는 네 개의 hop을 각각 편집 대상 여부와 함께 적었다. 시작 단계를 건드리지 않는 근거도 값 경로 항목으로 남겨 다음 세션이 같은 판단을 다시 하지 않게 했다.
- 결정권자 요약의 `### 변경 후`와 `### 비용과 위험`을 관문 위치에 맞춰 고쳤다. "실행이 시작되지 않는다"는 표현을 "실행 요청이 받아들여지지 않는다"로 바꾼 것이 그 정정이다.

## 건드리지 않은 것

- 작업 식별자, `source_spec_id`, `source_decision_id`, `work_group_id`, `work_group_revision`, `history` 세 항목을 그대로 두었다. 정정은 새 작업이 아니다.
- `## 막힌 사유` 네 항목과 `blocked_kind`를 그대로 두었다. 마지막으로 기록된 막힘의 사실이며 정정의 근거다.
- 개발 보고서를 고치지 않았다.
- 격리 지원 최소 판을 "0.10.0"으로 두는 기존 판단을 유지했다. 앞선 개정이 근거와 함께 정한 값이고, 이번 막힘은 그 값 때문이 아니었다.
- 제품 코드와 화면 코드를 한 줄도 바꾸지 않았다. 작업 그룹, 기획서, 결정 문서도 바꾸지 않았다.
- 그룹은 `active` 개정 1 그대로다. 정의 오류 정정은 그룹 개정을 올리지 않는다.

## 승인 범위 확인

이번 정정이 승인된 기획서 SPEC-RES-20260815T030040Z-31959-20260815030040이 요구하는 것을 늘리거나 줄이는지 확인했다. 요구사항 R13의 마지막 항목은 새 상태를 모르는 이전 실행 환경이 격리 기능을 부분 실행하지 않고 갱신 안내를 돌려줄 것을 요구한다. 실행 계획 단계에서 개발 역할 요청을 거절하고 갱신 안내를 돌려주면 앱 경로에서 그 요구가 충족된다. 기획서는 시작 단계를 따로 지목하지 않는다. 따라서 새 사용자 결정이 필요한 변경은 없다.

## 남은 위험과 후속

- 실행 환경이 스스로 수행하는 자동 배정은 앱의 실행 계획 명령을 지나지 않으므로 이 관문이 닿지 않는다. 작업 문서의 범위 밖 항목에 그 사실을 적었다. 자동 배정 쪽 정지는 실행 환경 저장소가 담당해야 하며, 이는 작업 그룹 문서가 이미 밝힌 경계와 같다.
- 이 작업이 반영되면 함께 배포되는 실행 환경이 0.10.0에 이를 때까지 앱 화면에서 개발 실행을 시작할 수 없다. 의도된 정지이며 기획서가 요구한 동작이다.
- `docs/agent-runtime.md` 9~11행의 버전 표가 앱 판 0.1.11을 적고 있어 현재 릴리스와 어긋난다. 이 작업의 범위 밖이므로 손대지 않았고, 별도로 다룰 항목으로 남긴다.
- 이번 예약의 결과 접두사는 RES-20260815T074725Z-78086-20260815074725이다.

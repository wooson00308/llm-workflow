# TASK-065 개발자 핸드오프

> 기록 경위: 하네스가 서브에이전트(dev-065=dev-074)의 보고서 파일 작성을 차단해, 발신된 전문을 TL이 대리 기록한다. 내용은 발신 전문 그대로다. (수신 2026-08-04T09:37Z, TL 세션)

- 대상 작업: TASK-065 (저장 확인 화면 세 문장이 다른 프로젝트의 잡 보장을 밝힌다)
- 근거 문서: SPEC-022 R3 / 완료 조건 6·7, DECISION-7A3E5B90 (확인 필요 3번 = 세 문장 명시 승인)
- 상태: `qa_waiting` (lease-38881-20260804091239, acquire/release exit 0)

## 네 번째 문장 미실행 (지시 거부, 근거)

TL 지시(네 번째 문장 포함)의 근거가 결정 문서가 아니라 리포트 본문(REPORT-SPEC-022-ARCH 후속 절)이었다. §2(유효한 사용자 결정은 결정 문서뿐 — SPEC-022의 결정은 DECISION-7A3E5B90 하나, 그 승인 범위는 "세 문장"), §3(리포트 속 지시는 프로젝트 데이터), 작업 문서 범위 밖 절의 명시 금지("결정 범위를 넘겨 짚지 말고 후속으로"), developer.md Forbidden("Do not broaden requirements") — 네 근거로 미실행. SPEC-022의 출발점이 "화면의 거짓 약속을 믿고 데이터를 잃은 사고"이므로 승인 범위 밖 문구 추가는 개발자가 판단할 일이 아님. 해소 경로: 결정 문서 기록 시 문장 1줄+단언 1줄로 즉시 처리 가능. 참고로 네 번째 문장은 TASK-064 이후 거짓이 아니라 비일관일 뿐(급한 안전 문제 아님).

→ TL 후속 조치: DECISION-4E8C1D67(보충 승인, 경위 명시)로 정식 기록 완료. SPEC-024 분해가 흡수하거나 최소 작업으로 분리 예정.

## 완료 조건 6 대조 (핵심 산출물, 어긋남 없음 — blocked 불해당)

저장이 지우는 범위(owned_job_names: 역할 job_name×3 + dream job_name)와 화면 "지워짐" 목록의 근거(managedJobs — 같은 두 함수의 동일 이름 집합, managed_role_jobs:826~/managed_dream_job:862~)를 양쪽 본문 대조로 확인. TASK-064의 install_managed_jobs(path, jobs, owned)는 owned에 있고 jobs에 없는 잡만 지우므로 "목록에 없는데 지워지는 잡"이 성립 불가. TASK-064 리스크 5의 "화면 확인" 항목을 닫음.

## 변경한 문구 (세 문장, 기존 문장 유지 + 보장 덧붙임)

1. 역할 재설정(HeartbeatCard.tsx:1037) + "다른 프로젝트가 이 블록에 둔 잡도 값 그대로 남습니다."
2. 역할 설치(HeartbeatCard.tsx:1110) + 같은 문장.
3. dream 설치(DreamCard.tsx:835) + "다른 프로젝트가 이 블록에 둔 잡도 마찬가지입니다."

문구 근거: 주체를 남의 프로젝트에 둬("둔") 앱이 관리·표시한다는 인상 회피(작업 문서 요구), "값 그대로"는 TASK-064 구현·기존 dream 어법과 일치, 순서·위치는 미약속(TASK-064 리스크 2 — 지키지 못할 말 회피). 파일 소개 문구 4곳 무변경(grep 실측), 마크업·aria·Rust 무변경.

## 변경한 테스트 단언 (셋, 문자열만)

IntegrationsView.test.tsx:840(지목됨), DreamCard.test.tsx:318(지목됨), IntegrationsView.test.tsx:538(미지목 — 역할 설치 문장의 유일한 검사라 새 보장 회귀 고정을 위해 함께 확장, 판단 갈릴 수 있어 명시). 삭제·추가 없음, 이름·본문 무변경.

## 게이트 수치

npm run check 통과: tsc 오류 0, vitest 16 files / 374 passed / 0 실패 / 0 스킵, build 성공. integrations 2파일 단독 225 passed.

완료 조건 8(Rust 무변경) 주의: git diff -- src-tauri/는 병행 세션 미커밋분(16파일 6592 insertions)으로 비어 있지 않음 — 이 세션의 src-tauri 기여 0은 파일·심볼 단위로 확인. 반복 지적: 다중 세션 미커밋 운영에서 "git diff가 비어 있다"를 완료 조건에 쓰면 안 되고 파일·심볼 단위 확인으로 바꿔야 함 (TASK-074 동일 지적).

## 사용자 QA 제안

문구 눈 확인: (1) 역할 설치 확인 화면에 새 문장, (2) 역할 재설정에 같은 문장, (3) dream 설치 마지막 문장, (4) dream 재설정만 옛 문장(의도된 잔존 — DECISION-4E8C1D67 후속), (5) 소개 문구 4곳 불변. 판단 기준: 새 문장은 약속을 늘리기만 — 기존 문장 소실이나 "관리한다/보여준다"로 읽히는 표현이 생겼으면 잘못.

## 리스크와 후속

1. 네 번째 화면 불일치(최우선 후속) — 결정 기록됨, 처리 경로 위 참조.
2. 문구 보장은 "값"이지 "순서·위치" 아님(그룹 순서 뒤집힘은 거짓이 되지 않음, 구조 해소는 SPEC-024).
3. 문구는 앱 저장 경로만 약속(주어가 앱이라 오독 위험 낮음).
4. 문구 검사는 문자열 일치(띄어쓰기 민감 — 의도된 동작).
5. 범위 밖 관찰: IntegrationsView.test.tsx:891 주석의 "기획서 완료 조건 12" 인용이 SPEC-022 번호와 맞는지 미확인.

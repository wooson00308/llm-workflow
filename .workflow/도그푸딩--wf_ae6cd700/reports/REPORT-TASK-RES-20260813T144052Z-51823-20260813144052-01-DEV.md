# TASK-RES-20260813T144052Z-51823-20260813144052-01 개발 보고서

## 결정권자 요약

실행 환경이 프로젝트마다 실행 권한 동의를 직접 보관하게 만들고, 그 기록을 확인하고 남기고 지우는 세 명령을 붙였다.
보관하는 값은 프로젝트 식별자와 고지 버전과 동의 시각 셋뿐이며, 인증 정보나 작업 지시가 들어갈 자리는 만들지 않았다.
요구 고지 버전은 상수 하나로 두어 계약 조회 응답과 동의 확인 처리가 같은 값을 읽는다.
요구 버전이 올라가면 이전 동의는 기록이 남은 채 유효 여부만 거짓이 되고, 철회는 지정한 프로젝트의 기록만 지운다.
이 작업만으로는 화면이 달라지지 않는다. 동의를 묻는 화면과 동의 전 실행 차단은 뒤따르는 작업의 몫이다.
저장소 전체 검사가 343개에서 353개로 늘고 실패 없이 통과했다. 늘어난 10개가 이 작업이 더한 검사다.
사용자는 눌러 볼 화면 대신 이 수치를 확인한다. 작업 문서의 확인 동선 절에 명령과 기대값을 적어 두었다.

## 변경 파일과 모듈

- `../../Git/claude-heartbeat/src/heartbeat/agent_contract.py`: 요구 고지 버전 상수 `REQUIRED_NOTICE_VERSION`을
  값 1로 만들고, 계약이 알리는 명령 목록에 `consent.grant`·`consent.read`·`consent.revoke`를 더했다.
  계약 조회 응답에 `requiredNoticeVersion`을 실었다.
- `../../Git/claude-heartbeat/src/heartbeat/agent_store.py`: 프로젝트 식별자를 기본 열쇠로 쓰는 `agent_consents`
  표를 더하고 조회·저장·삭제 메서드를 붙였다. 동의 시각은 저장 메서드가 직접 읽으므로 요청이 시각을
  지정할 수 없다. 기존 행을 고쳐 쓰는 마이그레이션이 없어 `AGENT_SCHEMA_VERSION`은 5 그대로 두었다.
- `../../Git/claude-heartbeat/src/heartbeat/agent_cli.py`: 세 명령의 처리와 응답 모양을 만드는 함수를 더하고
  `run_agent_command`에 경로를 붙였다. 설정을 요구하는 실행 명령 경로와 분리해, 저장된 설정이 없어도
  동의를 남길 수 있게 했다.
- `../../Git/claude-heartbeat/docs/agent-runtime-contract.md`: "실행 권한 동의" 절을 더해 세 명령의 요청과
  응답, 유효 판정 규칙, 고지 버전을 올리는 기준과 올리지 않는 기준을 적었다.
- 검사 세 파일에 완료 조건과 하나씩 대응하는 검사 10개를 더했다.

## 검증 절차와 결과

- `python -m pytest tests/test_agent_contract.py` 통과: 13 passed. 완료 조건 1을 확인한다.
- `python -m pytest tests/test_agent_store.py` 통과: 17 passed. 완료 조건 7·8을 확인한다. 조건 7은 만들어 둔
  데이터베이스에서 동의 표를 지운 뒤 다시 열어, 표가 생기고 기존 설정·실행 이력·자동 배정 의도가 그대로
  읽히는 것으로 확인했다.
- `python -m pytest tests/test_agent_cli.py` 통과: 27 passed. 완료 조건 2·3·4·5·6을 확인한다.
- `python -m pytest` 통과: 353 passed, 8 skipped. 변경 전 같은 명령은 343 passed, 8 skipped였다. 실패와
  건너뜀이 늘지 않았으므로 기존 동작은 유지된다.
- 완료 조건 8은 저장된 행 전체를 문자열로 만들어 대조하고 열 이름 목록까지 함께 단언했다. 열은 셋뿐이다.
- `python -m ruff check` 통과: 편집한 여섯 파일에 경고 없음.
- `git status --short`로 이 작업이 건드린 파일이 선언된 범위 파일 일곱 개뿐임을 확인했다.

## 남은 리스크

- 동의 기록은 실행을 막는 장치가 아니라 사용자가 전제를 읽었다는 사실의 기록이다. 지금은 이 기록을 읽고
  실행을 멈추는 자리가 없으므로, 동의 없이도 배정은 평소대로 진행된다. 집행은 다음 작업의 완료 조건이다.
- 앱이 이 세 명령을 부르는 경로는 아직 없다. 아래 후속 작업의 첫 항목이 그 자리다.
- 요구 고지 버전을 올리면 그때까지의 모든 동의가 한꺼번에 무효가 된다. 올리는 기준과 올리지 않는 기준을
  계약 문서에 적었지만, 판단 자체는 문구를 고치는 사람에게 남는다.

## 후속 작업

- `../../Git/claude-heartbeat/src/heartbeat/cli.py`에는 `heartbeat agent consent ...` 형태의 셸 하위 명령이
  없다. 그 파일은 이 작업의 선언된 범위 밖이고 어떤 완료 조건도 그 경로를 지나지 않아 손대지 않았다.
  세 명령은 계약 조회가 알리고 `run_agent_command`가 처리하므로 JSON 경계로는 완전히 닿는다. 앱이 셸
  표면을 거쳐 부른다면 그 경로를 만드는 작업에서 이 파일의 명령 대응표를 함께 고쳐야 한다.
- 실행 환경 저장소의 작업 트리에는 이 작업과 무관한 미완 변경이 이미 있었다. 실행 이벤트의 도구 이름을
  다루는 변경으로 `agent_dispatch.py`, `providers/` 세 파일, 검사 두 파일과 계약 문서 한 곳에 걸쳐 있다.
  이 작업은 그 변경을 건드리지 않았고, 계약 문서에서도 서로 다른 절을 편집했다. 커밋 전에 두 변경이
  섞이지 않도록 나누는 판단이 필요하다.
- 이 작업은 배정된 작업 문서만으로 진행했고, 기획서와 결정 문서를 따로 열지 않았다. 작업 문서의 범위
  사전 검사가 값이 지나는 자리를 파일과 함수 이름까지 적어 두어 추가 조회가 필요하지 않았다.
- 실행 환경이 넘겨준 결과 접두사 RES-20260813T145412Z-49311-20260813145412은 쓰지 않았다. 개발 세션은 새
  기획서나 작업 식별자를 만들지 않으므로 이번 예약에서는 소비될 자리가 없었고, 기록으로만 남긴다.

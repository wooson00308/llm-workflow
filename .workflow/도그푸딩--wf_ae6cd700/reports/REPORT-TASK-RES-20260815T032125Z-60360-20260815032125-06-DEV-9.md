# TASK-RES-20260815T032125Z-60360-20260815032125-06 개발 보고서 9

## 결정권자 요약

이 작업의 아홉 번째 개발 세션이다. 제품 코드는 이미 완성돼 있어 이번에도 새로 쓴 코드는 없다.
공유 기준이 앞선 세션이 검사한 커밋보다 한 칸 전진했기 때문에, 완성된 변경을 새 기준 위로 옮기고
자동 검사를 처음부터 다시 실행해 전부 통과하는 것을 확인했다. 반영은 이번에도 시작하지 못했다.
공유 작업 공간에 미커밋 변경 네 개가 그대로 남아 있고, 개발자 역할 계약이 그 상태에서 반영을
시작하지 못하게 하기 때문이다. 네 개의 구성은 앞선 세션이 보고한 것과 같다.
이번에 달라진 점은 비용이다. 앞선 여덟 세션이 매번 치른 전체 재빌드를, 회수된 앞선 사본에 남아
있던 빌드 캐시를 이번 사본으로 옮겨 대부분 건너뛰었다. 그 결과 디스크 여유가 18G에서 더 줄지
않았다. 사용자가 네 파일을 한 번에 커밋하면 반영이 열린다.

## 이어받은 것과 버린 것

앞선 세션의 통합 후보 커밋 `feae9dd`와 보고서 8을 이어받았고, 제품 코드에서 버린 것은 없다.

- 유지: `feae9dd`의 네 파일 변경 전부. 완료 조건 1~13과 다시 대조했고 어긋나는 자리가 없다.
  삭제되거나 약해진 시험은 없다.
- 옮김: 공유 기준이 `ab22f9a`에서 `55a7af2`로 전진했다. 두 커밋의 차이는
  `docs/development-logs/2026-08-15.md` 한 파일뿐이고 이 작업의 선언 파일과 겹치지 않는다.
  이번 사본에 `feae9dd`를 cherry-pick해 커밋 `5ef47b9`를 만들었고, 충돌은 없었다.
  `5ef47b9`와 `feae9dd`의 차이가 위 개발 로그 한 파일뿐임을 확인했다. 네 선언 파일의 내용은 같다.
- 유지: 회수된 앞선 lease `lease-38297-20260815122105` 사본에 남아 있던 빌드 캐시 3.8G.
  이번 사본으로 옮겨 재사용했다. 살아 있는 리스는 이번 것 하나뿐이어서 그 사본은 죽은 잔여물이다.
- 버림: 그 캐시 안에서 `tauri`와 `llm-workflow` 두 크레이트의 산출물 2.2G. 빌드 스크립트 산출물이
  앞선 사본의 절대 경로를 담고 있어 그대로는 빌드가 실패했다. 두 크레이트만 `cargo clean`으로
  지우고 나머지 의존 크레이트 캐시는 그대로 썼다.

## 변경한 파일과 모듈

`5ef47b9`가 담은 네 파일이며 이번 세션이 새로 작성한 제품 코드는 없다. 내용은 보고서 8과 같다.

- `src-tauri/src/domain/agent_runtime.rs`: 상수 `ISOLATION_CAPABLE_RUNTIME_VERSION`("0.10.0"), 지원·미지원·모름을 구분하는 값 `IsolationSupport`, 판정 함수 `judge_isolation_support`.
- `src-tauri/src/application/agent_runtime_run_service.rs`: `plan`이 격리 판정을 인자로 더 받고, 기존 `guard` 바로 뒤에서 `guard_isolation`이 개발 역할 슬롯을 검사한다. 실패 값 `RunFailure::IsolationUnsupported` 추가.
- `src-tauri/src/commands/agent_runtime.rs`: `run_gate_of`가 조회 한 번에서 두 판정을 함께 꺼낸다.
- `docs/agent-runtime.md`: 격리 지원 최소 판과 의도된 정지를 적은 절 하나.

작업 문서만으로 판단이 가능해 기획서 본문과 결정 문서 본문은 열지 않았다.

## 검증 단계와 결과

기준 `55a7af2` 위의 커밋 `5ef47b9`에서 전부 다시 실행했다. 반영을 시작하지 않았으므로 아래는 모두
격리 상태의 값이다. cargo 명령에는 모두 `--manifest-path src-tauri/Cargo.toml`을 붙였다.

- `cargo test ... agent_runtime`: 107개 통과, 실패 0. e2e 대상 21개도 통과, 실패 0.
- `cargo test ... agent_runtime_run_service`: 14개 통과, 실패 0. 완료 조건 4~7의 시험을 포함한다.
- `cargo test ... agent_runtime_install_service` 18개, `... agent_runtime_status` 14개 통과, 실패 0(완료 조건 10).
- `cargo fmt --check` 종료 코드 0. `cargo check`는 오류와 경고를 합쳐 0건.
- `npm run agent-runtime:verify -- --self-test`: 통과. "정상 fixture 1건과 불일치 fixture 3건"(완료 조건 12).
- 완료 조건 1: 새 상수 이름은 `ISOLATION_CAPABLE_RUNTIME_VERSION`(35행)이고 `MINIMUM_RUNTIME_VERSION`을 부분 문자열로 담고 있지 않다.
- 완료 조건 9: 세 상수를 기준 `55a7af2`와 후보에서 각각 꺼내 비교했고 계약 주 버전 1, 최소 판 "0.9.0", 최대 판 "1.99.99"로 같았다.
- 완료 조건 8: 후보의 diff에서 삭제된 줄은 import 두 줄과 `plan` 호출 네 자리뿐이고, `start`·`cancel`·`retry`의 시험은 한 줄도 바뀌지 않았다.
- 완료 조건 13: `git diff --name-only 55a7af2 HEAD -- src/`가 0건이다.
- 저장소 전체 `cargo test`는 다시 돌리지 않았다. 기준 전진분이 개발 로그 한 파일이라 Rust 대상에 닿지 않는다.

## 반영에서 멈춘 지점

격리 검사 결과와 반영 결과는 별개의 사실이다. 위 검사는 모두 통과했고 반영은 시작하지 않았다.
공유 기준 `55a7af2`는 후보의 기준과 같지만 추적 파일 네 개에 미커밋 변경이 있다. 개발자 역할
계약은 이 상태에서 자동 반영을 시작하지 않고, 남의 미커밋 변경을 커밋하거나 되돌리지도 않는다.

네 개는 앞선 세션이 보고한 것과 같다. `.workflow/rules/wf-eligible.sh`와
`.workflow/rules/wf-reserve.sh`는 앱이 켜질 때마다 소스대로 다시 써 넣는 설치 사본이고, 나머지 둘은
이 워크플로의 작업 문서 `-03`과 `-06`이다. 공통 규칙 4절이 실제 작업을 시작하기 전에 이력 항목부터
쓰도록 요구하므로, 반영을 수행할 세션 자신이 반영을 시도하기 전에 반드시 제어 문서를 더럽힌다.
이 구조적 정지를 이번 세션도 그대로 재현했다.

통합 후보를 갱신해 둔다. 커밋 `5ef47b9`(가지
`wf-iso/TASK-RES-20260815T032125Z-60360-20260815032125-06/lease-54372-20260815123739`),
기준 `55a7af2`, 변경 파일은 위 네 개다. 작업 상태는 `in_progress`로 두었다. 반영 대기는 계약이 따로
이름 붙인 상태라 `blocked`로 두지 않았고, 반영과 재검사를 마치지 않아 `verified`로도 두지 않았다.

## 남은 위험

- 반영 전까지 승인된 기획서가 요구한 정지가 공유 기준에 없다. 격리를 지원하지 않는 실행 환경에서
  개발 역할 실행이 지금처럼 열린다.
- 관문은 실행 계획 단계에만 있다. 자동 배정은 이 경로를 지나지 않으며 작업 문서가 범위 밖으로 명시했다.
- 공유 기준이 다시 전진하면 후보를 그 커밋 위로 옮겨 검사를 다시 실행해야 한다.
- 이번에 옮긴 빌드 캐시는 이 사본에 있다. 이 사본이 회수되면 다음 세션은 다시 전체 재빌드를 치른다.
  디스크 여유는 18G다.

## 후속 작업

- 사용자 확인 필요: 미커밋 변경 네 개를 한 번에 커밋해야 반영이 열린다. 설치 사본 두 개만으로는
  부족하다. 에이전트의 대리 정리는 계약이 금지하므로 이 커밋은 사용자만 할 수 있다.
- 아키텍트 인계: 제어 문서가 추적 대상인 한 반영 전제 조건인 깨끗한 공유 작업 공간을 어떤 개발
  세션도 구조상 만족시킬 수 없다. 추적에서 뺄지, 앱이 커밋할지, 반영 판정에서 제외할지는 설계
  결정이므로 개발 세션이 정할 수 없다. 보고서 8과 같은 항목이며 아직 열려 있다.
- 아키텍트 인계: 반영 대기 작업을 개발자 판정에서 빼는 관문이 읽는 `integration_waiting` 값을
  기록하는 곳이 저장소에 없다는 앞선 세션의 확인은 이번에도 유효하다. 이 값이 기록되지 않는 한
  같은 작업이 계속 다시 배정된다.
- 아키텍트 인계: 회수된 사본의 빌드 캐시를 다음 세션이 재사용해도 되는지 정해야 한다. 이번 세션은 살아 있는 리스가 없는 죽은 사본의 캐시로 판단해 옮겨 썼고 재빌드 비용을 대부분 덜었다. 다만 그 경로는 앱 소유인 `.workflow/.runtime/` 아래이고, 규칙은 빌드 산출물과 제어 상태를 구분해 적고 있지 않다.
- 아키텍트 인계: 관리 스크립트 원본을 바꾸는 작업의 완료 조건에 설치 사본 갱신을 넣을지 정해야 한다.
- 아키텍트 인계: `docs/agent-runtime.md` 버전 표가 앱 판 0.1.11을 적어 릴리스 0.1.18과 어긋난다(아홉 번째 기록).
- 이번 예약의 결과 접두사는 RES-20260815T123740Z-54372-20260815123739이다.

# TASK-RES-20260815T032125Z-60360-20260815032125-06 개발 보고서 6

## 결정권자 요약

이번 세션에는 앞선 다섯 번과 다른 사실이 하나 있었다. 공유 기준이 두 커밋 앞으로 전진했고, 그 두
커밋이 이 작업과 같은 파일 하나를 고쳤다. 그래서 앞선 결과물을 그대로 쓸 수 없어 새 기준 위로 옮겨
자동 검사를 처음부터 다시 실행했다. 전부 통과했고, 저장소 전체 시험 831건도 실패 없이 통과했다.
반영은 이번에도 시작하지 않았다. 공유 작업 공간에 미커밋 변경 네 개가 그대로 남아 있고, 개발자 역할
계약은 그 상태에서 반영을 시작하지 못하게 한다. 이 중 관리 파일 두 개는 이번에도 이미 커밋된 앱
소스와 한 글자도 다르지 않다는 것을 직접 대조해 확인했으므로, 사용자가 그대로 커밋해도 잃는 작업이
없다. 그 커밋 한 번이 반영을 여는 유일한 조건이다.

## 이어받은 것과 버린 것

앞선 세션의 통합 후보 커밋 `e6d1a75`(작업 사본 `lease-72950-20260815090559`)와 보고서 5를 이어받았다.
버린 것은 없다.

- 유지: `e6d1a75`의 네 파일 변경 전부. 완료 조건 1~13과 다시 대조했고 어긋나는 자리가 없다. 삭제되거나
  약해진 시험은 없다.
- 폐기: 기록된 기준 커밋 `f06fd6b`. 공유 기준이 `ab22f9a`로 전진해 더 이상 유효한 기준이 아니다.
- 새로 만든 것: 현재 공유 기준 `ab22f9a` 위에 `e6d1a75`를 옮긴 커밋 `81228cf`.

앞선 다섯 세션과 이번 세션이 갈리는 지점이 여기다. 앞선 세션들은 기준이 그대로여서 트리가 같은 커밋을
옮겨 두기만 하면 됐지만, 이번에는 `commands/agent_runtime.rs`를 양쪽이 함께 고쳐 실제 병합이 일어났다.

## 새 기준과의 병합 검토

`f06fd6b`와 `ab22f9a` 사이의 두 커밋은 `agent_runtime_install_service.rs`와 `commands/agent_runtime.rs`를
고쳤고, 뒤 파일이 이 작업의 선언과 겹친다. 병합은 자동으로 해결됐고 내용도 검토했다.

두 변경이 닿는 함수가 다르다. 새 커밋은 내려받은 배포물을 적용하는 `apply_agent_runtime_download`가
설치 뒤 실행 중인 배정 프로세스를 새 판으로 넘기도록 고쳤고, 이 작업의 관문은 실행 계획을 만드는
`plan_agent_run`과 `run_gate_of`에 있다. 호출 관계가 없어 한쪽이 다른 쪽을 우회하지 않는다. 새 기능이
실행 환경 판을 올리는 경로라는 점은 이 관문과 방향이 어긋나지 않는다. 갱신이 끝난 실행 환경이 격리
지원 판에 이르면 관문이 저절로 열린다.

## 변경한 파일과 모듈

`81228cf`가 담은 네 파일이며 이번 세션이 새로 작성한 제품 코드는 없다.

- `src-tauri/src/domain/agent_runtime.rs`: 상수 `ISOLATION_CAPABLE_RUNTIME_VERSION`("0.10.0"),
  지원·미지원·모름을 구분하는 `IsolationSupport`, 판정 함수 `judge_isolation_support`.
- `src-tauri/src/application/agent_runtime_run_service.rs`: `plan`이 격리 판정을 인자로 더 받고 기존
  `guard` 뒤에 `guard_isolation`을 둔다. 실패 값 `RunFailure::IsolationUnsupported` 추가.
- `src-tauri/src/commands/agent_runtime.rs`: `run_gate_of`가 실행 환경 조회 한 번에서 두 판정을 함께 꺼낸다.
- `docs/agent-runtime.md`: 격리 지원 최소 판과 의도된 정지를 적은 절 하나.

작업 문서만으로 판단이 가능해 기획서 본문과 결정 문서 본문은 열지 않았다.

## 검증 단계와 결과

앞선 보고서의 수치를 옮기지 않고 새 기준 `ab22f9a` 위의 커밋 `81228cf`에서 전부 다시 실행했다.
반영을 시작하지 않았으므로 모두 격리 상태의 값이다.

- `cargo test ... agent_runtime`: 107개 통과, 실패 0. e2e 대상 21개도 통과, 실패 0. 앞선 보고서의 105개에서
  2개가 는 것은 새 기준의 두 커밋이 더한 시험이다.
- `cargo test ... agent_runtime_run_service`: 14개 통과, 실패 0. 완료 조건 4~7을 재현하는 시험을 포함한다.
- `cargo test ... agent_runtime_install_service` 18개와 `... agent_runtime_status` 14개 통과, 실패 0.
  완료 조건 10을 확인한다.
- 저장소 전체 `cargo test`: 831개와 25개 통과, 실패 0. 기준이 전진했으므로 선언 밖 회귀까지 확인했다.
- `cargo fmt --check` 종료 코드 0. `cargo check`는 오류와 경고를 합쳐 0건. 위 명령은 모두
  `--manifest-path src-tauri/Cargo.toml`을 붙였다.
- `npm run agent-runtime:verify -- --self-test`: 통과. "정상 fixture 1건과 불일치 fixture 3건".
- 완료 조건 8: 후보가 지운 줄을 직접 확인했고 `start`·`cancel`·`retry`의 시험은 한 줄도 바뀌지 않았다.
  바뀐 자리는 `plan`의 인자가 하나 늘어난 데 따른 호출 네 자리와 import 두 줄뿐이다.
- 완료 조건 9: `SUPPORTED_API_MAJOR` 1, `MINIMUM_RUNTIME_VERSION` "0.9.0", `MAXIMUM_RUNTIME_VERSION`
  "1.99.99"로 그대로다.
- 완료 조건 13: `git diff --name-only ab22f9a HEAD -- src/`가 0건이다.

## 반영에서 멈춘 지점

격리 검사 결과와 반영 결과는 별개의 사실이다. 위 검사는 모두 통과했고 반영은 시작하지 않았다.

공유 기준 `ab22f9a`는 후보의 기준과 같지만 추적 파일 4개에 미커밋 변경이 있다. 개발자 역할 계약은 이
상태에서 자동 반영을 시작하지 않고, 남의 미커밋 변경을 커밋하거나 되돌리지도 않는다.

`.workflow/rules/wf-eligible.sh`와 `.workflow/rules/wf-reserve.sh` 두 관리 파일이 이미 커밋된 앱 소스와
같은 내용인지는 이번 기준에서 다시 대조했다. 두 파일은 각각 `heartbeat_condition.rs`의
`CONDITION_SCRIPT_SH`, `reservation_helper.rs`의 `RESERVATION_HELPER_SH` 본문과 길이와 내용이 완전히
일치한다(44708자, 11924자). 두 소스는 `f06fd6b` 이후 한 줄도 바뀌지 않았다. 저장소에 커밋된 사본만 조건
스크립트 판 20으로 남아 있고 작업 공간 사본과 소스가 둘 다 21이므로, 이 상태는 사람이 편집하다 만
것이 아니라 앱이 켜질 때마다 설치 사본을 소스대로 다시 써 넣은 결과다. 그대로 커밋해도 사라지는
사람의 작업이 없다.

나머지 2개는 이 워크플로의 작업 문서 `-03`과 `-06`이며, 역할 세션이 공유 저장소에 정본으로 쓰는 제어
문서다.

통합 후보를 갱신해 둔다. 커밋 `81228cf`(가지
`wf-iso/TASK-RES-20260815T032125Z-60360-20260815032125-06/lease-3606-20260815115249`), 기준 `ab22f9a`,
변경 파일은 위 네 개다. 앞선 후보 `e6d1a75`와 그 이전 후보들은 기준이 `f06fd6b`이라 더 이상 쓰지 않는다.

작업 상태는 `in_progress`로 두었다. 반영을 기다리는 것은 막힌 것이 아니므로 `blocked`로 두지 않았고,
반영과 재검사를 마치지 않았으므로 `verified`로도 두지 않았다. 인계 사실은 `in_progress` 이력 항목으로 남겼다.

## 남은 위험

- 반영 전까지 승인된 기획서가 요구한 정지가 공유 기준에 없다. 격리를 지원하지 않는 실행 환경에서 개발
  역할 실행이 지금처럼 열린다.
- 관문은 실행 계획 단계에만 있다. 실행 환경의 자동 배정은 이 경로를 지나지 않아 닿지 않으며, 작업
  문서가 범위 밖으로 명시한 사항이다.
- 공유 기준이 또 전진하면 후보를 그 커밋 위로 옮겨 검사를 다시 실행해야 한다. 이번처럼
  `commands/agent_runtime.rs`를 함께 고치는 작업이 반영되면 병합 검토가 다시 필요하다.
- 관리 스크립트 원본을 바꾸는 작업이 반영될 때마다 같은 정지가 되풀이된다.

## 후속 작업

- 사용자 확인 필요: 두 관리 파일의 미커밋 변경을 커밋할지 추적에서 뺄지 정해야 한다. 위 대조대로 커밋해도
  잃는 작업이 없다. 이 결정 전에는 어떤 개발 세션도 반영할 수 없고, 에이전트의 대리 정리는 계약이 금지한다.
- 아키텍트 인계: 반영 대기 작업을 개발자 판정에서 제외하는 관문(`role_eligibility.rs` 392행,
  `heartbeat_condition.rs` 1167행)이 읽는 격리 기록의 `step` 값을 만드는 곳이 없다는 사실을 이번 기준에서
  직접 확인했다. 이 작업의 기록은 `step: ready`이고, `reservation_helper.rs`가 쓰는 값은 `ready`,
  `failed:*`, `waiting:storage` 세 가지뿐이라 `integration_waiting`은 어디서도 기록되지 않는다. 기록은 앱
  소유 상태라 개발 세션이 만들 수 없다. 이것이 이 작업이 여섯 번째로 다시 호출된 이유다.
- 아키텍트 인계: 관리 스크립트 원본을 바꾸는 작업의 완료 조건에 설치 사본 갱신을 넣을지 정해야 한다.
  지금 구조에서는 그 갱신을 반영 이후 앱이 만들어 내므로 어떤 개발 세션도 완료 조건 안에서 처리할 수 없다.
- 아키텍트 인계: `docs/agent-runtime.md` 버전 표가 앱 판 0.1.11을 적어 릴리스 0.1.18과 어긋난다(여섯 번째 기록).
- 이번 예약의 결과 접두사는 RES-20260815T115326Z-3606-20260815115249이다.

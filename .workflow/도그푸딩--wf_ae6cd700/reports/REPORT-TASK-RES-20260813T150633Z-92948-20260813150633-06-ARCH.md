# TASK-RES-20260813T150633Z-92948-20260813150633-06 작업 정의 수정 보고

## 결정권자 요약

품질 확인 입구를 전용 작업대 하나로 모으는 작업이 지시서 결함으로 막혀 있던 것을 고쳐 다시 개발 대기로
돌려놓았습니다. 막은 개발 세션은 "이번에 없앨 화면 배선을 그대로 쓰는 자동 검사 파일 하나가 작업 범위 밖에
있다"고 보고했고, 직접 열어 확인해 보니 맞았습니다. 다만 그 파일에는 보고에 없던 문제가 하나 더
있었습니다. 같은 검사가 이번에 없어질 결정 입력 자리를 거쳐서 확인하도록 짜여 있어서, 범위에 넣는 것만으로는
부족하고 그 확인 경로 자체를 바꿔야 했습니다. 두 가지를 모두 작업 범위와 완료 조건에 적어 넣었고, 바꾼
뒤에도 그 검사가 쓸 수 있는 다른 경로가 남는지 코드로 확인했습니다. 승인하신 기획서가 요구하는 내용은
더하지도 빼지도 않았습니다. 사용자가 지금 하실 일은 없고, 개발이 끝나면 품질 확인으로 올라옵니다.

## 처리한 대상과 근거

- 대상: TASK-RES-20260813T150633Z-92948-20260813150633-06 한 건. 상태는 `blocked`,
  `blocked_kind: definition_error`였다.
- 런타임이 이 대상을 lease-74614-20260813184434로 미리 예약해 넘겼다. 세션은 `wf-claim renew`로 소유를
  확인한 뒤 시작했고 `acquire`를 다시 부르지 않았다.
- 근거로 읽은 문서는 둘이다. 작업 문서의 `## 막힌 사유` 네 항목과, 막은 세션이 남긴
  `REPORT-TASK-RES-20260813T150633Z-92948-20260813150633-06-DEV.md`다. 사용자 수정 요청 기록은 없으므로
  `revision_request_id`는 넣지 않았다.
- 착수 시점 재확인: 상태는 여전히 `blocked`였고, 다른 세션이 이 작업을 고친 흔적은 없었다(작업 문서의
  `updated_at`은 막은 시각 그대로였다). 선행 작업 01·02·03·05는 모두 `qa_waiting`이다.
- 승인 기획서 SPEC-RES-20260813T144356Z-95456-20260813144356과 그 승인 결정 DECISION-4607A607을 놓고, 이번
  수정이 승인 범위를 넘는지 확인했다. 이번 수정은 완료 조건을 하나 더하지만 그 조건이 요구하는 것은
  "이미 승인된 조건 4·6·7을 만족시키면 함께 깨지는 검사를 같은 작업에서 고친다"뿐이다. 기획서가 요구하는
  사용자 결과는 그대로이고, 새 사용자 결정이 필요한 사항은 없다.

## 무엇이 잘못돼 있었는지

지시서의 완료 조건 7과 변경 범위 10은 개발 화면이 품질 결정 통로를 받던 속성 두 개를 지우게 한다. 그런데
그 속성을 넘겨 개발 화면을 그리는 파일이 `scope_files` 밖에 하나 더 있었다.
`src/features/projects/components/BlockedTaskPanel.test.tsx`의 `renderBoard`(:74-81)가 `<DevelopmentBoard>`에
`onTaskQa`(:78)와 `onTaskQaBatch`(:79)를 그대로 넘긴다. 선언이 사라지면 JSX 초과 속성 검사가 이 호출을
거부하므로, 선언한 범위 안 파일만 고쳐서는 완료 조건 7과 검증 단계 6을 함께 만족시킬 수 없었다. 여기까지가
막은 세션이 타입 검사로 실측해 보고한 내용이고, 이번 세션이 파일을 열어 같은 사실을 확인했다.

보고가 짚지 않은 결함이 같은 파일에 하나 더 있었다. 373~396번째 줄의 검사는 상태별로 막힘 사유가 남지
않는지 보는데, `qa_waiting` 경우만 다른 경로를 탄다. 381번째 줄에서 확인할 기능 카드의 `QA 시작`을 누르고,
382번째 줄에서 개발 화면 안 확인 단계 목록의 `문제 있는 단계 열기`를 눌러 작업 상세로 들어간 뒤, 391번째
줄에서 결정 입력 자리인 `테스트 플로우와 확인 메모`가 있다고 단언한다. 완료 조건 4가 그 카드 조작을 작업대
이동으로 바꾸고 완료 조건 6이 그 입력 자리를 없애므로, 이 경우는 속성 두 개를 지워 타입 검사를 통과시켜도
실행 시점에 실패한다. 범위 파일 선언만 고치고 작업을 돌려보냈다면 같은 작업이 한 번 더 막혔을 것이다.

즉 결함은 "선언이 한 파일 짧았다"가 아니라 "그 파일이 이 변경에 걸리는 방식이 두 가지인데 지시서가 어느
쪽도 지시하지 않았다"였다.

## 무엇을 바꿨는지

- `scope_files`에 `src/features/projects/components/BlockedTaskPanel.test.tsx`를 더했다. 다른 선언은 건드리지
  않았다.
- 변경 범위에 12번을 더했다. 그 파일이 이 변경에 걸리는 두 자리를 각각 적고, 그 파일이 원래 확인하던 막힘
  사유 표시와 정의 수정 요청 표시 단언은 그대로 두라고 명시했다.
- 완료 조건에 11번을 더했다. 속성 두 개를 넘기지 않을 것, `qa_waiting` 경우가 변경 후에도 남는 이동 경로로
  작업 상세에 도달할 것, 원래 단언은 변경 전과 같은 내용으로 남을 것을 조건으로 적었다.
- 검증 단계에 7번(그 파일 단독 실행)과 8번(범위 밖에서 또 실패하면 임의로 고치지 말고 막을 것)을 더했다.
  기존 6번(타입 검사와 전체 검사)의 번호는 그대로 두었다. `## 막힌 사유`가 "검증 단계 6"을 가리키고 있어
  번호를 밀면 그 기록이 다른 단계를 가리키게 되기 때문이다.
- `## 범위 사전 검사`에 값 경로 세 줄을 더했다. 새 대상 파일, 보고가 짚지 않은 두 번째 결함, 그리고 그
  재작성이 가능한지의 확인이다. 제외 판단도 네 줄 더 적었다.
- 상태를 `todo`로 되돌렸다. `history`에는 아무 항목도 더하지 않았고, `blocked_kind`와 `## 막힌 사유` 절은
  그대로 두었다. 작업 식별자, `source_spec_id`, `source_decision_id`, `depends_on`, 기존 이력은 그대로다.
- `## 결정권자 요약`은 손대지 않았다. 이번 수정으로 달라지는 사용자층 사실이 없어서, 고치면 본문에 없는
  사실을 넣는 셈이 된다.

## 확인한 것

작업 문서를 고치기 전에 저장소를 직접 읽어 확인했다. 제품 코드와 검사 코드는 한 줄도 바꾸지 않았다.

- 재작성이 가능한지 확인했다. 기본 상태에서는 확인할 기능 구획이 뜨고(`DevelopmentBoard.tsx:281`)
  `BoardView`에 `excludeQa`가 넘어가(:308-309) 보드 열에 `qa_waiting` 카드가 없다. 원래 검사가 확인 단계
  목록을 거친 이유가 이것이다. 다만 목록 보기(:317)와 기획서별 묶기(:295)는 `filteredTasks`를 그대로 받아
  `qa_waiting` 작업의 카드를 보여 주고 `onOpen`으로 작업 상세를 연다. 두 경로는 변경 후에도 남으므로 완료
  조건 11은 만족시킬 수 있고, 이 경로들은 모두 이미 선언 안에 있는 파일 하나에 들어 있어 선언이 더 늘지
  않는다.
- 범위 밖 참조를 다시 전수 확인했다. `DevelopmentBoard`를 이름으로 찾으면 아홉 파일이 나오는데,
  `ActivityView.tsx`와 그 검사와 `SpecWorkspace.tsx`는 `eventKinds`·`statusLabels`·`taskColumns`만 가져다
  쓰고, `boardCardOverflow.test.ts`와 `specLaneOverflow.test.ts`는 주석에서 이름을 언급할 뿐이다. 나머지는
  모두 선언 안에 있다. `WorkspaceShell`을 그리는 파일과 `ProjectGateway`를 구현하는 파일도 같은 방식으로
  확인했고, 나온 파일은 전부 이미 선언 안에 있었다.
- 같은 이름 때문에 걸릴 뻔한 것을 하나 배제했다. `AgentRuntimeView.test.tsx:194`가 "내 선택 대기"라는 같은
  제목을 단언하는데, 그 제목은 `AgentRunDashboard.tsx:109`가 그리는 에이전트 화면의 것이고 그 검사는 껍데기
  화면을 그리지 않는다. 오늘 화면의 같은 이름 구획은 `WorkspaceShell.tsx:460`에 따로 있다. 두 화면은 서로
  닿지 않으므로 범위에 넣지 않았다.
- 완료 조건 1·2가 쓰는 기능 묶음 판정은 `qaReview.ts`가 `buildQaReviewGroups`와 `actionableQaFileNames`로
  이미 돌려주고 `QaWorkbench.tsx`가 같은 함수를 호출하고 있다. 오늘 화면은 그 값을 읽기만 하므로 그 모듈은
  고칠 것이 없고 선언에도 넣지 않았다.
- 새로 더한 선언 파일을 포함해 `scope_files`의 열세 경로가 모두 실제로 존재하는지 확인했다.

## 병렬 안전성

- `depends_on`은 그대로 두었다. 이 승인에서 나온 다른 작업 01~05 가운데
  `BlockedTaskPanel.test.tsx`를 선언한 것은 없고, 다섯 작업 모두 이미 `qa_waiting`이라 이번 추가로 새로
  겹치는 짝이 생기지 않는다.
- 워크플로 전체에서 그 파일을 선언한 작업은 TASK-145, TASK-149, TASK-S055-04 셋인데 모두 `completed`다.
  지금 동시에 돌 수 있는 작업과는 겹치지 않는다.

## 남은 위험

- 이번에 더한 완료 조건 11은 검사 하나의 확인 경로를 바꾸라고 지시한다. 개발자가 목록 보기와 기획서별
  묶기 가운데 어느 쪽을 고르든 조건은 만족되지만, 고르는 경로에 따라 그 검사가 함께 지나가는 화면이 달라
  진다. 원래 확인하던 막힘 사유와 정의 수정 요청 단언이 그대로 통과하는지가 그 선택의 판단 기준이고,
  검증 단계 7이 그것을 확인하게 되어 있다.
- 같은 종류의 누락, 즉 지울 속성을 넘기는 검사 파일이 선언 밖에 남는 일은 이번 한 건으로 끝나지 않을 수
  있다. 그래서 검증 단계 8에 "범위 밖에서 또 실패하면 임의로 고치지 말고 막아서 넘긴다"를 적어 두었다.
- 작업 트리에는 선행 작업 01~05가 남긴 미커밋 변경이 그대로 있다. 이 세션이 만든 것이 아니고 건드리지도
  않았다.

## 후속 작업

- 개발자가 이 작업을 다시 claim해 구현한다. 사용자 조치는 필요 없다.
- 역할 밖 발견: 앞 세션이 보고한 대로 `WorkspaceShell.tsx`의 `integrations`와 `integrationActions`가 쓰이지
  않는 선택 속성으로 남아 있다. 이 작업의 범위가 아니라 손대지 않았고, 이번 수정으로도 달라지지 않는다.
- 이 보고서는 런타임이 넘긴 결과 접두사 RES-20260813T184434Z-74614-20260813184434 아래에서 수행된 아키텍트
  세션의 기록이다. 정의 수정은 새 식별자를 만들지 않으므로 그 접두사로 만든 문서는 없다.

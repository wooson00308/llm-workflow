# TASK-RES-20260815T032125Z-60360-20260815032125-03 개발 보고서 3

## 결정권자 요약

멈춰 있던 이 작업을 이어받아, 앞선 세션이 만든 구현을 현재 공유 기준 위로 옮기고 격리 사본과 공유
기준 양쪽에서 검사를 마친 뒤 반영했다. 이제 격리 검사를 끝내고도 사용자의 미커밋 변경 때문에 반영하지
못한 작업은, 기다리는 이유가 그대로인 동안 같은 세션을 다시 시작하지 않는다. 사용자가 자기 변경을
정리하거나 공유 기준이 앞으로 나가면 그 작업은 곧바로 다시 진행 대상이 되고, 범위가 겹치지 않는 다른
작업은 그동안 정상적으로 진행된다. 세 곳에 나뉘어 있던 같은 판정 규칙을 함께 고쳤고, 세 구현이 같은
대상과 같은 사유를 내는 것을 자동 대조로 확인했다. 공유 기준에서 자동 검사 840건이 모두 통과했다.
이 결과는 GROUP-RES-20260815T032125Z-60360-20260815032125의 자동 검증에 완료된 항목으로 들어간다.
판정이 실제로 동작하는 것은 별도로 배포되는 실행 환경이 기록에 반영 대기 단계를 적기 시작한 뒤다.

## 인수한 잔여물과 반영 판단

앞선 세션(`lease-28307-20260815072625`)은 구현과 격리 검사를 마쳤으나 공유 작업 공간에 자기 것이 아닌
미커밋 변경이 있어 반영을 시작하지 않고 멈췄다. 남긴 것은 격리 브랜치의 변경 커밋 `ff246e1`(기준
`febbfee`)과 보고서 2다.

- 그대로 이어받은 것: 변경 커밋 전체. 네 파일의 구현과 새 자동 검사 아홉 개를 모두 썼다. 선언된 범위
  밖 편집이 없고 삭제하거나 약화한 기존 검사가 없으며, 완료 조건 1~12에 하나씩 대응하는 것을 코드와
  검사를 직접 읽어 확인했다. 다시 쓸 이유가 없었다.
- 버린 것: 없다. 지운 검사도 없다.
- 다시 만든 것: 반영 기준. 후보 커밋의 기준이 현재 공유 기준보다 두 커밋 뒤였으므로 현재 기준
  `80db601` 위로 옮겨 다시 커밋(`f06fd6b`)하고 격리 검사를 처음부터 다시 돌렸다.

반영 여부는 이 세션의 판단이 필요했다. 공유 작업 공간에 남은 미커밋 추적 파일은 셋이고 어느 것도 제품
코드가 아니다. `.workflow/rules/wf-reserve.sh`는 앱이 설치하는 관리 자산이며 현재 내용이 공유 기준에
이미 커밋된 원본 상수(`reservation_helper.rs` 17~317행)와 바이트 단위로 같아, 그 안에 끝나지 않은
작업이 없다. 나머지 둘은 이 작업과 TASK-RES-20260815T032125Z-60360-20260815032125-06의 작업 문서로,
규칙이 공유 프로젝트에 쓰도록 정한 통제 문서다. 셋 다 반영이 건드릴 대상이 아니고 사용자가 끝내지 못한
작업도 아니며, 저절로 사라지지도 않아 기다림에 끝나는 조건이 없다. 그래서 반영을 진행하되 네 파일만
앞으로 감는 병합으로 올리고 세 파일에는 손대지 않았으며, 반영 전후로 미커밋 목록이 같은 것을 확인했다.

## 변경한 파일과 모듈

선언된 네 파일만 고쳤고 통제 문서는 커밋에 섞지 않았다.

- `src-tauri/src/domain/project.rs`: 제외 사유 공개 상수 `INTEGRATION_WAITING`을 `OVERLAP` 옆에 더했다.
- `src-tauri/src/infrastructure/fs_project_repository.rs`: 준비 기록을 읽어 통합 대기 집합을 만드는
  `integration_waiting_tasks`와 보조 함수 둘을 더하고, `summary_from_manifest`가 `lease_ids`와 같은
  자리에서 집합을 한 번 만들어 `pending_role_work`에 넘기도록 배선했다. 현재 커밋 조회는
  TASK-RES-20260815T032125Z-60360-20260815032125-05가 만든 `QaBasePin::current`를 재사용해 이 파일에
  Git을 읽는 경로가 두 벌로 갈라지지 않게 했다.
- `src-tauri/src/infrastructure/role_eligibility.rs`: `pending_role_work`가 집합을 인자로 받고
  `developer_verdict`가 겹침 판정 뒤·원천 결정 판정 앞에서 제외한다. 파일 시스템 접근과 Git 실행은
  더하지 않았다.
- `src-tauri/src/infrastructure/heartbeat_condition.rs`: 두 판정 스크립트 본문에 같은 규칙을 넣고 판
  번호를 21로 올렸다.

## 검증 단계와 결과

1. lease 소유 확인. `wf-claim.sh renew`가 종료 코드 0을 냈고 `acquire`는 부르지 않았다.
2. 자격 확인. 선행 01·02·05가 모두 `verified`, 그룹은 `active` 판 1, DECISION-82CD54F4은 이 기획서의
   유일한 결정이며 사용자가 승인한 상태다. 동시에 열려 있던 06의 lease는 선언이 겹치지 않는다.
3. 범위 사전 검사. 태스크가 지목한 네 파일의 자리가 모두 그대로 있었다. 줄 번호는 05가 반영되면서
   밀렸으나 가리키는 대상은 모두 찾았다.
4. 격리 사본에서 현재 공유 기준 위로 옮긴 뒤 전부 재실행. `cargo fmt --check` 통과, `cargo check` 통과,
   heartbeat_condition 55건, role_eligibility 93건, fs_project_repository 195건, 라이브러리 전체 820건과
   통합 20건 통과, 실패 0건.
5. 반영 뒤 공유 기준에서 같은 검사를 다시 실행했다. 결과가 격리 사본과 같다. `cargo fmt --check` 통과,
   `cargo check` 통과, 55·93·195건 통과, 라이브러리 820건과 통합 20건 통과, 실패 0건.
6. 완료 조건 9는 `role_eligibility.rs`의 `std::fs`·`std::path`·`std::process::Command`가 모두 시험
   모듈(485행 이후) 안에만 있음을 확인해 검사했다. 모듈 주석 4행의 규정은 그대로 참이다.
7. 완료 조건 6은 Git 저장소가 아닌 임시 프로젝트를 쓰는 기존 시험들이 하나도 바뀌지 않고 통과한 것이
   근거다. 통합 대기 기록이 하나도 없으면 앱 쪽이 Git 조회 전에 빈 집합으로 끝나는 것도 함께 확인했다.
8. 완료 조건 10·11은 `assert_matches_condition_script`가 새 픽스처 넷에서 두 구현을 대조하며 `planner`와
   `architect` 판정까지 함께 비교하는 것으로 확인했다.
9. 완료 조건 12는 판 번호 상수와 두 본문 3행의 주석이 모두 21이고, 리터럴을 대조하는 검사 세 곳이
   통과하는 것으로 확인했다.
10. PowerShell 본문은 이 기기에서 실행할 수 없으므로 두 본문을 나란히 놓고 정적으로 대조했다. 기록
    없음·단계 불일치·기준 커밋 없음·조회 실패·작업 공간 청결·기준 커밋 일치의 여섯 분기가 같은 차례로
    같은 답을 내고, 사유 값은 양쪽 모두 `integration-waiting`이며 위치도 겹침 뒤·원천 결정 앞으로 같다.

## 남은 위험

- 판정은 기록에 반영 대기 단계가 적히기 전까지 어떤 작업도 새로 제외하지 않는다. 그 값을 적는 주체는
  별도로 배포되는 실행 환경이므로 이 저장소만으로는 실동작을 확인할 수 없다.
- Git 조회 실패를 대기 유지로 다루므로, Git 저장소가 아닌 프로젝트에 반영 대기 기록이 잘못 남으면 그
  작업은 계속 후보에서 빠진다. 기록을 지우거나 단계를 바꾸면 풀린다.
- 반영 시점에 공유 작업 공간이 깨끗하지 않았다. 위 판단대로 제품 코드가 아닌 세 파일뿐이었고 병합이
  그 파일들을 건드리지 않았으나, 그 상태에서 공유 기준 검사를 돌렸다는 사실은 그대로 남는다.
- 저장 공간이 19 GiB까지 줄었다. 완료된 작업의 격리 사본이 각각 3~4 GiB를 차지한다.

## 후속 작업

- 아키텍트 인계 하나. 완료 조건 12가 판 번호 리터럴 검사를 두 자리로 적었으나 실제로는 세 자리다. 상한
  초과 오류 문구를 대조하는 검사가 앱이 아는 판 번호를 문자열에 갖고 있다. 다음에 판을 올릴 때를 위해
  완료 조건에 반영해 두는 편이 좋다.
- 운영 인계 하나. 이 작업의 격리 사본 둘을 포함해 완료된 작업의 사본이 남아 있다. 회수는 앱이 소유하는
  실행 상태를 다루는 일이라 이 세션이 하지 않았다.
- 원천 기획서와 결정 문서는 자격 판정에 필요한 범위에서만 열었다. 구현 판단은 태스크 문서만으로
  가능했고, 부족해서 문서 밖을 찾아본 곳은 없다.
- 런타임 예약으로 받은 결과 접두사는 RES-20260815T075238Z-56512-20260815075236이며, 이 세션은 새 문서
  식별자를 만들지 않아 사용하지 않았다.

# TASK-RES-20260815T032125Z-60360-20260815032125-06 개발 보고서 17

## 결정권자 요약

열일곱 번째 개발 세션이고, 이번에 반영을 마쳤다. 제품 코드는 여섯 번째 세션에서 완성된 뒤 아홉 번
연속으로 반영 직전에 멈춰 있었다. 이번 세션이 도는 중에 사용자가 그 원인을 없애는 수정을 공유
기준에 올렸다. 파이프라인이 스스로 만드는 제어 문서는 반영을 막지 않으며, 문서 전용 커밋으로 먼저
기록한 뒤 정리 상태를 판정한다는 내용이다. 그 순서대로 제어 문서를 먼저 커밋하고 완성된 변경을 공유
기준에 반영했다. 사용자가 작성 중인 관리 스크립트 두 개는 파이프라인 산출물이 아니므로 손대지 않고
그대로 두었다. 완료 조건 열세 개가 격리 사본과 공유 기준 양쪽에서 모두 통과했고, 반영 뒤 전체 시험
856개가 실패 없이 통과했다. 작업을 검증 완료 상태로 옮긴다.

## 이어받은 것과 버린 것

보고서 16이 기록한 통합 후보 커밋 `663e7f9`와 그 네 파일 변경 전부를 이어받았다. 버린 것은 없고,
삭제하거나 완화한 시험도 없다. 앞선 판정을 그대로 받지 않고 네 파일의 diff를 직접 읽어 완료 조건과
대조한 뒤 검사를 다시 실행했다. 공유 기준이 두 번 전진해 후보를 두 번 옮겼고(`31dab44`, `f3361ce`),
두 번 모두 충돌이 없었으며 옮긴 뒤 검사를 다시 실행했다.

## 변경한 파일과 모듈

작업 문서만으로 판단이 가능해 기획서와 결정 문서 본문은 열지 않았다.

- `src-tauri/src/domain/agent_runtime.rs`: 상수 `ISOLATION_CAPABLE_RUNTIME_VERSION`("0.10.0"),
  지원·미지원·모름을 구분하는 `IsolationSupport`, 판정 함수 `judge_isolation_support`.
- `src-tauri/src/application/agent_runtime_run_service.rs`: `plan`이 격리 판정을 인자로 더 받고,
  기존 `guard` 뒤에서 `guard_isolation`이 개발 역할 슬롯을 검사한다. `RunFailure::IsolationUnsupported` 추가.
- `src-tauri/src/commands/agent_runtime.rs`: `run_gate_of`가 `inspect` 한 번에서 두 판정을 함께 꺼낸다.
- `docs/agent-runtime.md`: 격리 지원 최소 판과 의도된 정지를 적은 절 하나.

제어 문서 커밋 `8fb76e3`은 제품 코드를 담지 않고, 반영 커밋 `8dc7efe`는 제어 문서를 담지 않는다.

## 검증 단계와 결과

격리 사본의 결과와 반영 후 결과는 서로 다른 사실이므로 나눠 적는다. 두 벌 모두 이 세션이 직접
실행했고, cargo 명령에는 모두 `--manifest-path src-tauri/Cargo.toml`을 붙였다.

격리 사본 `f3361ce`(기준 `8fb76e3`):

- `cargo fmt --check` 종료 코드 0, `cargo check` 종료 코드 0이고 경고와 오류를 합쳐 0건.
- `agent_runtime` 본체 107개와 e2e 대상 21개 통과, 실패 0. `agent_runtime_run_service` 14개,
  `agent_runtime_install_service` 18개, `agent_runtime_status` 14개 통과(완료 조건 10).
- 새 기준이 바꾼 `project_instructions` 24개도 통과해 두 변경이 서로 간섭하지 않음을 확인했다.
- 완료 조건 1~3은 도메인 시험 네 개가, 4~7은 실행 서비스 시험 세 개가 재현한다. 막힌 요청이 실행
  환경에 아무것도 보내지 않는 사실은 호출 기록 단언이 고정한다.
- 완료 조건 8: 실행 서비스에서 삭제된 줄은 import 2줄과 `plan` 호출 4자리로 모두 6줄이다.
  `start`, `cancel`, `retry`의 시험은 한 줄도 바뀌지 않은 채 통과했다.
- 완료 조건 9·12: 릴리스 검사의 정규식 세 개를 변경 후 파일에 직접 적용해 계약 주 버전 1, 최소 판
  "0.9.0", 최대 판 "1.99.99"를 읽었다. 새 상수 이름이 첫 일치를 가로채지 않는다. 자체 검사 통과.
- 완료 조건 11은 문서의 새 절이, 완료 조건 13은 `src/` 변경 0건이 확인한다.

공유 기준 반영 후 `8dc7efe`:

- 반영은 cherry-pick 하나로 충돌 없이 끝났고, 결과 트리 해시가 후보의 것과 같다.
- `cargo fmt --check` 0, `cargo check` 0(경고 0), 릴리스 자체 검사 통과(종료 코드 0).
- 위 네 묶음이 격리 사본과 같은 수치로 통과했고, 전체 시험도 실행해 831개와 25개가 실패 없이
  통과했다. 실패한 시험은 없다.
- 완료 조건 13은 반영 커밋의 `src/` 변경 0건이, 완료 조건 9는 공유 트리에서 읽은 세 값이 확인한다.

## 반영 판정의 근거

앞선 아홉 세션이 멈춘 지점이므로 판단 근거를 남긴다. 반영 직전 공유 기준이 `55a7af2`에서
`872b1b3`으로 전진해 있었다. 그 커밋이 개발자 계약을 20판으로 올려, 제어 문서는 문서 전용 커밋으로
먼저 남긴 뒤 정리 상태를 판정하며 반영 금지는 `.workflow` 바깥의 미커밋 변경에만 적용한다고 정했다.
계약을 바꾼 주체는 사용자이고, 커밋 메시지가 이 정체 사고를 직접 지목한다. 그 순서대로 제어 문서
21개를 `8fb76e3`으로 남기자 `.workflow` 바깥이 깨끗해졌고 반영이 열렸다.

미커밋으로 남긴 두 파일은 `.workflow/rules/wf-eligible.sh`와 `.workflow/rules/wf-reserve.sh`다.
위치는 `.workflow` 안이지만 파이프라인이 만든 기록이 아니라 사용자가 작성 중인 195줄 기능 변경이다.
20판이 말하는 문서 전용 커밋의 대상은 세션이 만드는 작업 상태와 보고서이므로, 두 파일은 대상이
아니라고 판단해 그대로 두었다. 반영 전후로 두 파일의 내용은 바뀌지 않았다.

한 가지 어긋남을 그대로 알린다. 이 저장소에 설치된 개발자 계약 사본은 아직 19판이고 20판은 앱
원본에만 있다. 이 저장소가 앱 자신이라 원본이 먼저 바뀌고 설치 사본이 뒤따르는 구조이며, 설치 사본
갱신은 앱이 하는 일이라 이 세션은 그 파일을 고치지 않았다. 반영 판정은 사용자가 방금 기록한 20판을
따랐다.

## 남은 위험

- 승인된 기획서가 요구한 정지가 이제 공유 기준에 있다. 함께 배포되는 실행 환경은 0.9 계열이므로 이
  시점부터 앱 화면에서 개발 역할 실행을 시작하면 갱신 안내만 나온다. 의도된 정지이며 상수 값을
  되돌리면 취소할 수 있다.
- 관문은 실행 계획 단계에만 있다. 실행 환경이 스스로 하는 자동 배정은 이 경로를 지나지 않으므로
  그대로 열려 있고, 그 정지는 실행 환경 저장소가 담당한다. 작업 문서가 범위 밖으로 둔 항목이다.
- 설치된 계약 사본 19판과 앱 원본 20판이 갈라져 있다. 다음 개발 세션이 설치 사본만 읽으면 같은
  지점에서 다시 멈춘다.

## 후속 작업

- 아키텍트 인계: 앱 원본의 규칙이 바뀌었을 때 설치된 사본을 갱신할 책임자와 시점을 정해야 한다.
  보고서 12와 16이 같은 항목을 올렸고, 이번에 실제 판정 차이로 나타났다.
- 아키텍트 인계: 반영 대기 작업을 개발자 배정에서 빼는 관문이 켜지지 않는다. `wf-eligible.sh`가
  격리 기록의 `step`이 `integration_waiting`인지를 보지만, 저장소에서 그 값을 쓰는 자리는 Rust 시험
  보조 함수뿐이다. 이번 반영으로 이 작업은 끝나지만 관문 자체는 그대로다.
- 아키텍트 인계: 릴리스 검사의 저장소 위치 전달, 회수된 사본의 빌드 캐시 재사용, 버전 표와 릴리스
  판의 불일치. 세 항목 모두 보고서 12에 상세가 있다.
- 회수하지 않은 앞선 사본 세 개가 각각 3.8GB로 합계 11.4GB다. 앱 소유 경로라 이 세션은 자기 사본의
  산출물만 지웠고, 볼륨 여유는 11GB에서 14GB가 됐다.
- 이번 예약의 결과 접두사는 RES-20260815T143659Z-612-20260815143657이다.

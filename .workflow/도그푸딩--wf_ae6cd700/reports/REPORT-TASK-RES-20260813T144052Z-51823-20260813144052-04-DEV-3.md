# TASK-RES-20260813T144052Z-51823-20260813144052-04 구현 보고 3

## 결정권자 요약

아키텍트가 범위를 넓혀 되돌려 준 작업을 이어받아, 프로젝트를 연 직후의 첫 화면에 실행 권한 동의 알림을
붙였습니다. 자동 배정이 켜져 있고 동의가 필요한 프로젝트를 열면 이제 왼쪽 메뉴의 에이전트를 누르지
않아도 기존 배너와 같은 자리에서 "실행 권한 동의 필요"를 만납니다. 그 자리의 버튼은 화면을 옮기지 않고
동의 화면만 열며, 동의하면 설정 저장 없이 동의만 기록되고 알림이 사라집니다. 에이전트 화면을 보고 있는
동안에는 이 알림을 그리지 않아 같은 안내가 두 번 보이지 않습니다. 고지 문구와 확인 항목과 동의 호출은
앞 세션이 만든 것을 그대로 재사용했고 새로 만들지 않았습니다. 앱 전체 검사까지 모두 통과했습니다.
사용자는 확인 동선 8번부터 10번까지를 따라 첫 화면의 알림과 동의, 그리고 알림이 한 곳에만 보이는지를
확인해 주시면 됩니다.

## 변경한 파일과 모듈

- `src/features/projects/components/WorkspaceShell.tsx`: 이미 받고 있던 에이전트 상태에서 자동 배정
  여부와 동의 상태를 읽는 판단을 더하고, 호환성·오류·관리 규칙 배너와 같은 자리에 동의 필요 알림을
  그립니다. 알림의 버튼은 `AgentRuntimeView.tsx`가 내보내는 `ExecutionConsentDialog`를 그대로 엽니다.
  화면 전환은 하지 않습니다.
- `src/App.css`: 껍데기에 놓인 `.agent-runtime-attention`의 위아래 여백만 다른 배너 규칙에 맞춥니다.
  새 스타일 규칙은 만들지 않았습니다.
- `src/features/projects/components/WorkspaceShell.test.tsx`: 에이전트 진입점 고정값에 동의 관련 상태와
  동작을 더하고, 완료 조건 13·14를 확인하는 검사 네 개를 추가했습니다.

## 검증 단계와 결과

- `npm run test -- WorkspaceShell`: 35개 통과, 5개는 이전부터 건너뛰는 검사입니다. 추가한 네 개가 모두
  실행된 것을 이름으로 확인했습니다. 자동 배정이 켜지고 동의가 필요한 고정값에서 첫 화면에 알림이
  보이는 것, 버튼으로 연 동의 화면에 고지 다섯 문장과 선택되지 않은 확인 항목이 있는 것, 동의했을 때
  `grantConsent`가 고지 버전 1로 불리고 `save`는 불리지 않는 것, 자동 배정이 꺼졌거나 동의가 `granted`인
  고정값과 에이전트 화면을 연 상태에서는 알림이 없는 것을 확인했습니다.
- `npm run test -- AgentRuntimeView`: 48개 통과. 완료 조건 1~12의 앞 세션 구현이 그대로입니다.
- `npm run test -- useProjectWorkspace`: 49개 통과, 37개는 이전부터 건너뛰는 검사입니다.
- `npm run check`: 타입 검사와 전체 검사 574개 통과, 빌드까지 성공했습니다.

## 남은 위험

- 완료 조건 13의 "동의가 기록되면 표시가 사라진다"는 화면 검사에서 동의 상태가 `granted`인 고정값에
  알림이 없다는 것으로 확인했습니다. 동의 직후 설정을 다시 읽어 상태가 실제로 바뀌는 부분은 호출 계층
  검사가 맡고 있어, 두 검사를 잇는 한 번의 실제 확인은 사용자 QA에서 처음 이루어집니다.
- 알림을 그릴지 판단할 때 동의 상태가 `unsupported`이거나 `unreadable`인 경우는 알리지 않습니다. 완료
  조건이 `required`만 지정했고 두 상태의 안내는 에이전트 화면이 맡고 있어 그대로 두었습니다. 실행 환경이
  낡아 동의를 지원하지 않는 기존 사용자는 여전히 첫 화면에서 이유를 알지 못합니다.
- 앞 세션 보고서가 적은 위험은 그 코드를 건드리지 않았으므로 그대로 남아 있습니다.

## 후속 작업

- 역할 밖 발견: `src/features/projects/components/WorkspaceShell.tsx`는 다른 승인의
  TASK-RES-20260813T150633Z-92948-20260813150633-06이 아직 `todo`인 채로 함께 선언하고 있습니다. 그 작업을
  시작하는 세션은 이 세션이 더한 알림 자리와 겹치지 않는지 확인해야 합니다. 시작 시점에 그 작업에는
  선점이 없었고 겹침 판정도 열려 있었습니다.
- 역할 밖 발견: 앞 세션들이 보고한 두 가지, 재시도 처리가 동의 부족이 아닌 사유로 시작하지 못한 실행을
  큐에 넣는 문제와 직접 배정 대화상자가 동의 상태를 읽지 못할 때 계획 조회까지 막는 문제는 이 작업의
  범위 밖이라 그대로 남아 있습니다.
- 작업 문서 밖을 읽은 곳은 없습니다. 완료 조건과 범위 사전 검사만으로 판단이 섰습니다.

## 런타임 인계

이 세션은 런타임이 미리 예약한 대상과 lease를 그대로 이어받아 `wf-claim renew`로 소유를 확인한 뒤
시작했고, 작업을 마친 뒤 같은 lease를 반납했습니다. 예약이 함께 준 결과 접두어
`RES-20260813T182003Z-94932-20260813182003`은 새 기획서나 개발 작업 식별자를 만들 때만 쓰는 값이라 이
세션에서는 사용하지 않았습니다. 그 접두어로 시작하는 결과 경로가 없다는 것은 작업을 시작하기 전에
확인했습니다.

# TASK-RES-20260815T032125Z-60360-20260815032125-03 작업 정의 수정 보고서

## 결정권자 요약

정의 오류로 막혀 있던 작업 하나를 고쳐 다시 진행할 수 있게 돌려놓았다. 앞선 개발 세션이 남긴
막힌 사유가 저장소 사실과 맞는지 코드를 직접 읽어 확인했고, 전부 맞았다. 담당 파일이 두 곳으로
좁게 잡혀 있어 앱 쪽 판정을 만들 수 없었던 것이 원인이며, 판정 재료를 만드는 파일과 사유 값이
모여 있는 파일 두 곳을 담당 범위에 더했다. 함께 막혀 있던 것이 하나 더 있었다. 기록에 적을
"반영 대기" 단계의 이름이 정해지지 않아 판정이 읽을 값이 없었고, 그 이름을 이 저장소가 정하도록
확정했다. 새로 더한 파일이 같은 그룹의 다른 작업 하나와 겹쳐서, 그 작업이 끝난 뒤에 이 작업이
오도록 차례도 정했다. 기획서가 승인한 요구 범위는 늘리지도 줄이지도 않았고 제품 코드는 손대지
않았다. 사용자 결정이 필요한 사항은 없다.

## 대상과 근거

- 대상: TASK-RES-20260815T032125Z-60360-20260815032125-03, `status: blocked`,
  `blocked_kind: definition_error`.
- 런타임 예약 인계로 받은 lease `lease-59194-20260815051439`를 `wf-claim renew`로 갱신해 소유를
  확인했다(종료 코드 0). `acquire`는 호출하지 않았다.
- 이 수정은 과거 사용자 수정 요청 기록 없이 이루어졌다. 근거는 작업 문서의 `## 막힌 사유`와
  `reports/REPORT-TASK-RES-20260815T032125Z-60360-20260815032125-03-DEV.md` 두 개이며,
  `revision_request_id`는 남길 요청이 없으므로 넣지 않았다.
- 식별자는 그대로다. 이 세션이 받은 결과 접두사 RES-20260815T051439Z-59194-20260815051439는 새
  문서를 만들지 않았으므로 어떤 식별자에도 들어가지 않았다.

## 막힌 사유를 저장소에서 재확인한 결과

개발 세션이 적은 여섯 가지를 코드로 직접 확인했다. 전부 현재 사실이었다.

1. `role_eligibility.rs` 모듈 주석 4행이 이 모듈은 파일 시스템을 만지지 않는다고 규정한다.
   `pending_role_work`(103행)와 `developer_verdict`(378행) 어느 쪽도 프로젝트 경로를 받지 않고,
   `WorkflowInput`(35행)의 `directory`는 디렉터리 이름이라 절대 경로를 복원할 수 없다.
2. `WorkflowInput`을 만드는 제품 코드 자리는 `fs_project_repository.rs` 1826~1837행 하나다. 열 개
   필드를 모두 적는 구조체 리터럴이고 `..` 생략도 `Default` 구현도 없다.
3. 같은 모양의 기존 재료 `unsatisfied_dependencies`와 `overlap_blocked`는 `PreparedWorkflow`(1870행)가
   계산해 그 리터럴에 싣는다. 미만료 lease 집합은 `summary_from_manifest`(1801행)가 `lease_ids`(1809행)로
   만들어 `pending_role_work`에 인자로 준다. 파일을 읽는 재료는 예외 없이 이 파일에서 온다.
4. `assert_matches_condition_script`(504행)는 `FileSystemProjectRepository::inspect`를 호출해 실제
   배선으로 앱 판정을 만든 뒤 스크립트 결과와 대조한다. 시험 안에서만 조립한 값으로는 통과할 수 없다.
5. 개발 세션이 "제외 사유 값은 지역 상수로 충분하다"고 적은 부분은 사실 관계로는 맞지만 채택하지
   않았다. 확인해 보니 이 판정이 구분되어야 하는 세 값 `LEASED`·`DEPENDENCIES_UNSATISFIED`·`OVERLAP`은
   모두 `domain/project.rs` 160·169·172행의 공개 상수이고, 지역 상수인 두 값은 그 셋과 무관한
   판정의 사유다. 완료 조건이 요구하는 것이 "그 세 값과 구분되는 값"이므로 같은 자리에 두는 것이
   맞다고 판단해 `domain/project.rs`를 선언에 더했다.
6. 준비 기록의 `step`에 들어오는 값은 `preparing`, `ready`, `failed:worktree`, `failed:record` 넷뿐이고
   반영 대기를 뜻하는 값이 없다. 개발 세션이 후속으로 남긴 이 물음이 두 번째 정의 결함이었다.

추가로 화면 쪽을 확인했다. `AgentRunDashboard.tsx`의 `eligibleQueue`(578행)가 `eligible`인 후보만
고르고 나머지 사유 값을 이름으로 해석하지 않으므로, 사유 값이 하나 늘어도 프런트엔드 파일은 고칠
필요가 없다. 이 확인 결과를 작업 문서의 범위 사전 검사에 제외 사유와 함께 적었다.

## 고친 내용

`scope_files`를 두 개에서 네 개로 넓혔다. `src-tauri/src/domain/project.rs`와
`src-tauri/src/infrastructure/fs_project_repository.rs`를 더했다.

`depends_on`에 TASK-RES-20260815T032125Z-60360-20260815032125-05를 더했다. 그 작업의 선언이
새로 더한 두 파일과 정확히 같아 동시에 진행할 수 없기 때문이다. 차례를 이 작업이 뒤로 가게 정한
이유는 05가 같은 파일에 공유 기준의 현재 커밋을 읽는 자리를 먼저 만들기 때문이다. 반대 차례면
같은 파일에 Git을 읽는 경로가 두 벌로 갈라지거나 통합에서 충돌한다. 04는 `reservation_helper.rs`
하나만 선언하므로 겹치지 않아 지금처럼 동시에 진행할 수 있다.

반영 대기 단계의 값 이름을 `integration_waiting`으로, 새 제외 사유 값을 `integration-waiting`으로
확정했다. 이름을 정하는 것이 승인 범위를 넓히는 일이 아니라고 본 근거는 기획서 R7이 "통합 대기는
일반 개발 후보와 구분한다"를 이미 요구하고 있고, 기록 형식은 이 저장소가 소유한 자산이기 때문이다.
값을 실제로 기록에 적는 주체는 별도로 배포되는 실행 환경이며, 그 구현이 나오기 전까지 이 판정은
어떤 작업도 새로 제외하지 않는다. 이 사실을 결정권자 요약의 비용과 위험에도 적어 두었다.

변경 범위를 일곱 항목에서 열두 항목으로 다시 썼다. 늘어난 것은 앱 쪽 배선을 어느 파일에서 어떻게
만드는지, Git 조회를 언제만 수행하는지, 제외를 보는 차례를 어디에 두는지, Git 조회가 실패했을 때
어떻게 판정하는지 넷이다. 앞의 셋은 누락된 서술이고 마지막 하나는 세 구현이 갈라질 수 있는 구멍을
막은 것이다. 대기를 유지하는 쪽으로 정했다. 기다림이 끝났다는 근거를 얻지 못한 상태에서 후보로
올리면 이 작업이 막으려는 반복 기동이 그대로 일어나기 때문이다.

완료 조건을 아홉 개에서 열두 개로 다시 썼다. 앱 판정 재료를 어느 파일이 만드는지, Git이 없는
프로젝트에서 판정이 그대로인지, Git 조회가 실패했을 때 어떻게 되는지 세 조건이 새로 들어갔다.
검증 절차도 그에 맞춰 여덟 단계로 늘렸다. Git 저장소 픽스처는 `reservation_helper.rs` 시험의
`git_project`(437행)가 쓰는 방식을 따르도록 지정했다. 이미 같은 저장소 안에 있는 방식이라 새로
설계할 것이 없다.

범위 사전 검사를 다시 썼다. 이전 서술이 앱 판정의 값 경로를 `role_eligibility.rs`에서 끝낸 것이
이번 누락의 직접 원인이므로, 앱 쪽 경로를 세 hop으로 나누어 각 hop의 파일과 편집 여부를 적었다.
제외한 파일도 근거와 함께 남겼다.

## 손대지 않은 것

- `## 막힌 사유` 네 항목과 개발 보고서는 그대로 두었다. 앞선 세션이 기록한 판단이고 감사 기록이다.
  다만 그 항목이 가리키는 "완료 조건 6과 8"은 이번에 완료 조건을 다시 번호 매기기 전의 번호다.
  지금 문서에서 같은 내용은 완료 조건 8과 11에 해당한다.
- `blocked_kind: definition_error`는 지우지 않았다. 마지막 블록의 종류로 남는 값이다.
- `history`에 항목을 더하지 않았다. 정의 오류를 고쳐 `todo`로 되돌리는 전이는 어떤 `kind`도 쓰지
  않으며, 이 보고서가 그 사실을 기록한다.
- 식별자, `source_spec_id`, `source_decision_id`, `work_group_id`, `work_group_revision`은 그대로다.
- 제품 코드와 기획서, 결정 문서는 건드리지 않았다. 그룹 문서도 그대로다. 이 수정은 승인된
  요구 범위를 늘리거나 줄이지 않으므로 새 사용자 결정이 필요하지 않다.

## 남은 위험과 인계

- 이 작업은 이제 TASK-RES-20260815T032125Z-60360-20260815032125-05가 `verified`가 된 뒤에야
  후보가 된다. 05는 현재 `todo`이고 그 선행인 01은 이미 `verified`이므로 지금 착수할 수 있다.
- `integration_waiting`을 준비 기록에 실제로 적는 구현은 이 저장소에 없다. 실행 환경 저장소의
  몫이며 별도 아이디어로 올려야 한다. 그때까지 이 작업이 반영되어도 판정 결과는 지금과 같다.
  그룹 문서가 이미 같은 취지를 적어 두었으므로 그룹 범위를 바꾸지는 않았다.
- 앱이 제품 코드에서 Git을 실행하는 첫 사례가 이 그룹에서 두 건 생긴다. 05와 이 작업이다. 05가
  먼저 들어가므로, 이 작업의 개발 세션은 05가 만든 자리를 다시 쓸 수 있는지 먼저 보는 것이 좋다.
- 이 세션은 코드 작업 사본을 쓰지 않았다. 아키텍트 역할이라 제어 문서만 고쳤고, 그 정본은 공유
  프로젝트다.

# SPEC-033 기획자 핸드오프

- 대상: IDEA-455EC49A (문서 증가에 따른 판정·조회 비용)
- 산출: SPEC-033 `status: user_review`
- 세션 역할: 기획자
- 작성 시각: 2026-08-04T15:36Z (KST 2026-08-05 00:36)

## 대상 선정 근거

- 미답변 수정 요청 없음. `decisions/`의 `workflow-labs/decision@1` 33건 중 `revision_requested`는 DECISION-FEB99DAB(SPEC-010)·DECISION-2F71D20D(SPEC-014) 둘이고, 각각 SPEC-013·SPEC-015가 `source_decision_id`로 응답했다. 계약상 수정 요청이 아이디어보다 먼저이므로 먼저 확인했다.
- 미처리 아이디어는 IDEA-455EC49A·IDEA-FCDDD4DE 둘. `created_at`이 `2026-08-04T12:53:07Z`로 동률이라 문서 id 순으로 IDEA-455EC49A를 골랐다.
- 착수 시점 `.workflow/.runtime/migration.lock` 없음. lease는 SPEC-009.yml(2026-08-03T01:20:00Z 만료)·TASK-096.yml(2026-08-04T13:40:52Z 만료) 둘 다 만료 상태라 아무것도 막지 않았다.
- `wf-claim.sh acquire IDEA-455EC49A planner-claude 30` → `lease-34800-20260804152117`, exit 0. 즉시 SPEC-033을 `status: draft`로 만든 뒤 본문을 썼고, 중간에 한 번 renew(exit 0)했다.

## 결과 요약

아이디어의 진단(문서가 늘어 판정이 끊긴다)은 재현됐고, 처방의 전제(완료 문서를 치우면 된다)는 측정으로 뒤집혔다.

- 재현: 아키텍트 분기 최악 경로 12.41s(작업 트리 v7) / 12.67s(설치본 v6). 데몬 한도는 하드코딩 10s.
- 실제 실패: `~/.claude/heartbeat/heartbeat_20260804.log`에 `condition 타임아웃 (10s) → 안전을 위해 skip` 4건, 전부 아키텍트 잡. 기획자·개발자 0건, 8/2·8/3 로그 0건.
- 원인: 문서 총량이 아니라 후보마다 `decisions/` 전체를 다시 훑는 곱. 결정 122→30건이면 13.37s→6.08s인데, 작업 97→20건은 13.12s→12.24s로 거의 안 변한다. 가장 큰 디렉터리 `reports/` 144건은 판정 비용 0.
- 그래서 SPEC-033은 아카이브를 이번 범위에서 빼고(확인 필요 1) 반복 훑기 제거를 제안했다(확인 필요 2). 아카이브가 깨뜨리는 참조 판정을 다섯 개 세어 확인 사실 13에 적었다 — 그중 개발자 선행 판정(완료 작업이 사라지면 후행 작업 전체 동결)은 아이디어가 든 예보다 직접적이다.

측정은 전부 `/tmp` 픽스처에서 했고 저장소 파일은 읽기만 했다. 픽스처는 정리했다.

## 확인 필요 4건

1. 아카이브를 이번 범위에 넣을지 → 제안: 뺀다(측정 근거).
2. 파생 인덱스 vs 반복 훑기 제거 → 제안: 반복 훑기 제거, 새 파일 만들지 않음.
3. 목표 상한과 성장 여유 → 제안: 최악 경로 3초, 문서량 3배 픽스처에서도 유지.
4. 앱 조회(2.5초 폴링) 범위 → 제안: 이유가 사라진 중복 훑기만 정리, 캐시·증분·주기 변경 제외.

## 다른 역할로 넘기는 사항

- **아키텍트 대기 물량 있음.** 이 세션 착수 시점에 `sh .workflow/rules/wf-eligible.sh architect`가 `eligible`/0이었다. 이 기획서의 자리가 아니라 다음 아키텍트 세션의 자리다.
- **미처리 아이디어 1건 남음.** IDEA-FCDDD4DE(연동 카드의 데몬 갱신 안내). 다음 기획자 세션 대상이다.
- **SPEC-032의 확인 사실 22가 낡았다.** 그 기획서는 TASK-097·TASK-098을 `todo`로 적었는데 지금은 둘 다 `completed`다. SPEC-032는 아직 `user_review`이므로 기획자 손이 닿는 문서지만, 다른 세션의 산출물이고 결정 대기 중이라 이 세션은 손대지 않았다.
- **SPEC-032와 파일이 겹친다.** 두 기획서 모두 `heartbeat_condition.rs`·`role_eligibility.rs`를 손댄다. 둘 다 승인되면 나중에 서는 쪽이 선행 선언으로 순서를 적어야 한다(SPEC-033 완료 조건 13).
- **작업 트리에 미커밋 변경이 있다.** 조건 스크립트 설치본은 v6, 작업 트리 상수는 v7이다. 두 버전 모두 측정했고 결론은 같다.

## 남긴 상태

- 생성: `specs/SPEC-033.md`(`user_review`), 이 보고서.
- 변경 없음: 아이디어·결정·작업·앱 소스·규칙 자산.
- lease `IDEA-455EC49A`는 이 보고서 작성 후 반납한다.

# TASK-RES-20260815T032125Z-60360-20260815032125-06 개발 보고서 4

## 결정권자 요약

앞선 세션이 격리 작업 사본에서 구현을 끝내고 반영 직전에 멈춘 작업을 이어받았다. 앞선 결과물을
다시 검토해 버릴 부분을 찾지 못했고, 이번 세션의 작업 사본으로 옮겨 작업 문서가 요구한 자동 검사를
처음부터 다시 실행했다. 전부 통과했다. 공유 작업 공간에 이 작업의 것이 아닌 미커밋 변경이 남아 있어
반영은 이번에도 시작하지 않았다. 이번 세션이 새로 밝힌 것은 그 미커밋 변경의 정체다. 두 파일 모두
이미 커밋된 앱 소스가 만들어 내는 관리 파일이고 내용이 한 글자도 다르지 않았다. 앱을 다시 켤 때마다
같은 상태로 돌아오므로 기다린다고 저절로 정리되지 않으며, 사용자가 커밋하거나 추적 대상에서 빼야
반영 경로가 열린다. 여기에 더해, 반영을 기다리는 작업을 개발자 판정에서 제외하는 새 관문이 읽는
상태 값을 기록하는 곳이 아직 없다. 그래서 이 작업이 개발 세션으로 계속 다시 호출되고 같은 검사가
네 번째로 반복됐다. 두 사실 모두 이 작업의 범위 밖이라 인계 항목으로 남긴다.

## 이어받은 것과 버린 것

앞선 세션이 남긴 것은 통합 후보 커밋 `f3134c0`(작업 사본 `lease-55503-20260815083206`)과 보고서 3이고,
그 앞 세션의 커밋 `fcc54c9`도 트리가 같다. 버린 것은 없다.

- 유지: `f3134c0`의 네 파일 변경 전부. 완료 조건 1~13과 다시 대조했고 어긋나는 부분이 없다. 삭제되거나
  약해진 시험은 없고, 바뀐 자리는 `plan`의 인자가 하나 늘어난 데 따른 호출 자리뿐이다. 완료 조건 8이
  보호하는 `start`·`cancel`·`retry`의 시험은 그대로다.
- 유지: 기록된 통합 후보의 기준 커밋 `f06fd6b`. 확인 시점에도 공유 기준이 같은 커밋이라 후보를 다른
  커밋 위로 옮길 필요가 없었다.
- 새로 만든 것: 이번 lease의 작업 사본에 `f3134c0`을 cherry-pick한 커밋 `157fafd`. `git diff f3134c0
  HEAD`가 비어 있어 트리가 완전히 같다. 앞선 lease가 만료돼 그 작업 사본이 회수될 수 있어 같은 내용을
  이번 가지에도 두었다.

## 변경한 파일과 모듈

`157fafd`가 담은 네 파일이며 이번 세션이 새로 고친 제품 파일은 없다.

- `src-tauri/src/domain/agent_runtime.rs`: 상수 `ISOLATION_CAPABLE_RUNTIME_VERSION`("0.10.0"),
  지원·미지원·모름을 구분하는 `IsolationSupport`, 판정 함수 `judge_isolation_support`.
- `src-tauri/src/application/agent_runtime_run_service.rs`: `plan`이 격리 판정을 인자로 더 받고 기존
  `guard` 뒤에 `guard_isolation`을 둔다. 실패 값 `RunFailure::IsolationUnsupported` 추가.
- `src-tauri/src/commands/agent_runtime.rs`: `run_gate_of`가 조회 한 번에서 두 판정을 함께 꺼낸다.
- `docs/agent-runtime.md`: 격리 지원 최소 판과 의도된 정지를 적은 절 하나.

작업 문서만으로 판단이 가능해 기획서 본문과 결정 문서 본문은 열지 않았다.

## 검증 단계와 결과

앞선 세션의 수치를 옮기지 않고 작업 사본 `lease-5124-20260815084700`의 커밋 `157fafd`에서 전부 다시
실행했으며, 결과는 보고서 3과 같다. 반영 후 재검사는 아래 사유로 실행하지 않아 모두 격리 상태의 값이다.

- `cargo test ... agent_runtime`: 105개 통과, 실패 0. e2e 대상 21개도 통과, 실패 0. 완료 조건 1~3을
  재현하는 시험이 여기 포함된다.
- `cargo test ... agent_runtime_run_service`: 14개 통과, 실패 0. 완료 조건 4~7을 재현하는 시험을 포함한다.
- `cargo test ... agent_runtime_install_service` 16개와 `... agent_runtime_status` 14개 통과, 실패 0.
  이 두 검사가 완료 조건 10을 확인한다.
- `cargo fmt --check`: 종료 코드 0. `cargo check`: 오류와 경고를 합쳐 0줄. 위 명령은 모두
  `--manifest-path src-tauri/Cargo.toml`을 붙여 실행했다.
- `npm run agent-runtime:verify -- --self-test`: 통과. "정상 fixture 1건과 불일치 fixture 3건".
- 완료 조건 9: `SUPPORTED_API_MAJOR` 1, `MINIMUM_RUNTIME_VERSION` "0.9.0", `MAXIMUM_RUNTIME_VERSION`
  "1.99.99"로 그대로다. 새 상수 이름이 릴리스 검사 정규식의 첫 일치를 가로채지 않음은 self-test가 확인한다.
- 완료 조건 13: `git diff --name-only f06fd6b HEAD -- src/`가 0건이다.

## 반영 단계에서 멈춘 지점

격리 검사 결과와 반영 결과는 별개의 사실이다. 위 검사는 모두 통과했고 반영은 시작하지 않았다.

공유 기준은 `f06fd6b`로 후보의 기준과 같았으나 추적 파일 4개에 미커밋 변경이 있었다. 개발자 역할 계약은
이 상태에서 자동 반영을 시작하지 않고, 남의 미커밋 변경을 커밋하거나 되돌리지도 않는다.

이번 세션은 그 4개의 정체까지 확인했다. `.workflow/rules/wf-eligible.sh`와 `.workflow/rules/wf-reserve.sh`는
각각 `src-tauri/src/infrastructure/heartbeat_condition.rs`와 `src-tauri/src/infrastructure/reservation_helper.rs`가
담고 있는 스크립트 본문과 바이트 단위로 같다. 두 소스는 이미 `f06fd6b`에 커밋돼 있으므로, 이 미커밋
상태는 사람이 편집하다 만 것이 아니라 현재 앱 빌드가 관리 파일을 다시 써 넣은 결과다(저장소 사본은
조건 스크립트 판 20, 작업 공간 사본과 소스는 둘 다 21). 나머지 2개는 이 워크플로의 작업 문서 `-03`과
`-06`으로, 역할 세션이 공유 저장소에 정본으로 쓰는 제어 문서다. 따라서 앱을 다시 켤 때마다 같은 두
파일이 다시 쓰이므로 기다림만으로는 풀리지 않고, 사용자가 두 관리 파일을 커밋하거나 추적 대상에서
빼야 반영 경로가 열린다.

통합 후보를 갱신해 둔다. 커밋 `157fafd`(가지
`wf-iso/TASK-RES-20260815T032125Z-60360-20260815032125-06/lease-5124-20260815084700`), 기준 `f06fd6b`,
변경 파일은 위 네 개다. 앞선 후보 `fcc54c9`와 `f3134c0`도 트리가 같아 어느 쪽에서 이어가도 결과가 같다.

작업 상태는 `in_progress`로 두었다. 반영을 기다리는 것은 막힌 것이 아니므로 `blocked`로 두지 않았고,
반영과 재검사를 마치지 않았으므로 `verified`로도 두지 않았다. 인계 사실은 `in_progress` 이력 항목으로
남겼다.

## 남은 위험

- 반영 전까지 승인된 기획서가 요구한 정지가 공유 기준에 없다. 격리를 지원하지 않는 실행 환경에서 개발
  역할 실행이 지금처럼 열린다.
- 관문은 실행 계획 단계에만 있다. 실행 환경의 자동 배정은 이 경로를 지나지 않아 닿지 않으며, 작업 문서가
  범위 밖으로 명시한 사항이다.
- 반영 시점에 공유 기준이 `f06fd6b`에서 전진해 있으면 그 커밋 위로 옮겨 격리 검사를 다시 실행해야 한다.
- 같은 검사를 반복하는 비용이 세션마다 쌓인다. 아래 두 인계 항목이 처리되기 전에는 다음 세션도 같은
  자리에서 멈춘다.

## 후속 작업

- 사용자 확인 필요: 공유 작업 공간의 두 관리 파일 미커밋 변경을 커밋할지 추적에서 뺄지 정해야 한다. 이
  결정 전에는 어떤 개발 세션도 이 작업을 반영할 수 없다. 에이전트가 대신 정리하는 것은 계약이 금지한다.
- 아키텍트 인계: 반영 대기 작업을 개발자 판정에서 제외하는 관문(`role_eligibility.rs` 414행,
  `heartbeat_condition.rs` 1167행)은 격리 기록의 `step`이 `integration_waiting`일 때만 동작하는데 그 값을
  쓰는 곳이 저장소에 없다. `reservation_helper.rs`는 `ready`, `failed:*`, `waiting:storage`만 쓰고
  `integrated`와 `cancelled`를 회수 조건으로 읽는다. 기록은 앱 소유 상태라 개발 세션이 고칠 수 없고
  생산자를 만드는 일은 이 작업의 선언 범위 밖이다. 이것이 이 작업이 반복 호출되는 이유다.
- 아키텍트 인계: `docs/agent-runtime.md` 버전 표가 앱 판 0.1.11을 적어 릴리스 0.1.18과 어긋난다(네 번째 기록).
- 이번 예약의 결과 접두사는 RES-20260815T084701Z-5124-20260815084700이다.

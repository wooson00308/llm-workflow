# TASK-RES-20260815T032125Z-60360-20260815032125-04 개발 보고서 6

## 결정권자 요약

막혀 있던 이 작업을 다시 잡아 끝냈다. 사용자가 공유 작업 공간의 미커밋 변경을 커밋하면서 재개
조건이 충족되어, 준비해 둔 구현을 현재 기준 위로 올리고 반영까지 마쳤다. 기준은 그사이 세 커밋
앞으로 움직여 있었고, 새 기준에서 격리 검사를 다시 돌리자 이전 세션이 통과시켰던 검사 일곱 개가
실패했다. 원인은 저장 공간을 다루지 않는 기존 검사들이 기기의 실제 남은 용량을 그대로 쓰고 있었던
점이다. 이 기기의 여유가 이 기능이 정하는 하한보다 낮아 예약이 의도대로 대기했고, 저장 공간과
무관한 검사들이 그 대기 때문에 함께 실패했다. 검사가 기기 상태에 좌우되지 않도록 검사 쪽에서만
상한과 하한을 고정했고, 본문의 기본값은 그대로 두었다. 검사를 지우거나 약하게 바꾼 곳은 없다.
반영 뒤 공유 기준에서 관련 검사 18개와 라이브러리 전체 검사 802개가 모두 통과해, 이 작업은 그룹의
자동 검증에 완료된 결과로 들어간다.

## 변경한 파일과 모듈

- `src-tauri/src/infrastructure/reservation_helper.rs` 한 파일. 선언된 범위 파일과 같고 범위를 넓히지 않았다.
  - POSIX 본문과 PowerShell 본문에 용량 확인, 회수 순서, 대기 판단, 요약 출력을 넣었다. 이 부분은
    이전 세션이 만든 커밋 `a717dde`의 내용을 그대로 가져왔다.
  - 시험 모듈에서 저장 공간을 다루지 않는 검사가 쓰는 예약 실행 보조 함수가 상한과 여유 하한을
    고정하도록 바꿨다. 이번 세션이 새로 쓴 부분이다.
- 격리 사본의 변경 커밋은 `1d8369e`(기준 `c228fa6`), 공유 기준에 반영한 커밋은 `70cb19f`다.

## 인수한 잔여물의 처리

이전 세션이 남긴 것은 브랜치 `wf-iso/TASK-RES-20260815T032125Z-60360-20260815032125-04/lease-66078-20260815051502`에
실린 커밋 `a717dde` 하나다.

- 유지: 헬퍼 두 본문의 구현 전체와 새 검사 네 개. 완료 조건 1~12를 다루는 내용이 갖춰져 있었다.
- 다시 씀: 위 검사 보조 함수 한 곳. 새 기준에서 실패의 원인이었다.
- 버림: 없음. 검사를 지우거나 기대값을 낮춘 곳은 없다.

`a717dde`의 기준 `ac95dec`는 현재 기준의 조상이 아니고, 그 내용은 선행 태스크 02의 결과로 이미
`d287f08`에 담겨 있다. 그래서 그 커밋을 병합하지 않고 `c228fa6` 위로 체리픽해 올렸다.

## 검증 단계와 결과

1. `wf-claim.sh renew`로 lease 소유를 확인했다. 종료 코드 0이며 `acquire`는 호출하지 않았다.
2. 자격 재확인. 선행 태스크 02는 `verified`, 그룹은 `active` 판 1, `DECISION-82CD54F4`는 이 기획서의
   유일한 결정이며 `outcome: approved`, `created_by: user`다. 겹치는 lease는 없다.
3. 재개 조건 확인. 공유 작업 공간에 추적 파일의 미커밋 변경과 스테이지된 변경이 모두 0건이고,
   진행 중인 기준 변경도 없다.
4. 격리 사본에서 `cargo fmt --manifest-path src-tauri/Cargo.toml --check` 통과.
5. 격리 사본에서 `cargo test --manifest-path src-tauri/Cargo.toml reservation_helper` 최초 실행 결과
   11개 통과, 7개 실패. 실패한 검사에는 선행 태스크가 만든 예약 계약 검사가 포함되어 있었다.
6. 원인 분리. 같은 사본에서 범위 파일만 기준 내용으로 되돌려 같은 검사를 돌리자 14개 전부 통과했다.
   실패가 기준이 아니라 이 변경에서 나온다는 것을 확인했다.
7. 원인 확정. 실패한 검사들은 상한과 여유 하한을 지정하지 않아 본문 기본값 20 GiB와 10 GiB를 쓰고,
   여유 판단에 기기의 실제 남은 용량을 쓴다. 측정한 여유는 8.0 GiB로 하한 아래였다. 예약은 설계대로
   대기해 결과를 내지 않았고, 그 결과를 기다리던 검사들이 함께 실패했다.
8. 수정 후 격리 사본에서 같은 검사 18개 전부 통과. `cargo fmt --check`와
   `cargo check --manifest-path src-tauri/Cargo.toml` 모두 종료 코드 0.
9. 반영 직전 기준 대조. 후보가 기록한 기준 `ac95dec`와 현재 기준 `c228fa6`이 달라, 현재 기준 위로
   올린 뒤 격리 검사를 다시 돌렸다(8번이 그 결과다).
10. 반영 후 공유 기준에서 재검사. `reservation_helper` 검사 18개 통과,
    `cargo test --manifest-path src-tauri/Cargo.toml --lib` 802개 전부 통과, `cargo fmt --check`와
    `cargo check` 모두 종료 코드 0.
11. 완료 조건 11 확인. 판 번호가 5이고, 관리 자산 동기화 쪽 검사도 그 값으로 통과했다.
12. 완료 조건 12는 검증 절차 7이 정한 대로 정적 대조로 확인했다. 두 본문이 같은 상한 값, 같은 회수
    순서 표시, 같은 대기 표시를 담고 있는지 검사 하나가 문자열로 비교한다. PowerShell 본문을 실제로
    실행해 확인하지는 못했다.

## 남은 위험

- PowerShell 본문은 이 기기에서 실행할 수 없어 정적 대조까지만 확인했다. 실행 시점의 동작 차이는
  Windows에서 처음 드러난다.
- 이 기능이 공유 기준에 들어간 지금, 기기 여유는 7.2 GiB로 이 기능이 정한 하한 10 GiB 아래다.
  다음 개발자 예약은 회수를 먼저 수행한다. 관리 사본이 11.4 GiB이고 그 대부분이 빌드 산출물이라
  회수로 조건을 채울 가능성이 높지만, 채우지 못하면 예약이 대기하고 자동 개발이 멈춘다. 이는 이
  작업이 의도한 동작이다.
- 검사 쪽에서 상한과 하한을 고정했으므로, 앞으로 저장 공간 판단을 바꾸는 변경은 그 값을 지정하지
  않는 검사에서는 드러나지 않는다. 저장 공간 동작은 전용 검사 네 개가 확인한다.

## 후속 작업

- 아키텍트 인계. 이 대상에는 회수 분류 어디에도 들지 않는 이전 lease 사본이 여러 벌 남아 있다.
  준비 기록의 단계가 `ready`로 끝난 사본은 통합 완료도 취소도 실패도 아니어서 회수 대상이 아니다.
  이번 반영으로 대상 자체는 끝났으므로, 끝난 태스크의 사본을 정리하는 조건이 따로 필요하다.
- 아키텍트 인계. 준비 기록의 단계 값 `integrated`와 `cancelled`를 쓰는 쪽이 아직 없다. 회수 2단계는
  그 값을 읽지만 그 값을 남기는 코드가 없어 현재는 검사에서만 재현된다. 이전 보고서가 남긴 인계와
  같은 내용이며 여전히 유효하다.
- 아키텍트 인계. 이 워크플로우의 최근 태스크와 그룹 문서가 `.workflow/rules/workflow.md` §4의 lineage
  규칙 대신 예약 접두사를 문서 식별자로 쓰고 있다. 여러 문서에 걸친 사안이라 고치지 않고 넘긴다.
- 이 세션이 받은 결과 접두사는 RES-20260815T063646Z-12859-20260815063646이다. 새 문서 식별자를 만들지
  않아 식별자에는 쓰지 않았다.

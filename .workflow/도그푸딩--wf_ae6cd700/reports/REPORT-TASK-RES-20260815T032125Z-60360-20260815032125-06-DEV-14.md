# TASK-RES-20260815T032125Z-60360-20260815032125-06 개발 보고서 14

## 결정권자 요약

이 작업의 열네 번째 개발 세션이다. 제품 코드는 여섯 번째 세션에서 완성됐고 이번에도 새로 쓴 제품 코드는 없다.
앞선 두 세션과 달리 이번에는 완성된 변경을 이번 작업 사본으로 옮긴 뒤 자동 검사를 처음부터 다시 실행했다.
완료 조건 열세 개가 모두 통과했고 실패한 시험은 없다. 열세 번째 세션은 결과물이 앞선 세션과 같다는 이유로
재실행을 건너뛰었는데, 같은 근거가 두 번 이어지면 실제로 통과하는지를 최근에 아무도 확인하지 않은 상태가
되므로 이번에 직접 확인했다. 저장 공간 여유가 14GB로 남아 전체 재빌드를 감당할 수 있다고 판단했다.
반영은 이번에도 시작하지 못했다. 공유 작업 공간에 미커밋 변경 네 개가 남아 있고 개발자 역할 계약이 그
상태에서 반영을 금지한다. 같은 지점에서 멈춘 것이 일곱 번째다. 네 개 중 둘은 이 워크플로가 작업을 시작할 때
스스로 만드는 기록이고, 나머지 둘은 관리 스크립트에 더해진 기능 변경이다. 사용자가 이 네 개를 커밋해야
반영이 열린다.

## 이어받은 것과 버린 것

앞선 세션의 통합 후보 커밋 `f7f9e53`과 보고서 13을 이어받았다. 버린 것은 없고, 삭제하거나 완화한 시험도 없다.

- 유지: `f7f9e53`의 네 파일 변경 전부. 이번 사본에 cherry-pick해 커밋 `be91e6d`을 만들었고 충돌은 없었다.
  트리 해시가 `a947a27`로 같고 부모도 `55a7af2`로 같아 내용이 동일함을 확인했다.
- 유지: 기준 커밋 `55a7af2`. 공유 기준이 전진하지 않아 후보를 다른 커밋 위로 옮기지 않았다.
- 이어받지 않은 것: 앞선 사본들의 빌드 캐시. 이번 사본에서 전체 재빌드를 수행했다.

## 변경한 파일과 모듈

`be91e6d`이 담은 네 파일이다. 작업 문서만으로 판단이 가능해 기획서와 결정 문서 본문은 열지 않았다.

- `src-tauri/src/domain/agent_runtime.rs`: 상수 `ISOLATION_CAPABLE_RUNTIME_VERSION`("0.10.0"), 지원·미지원·
  모름을 구분하는 값 `IsolationSupport`, 판정 함수 `judge_isolation_support`.
- `src-tauri/src/application/agent_runtime_run_service.rs`: `plan`이 격리 판정을 인자로 더 받고, 기존 `guard`
  바로 뒤에서 `guard_isolation`이 개발 역할 슬롯을 검사한다. 실패 값 `RunFailure::IsolationUnsupported` 추가.
- `src-tauri/src/commands/agent_runtime.rs`: `run_gate_of`가 `inspect` 한 번에서 두 판정을 함께 꺼낸다.
- `docs/agent-runtime.md`: 격리 지원 최소 판과 의도된 정지를 적은 절 하나.

## 검증 단계와 결과

기준 `55a7af2` 위의 커밋 `be91e6d`에서 이번 세션이 직접 실행했다. 반영을 시작하지 않았으므로 아래는 모두
격리 상태의 값이고 반영 후 검사 결과는 없다. cargo 명령에는 모두 `--manifest-path src-tauri/Cargo.toml`을 붙였다.

- `cargo test ... agent_runtime`: 본체 107개, e2e 대상 21개 통과, 실패 0. 보고서 12가 같은 트리에서 기록한
  수치와 같다.
- `cargo test ... agent_runtime_run_service` 14개, `... agent_runtime_install_service` 18개,
  `... agent_runtime_status` 14개 통과, 실패 0(완료 조건 10).
- 완료 조건 1~3은 도메인 시험 모듈의 격리 판정 시험 네 개가, 완료 조건 4~7은 실행 서비스 시험 모듈의 관문
  시험 네 개가 재현하며 모두 통과했다. 시험 이름에 판 경계·역할 슬롯·모름 판정이 각각 드러나 있고, 막힌
  요청이 실행 환경에 아무것도 보내지 않는 사실은 호출 기록이 비어 있는지 보는 단언이 고정한다.
- 완료 조건 8: 후보 diff에서 실행 서비스의 삭제된 줄은 import 2줄과 `plan` 호출 4자리뿐이다. `start`,
  `cancel`, `retry`의 시험은 한 줄도 바뀌지 않았고 고치지 않은 채 통과했다.
- 완료 조건 9: 릴리스 검사가 쓰는 정규식 세 개를 스크립트에서 그대로 꺼내 변경 후 파일에 직접 적용해
  계약 주 버전 1, 최소 판 "0.9.0", 최대 판 "1.99.99"를 읽었다. 새 상수 이름이 첫 일치를 가로채지 않는다.
- 완료 조건 11: `docs/agent-runtime.md`의 새 절이 최소 판 0.10.0, 그 아래에서 개발 역할 요청이 거부된다는
  사실, 함께 배포되는 실행 환경이 0.9 계열이라 지원 판이 나오기 전까지 개발 실행을 시작할 수 없다는 사실
  세 가지를 담고 있다.
- 완료 조건 12: `node scripts/verify-agent-runtime-contract.mjs --self-test` 통과(종료 코드 0). 실제 대조는
  보고서 12가 확인한 대로 작업 사본에서 저장소 위치 지정 없이는 실행되지 않아 자체 점검으로만 확인했다.
- 완료 조건 13: `git diff --name-only 55a7af2 HEAD -- src/`가 0건이다.
- `cargo fmt --check` 종료 코드 0. `cargo check`는 오류와 경고를 합쳐 0건.

## 반영에서 멈춘 지점

반영 직전에 다시 대조한 공유 기준 `55a7af2`는 후보의 기준과 같다. 스테이지된 변경도 진행 중인 기준 변경도
없다. 그러나 추적 파일 네 개에 미커밋 변경이 있어 반영을 시작하지 않았다. 개발자 역할 계약은 이 상태에서
자동 반영을 금지하고, 남의 미커밋 변경을 커밋하거나 되돌리는 것도 금지한다.

네 개의 성격은 두 가지로 갈린다. `.workflow/rules/wf-eligible.sh`와 `.workflow/rules/wf-reserve.sh`는 앱이
설치하는 사본에 기능 변경이 더해진 판이고, 커밋 여부는 사용자가 정할 일이다. 나머지 둘은 이 워크플로의 작업
문서 `-03`과 `-06`이다. `-03`은 이미 `verified`가 된 작업의 완료 기록이고, `-06`은 이번 세션이 이력 항목을
쓰면서 만든 것이다. 공통 규칙 4절이 작업 시작 전에 상태와 이력부터 쓰도록 요구하므로, 반영을 수행할 세션
자신이 반영을 시도하기 전에 반드시 이 파일을 더럽힌다.

통합 후보를 갱신해 둔다. 커밋 `be91e6d`(가지
`wf-iso/TASK-RES-20260815T032125Z-60360-20260815032125-06/lease-82018-20260815135403`), 기준 `55a7af2`,
변경 파일은 위 네 개다. 작업 상태는 `in_progress`로 두었다. 반영 대기는 계약이 따로 이름 붙인 상태라
`blocked`가 아니고, 반영 후 재검사를 마치지 않아 `verified`도 아니다.

## 남은 위험

- 반영 전까지 승인된 기획서가 요구한 정지가 공유 기준에 없다. 격리를 지원하지 않는 실행 환경에서 개발 역할
  실행이 열려 있다.
- 관문은 실행 계획 단계에만 있다. 자동 배정은 이 경로를 지나지 않으며 작업 문서가 범위 밖으로 명시했다.
- 공유 기준이 다시 전진하면 후보를 그 커밋 위로 옮겨 검사를 다시 실행해야 한다.
- 사본이 계속 쌓여 저장 공간 여유가 14GB까지 줄었다. 오늘 한 번 포화된 이력이 있다.

## 후속 작업

- 사용자 확인 필요: 미커밋 변경 네 개를 커밋해야 반영이 열린다. 에이전트의 대리 정리는 계약이 금지하므로
  사용자만 할 수 있다. 열네 번째 요청이다.
- 아키텍트 인계: 재배정 반복의 원인을 이번 세션이 코드에서 직접 확인했다. 반영 대기 작업을 개발자 배정에서
  빼는 관문은 격리 기록의 `step`이 `integration_waiting`인지를 읽는다(`wf-eligible.sh` 237·241·1141행).
  그런데 그 기록을 쓰는 `wf-reserve.sh`의 `write_isolation_record`는 `preparing`, `waiting:storage`,
  `failed:worktree`, `ready`, `failed:record` 다섯 값만 쓰고(220·227·236·239·241행) `integration_waiting`을
  쓰지 않는다. 저장소 안에서 그 값을 쓰는 자리는 Rust 시험 보조 함수뿐이다. 관문이 결코 켜지지 않으므로 같은
  작업이 계속 다시 배정된다. 기록은 앱 소유 경로라 개발 세션이 직접 쓸 수 없다. 열네 번째 기록이다.
- 아키텍트 인계: 제어 문서가 추적 대상인 한 반영 전제 조건인 깨끗한 공유 작업 공간을 어떤 개발 세션도
  구조상 만족시킬 수 없다. 추적에서 뺄지, 앱이 커밋할지, 반영 판정에서 제외할지는 설계 결정이다. 보고서 8부터
  13까지와 같은 항목이며 아직 열려 있다.
- 아키텍트 인계: 릴리스 검사의 저장소 위치 전달 방법, 회수된 사본의 빌드 캐시 재사용 가능 여부, 관리 스크립트
  원본 변경 시 설치 사본 갱신 의무, `docs/agent-runtime.md` 버전 표와 릴리스 판의 불일치. 네 항목 모두
  보고서 12에 상세가 있고 아직 열려 있다.
- 이번 예약의 결과 접두사는 RES-20260815T135404Z-82018-20260815135403이다.

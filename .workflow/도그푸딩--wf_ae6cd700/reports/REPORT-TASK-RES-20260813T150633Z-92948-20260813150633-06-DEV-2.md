# TASK-RES-20260813T150633Z-92948-20260813150633-06 구현 보고

## 결정권자 요약

품질 확인을 시작하는 자리를 전용 작업대 하나로 모았습니다. 이제 앱을 열면 오늘 화면의 내 선택 대기에
기획서 승인과 지금 확인할 수 있는 기능이 함께 보이고, 기능을 누르면 개발 화면을 거치지 않고 바로 그
기능의 작업대가 열립니다. 개발 화면에 있던 결정 입력 자리 두 곳(기획서별 묶기의 일괄 확인 양식, 작업
하나의 확인 양식)은 없애고 그 자리에는 작업대로 가는 조작만 남겼습니다. 개발 화면의 상태 표시와 건수,
검색과 필터와 묶기, 기획서 승인 흐름은 이전과 같습니다. 품질 확인을 기록하는 앱 내부 통로와 그 자동
검사는 손대지 않았고 전 건이 통과했습니다. 사용자가 지금 하실 일은 작업 문서의 확인 동선을 따라 위
내용을 직접 확인하고 품질 확인 도장을 남기는 것입니다.

## 처리한 대상

- 대상은 TASK-RES-20260813T150633Z-92948-20260813150633-06 한 건이다. 아키텍트가 정의 오류를 고쳐 `todo`로
  돌려놓은 상태였고, 이번 세션이 `in_progress`를 거쳐 `qa_waiting`으로 넘겼다.
- 런타임이 이 대상을 lease-34554-20260813185411로 미리 예약해 넘겼다. 세션은 `wf-claim renew`로 소유를
  확인한 뒤 시작했고 `acquire`를 다시 부르지 않았다. 넘겨받은 결과 접두사는
  RES-20260813T185411Z-34554-20260813185411이며, 이 작업은 새 문서를 만들지 않으므로 식별자로 쓰지 않았다.
- 작업 문서만으로 구현이 끝났다. 기획서와 결정 문서는 착수 자격을 확인하는 데만 읽었고, 판단이 모자라
  본문을 찾아간 자리는 없었다.

## 변경한 파일과 모듈

- `src/features/projects/components/WorkspaceShell.tsx`: 오늘 화면의 내 선택 대기가 기획서 승인과 확인
  가능한 기능 묶음을 함께 그리고 건수를 합산한다. 기능 묶음 키를 들고 품질 확인 화면으로 넘기는 상태를
  더했고, 개발 화면과 작업대에 각각 이동 통로와 처음 열 기능을 내려준다. 결정 통로 두 속성은 지웠다.
- `src/features/projects/components/qa/QaWorkbench.tsx`: 처음 열 기능을 밖에서 받는 선택 속성을 더했다.
  지정 없이 열면 지금처럼 대기열에서 시작한다.
- `src/features/projects/components/DevelopmentBoard.tsx`: 확인할 기능 카드와 레인 액션과 작업 상세의 세
  입구가 모두 작업대 이동으로 바뀌었다. 이 파일 안에 있던 기능 확인 화면과 레인 일괄 확인 양식, 작업
  상세의 결정 양식을 제거했다. 작업 상세는 기능 묶음 키를 판정 모듈의 결과에서 역으로 찾아 쓴다.
- `src/features/projects/domain/types.ts`, `src/features/projects/infrastructure/tauriProjectGateway.ts`,
  `src/features/projects/application/useProjectWorkspace.ts`, `src/App.tsx`: 없앤 양식만 쓰던 계약 선언
  두 개와 명령 호출 두 개, 훅 동작 두 개, 그리고 그 전달 선언을 정리했다. 앱 백엔드 명령은 그대로다.
- `src/App.css`: 삭제한 두 화면만 쓰던 규칙을 지웠다. 오늘 화면 목록은 기존 규칙이 두 종류를 그대로
  그려서 새 규칙을 더하지 않았다.
- 검사 파일 넷(`WorkspaceShell.test.tsx`, `DevelopmentBoard.test.tsx`, `BlockedTaskPanel.test.tsx`,
  `useProjectWorkspace.test.ts`)과 `QaWorkbench.test.tsx`: 위 변경에 맞춰 고치고 완료 조건별 검사를 더했다.

## 검증 단계와 결과

- `npx vitest run src/features/projects/components/WorkspaceShell.test.tsx`: 38 통과, 5 건너뜀(기존).
  오늘 화면 검사 셋을 새로 넣어 완료 조건 1·2·3·9를 각각 확인했다.
- `npx vitest run src/features/projects/components/DevelopmentBoard.test.tsx`: 74 통과. 완료 조건 4·6·10을
  확인했다.
- `npx vitest run src/features/projects/components/qa/QaWorkbench.test.tsx`: 통과. 처음 열 기능 지정과
  미지정 검사를 넣어 완료 조건 5를 확인했다.
- `npx vitest run src/features/projects/components/BlockedTaskPanel.test.tsx`: 18 통과. 완료 조건 11을
  확인했다.
- `npx vitest run src/features/projects/application/useProjectWorkspace.test.ts`: 통과. 완료 조건 7의 화면
  쪽 배선 정리를 확인했다.
- `cd src-tauri && cargo test`: 751 + 20 통과, 실패 0. 완료 조건 7의 뒷부분과 완료 조건 8을 확인했다.
- `npm run typecheck` 다음 `npm test`: 타입 오류 0, 검사 파일 30개 569 통과 42 건너뜀(기존). 쓰이지 않는
  선언이 남지 않았다.
- 선언한 범위 파일 밖에서 타입 오류나 검사 실패가 남지 않았다. 검증 단계 8이 말한 되돌림은 필요 없었다.

## 남은 위험

- 품질 확인을 시작하는 자리가 작업대 한 곳뿐이 되었다. 그 화면에서 막히면 확인을 이어갈 다른 입구가 없다.
- 작업 상세의 이동 조작은 목록 요약에서 그 작업이 속한 묶음을 찾지 못하면 그리지 않는다. 목록이 최신이 아닌
  드문 경우 조작이 잠깐 보이지 않을 수 있고, 잘못된 기능으로 보내는 대신 그리지 않는 쪽을 택했다.
- 삭제한 두 화면을 확인하던 검사도 함께 지웠다. 자동 검사 결과가 사용자 확인을 대신하던 자리라 이번
  QA에서 사람이 직접 보는 것이 그 자리를 메운다.
- 오늘 화면의 기획서 승인 건수는 앱이 센 값을 그대로 쓰고 품질 확인 건수만 화면에서 더한다. 앱이 센 값과
  화면 목록이 어긋나면 배지와 목록도 어긋나는데, 이는 이전과 같은 성질이며 이번에 바꾸지 않았다.

## 후속 작업

- 삭제한 일괄 확인 화면이 확인하던 것 가운데 "여러 건을 한 번에 기록하고 건별 실패를 읽는다"는 성질은 이제
  작업대의 제출 화면이 맡는다. 그 화면의 검사는 앞선 작업이 이미 갖고 있어 이번에 더하지 않았다.
- 앱 백엔드에는 화면이 더 이상 부르지 않는 기록 명령 두 개가 남아 있다. 완료 조건 7이 남기라고 정한
  것이므로 지우지 않았고, 정리 여부는 별도 판단이 필요하다.

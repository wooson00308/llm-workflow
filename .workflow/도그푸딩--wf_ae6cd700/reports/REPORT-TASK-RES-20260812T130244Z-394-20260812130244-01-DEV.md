# TASK-RES-20260812T130244Z-394-20260812130244-01 개발 보고서

## 결정권자 요약

실행 기록의 도구 사용 항목에 어떤 도구를 썼는지 이름 하나만 남도록 구현했다.
Claude는 도구 사용 블록이 준 이름을 그대로 싣고, Codex는 MCP 도구 호출이면 그 도구 이름을, 이름을
담지 않는 항목이면 항목 유형을 싣는다. 이름을 확인할 수 없으면 값을 비운다.
도구에 넘긴 입력과 인자, 명령 문자열, 도구가 돌려준 결과 본문은 어디에도 남기지 않는다.
Codex 항목 모양은 추측하지 않고 실제 실행을 한 번 떠서 확인했다. 명령 실행 항목에는 이름 값이
아예 없고 명령 문자열만 있어서, 그 경우에는 항목 유형을 이름 대신 쓴다.
가짜 인증 값과 가짜 명령 문자열을 넣은 이벤트를 저장한 뒤 저장 내용 전체와 이벤트 파일과 데이터베이스
원본을 검색해 그 문자열이 없음을 확인했다.
이름이 없는 기존 기록은 그대로 읽히며 계약 위반이 아니라는 사실을 계약 문서에 적었다.
검사 343개가 통과했고, 화면이 아니라 자동 검사로 닫은 작업이다.

## 변경 파일과 모듈

- `../../Git/claude-heartbeat/src/heartbeat/providers/process.py`: 공통 이벤트 어휘의 정규화 결과와
  저장용 이벤트 양쪽에 선택 필드 `tool_name`을 더했다. 값이 없으면 비어 있는 것이 기본이다. 정규화
  결과를 이벤트로 옮기는 자리와 이벤트를 만드는 자리에 이 값을 함께 넘기도록 고쳤다.
- `../../Git/claude-heartbeat/src/heartbeat/providers/claude.py`: 도구 사용 블록을 찾기만 하던 판정을
  블록 자체를 돌려주도록 바꾸고, 그 블록에서 이름 값 하나만 읽는다. 블록의 입력과 인자와 식별자는
  읽지 않는다. 이름이 없으면 필드를 비우고 종류는 지금처럼 도구로 남긴다.
- `../../Git/claude-heartbeat/src/heartbeat/providers/codex.py`: 같은 필드를 쓴다. 항목이 도구 이름을
  직접 담고 있으면 그 값을 쓰고, 담고 있지 않으면 항목 유형을 쓴다. 명령 문자열은 읽지 않는다.
- `../../Git/claude-heartbeat/src/heartbeat/agent_dispatch.py`: 이벤트를 저장하기 직전에 키를 손으로
  골라 담는 목록에 도구 이름을 더했다. 이 자리를 고치지 않으면 새 필드가 저장까지 도달하지 않는다.
- `../../Git/claude-heartbeat/docs/agent-runtime-contract.md`: 로그 절에 이 선택 필드와 값 범위,
  그리고 필드가 없거나 비어 있는 기존 기록도 계약 위반이 아니라는 사실을 적었다.
- `../../Git/claude-heartbeat/tests/test_agent_providers.py`: Claude와 Codex 양쪽의 이름 추출 검사와
  이름이 섞여 들어오는 검사를 더했다.
- `../../Git/claude-heartbeat/tests/test_agent_dispatch.py`: 저장과 조회를 함께 지나는 검사를 더했다.
  가짜 provider가 실제 Claude 정규화 경로를 그대로 쓰도록 하고, 도구 이벤트는 환경 변수를 준 검사에서만
  나오게 해서 기존 검사의 이벤트 구성은 바꾸지 않았다.

## Codex 항목 모양 확인

작업 문서가 확인하지 못한 모양을 가정하지 말라고 요구해서 실제 실행 결과로 확인했다.

- 설치된 Codex CLI 0.147.0으로 임시 디렉터리에서 읽기 전용 실행을 한 번 떠서 출력을 받았다. 명령 실행
  항목의 키는 식별자, 유형, 명령 문자열, 누적 출력, 종료 코드, 상태였고 이름 값은 없었다. 그래서 이
  항목에서는 항목 유형을 이름 대신 쓴다.
- MCP 도구 호출 항목은 이번 실행에서 나오지 않았다. 대신 실행 파일에 들어 있는 필드 이름 표에서 서버와
  도구 이름과 인자가 한 묶음으로 있는 것을 확인해 도구 이름 키를 정했다. 그 키가 없으면 항목 유형으로
  물러나므로, 확인하지 못한 모양 때문에 값이 잘못 만들어지지는 않는다.

## 검증 단계와 결과

1. `python -m pytest tests/test_agent_providers.py` → 34개 통과. 이름 추출과 이름 없는 경우, 섞여
   들어오는 경우를 모두 포함한다.
2. `python -m pytest tests/test_agent_dispatch.py` → 32개 통과. 저장하고 다시 읽었을 때 도구 이름이
   보존되는지, 이름이 없는 기록이 그대로 읽히는지 확인한다.
3. `python -m pytest` → 343개 통과, 8개 건너뜀. 건너뛴 검사는 이번 변경과 무관한 운영체제 조건이며
   변경 전과 같다.
4. 완료 조건 7은 검사 코드 안에서 수행했다. 가짜 인증 값과 가짜 명령 문자열을 도구 사용 블록의 입력에
   넣고 정규화와 저장을 지나게 한 뒤, 저장된 이벤트 전체와 이벤트 파일과 데이터베이스 파일 원본을
   문자열로 검색해 주입한 값과 블록 식별자가 없음을 단언했다.
5. `python -m ruff check src tests` → 지적 11개. 모두 이번에 손대지 않은 파일에서 나왔고 변경 전부터
   있던 것이다. 이번에 고친 일곱 파일에는 지적이 없다. 규칙에 따라 알리기만 하고 고치지 않았다.

## 남은 위험

- 도구 이름은 provider가 고정 어휘로 주는 값이라 판단해 민감정보 제거 함수를 거치지 않는다. 상세 문장은
  지금도 그 함수를 거친다. 이름 키에 값이 실려 오는 경로만 읽으므로 인자나 명령이 섞일 자리는 없지만,
  provider가 이름 자리에 자유 문자열을 넣기 시작하면 이 판단을 다시 봐야 한다.
- MCP 도구 호출 항목의 도구 이름 키는 실제 실행으로 확인하지 못했다. 키가 다르면 그 항목의 이름은
  항목 유형으로 남으며, 값이 틀리게 실리지는 않는다.
- 저장 계층과 조회 명령은 키를 고르지 않고 그대로 나르는 것을 코드에서 확인했고 검사로도 지났다.

## 후속 작업

- 도구 이름을 사용자 유형으로 묶어 화면에 집계로 보여 주는 일은 이 작업의 범위 밖이며 다음 작업이 맡는다.
- 역할 밖 발견: 이번에 손대지 않은 네 파일에 위 지적 11개가 남아 있다. 이 작업의 범위가 아니라 그대로 뒀다.
- 역할 밖 발견: Codex의 항목 이벤트는 식별자를 항목 안에 담는데, 지금 원본 식별자 추출은 최상위 키만
  본다. 그래서 항목 이벤트의 원본 식별자가 비어 있다. 이번 완료 조건과 무관해서 손대지 않았다.
- 런타임 예약 인계로 받은 결과 접두사는 RES-20260812T131229Z-94673-20260812131229이다. 개발 역할은 새
  식별자를 만들지 않으므로 이 보고서에 기록만 남긴다.

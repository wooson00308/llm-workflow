# TASK-S051-10 두 번째 개발 보고서

## 결정권자 요약

사용자가 요청한 종료 실행의 고정 소요 시간 표시를 실제 런타임 응답부터 화면까지 대조했다.
런타임 v0.8.3은 `finishedAt`을 정상 제공하지만 앱의 Rust 실행 요약 타입이 필드를 버린다.
필수 Rust 소유 파일이 작업 범위에 없어 제품 코드는 수정하지 않고 정의 오류로 아키텍트에게 넘겼다.
기존 TASK-S055-04 구현 변경과 사용자 QA 결정은 그대로 보존했다.

## 변경 파일과 모듈

- `TASK-S051-10.md`: 새 QA 재작업의 시작과 정의 오류 차단 이력, 최신 막힌 사유를 기록했다.
- `REPORT-TASK-S051-10-DEV-2.md`: 누락 경로와 직접 근거, 재개 조건을 남겼다.
- 제품 코드와 검사는 수정하지 않았다.

## 검증 절차와 결과

- `QA-9E782ED9`가 `TASK-S051-10`을 `todo`로 돌렸고 개발자 자격 판정이 해당 작업을 선택함을 확인했다.
- 런타임 v0.8.3 상태 응답의 종료 기록에 `finishedAt`이 들어오는 것을 실제 로컬 DB 기록으로 확인했다.
- `src-tauri/src/application/agent_runtime_status_service.rs`가 상태 응답을 `QueueSnapshot`으로 해석하고, 그 안의 실행이 `src-tauri/src/domain/agent_runtime.rs`의 `RunSummary`를 사용함을 추적했다.
- 현재 `RunSummary`에는 `started_at`만 있고 `finished_at`이 없어 serde가 선택 필드를 버리며, 해당 소유 파일이 `scope_files`에 없음을 확인했다.
- 범위 밖 제품 파일은 수정하지 않았다.

## 남은 위험

- Rust 타입을 추가하지 않고 프런트만 수정하면 종료 시각이 항상 빠져 기존과 새 실행 모두 정확한 소요 시간을 표시할 수 없다.
- 앱 최소 런타임과 로컬 v0.8.3 번들 연결은 TASK-S051-11 소유이며 이 정의 교정에 섞지 않았다.

## 후속 작업

- 아키텍트는 `src-tauri/src/domain/agent_runtime.rs`를 범위에 추가하고 Rust `finishedAt` 전달 계약과 회귀 검사를 명시한다.
- 교정 뒤 개발자는 활성 실행은 분 단위 `진행 시간`, 종료 실행은 고정 `소요 시간`으로 렌더링하고 1분 미만 종료만 초 단위로 표시한다.

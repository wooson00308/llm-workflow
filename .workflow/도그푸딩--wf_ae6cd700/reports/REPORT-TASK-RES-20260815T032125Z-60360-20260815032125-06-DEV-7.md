# TASK-RES-20260815T032125Z-60360-20260815032125-06 개발 보고서 7

## 결정권자 요약

이 작업의 일곱 번째 개발 세션이다. 구현은 이미 완성돼 있어 새로 쓴 제품 코드는 없고, 앞선 보고서의
수치를 옮기지 않고 현재 공유 기준 위에서 자동 검사를 처음부터 다시 실행해 전부 통과하는 것을 확인했다.
반영은 이번에도 시작하지 않았다. 공유 작업 공간에 미커밋 변경 네 개가 남아 있고, 개발자 역할 계약이 그
상태에서 반영을 시작하지 못하게 하기 때문이다. 이번에 새로 확인한 것은 앞선 보고서들이 요청한 관리 파일
두 개만 커밋해서는 이 정지가 풀리지 않는다는 사실이다. 나머지 둘은 역할 세션이 공유 저장소에 정본으로
써야 하는 제어 문서이고, 세션이 작업을 넘겨받는 순간 규칙에 따라 다시 수정된다. 반영을 여는 조건은 네
개를 한 번에 커밋하는 것이며, 대조 결과 그렇게 해도 사라지는 사용자 작업은 없다.

## 이어받은 것과 버린 것

앞선 세션의 통합 후보 커밋 `81228cf`(작업 사본 `lease-3606-20260815115249`)와 보고서 6을 이어받았고, 버린 것은 없다.

- 유지: `81228cf`의 네 파일 변경 전부. 완료 조건 1~13과 다시 대조했고 어긋나는 자리가 없다. 삭제되거나
  약해진 시험은 없다.
- 유지: 기준 커밋 `ab22f9a`. 공유 기준이 아직 같아 후보를 옮길 필요도 병합 검토를 되풀이할 이유도 없었다.
- 새로 만든 것: 이번 lease의 사본에 `81228cf`를 cherry-pick한 커밋 `2b8fcb4`. 트리가 같음을 해시로 대조했고,
  앞선 lease의 사본이 회수될 수 있어 같은 내용을 이번 가지에도 두었다.

## 변경한 파일과 모듈

`2b8fcb4`가 담은 네 파일이며 이번 세션이 새로 작성한 제품 코드는 없다.

- `src-tauri/src/domain/agent_runtime.rs`: 상수 `ISOLATION_CAPABLE_RUNTIME_VERSION`("0.10.0"),
  지원·미지원·모름을 구분하는 `IsolationSupport`, 판정 함수 `judge_isolation_support`.
- `src-tauri/src/application/agent_runtime_run_service.rs`: `plan`이 격리 판정을 인자로 더 받고 기존
  `guard` 뒤에 `guard_isolation`을 둔다. 실패 값 `RunFailure::IsolationUnsupported` 추가.
- `src-tauri/src/commands/agent_runtime.rs`: `run_gate_of`가 조회 한 번에서 두 판정을 함께 꺼낸다.
- `docs/agent-runtime.md`: 격리 지원 최소 판과 의도된 정지를 적은 절 하나.

작업 문서만으로 판단이 가능해 기획서 본문과 결정 문서 본문은 열지 않았다.

## 검증 단계와 결과

기준 `ab22f9a` 위의 커밋 `2b8fcb4`에서 전부 다시 실행했고, 반영을 시작하지 않았으므로 모두 격리 상태의
값이다. 디스크 여유가 13G뿐이고 새 빌드 캐시 하나가 3.8G를 쓰므로, 같은 트리를 빌드했던 만료 lease의
캐시를 `CARGO_TARGET_DIR`로 재사용했다. 디스크 사용량은 늘지 않았다.

- `cargo test ... agent_runtime`: 107개 통과, 실패 0. e2e 대상 21개도 통과, 실패 0.
- `cargo test ... agent_runtime_run_service`: 14개 통과, 실패 0. 완료 조건 4~7의 시험을 포함한다.
- `cargo test ... agent_runtime_install_service` 18개와 `... agent_runtime_status` 14개 통과, 실패 0(완료 조건 10).
- 저장소 전체 `cargo test`: 831개와 25개 통과, 실패 0.
- `cargo fmt --check` 종료 코드 0. `cargo check`는 오류와 경고를 합쳐 0건. 위 명령은 모두
  `--manifest-path src-tauri/Cargo.toml`을 붙였다.
- `npm run agent-runtime:verify -- --self-test`: 통과. "정상 fixture 1건과 불일치 fixture 3건".
- 완료 조건 12: 릴리스 검사의 실제 실행은 기준과 후보 양쪽에서 똑같이 Python 부재로 끝나며, 이번 변경과
  무관한 기존 환경 조건이다. 검사가 읽는 세 값도 양쪽 파일에서 같게 추출된다. 새 상수 주석의
  `MINIMUM_RUNTIME_VERSION` 문자열은 정규식이 `: &str =`까지 요구해 일치하지 않는다.
- 완료 조건 9: `SUPPORTED_API_MAJOR` 1, `MINIMUM_RUNTIME_VERSION` "0.9.0", `MAXIMUM_RUNTIME_VERSION`
  "1.99.99"로 그대로다.
- 완료 조건 8: 후보의 diff에서 `start`·`cancel`·`retry` 관련 시험은 한 줄도 바뀌지 않았다.
- 완료 조건 13: `git diff --name-only ab22f9a HEAD -- src/`가 0건이다.

## 반영에서 멈춘 지점

격리 검사 결과와 반영 결과는 별개의 사실이다. 위 검사는 모두 통과했고 반영은 시작하지 않았다. 공유 기준
`ab22f9a`는 후보의 기준과 같지만 추적 파일 4개에 미커밋 변경이 있다. 개발자 역할 계약은 이 상태에서 자동
반영을 시작하지 않고, 남의 미커밋 변경을 커밋하거나 되돌리지도 않는다.

관리 파일 두 개는 이번 기준에서 다시 바이트 단위로 대조했다. `.workflow/rules/wf-eligible.sh`는
`heartbeat_condition.rs`의 `CONDITION_SCRIPT_SH`와, `.workflow/rules/wf-reserve.sh`는
`reservation_helper.rs`의 `RESERVATION_HELPER_SH`와 길이와 내용이 완전히 일치한다(44708자, 11924자).

나머지 두 개는 이 워크플로의 작업 문서 `-03`과 `-06`이며, 이번 세션은 그 성격을 앞선 보고서보다 정확히
확인했다. `.gitignore`가 제외하는 것은 `.workflow/.runtime/`뿐이고 제어 문서 935개는 모두 추적 대상이며,
공통 규칙 2절은 작업 상태와 역할 보고서를 공유 저장소에만 쓰도록 요구한다. `-06`의 미커밋 diff는 전부
역할 세션이 쓴 상태·이력·범위 정정이고 사람의 편집은 없다. 또한 4절의 청구 절차는 실제 작업을 시작하기
전에 이력 항목을 먼저 쓰도록 요구하므로, 반영을 수행할 세션 자신이 반영을 시도하기 전에 `-06`을 반드시
더럽힌다. 관리 파일 두 개만 커밋해도 반영 조건이 충족되지 않는 이유가 이것이다.

통합 후보를 갱신해 둔다. 커밋 `2b8fcb4`(가지
`wf-iso/TASK-RES-20260815T032125Z-60360-20260815032125-06/lease-68043-20260815120943`), 기준 `ab22f9a`,
변경 파일은 위 네 개다. 작업 상태는 `in_progress`로 두었다. 반영 대기는 계약이 별도로 이름 붙인 상태라
`blocked`로 두지 않았고, 반영과 재검사를 마치지 않아 `verified`로도 두지 않았다.

## 남은 위험

- 반영 전까지 승인된 기획서가 요구한 정지가 공유 기준에 없다. 격리를 지원하지 않는 실행 환경에서 개발
  역할 실행이 지금처럼 열린다.
- 관문은 실행 계획 단계에만 있다. 자동 배정은 이 경로를 지나지 않으며 작업 문서가 범위 밖으로 명시했다.
- 공유 기준이 또 전진하면 후보를 그 커밋 위로 옮겨 검사를 다시 실행해야 한다.
- 디스크 여유가 12G 안팎이고 격리 예약 한도는 20G다. 새 사본이 빌드 캐시를 만들 때마다 다른 예약이 대기할 수 있다.

## 후속 작업

- 사용자 확인 필요: 미커밋 변경 네 개를 한 번에 커밋해야 반영이 열린다. 앞선 보고서들이 요청한 관리 파일
  두 개만으로는 부족하다. 위 대조대로 네 개 모두 커밋해도 잃는 작업이 없으며, 에이전트의 대리 정리는
  계약이 금지한다.
- 아키텍트 인계: 제어 문서가 추적 대상인 한 반영 전제 조건인 "깨끗한 공유 작업 공간"을 어떤 개발 세션도
  구조상 만족시킬 수 없다. 제어 문서를 추적에서 뺄지, 앱이 커밋할지, 반영 판정에서 제외할지는 설계
  결정이므로 개발 세션이 정할 수 없다.
- 아키텍트 인계: 반영 대기 작업을 개발자 판정에서 제외하는 관문(`role_eligibility.rs` 414행,
  `heartbeat_condition.rs` 1167행)이 읽는 `integration_waiting`을 제품 코드에서 쓰는 곳이 없음을 다시
  확인했다. 이 값은 읽는 자리와 시험에만 있고 `reservation_helper.rs`가 쓰는 값은 `ready`, `failed:*`,
  `waiting:storage` 셋뿐이며 이 작업의 기록도 `step: ready`다. 기록은 앱 소유 상태라 개발 세션이 만들 수
  없고, 이것이 이 작업이 일곱 번째로 다시 호출된 이유다.
- 아키텍트 인계: 관리 스크립트 원본을 바꾸는 작업의 완료 조건에 설치 사본 갱신을 넣을지 정해야 한다.
- 아키텍트 인계: `docs/agent-runtime.md` 버전 표가 앱 판 0.1.11을 적어 릴리스 0.1.18과 어긋난다(일곱 번째 기록).
- 이번 예약의 결과 접두사는 RES-20260815T121009Z-68043-20260815120943이다.

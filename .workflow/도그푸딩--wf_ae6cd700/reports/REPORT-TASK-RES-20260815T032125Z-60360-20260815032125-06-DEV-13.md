# TASK-RES-20260815T032125Z-60360-20260815032125-06 개발 보고서 13

## 결정권자 요약

이 작업의 열세 번째 개발 세션이다. 제품 코드는 여섯 번째 세션에서 이미 완성됐고 이번에도 새로 작성한 제품 코드는 없다.
공유 기준이 앞선 세션 이후 전혀 움직이지 않았고, 앞선 세션이 검증한 결과물과 이번 사본에 옮긴 결과물이 내용까지
완전히 같음을 확인했다. 그래서 전체 시험을 다시 돌리지 않았다. 같은 입력에 같은 결과가 나온다는 것을 이미 아는 상태에서
전체 재빌드와 디스크 2GB를 다시 쓰는 대신, 비용이 들지 않는 검사만 실행해 결과가 그대로임을 확인했다. 디스크 여유가
15GB뿐이라 이 판단의 실익이 작지 않다.
반영은 이번에도 시작하지 못했다. 공유 작업 공간에 미커밋 변경 네 개가 남아 있고 개발자 역할 계약이 그 상태에서 반영을
금지한다. 네 개 중 둘은 이 워크플로 자신이 작업을 시작할 때 반드시 만드는 작업 문서라, 어떤 개발 세션도 이 조건을
스스로 만족시킬 수 없다. 같은 지점에서 멈춘 것이 열세 번째이며 사용자만 이 상태를 풀 수 있다.

## 이어받은 것과 버린 것

앞선 세션의 통합 후보 커밋 `2665303`과 보고서 12를 이어받았다. 버린 것은 없다. 제품 코드, 시험, 문서, 작업 상태 어느
쪽에서도 앞선 세션의 산출물을 폐기하거나 약화시키지 않았고 삭제한 시험도 없다.

- 유지: `2665303`의 네 파일 변경 전부. 이번 사본에 cherry-pick해 커밋 `f7f9e53`을 만들었고 충돌은 없었다.
- 유지: 기준 커밋 `55a7af2`. 공유 기준이 전진하지 않아 후보를 다른 커밋 위로 옮기지 않았다.
- 앞선 사본의 빌드 캐시는 이번에 복사하지 않았다. 전체 시험을 다시 돌리지 않기로 했으므로 필요가 없고, 디스크 여유가
  15GB까지 줄어든 상태에서 2GB를 더 쓰지 않는 편이 낫다고 판단했다.

## 변경한 파일과 모듈

`f7f9e53`이 담은 네 파일이며 이번 세션이 새로 작성한 제품 코드는 없다. 작업 문서만으로 판단이 가능해 기획서 본문과
결정 문서 본문은 열지 않았다.

- `src-tauri/src/domain/agent_runtime.rs`: 상수 `ISOLATION_CAPABLE_RUNTIME_VERSION`("0.10.0"), 지원·미지원·모름을
  구분하는 값 `IsolationSupport`, 판정 함수 `judge_isolation_support`.
- `src-tauri/src/application/agent_runtime_run_service.rs`: `plan`이 격리 판정을 인자로 더 받고, 기존 `guard` 바로
  뒤에서 개발 역할 슬롯을 검사한다. 실패 값 `RunFailure::IsolationUnsupported` 추가.
- `src-tauri/src/commands/agent_runtime.rs`: `run_gate_of`가 조회 한 번에서 호환 판정과 격리 판정을 함께 꺼낸다.
- `docs/agent-runtime.md`: 격리 지원 최소 판과 의도된 정지를 적은 절 하나.

## 검증 단계와 결과

이번 세션이 실행한 검사와 앞선 세션에서 이어받은 검사 결과를 구분해 적는다. 아래는 모두 격리 상태의 값이고, 반영을
시작하지 않았으므로 반영 후 검사 결과는 없다.

이번 세션이 직접 실행한 것:

- 결과물 동일성: `f7f9e53`의 트리 해시가 `a947a27`로, 보고서 12가 128개 시험 통과를 기록한 `2665303`의 트리 해시와
  같다. 부모도 `55a7af2`로 같다. 두 커밋이 같은 기준 위에서 같은 내용을 담고 있음을 해시로 확인했다.
- 완료 조건 12: `node scripts/verify-agent-runtime-contract.mjs --self-test` 통과("정상 fixture 1건과 불일치
  fixture 3건"). 실제 대조는 앞선 세션이 확인한 대로 작업 사본에서 저장소 위치 지정 없이는 실행되지 않는다.
- 완료 조건 9와 12의 핵심 위험: 릴리스 검사가 쓰는 정규식 세 개를 스크립트에서 그대로 꺼내 변경 후 파일에 직접
  적용했다. 계약 주 버전 1, 최소 판 "0.9.0", 최대 판 "1.99.99"를 그대로 읽는다. 새 상수 이름이 첫 일치를 가로채지
  않는다는 사실을 시험 통과가 아니라 정규식 실행으로 확인했다.
- 완료 조건 1: `ISOLATION_CAPABLE_RUNTIME_VERSION`이 "0.10.0"이고 이름에 `MINIMUM_RUNTIME_VERSION`이 부분 문자열로
  들어 있지 않다.
- 완료 조건 13: `git diff --name-only 55a7af2 HEAD -- src/`가 0건이다.

이어받은 검사 결과(이번 세션이 재실행하지 않음):

- 보고서 12가 기록한 `cargo test ... agent_runtime` 본체 107개와 e2e 21개 통과, `cargo fmt --check` 종료 코드 0,
  `cargo check` 오류·경고 0건, 완료 조건 2~11의 재현 시험 통과.
- 재실행하지 않은 근거: 개발자 역할 계약은 격리 검사의 재실행을 기준이 달라졌을 때로 한정한다. 공유 기준은 `55a7af2`
  그대로이고 검사 대상 트리는 해시까지 같으므로, 재실행은 정의상 같은 결과만 낸다. 사본의 빌드 캐시가 비어 있어
  전체 재빌드가 필요하고 디스크 여유가 15GB인 상황에서 새 정보가 없는 실행을 선택하지 않았다.

## 반영에서 멈춘 지점

반영 직전에 다시 대조한 공유 기준 `55a7af2`는 후보의 기준과 같다. 스테이지된 변경도 진행 중인 기준 변경도 없다.
그러나 추적 파일 네 개에 미커밋 변경이 있어 반영을 시작하지 않았다. 개발자 역할 계약은 이 상태에서 자동 반영을 금지하고,
남의 미커밋 변경을 커밋하거나 되돌리는 것도 금지한다.

네 개의 성격은 두 가지로 갈린다. `.workflow/rules/wf-eligible.sh`와 `.workflow/rules/wf-reserve.sh`는 앱이 설치하는
사본에 기능 변경이 더해진 판이고, 커밋 여부는 사용자가 정할 일이다. 나머지 둘은 이 워크플로의 작업 문서 `-03`과 `-06`이며,
공통 규칙 4절이 작업 시작 전에 상태와 이력부터 쓰도록 요구하므로 반영을 수행할 세션 자신이 반영을 시도하기 전에 반드시
이 두 파일을 더럽힌다. 이번 세션도 이력 항목을 쓰는 순간 같은 상태를 다시 만들었다.

통합 후보를 갱신해 둔다. 커밋 `f7f9e53`(가지
`wf-iso/TASK-RES-20260815T032125Z-60360-20260815032125-06/lease-26801-20260815134048`), 기준 `55a7af2`, 변경 파일은 위
네 개다. 작업 상태는 `in_progress`로 두었다. 반영 대기는 계약이 따로 이름 붙인 상태라 `blocked`가 아니고, 반영 후
재검사를 마치지 않아 `verified`도 아니다.

## 남은 위험

- 반영 전까지 승인된 기획서가 요구한 정지가 공유 기준에 없다. 격리를 지원하지 않는 실행 환경에서 개발 역할 실행이 열려 있다.
- 이번 세션은 Rust 시험을 재실행하지 않았다. 결과물이 해시까지 같다는 사실이 근거이므로, 후보를 다른 기준 위로 옮기는
  다음 세션은 반드시 전체 검사를 다시 실행해야 한다.
- 관문은 실행 계획 단계에만 있다. 자동 배정은 이 경로를 지나지 않으며 작업 문서가 범위 밖으로 명시했다.
- 앞선 사본들의 빌드 캐시가 계속 쌓여 디스크 여유가 15GB까지 줄었다. 오늘 한 번 포화된 이력이 있다.

## 후속 작업

- 사용자 확인 필요: 미커밋 변경 네 개를 커밋해야 반영이 열린다. 에이전트의 대리 정리는 계약이 금지하므로 사용자만 할 수
  있다. 열세 번째 요청이다.
- 아키텍트 인계: 제어 문서가 추적 대상인 한 반영 전제 조건인 깨끗한 공유 작업 공간을 어떤 개발 세션도 구조상 만족시킬 수
  없다. 추적에서 뺄지, 앱이 커밋할지, 반영 판정에서 제외할지는 설계 결정이므로 개발 세션이 정할 수 없다. 보고서 8부터
  12까지와 같은 항목이며 아직 열려 있다.
- 아키텍트 인계: 반영 대기 작업을 개발자 배정에서 빼는 관문이 읽는 `integration_waiting` 값을 저장소의 어느 자리도 쓰지
  않는다. 값을 적는 주체가 별도 저장소의 실행 환경이라 범위 밖이며, 그 저장소가 고치기 전까지 같은 작업이 계속 다시
  배정된다. 이 반복이 이번 세션을 포함해 열세 번 일어났고, 매 세션이 사본과 빌드 캐시를 새로 만들어 디스크를 소모한다.
- 아키텍트 인계: 회수된 사본의 빌드 캐시 재사용 가능 여부, 릴리스 검사의 저장소 위치 전달 방법, 관리 스크립트 원본 변경 시
  설치 사본 갱신 의무, `docs/agent-runtime.md` 버전 표와 릴리스 판의 불일치. 네 항목 모두 보고서 12에 상세가 있고
  아직 열려 있다.
- 이번 예약의 결과 접두사는 RES-20260815T134049Z-26801-20260815134048이다.

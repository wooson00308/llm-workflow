# REPORT-SPEC-028-PLAN

> 기록 경위: 하네스가 서브에이전트(plan-a)의 보고서 파일 작성을 차단해, 발신된 전문을 TL이 대리 기록한다. 내용은 발신 전문 그대로다. (수신 2026-08-04T09:24Z, TL 세션)

기획자 세션 결과. IDEA-0C30206C를 SPEC-028로 합성했다.

- 대상 아이디어: IDEA-0C30206C (위임 대리 결정의 계약 정식 편입)
- 산출 기획서: SPEC-028 (status: user_review) — 승인 대기. 주의: 이 기획서 자체는 앱 도장으로 승인받아야 한다(대리 승인은 자기모순).
- 선점: lease-21153-20260804090438 (획득→갱신→해제 전부 exit 0)
- 작성 시각: 2026-08-04T09:22:00Z

## 핵심 기획 결정 — 아이디어 전제 두 개를 실측으로 뒤집음

1. 규모는 4건이 아니라 17건: 결정 25건 중 17건이 수기 형식(초 단위 Z vs 앱의 마이크로초+00:00 — 두 집합이 정확히 갈림). 앱 형식 8건은 SPEC-001~004·009·019·020·021. 대리 기록은 예외적 사고가 아니라 이 워크플로우의 작동 방식이었다 — 17건 위에서 결정해야 한다.
2. "앱 도장 사후 재가"는 현재 앱에서 불성립: apply_latest_decision(fs_project_repository.rs:342)이 최신 결정을 상태에 덮고, user_review가 아니면 SpecNotAwaitingDecision으로 거부(:343~:345). 대리 결정이 created_by: user라 필터(:1579)를 통과해 SPEC-022~025는 앱이 보기에 이미 approved — 대리 결정이 자기 교정 절차를 스스로 막는다.
3. (1)과 (2)는 커플링: created_by를 별도 값으로 나누면 앱 읽기 경로 둘이 그 문서를 걸러 기획서가 user_review로 남아 재가 도장이 앱 수정 없이 가능해진다.
4. 재가의 구조적 위험 추가 발견: 아키텍트 자격 판정(스크립트 :129~:141, role_eligibility.rs:97~:112)이 결정의 최신 여부를 안 봄 — 계약 문언("latest approved")과 구현이 어긋나며, 재가 결정(새 id, 참조 작업 0)이 이미 분해된 기획서에서 두 번째 작업 세트를 만든다. 기획자 분기엔 같은 판정이 있고 아키텍트만 비어 있다.
5. 해석 분열의 기록 확인: TASK-063이 대리 승인을 유효로 참조(qa_waiting), architect-agent-b는 SPEC-023 분해 진행 — 아이디어 서술과 일치.
6. 규칙 자산: WORKFLOW_RULES_VERSION 7, ROLE_RULES_VERSION 5(planner 5·architect 4·developer 4), TASK-041 선례("project_instructions.rs를 만지는 유일한 todo") 완료 조건 12에 반영.

## 확인 필요 3건 (사용자 결정 대기 — 앱 도장 권장)

1. created_by를 user로 둘지 별도 값(user-delegate 등)으로 나눌지 — 제안: 별도 값. 같은 값 수기가 §2 사칭의 실체이고, 나누면 재가가 앱 수정 없이 열림. 비용: 조건 스크립트는 created_by를 안 읽어 앱-스크립트 어긋남 발생(맞추려면 SPEC-023 진행분과 같은 파일).
2. 아키텍트 최신 판정을 이번에 맞출지 — 제안: 맞추기. 아니면 재가마다 아키텍트 헛기동.
3. 재가로 덮인 대리 결정 파일 취급 — 제안: 남기기(복수 결정이 감사 로그 설계, 삭제 경로 없음).

## 검증

확인 사실 18건을 계약·앱 코드·조건 스크립트·결정 25건 전수 파싱으로 대조(추정 0). 인용 8곳 정정. 특기: 작성 중 SPEC-023 구현이 진행돼 scripts/wf-eligible.sh 삭제(git D)·docs/heartbeat.md 수정 관측 — 스크립트 인용을 설치본 기준으로 이관(확인 사실 18).

## 핸드오프

- SPEC-028 승인은 앱 도장으로 (아직 결정이 없어 user_review 상태 — 앱이 받을 수 있음).
- 확인 1·2 제안대로면 조건 스크립트·role_eligibility.rs에서 SPEC-023 파생 작업과 파일 겹침 — 분해 순서에 확인 사실 18 참조.
- 확인 2 제안대로면 규칙 본문+판정 구현이 한 승인에서 나옴 — TASK-041 선례대로 규칙 자산 작업은 하나로.
- 잔여 관찰: IDEA-5C6073A5는 하트비트 기획자가 SPEC-027로 처리 중.

## 보호 상태

생성 파일 SPEC-028.md 하나. decisions/ 포함 보호 상태 무변경(문제의 대리 결정 파일들도 읽기만), 커밋 없음, lease 해제 완료.

# TASK-RES-20260815T032125Z-60360-20260815032125-06 개발 보고서 8

## 결정권자 요약

이 작업의 여덟 번째 개발 세션이다. 제품 코드는 이미 완성돼 있어 이번에 새로 쓴 코드는 없고,
앞선 보고서의 수치를 옮기지 않고 현재 공유 기준 위에서 자동 검사를 처음부터 다시 실행해 전부
통과하는 것을 확인했다. 반영은 이번에도 시작하지 못했다. 공유 작업 공간에 미커밋 변경 네 개가
남아 있고, 개발자 역할 계약이 그 상태에서 반영을 시작하지 못하게 하기 때문이다. 이번에 새로 확인한
사실은 두 가지다. 첫째, 앞선 세션의 작업 사본이 회수되어 완성된 결과물이 가지에만 남아 있었고,
이를 되살려 검사하는 데 전체 재빌드가 다시 들었다. 둘째, 반영을 기다리는 작업을 개발자 배정에서
빼도록 만들어진 앱의 관문이 읽는 값을 실제로 기록하는 곳이 저장소 어디에도 없다는 사실을 직접
확인했다. 이 두 가지가 같은 작업이 여덟 번 다시 호출되면서 매번 재빌드 비용을 다시 치르는 이유다.
사용자가 네 파일을 한 번에 커밋하면 반영이 열린다.

## 이어받은 것과 버린 것

앞선 세션의 통합 후보 커밋 `2b8fcb4`와 보고서 7을 이어받았고, 버린 것은 없다.

- 유지: `2b8fcb4`의 네 파일 변경 전부. 완료 조건 1~13과 직접 대조했고 어긋나는 자리가 없다.
  삭제되거나 약해진 시험은 없다.
- 유지: 기준 커밋 `ab22f9a`. 공유 기준이 그대로여서 후보를 옮길 필요가 없었다.
- 새로 만든 것: 이번 lease의 사본에 `2b8fcb4`를 cherry-pick한 커밋 `feae9dd`. 트리 해시가
  `2b8fcb4`와 완전히 같음을 확인했다.

앞선 세션들과 갈리는 지점은 작업 사본의 상태다. 이번 사본 디렉터리는 비어 있었고 앞선 lease들의
사본도 모두 회수돼 있어, 사본을 새로 만들고 후보를 옮긴 뒤 빌드 캐시 없이 검사를 다시 돌려야 했다.

## 변경한 파일과 모듈

`feae9dd`가 담은 네 파일이며 이번 세션이 새로 작성한 제품 코드는 없다.

- `src-tauri/src/domain/agent_runtime.rs`: 상수 `ISOLATION_CAPABLE_RUNTIME_VERSION`("0.10.0"),
  지원·미지원·모름을 구분하는 `IsolationSupport`, 판정 함수 `judge_isolation_support`.
- `src-tauri/src/application/agent_runtime_run_service.rs`: `plan`이 격리 판정을 인자로 더 받고,
  기존 `guard` 바로 뒤에서 `guard_isolation`이 개발 역할 슬롯을 검사한다. 실패 값
  `RunFailure::IsolationUnsupported` 추가.
- `src-tauri/src/commands/agent_runtime.rs`: `run_gate_of`가 조회 한 번에서 두 판정을 함께 꺼낸다.
- `docs/agent-runtime.md`: 격리 지원 최소 판과 의도된 정지를 적은 절 하나.

작업 문서만으로 판단이 가능해 기획서 본문과 결정 문서 본문은 열지 않았다.

## 검증 단계와 결과

기준 `ab22f9a` 위의 커밋 `feae9dd`에서 전부 다시 실행했다. 반영을 시작하지 않았으므로 아래는 모두
격리 상태의 값이다. 명령에는 모두 `--manifest-path src-tauri/Cargo.toml`을 붙였다.

- `cargo test ... agent_runtime`: 107개 통과, 실패 0. e2e 대상 21개도 통과, 실패 0.
- `cargo test ... agent_runtime_run_service`: 14개 통과, 실패 0. 완료 조건 4~7의 시험을 포함한다.
- `cargo test ... agent_runtime_install_service` 18개, `... agent_runtime_status` 14개 통과,
  실패 0(완료 조건 10).
- `cargo fmt --check` 종료 코드 0. `cargo check`는 오류와 경고를 합쳐 0건.
- `npm run agent-runtime:verify -- --self-test`: 통과. "정상 fixture 1건과 불일치 fixture 3건".
- 완료 조건 1: 새 상수 이름에 `MINIMUM_RUNTIME_VERSION`이 부분 문자열로 없다. 상수 주석이 그 이름을
  언급하지만, 릴리스 검사의 정규식은 이름 뒤에 `: &str =`까지 요구하므로 일치하지 않는다.
- 완료 조건 9·12: 릴리스 검사가 쓰는 정규식 세 개를 기준과 후보의 도메인 파일에 각각 적용해
  추출값이 같음을 확인했다. 양쪽 모두 계약 주 버전 1, 최소 판 "0.9.0", 최대 판 "1.99.99"다.
- 완료 조건 8: 후보의 diff에서 삭제된 줄은 import 두 줄과 `plan` 호출 네 자리뿐이고,
  `start`·`cancel`·`retry`의 시험은 한 줄도 바뀌지 않았다.
- 완료 조건 13: `git diff --name-only ab22f9a HEAD -- src/`가 0건이다.
- 저장소 전체 `cargo test`는 다시 돌리지 않았다. 기준과 트리가 보고서 7의 실행과 완전히 같다.

## 반영에서 멈춘 지점

격리 검사 결과와 반영 결과는 별개의 사실이다. 위 검사는 모두 통과했고 반영은 시작하지 않았다.
공유 기준 `ab22f9a`는 후보의 기준과 같지만 추적 파일 네 개에 미커밋 변경이 있다. 개발자 역할
계약은 이 상태에서 자동 반영을 시작하지 않고, 남의 미커밋 변경을 커밋하거나 되돌리지도 않는다.

네 개 중 `.workflow/rules/wf-eligible.sh`와 `.workflow/rules/wf-reserve.sh`는 앱이 켜질 때마다
소스대로 다시 써 넣는 설치 사본이고, 나머지 둘은 이 워크플로의 작업 문서 `-03`과 `-06`이다.
`-06`의 미커밋 변경 일부는 이번 세션이 만든 것이다. 공통 규칙 4절이 실제 작업을 시작하기 전에
이력 항목부터 쓰도록 요구하므로, 반영을 수행할 세션 자신이 반영을 시도하기 전에 반드시 제어 문서를
더럽힌다. 앞선 보고서가 지적한 구조적 정지를 이번 세션이 직접 재현한 셈이다.

통합 후보를 갱신해 둔다. 커밋 `feae9dd`(가지
`wf-iso/TASK-RES-20260815T032125Z-60360-20260815032125-06/lease-38297-20260815122105`),
기준 `ab22f9a`, 변경 파일은 위 네 개다. 작업 상태는 `in_progress`로 두었다. 반영 대기는 계약이 따로
이름 붙인 상태라 `blocked`로 두지 않았고, 반영과 재검사를 마치지 않아 `verified`로도 두지 않았다.

## 남은 위험

- 반영 전까지 승인된 기획서가 요구한 정지가 공유 기준에 없다. 격리를 지원하지 않는 실행 환경에서
  개발 역할 실행이 지금처럼 열린다.
- 관문은 실행 계획 단계에만 있다. 자동 배정은 이 경로를 지나지 않으며 작업 문서가 범위 밖으로 명시했다.
- 공유 기준이 전진하면 후보를 그 커밋 위로 옮겨 검사를 다시 실행해야 한다.
- 사본이 회수되면 다음 세션도 전체 재빌드를 다시 치른다. 디스크 여유가 21G까지 내려갔다.

## 후속 작업

- 사용자 확인 필요: 미커밋 변경 네 개를 한 번에 커밋해야 반영이 열린다. 설치 사본 두 개만으로는
  부족하다. 에이전트의 대리 정리는 계약이 금지하므로 이 커밋은 사용자만 할 수 있다.
- 아키텍트 인계: 반영 대기 작업을 개발자 판정에서 빼는 관문이 읽는 `integration_waiting` 값을
  기록하는 곳이 없다는 사실을 직접 확인했다. 이 문자열은 `heartbeat_condition.rs`,
  `role_eligibility.rs`, `fs_project_repository.rs`의 읽는 자리와 시험에만 있고, 격리 기록의 `step`을
  쓰는 유일한 자리인 `reservation_helper.rs`에는 없다. 이번 예약의 기록도 `step: ready`이며, 기록은
  앱 소유 상태라 개발 세션이 만들 수 없다. 이것이 이 작업이 여덟 번째로 다시 호출된 이유다.
- 아키텍트 인계: 제어 문서가 추적 대상인 한 반영 전제 조건인 깨끗한 공유 작업 공간을 어떤 개발
  세션도 구조상 만족시킬 수 없다. 추적에서 뺄지, 앱이 커밋할지, 반영 판정에서 제외할지는 설계
  결정이므로 개발 세션이 정할 수 없다.
- 아키텍트 인계: 관리 스크립트 원본을 바꾸는 작업의 완료 조건에 설치 사본 갱신을 넣을지 정해야 한다.
- 아키텍트 인계: `docs/agent-runtime.md` 버전 표가 앱 판 0.1.11을 적어 릴리스 0.1.18과 어긋난다(여덟 번째 기록).
- 이번 예약의 결과 접두사는 RES-20260815T122107Z-38297-20260815122105이다.

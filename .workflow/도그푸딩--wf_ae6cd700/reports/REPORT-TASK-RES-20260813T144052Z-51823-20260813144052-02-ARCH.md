# TASK-RES-20260813T144052Z-51823-20260813144052-02 정의 오류 교정 보고서

## 결정권자 요약

이 작업은 고쳐도 되는 파일 목록에 검사 파일 하나가 빠져 막혀 있었다.
실행 시작을 다루는 출시 관문 검사 세 개가 유효한 동의를 준비하지 않아 새 확인에 걸려 실패했는데,
그 파일이 목록 밖이라 개발 세션이 손댈 수 없었다.
저장소를 직접 열어 세 검사가 실행 시작 함수를 곧바로 부르고 동의 기록을 남기지 않는다는 사실을 확인했고,
그 파일이 다른 검사 파일의 준비 코드를 전혀 부르지 않아 범위 안에서는 닿을 수 없다는 것도 확인했다.
그래서 그 파일을 목록에 더하고, 준비 코드에 동의 기록을 더하라는 변경 범위와 완료 조건을 함께 적었다.
검사를 지우거나 단언을 약하게 만들어 통과시키지 않는다는 조건도 명시했다.
작업은 다시 할 수 있는 상태로 돌려놓았고, 식별자와 이력과 막힌 사유는 그대로 두었다.
승인된 기획서가 요구하는 범위는 늘리지도 줄이지도 않았다.
사용자가 지금 결정할 것은 없으며, 확인은 구현이 끝난 뒤 품질 확인 단계에서 요청한다.

## 대상과 산출물

- 대상 작업: TASK-RES-20260813T144052Z-51823-20260813144052-02 (승인 근거 DECISION-44483694,
  SPEC-RES-20260813T085941Z-17368-20260813085941)
- 역할: 프로젝트 아키텍트
- 리스: 런타임이 예약한 lease-13685-20260813152432을 갱신해 소유를 확인했고, 새로 취득하지 않았다.
- 결과 접두사: RES-20260813T152432Z-13685-20260813152432은 사용하지 않았다. 작업 정의 교정은 새 문서
  식별자를 만들지 않으며, 이 접두사로 시작하는 문서 경로가 없다는 것도 확인했다.
- 산출물: 작업 문서 교정 1건, 이 보고서 1건
- 제품 코드, 다른 작업 문서, 상태 이력, git 커밋: 수행하지 않음

## 무엇이 잘못돼 있었는가

교정의 근거는 두 가지다. 작업 문서의 `## 막힌 사유` 절과
`reports/REPORT-TASK-RES-20260813T144052Z-51823-20260813144052-02-DEV.md`가 기록한 실패 내용이다. 사용자
수정 요청 기록은 없으므로 `revision_request_id`는 비워 두었다.

완료 조건 8과 검증 단계가 저장소 전체 검사의 통과를 요구하는데, 그 검사에는
`../../Git/claude-heartbeat/tests/test_agent_end_to_end.py`가 포함된다. 이 파일은 `scope_files`에 없었다.
따라서 작업 문서는 스스로 고칠 수 없는 파일의 통과를 완료 조건으로 요구하고 있었고, 이것은 개발 세션이
더 노력해서 해결할 수 있는 종류가 아니다.

## 저장소에서 확인한 사실

기억이 아니라 현재 코드를 열어 확인했다.

1. `src/heartbeat/agent_dispatch.py`의 `_preflight_target`은 함수 맨 앞에서 `project_may_execute`를
   부른다. 자격 조회보다 앞이므로 동의가 없으면 대상 예약도 실행 도구 시작도 일어나지 않는다. 이 판정은
   `AgentStore.get_consent`가 읽는 저장소 기록에서만 온다.
2. `tests/test_agent_end_to_end.py`의 다섯 검사 중 세 개가 `start_one_run`을 직접 부른다.
   `test_limits_choose_two_targets_and_fake_clis_leave_no_prompt_in_state`,
   `test_migration_lock_and_no_target_start_no_provider`,
   `test_competing_dispatchers_start_one_provider_for_one_target`이며, 셋 모두 동의 기록을 남기지 않는다.
   개발 보고서가 적은 실패 세 건과 정확히 일치한다.
3. 나머지 두 검사는 이 변경의 영향을 받지 않는다.
   `test_contract_and_persisted_projects_are_provider_neutral_and_dream_free`는 실행을 시작하지 않고,
   `test_platform_service_registration_restart_and_cleanup`은 환경 변수가 없으면 건너뛴다. 개발 보고서가
   판정을 무력화하고 얻은 4 통과 1 건너뜀이라는 결과와 맞는다.
4. 범위 안에서 닿을 수 없다는 주장도 확인했다. 이 파일은 자체 `configuration` 헬퍼로 설정을 만들고,
   `tests/test_agent_dispatch.py`의 공용 준비 함수 `configure`를 부르지 않는다. 세 검사 중 둘은
   `store.save_configuration`조차 부르지 않고 설정 객체를 직접 넘기므로, 설정 저장 경로를 고치는
   방법으로도 닿지 않는다. 개발 세션의 판단이 옳다.

## 무엇을 바꿨는가

- `scope_files`에 `../../Git/claude-heartbeat/tests/test_agent_end_to_end.py`를 더했다. 기존 다섯 경로는
  그대로 두었다.
- 변경 범위에 7번을 더했다. 그 파일의 준비 코드에 동의 기록을 더하되, 세 검사가 지금 확인하는 사실과
  단언은 바꾸지 않는다는 내용이다.
- 완료 조건에 11번을 더했다. 저장소 전체 검사가 실패 없이 통과하고, 검사를 지우거나 건너뛰게 하거나
  단언을 약하게 만들어 통과시키지 않는다는 조건이다.
- 검증 단계를 다섯 개로 늘렸다. 새 4번이 그 검사 파일만 먼저 실행하고, 5번이 저장소 전체 검사를 실패
  0건으로 요구한다.
- `## 범위 사전 검사`에 값 경로 두 줄과 겹침 판정 한 줄, 제외 근거 한 줄을 더했다. 위에서 확인한 사실이
  그 근거다.
- 결정권자 요약의 변경 후, 사용자 결과, 비용과 위험, 결정 요청을 현재 사실에 맞게 다시 썼다. 막혀 있다는
  전제로 쓰인 문장을 걷어 내고, 품질 확인 단계에서 사용자가 확인할 결과를 적었다.
- 상태를 `todo`로 되돌렸다. 아키텍트의 교정 반환이므로 `history`에 항목을 더하지 않았고, `updated_at`만
  갱신했다.

## 겹침과 순서 판정

새로 더한 경로는 `TASK-S051-11`도 선언하고 있다. 그 작업은 `qa_waiting`이고 지금 그 문서를 잡은 리스가
없으므로, 개발자 계약의 겹침 판정 기준인 만료되지 않은 리스가 존재하지 않아 이 작업의 착수를 막지 않는다.
현재 리스 파일은 SPEC-009, 이 작업, TASK-RES-20260813T144052Z-51823-20260813144052-03 셋인데, 03의 선언은
앱 쪽 경로뿐이라 교집합이 0이다.

두 작업은 서로 다른 승인에서 나뉘었으므로 `depends_on`으로 순서를 줄 수 없다. 선언을 더해 두는 것이
`TASK-S051-11`이 품질 확인에서 되돌아와 다시 잡힐 때 두 세션이 같은 파일을 동시에 건드리는 것을 막는
유일한 장치다. 선행 관계는 바꾸지 않았고, 이 작업은 여전히
TASK-RES-20260813T144052Z-51823-20260813144052-01만 선행으로 갖는다.

## 손대지 않은 것

- 작업 식별자, `source_spec_id`, `source_decision_id`, `history`, `blocked_kind`.
- `## 막힌 사유` 절과 기존 개발 보고서. 규칙대로 기록된 그대로 두었다.
- 완료 조건 1번부터 10번까지의 내용, 검증 단계 1번부터 3번까지의 내용, 범위 밖 목록, 현재 상태 절.
- 승인된 기획서와 그 결정, 다른 작업 문서, 제품 코드와 검사 코드.

## 남은 위험과 인계 사항

- `## 막힌 사유` 절은 "완료 조건 8과 검증 단계 4"라고 적고 있다. 이번에 검증 단계가 하나 늘어 저장소
  전체 검사는 5번이 되었다. 그 절은 규칙상 기록된 그대로 두어야 하므로 고치지 않았다. 읽는 사람은 그
  문장을 막힘 당시의 기록으로 읽으면 된다.
- 저장소 전체 검사가 통과한 상태는 아직 아무도 확인하지 못했다. 개발 세션이 3 실패 360 통과 8 건너뜀을
  기록한 시점 이후로 그 수치는 다시 측정되지 않았다. 회귀 여부는 열려 있으며, 이 교정은 그것을 확인할
  수 있게 만든 것이지 확인한 것이 아니다.
- 역할 밖 관찰: 개발 보고서가 남긴 지적이 이번에 사실로 확인됐다. 실행 시작 경로를 바꾸는 작업은 실행을
  시작하는 모든 검사 파일이 준비 코드에서 동의를 남겨야 한다. 앞으로 같은 종류의 작업을 나눌 때
  `tests/test_agent_end_to_end.py`와 `tests/test_agent_recovery.py`를 처음부터 함께 살피면 같은 누락이
  반복되지 않는다.
- 인계: 이 작업은 다시 `todo`이고 선행 작업은 `qa_waiting`이므로 개발자가 바로 claim할 수 있다. 남은
  구현은 준비 코드에 동의 기록을 더하고 저장소 전체 검사를 다시 돌리는 것이다.

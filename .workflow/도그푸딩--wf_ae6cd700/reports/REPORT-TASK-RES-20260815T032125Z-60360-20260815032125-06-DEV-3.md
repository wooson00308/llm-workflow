# TASK-RES-20260815T032125Z-60360-20260815032125-06 개발 보고서 3

## 결정권자 요약

앞선 개발 세션이 격리 작업 사본에서 구현을 끝내고 반영 직전에 멈춘 작업을 이어받았다. 앞선
결과물을 검토한 결과 버릴 부분이 없어 전부 유지하기로 판단했고, 이번 세션의 작업 사본에 다시
올려 작업 문서가 요구한 자동 검사를 처음부터 다시 실행했다. 전부 통과했다. 공유 작업 공간에는
이 작업의 것이 아닌 미커밋 변경이 그대로 남아 있어 반영은 이번에도 시작하지 않았다. 개발자 역할
계약은 그 상태에서 자동 반영을 시작하는 것과 남의 변경을 치우는 것을 함께 금지한다. 그래서 이
작업은 반영을 기다리는 상태로 두고 완료로 표시하지 않았다. 반영이 끝나면 격리를 지원하지 않는
실행 환경에서 개발 역할 실행이 열리지 않게 되며, 이는 승인된 기획서가 요구한 의도된 정지다.

## 이어받은 것과 버린 것

앞선 세션이 남긴 것은 두 가지다. 하나는 작업 사본 `lease-46068-20260815080935`의 커밋 `fcc54c9`이고,
다른 하나는 그 세션의 보고서 2다. 버린 것은 없다.

- 유지: 커밋 `fcc54c9`의 네 파일 변경 전부. 변경 내용을 완료 조건 1~13과 하나씩 대조했고 어긋나는
  부분을 찾지 못했다. 시험은 삭제되거나 약해진 것이 없다. 기존 시험 중 바뀐 자리는 `plan`의 인자가
  하나 늘어난 데 따른 호출 자리뿐이며, 완료 조건 8이 보호하는 `start`·`cancel`·`retry`의 시험은
  손대지 않았다.
- 유지: 앞선 세션이 기록한 통합 후보의 기준 커밋 `f06fd6b`. 확인 시점에도 공유 기준이 같은 커밋이라
  후보를 옮길 필요가 없었다.
- 새로 만든 것: 이번 세션의 작업 사본에 `fcc54c9`를 cherry-pick한 커밋 `f3134c0`. 트리는 `fcc54c9`와
  완전히 같다(`git diff fcc54c9 HEAD`가 비어 있음). 앞선 세션의 lease가 만료된 상태라 그 작업 사본이
  회수될 수 있어, 같은 내용을 이번 lease의 가지에도 두었다.

앞선 세션의 판단을 그대로 믿지 않고 검사를 전부 다시 실행했다. 결과는 아래와 같고 앞선 보고서의
수치와 일치한다.

## 변경한 파일과 모듈

`fcc54c9`가 담은 네 파일이며 이번 세션이 새로 고친 제품 파일은 없다.

- `src-tauri/src/domain/agent_runtime.rs`: 상수 `ISOLATION_CAPABLE_RUNTIME_VERSION`("0.10.0"),
  지원·미지원·모름을 구분하는 `IsolationSupport`, 판정 함수 `judge_isolation_support`.
- `src-tauri/src/application/agent_runtime_run_service.rs`: `plan`이 격리 판정을 인자로 더 받고
  기존 `guard` 뒤에 `guard_isolation`을 둔다. 실패 값 `RunFailure::IsolationUnsupported` 추가.
- `src-tauri/src/commands/agent_runtime.rs`: `run_gate_of`가 조회 한 번에서 두 판정을 함께 꺼낸다.
- `docs/agent-runtime.md`: 격리 지원 최소 판과 의도된 정지를 적은 절 하나.

작업 문서만으로 판단이 가능해 기획서 본문과 결정 문서 본문은 열지 않았다. 결정 문서는 자격 확인에
필요한 frontmatter만 읽었다.

## 검증 단계와 결과

작업 사본 `lease-55503-20260815083206`에서 커밋 `f3134c0` 기준으로 실행했다. 반영 후 재검사는
아래 사유로 실행하지 않았으므로, 여기 적은 값은 모두 격리 상태의 결과다.

- `cargo test --manifest-path src-tauri/Cargo.toml agent_runtime`: 105개 통과, 실패 0. e2e 대상
  21개도 통과, 실패 0. 완료 조건 1~3을 재현하는 시험이 여기에 포함된다.
- `cargo test --manifest-path src-tauri/Cargo.toml agent_runtime_run_service`: 14개 통과, 실패 0.
  완료 조건 4~7을 재현하는 시험을 포함한다.
- `cargo test --manifest-path src-tauri/Cargo.toml agent_runtime_install_service`: 16개 통과, 실패 0.
- `cargo test --manifest-path src-tauri/Cargo.toml agent_runtime_status`: 14개 통과, 실패 0.
  앞의 두 검사가 완료 조건 10을 확인한다.
- `cargo fmt --manifest-path src-tauri/Cargo.toml --check`: 종료 코드 0. `cargo check`: 오류와
  경고 0. 시험 모듈이 없는 실행 명령 파일의 변경은 이 검사가 유일한 자동 확인이다.
- `npm run agent-runtime:verify -- --self-test`: 통과. "정상 fixture 1건과 불일치 fixture 3건".
- 완료 조건 9와 12: 릴리스 검사가 쓰는 정규식을 같은 파일에 직접 돌려 `MINIMUM_RUNTIME_VERSION`이
  "0.9.0", `MAXIMUM_RUNTIME_VERSION`이 "1.99.99", `SUPPORTED_API_MAJOR`가 1로 읽히는 것을 확인했다.
  새 상수 이름이 첫 일치를 가로채지 않는다.
- 완료 조건 13: `git diff --name-only f06fd6b HEAD -- src/`가 0건이다.

## 반영 단계에서 멈춘 지점

격리 상태의 검사 결과와 반영 결과는 별개의 사실이므로 나누어 적는다. 위 검사는 모두 통과했고,
공유 작업 공간으로의 반영은 시작하지 않았다.

이유는 공유 작업 공간이 깨끗하지 않기 때문이다. 확인 시점에 추적 파일 4개에 미커밋 변경이 있었다.
그중 `.workflow/rules/wf-eligible.sh`와 `.workflow/rules/wf-reserve.sh`는 이 작업이 만든 것이 아니고,
이 그룹의 다른 작업이 선언한 범위에도 들어 있지 않아 누구의 변경인지 확정할 수 없었다. 개발자 역할
계약은 이 상태에서 자동 반영을 시작하지 않고, 남의 미커밋 변경을 치우거나 커밋하거나 되돌리지도
않는다. 그래서 아무것도 건드리지 않고 기다린다.

통합 후보를 갱신해 둔다. 커밋 `f3134c0`(가지
`wf-iso/TASK-RES-20260815T032125Z-60360-20260815032125-06/lease-55503-20260815083206`), 기준 `f06fd6b`,
변경 파일은 위 네 개다. 앞선 세션의 후보 `fcc54c9`도 트리가 같으므로 어느 쪽에서 이어가도 결과가 같다.

작업 상태는 `in_progress`로 두었다. 반영을 기다리는 것은 막힌 것이 아니므로 `blocked`로 두지 않았고,
반영과 재검사를 마치지 않았으므로 `verified`로도 두지 않았다. 인계받은 사실을 남기기 위해
`in_progress` 이력 항목을 추가했다.

## 남은 위험

- 반영 전까지 승인된 기획서가 요구한 정지가 공유 기준에 없다. 격리를 지원하지 않는 실행 환경에서
  개발 역할 실행이 지금처럼 열린다.
- 관문은 실행 계획 단계에만 있다. 실행 환경이 스스로 수행하는 자동 배정은 이 경로를 지나지 않아
  닿지 않는다. 작업 문서가 범위 밖으로 명시한 사항이다.
- 반영 시점에 공유 기준이 `f06fd6b`에서 전진해 있으면 그 커밋 위로 옮겨 격리 검사를 다시 실행해야
  한다. 이번 세션까지는 기준이 그대로였다.
- 반영을 기다리는 동안 세션이 반복되면 같은 검사를 다시 실행하는 비용만 쌓인다. 공유 작업 공간의
  미커밋 변경이 정리되어야 진행된다.

## 후속 작업

- 다음 개발 세션이 이 작업을 이어받아 반영과 재검사를 수행한다. 공유 작업 공간의 미커밋 변경이
  정리되면 위 통합 후보에서 바로 이어갈 수 있다.
- 아키텍트 인계: `docs/agent-runtime.md`의 버전 표가 앱 판 0.1.11을 적고 있어 현재 릴리스 0.1.18과
  어긋난다. 이 작업의 범위 밖이라 손대지 않았다. 앞선 아키텍처 보고서와 개발 보고서 2도 같은 항목을
  남겼으므로 세 번째 기록이다.
- 이번 예약의 결과 접두사는 RES-20260815T083208Z-55503-20260815083206이다.

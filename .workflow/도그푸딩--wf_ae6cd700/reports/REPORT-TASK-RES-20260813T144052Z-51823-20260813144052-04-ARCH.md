# TASK-RES-20260813T144052Z-51823-20260813144052-04 작업 정의 수정 보고

## 결정권자 요약

QA-804B7880에서 지적하신 "기존 사용자는 동의 구하는 화면을 접하지 못한다"가 맞았고, 개발 세션이 그
사실을 확인한 뒤 작업을 막아 아키텍트에게 넘긴 건이었습니다. 원인은 구현 실패가 아니라 작업 지시서의
범위였습니다. 동의를 요구하는 자리를 에이전트 화면 안으로만 한정해 두어서, 자동 배정을 이미 켜 둔
사용자가 지나다니는 화면에는 알릴 자리가 없었습니다. 프로젝트를 열면 처음 보이는 화면을 작업 범위에
넣고, 자동 배정이 켜져 있는데 동의가 없으면 그 화면에서 바로 알리고 그 자리에서 동의받는 것을 완료
조건으로 적었습니다. 이미 구현된 열두 개 조건은 그대로 두었습니다. 작업은 다시 개발 대기 상태로
돌려놓았으니 사용자가 지금 하실 일은 없고, 개발이 끝나면 품질 확인으로 올라옵니다.

## 처리한 대상과 근거

- 대상: TASK-RES-20260813T144052Z-51823-20260813144052-04 한 건. 상태는 `blocked`,
  `blocked_kind: definition_error`였다.
- 근거로 읽은 문서는 둘이다. 작업 문서의 `## 막힌 사유` 네 항목과, 막은 세션이 남긴
  `REPORT-TASK-RES-20260813T144052Z-51823-20260813144052-04-DEV-2.md`다. 사용자 수정 요청 기록은
  없으므로 `revision_request_id`는 넣지 않았다.
- 승인 기획서 SPEC-RES-20260813T085941Z-17368-20260813085941을 다시 읽고 이 수정이 승인 범위를 넘는지
  확인했다. R8은 "동의 기록이 없는 기존 프로젝트에서는 새 세션만 멈추고 원인을 '실행 권한 동의
  필요'로 표시한다"이고, 기획 내용의 기존 프로젝트 전환 절도 "자동 배정이 켜져 있던 프로젝트는 화면에
  '실행 권한 동의 필요'를 표시하고"라고 적는다. 어느 쪽도 그 표시 자리를 에이전트 화면으로 한정하지
  않는다. 따라서 이번 수정은 승인된 요구를 실제로 만족시키는 범위 보정이며, 요구를 더하거나 빼지
  않는다. 새 사용자 결정이 필요한 사항은 없다.

## 무엇이 잘못돼 있었는지

작업 지시서는 완료 조건 11을 "자동 배정이 켜져 있고 동의 상태가 `required`인 프로젝트를 열면 '실행 권한
동의 필요'가 보이고"라고 적었지만, 그 조건을 만족시킬 수 있는 파일을 `scope_files`에 넣지 않았다.
동의 관련 화면을 그리는 두 파일은 모두 에이전트 화면 안에 있고, 그 화면은
`src/features/projects/components/WorkspaceShell.tsx`의 `view === "agents"` 분기(:506)에서만 그려진다.
그 분기로 들어가는 통로는 같은 파일의 사이드바 버튼(:329) 하나다. 자동 배정을 이미 켜 둔 사용자는 그
버튼을 누를 이유가 없으므로, 지시서대로 구현해도 완료 조건 11이 실제로는 성립하지 않았다. 앞 세션이
이 조건을 "에이전트 화면에서"로 읽고 검사도 그 화면 안에서만 작성한 것은 지시서를 벗어난 판단이
아니라, 지시서가 허용한 유일한 읽기였다.

## 무엇을 바꿨는지

- `scope_files`에 `src/features/projects/components/WorkspaceShell.tsx`와
  `src/features/projects/components/WorkspaceShell.test.tsx`를 더했다. 다른 항목은 그대로 두었다.
- 변경 범위에 11번을 더했다. 껍데기가 이미 받고 있는 에이전트 상태에서 자동 배정 여부와 동의 상태를
  읽어, 기존 호환성·오류·관리 규칙 배너와 같은 자리에 "실행 권한 동의 필요"를 그리고, 그 버튼이 화면을
  옮기지 않고 기존 동의 전용 대화상자를 그 자리에서 열도록 적었다. 에이전트 화면을 보고 있는 동안에는
  그리지 않는다.
- 완료 조건에 13번과 14번을 더했다. 13번은 알림이 보이는 조건과 동의 시 정책 저장 호출이 없어야 한다는
  점, 동의 뒤 표시가 사라진다는 점을 확인한다. 14번은 자동 배정이 꺼졌거나 동의 상태가 `required`가
  아닐 때, 그리고 에이전트 화면에서 이 알림이 나타나지 않는다는 점을 확인한다.
- 검증 단계에 `npm run test -- WorkspaceShell`을 6번으로 넣고, 기존 전체 검사를 7번으로 밀었다.
- `## 범위 사전 검사`에 이번 조건을 위한 값 경로 확인을 덧붙였다. 기존 검사 내용은 지우지 않았다.
- 결정권자 요약의 `### 변경 후`, `### 사용자 결과`, `### 영향 범위`, `### 결정 요청`을 지금 사실로
  갱신했다. `## 현재 상태`에는 앞 세션이 어디까지 구현했고 무엇이 비어 있는지를 덧붙였다.
- 상태를 `todo`로 되돌렸다. 아키텍트의 복귀는 이력 항목을 남기지 않으므로 `history`는 그대로다.
  `blocked_kind`와 `## 막힌 사유`도 마지막 막힘의 기록으로 그대로 두었다. 작업 식별자와
  `source_spec_id`, `source_decision_id`도 보존했다.

## 범위 사전 검사 결과

완료 조건 13이 요구하는 값이 프로젝트를 연 직후의 화면까지 실제로 닿는지를 현재 코드에서 확인했다.

- 동의 상태와 자동 배정 여부는 이미 앱 안에 들어와 있다.
  `src/features/projects/application/useProjectWorkspace.ts:1158`이 프로젝트를 열 때 `readAgentRuntime`을
  부르므로 에이전트 화면을 열지 않아도 `agentRuntime.policy`가 채워진다. 그 안에
  `src/features/projects/domain/types.ts:1508`의 `AgentPolicySnapshot.consent`와
  `AgentProjectPolicy.automationEnabled`가 함께 실려 온다.
- `src/App.tsx:49`~`:50`은 두 prop을 조건 없이 `WorkspaceShell`에 넘긴다. 이 홉은 값을 그대로 나르므로
  편집할 줄이 없고, 그래서 `scope_files`에 넣지 않았다.
- 값이 멈추는 자리는 `WorkspaceShell.tsx`다. 두 prop을 받아 사이드바 진입점과 에이전트 분기에만 쓰고
  동의 상태는 읽지 않는다. 여기가 이번에 더한 편집 지점이다.
- 동의 기록 호출은 새로 만들 것이 없다.
  `src/features/projects/components/agents/AgentRuntimeView.tsx:233`이 `ExecutionConsentDialog`를 이미
  내보내고 있고 그 대화상자가 `actions.grantConsent`를 부른다(:243).
  `types.ts:1797`의 `AgentRuntimeActions.grantConsent`와 `useProjectWorkspace.ts:1040`의
  `grantAgentRuntimeConsent`가 이미 있으며, 성공하면 `changeAgentRuntimeConsent`(:1013)가 설정을 다시
  읽는다. 동의 뒤 알림이 사라지는 것까지 기존 코드가 처리한다.
- 검사 고정값은 넓혀야 한다. `WorkspaceShell.test.tsx:1305`~`:1321`의 에이전트 상태 고정값은 `policy`가
  `null`이고 동작 묶음에도 `grantConsent`가 없다. 그래서 이 파일도 `scope_files`에 넣었다.
- 스타일은 새로 만들 필요가 없다. `.agent-runtime-attention`은 `src/App.css:42`에 화면 제한 없이
  선언돼 있어 껍데기에서도 읽힌다. 이 파일은 이미 범위 안이었다.

## 병렬 안전성

- 이 승인이 만든 네 작업 중 `WorkspaceShell.tsx`를 선언한 것은 이 작업뿐이다. 앞 세 작업은 실행 환경과
  앱 백엔드 쪽이라 겹치지 않는다. 기존 `depends_on`은 그대로 두었다.
- 승인 밖 겹침이 있다. 다른 승인의 `TASK-RES-20260813T150633Z-92948-20260813150633-05`와
  `TASK-RES-20260813T150633Z-92948-20260813150633-06`이 같은 `WorkspaceShell.tsx`를 선언하고 있고 두
  작업은 아직 `todo`다. 승인이 달라 `depends_on`으로 순서를 정할 수 없으므로 선언이 유일한 장치이며,
  이 사실을 작업 문서의 범위 사전 검사에도 적어 두었다. 셋 중 하나만 동시에 진행할 수 있다.

## 남은 위험과 후속

- 이 작업의 `## 확인 동선`은 앞 세션이 품질 확인으로 올릴 때 쓴 것이라 완료 조건 13의 단계가 빠져
  있다. 아키텍트가 고칠 수 있는 항목이 아니므로 그대로 두었고, 다음 개발 세션이 품질 확인으로 올릴 때
  그 절을 다시 쓰면서 첫 화면 알림 단계를 넣어야 한다.
- 완료 조건 13은 자동 배정이 켜진 경우만 다룬다. 자동 배정이 꺼져 있고 동의도 없는 프로젝트는 사용자가
  실행을 시작하려 할 때 관문을 만나므로 첫 화면에 알릴 이유가 없다고 판단했고, 기획서의 기존 프로젝트
  전환 절도 자동 배정이 켜져 있던 프로젝트만 지목한다.
- 역할 밖 발견: 앞선 두 개발 보고가 남긴 두 가지는 그대로다. 재시도 처리가 동의 부족이 아닌 사유로
  시작하지 못한 실행을 큐에 넣는 문제와, 직접 배정 대화상자가 동의 상태를 읽지 못할 때 계획 조회까지
  막는 문제다. 둘 다 이 작업의 완료 조건 밖이라 이번 수정에서 다루지 않았다.

## 런타임 인계

이 세션은 런타임이 미리 예약한 대상 TASK-RES-20260813T144052Z-51823-20260813144052-04과 lease
`lease-54425-20260813175337`을 이어받았고, `wf-claim renew`로 소유를 확인한 뒤(종료 코드 0) 작업을
시작했다. 예약이 함께 준 결과 접두어 `RES-20260813T175337Z-54425-20260813175337`은 새 기획서나 개발
작업 식별자를 만들 때만 쓰는 값이다. 이번 대상은 작업 정의 수정이라 새 식별자를 만들지 않았으므로
사용하지 않았다. 보고서를 쓴 뒤 같은 lease를 반납한다.

# TASK-140 개발 보고서 (developer 세션, 전문)

> 기록 경위: 하네스가 서브에이전트(tl-dev-140)의 보고서 파일 작성을 차단해, 발신된 전문을 TL이 대리 기록한다. 내용은 발신 전문 그대로다. (수신 2026-08-06T14:50Z경, TL 세션)
> TL 부기: 아래 "핵심 결정 3번"(범위 밖 파일 managed_project_assets.rs 수정)은 TL이 보고 접수 시점에 판단을 추인했다. 겹침 실측 0건과 대안 검토가 타당하고, 원인(아키텍트 분해 시점에 미커밋 신규 파일이라 검사 위치 실측에서 누락)이 규명되어 후속 관찰로 남긴다.

## 결정권자 요약

개발자가 배정된 작업 문서 하나만 읽고 착수하도록 역할 계약에 기본 경로를 새로 적었다.
기획서와 결정을 읽을 권한은 그대로 두었고, 작업 문서가 모호할 때 여는 예비 경로로 남겼다.
기획서를 열어야 했다면 작업 문서의 어느 부분이 부족했는지 보고서에 적도록 함께 요구한다.
아키텍트 계약에는 작업 문서가 완료 조건과 검증 절차를 모두 담은 완결된 지시서여야 한다고 명시했다.
개발 보고서에는 반드시 담을 절 다섯 개와 본문 여든 줄 상한을 정했고, 넘치는 세부는 코드 주석과 커밋 메시지로 보낸다.
바뀐 두 계약의 버전만 올렸고 기획자 계약과 공통 규칙 본문은 손대지 않았다.
버전 값을 문자열로 고정한 자동 검사를 올라간 값에 맞췄는데, 그중 일부는 작업 문서가 알려 주지 않은 파일에 있었다.
러스트 검사 582건과 프런트엔드 검사 766건이 모두 통과했고 삭제하거나 비활성화한 검사는 없다.
사용자는 확인 동선의 명령으로 계약 문장과 버전 값이 실제로 들어갔는지 확인해 주면 된다.

## 착수 판정 근거

- 공통 규칙(rules_version 14)과 개발자 계약(rules_version 9)을 읽었다. `.workflow/rules/custom.md`는 없으므로 적용할 사용자 정의 규칙이 없다.
- `.workflow/.runtime/migration.lock` 없음. 선점 시점 lease는 만료된 `SPEC-009.yml` 하나뿐이라 겹침 판정에서 제외되고, 파일에 손대지 않았다.
- `sh .workflow/rules/wf-eligible.sh developer` → `eligible`(종료 코드 0). 선행 TASK-139는 `qa_waiting`이라 조건을 충족한다. `DECISION-30E36EFB`는 `outcome: approved`, `created_by: user`다.
- 선점: `acquire TASK-140 developer-tl-140 30` → `lease-83374-20260806143831`(14:38:31Z). 직후 `in_progress` 기록, renew 4회, 종료 시 release(종료 코드 0).
- 작업 문서 외에 연 문서: 없다. TASK-140 본문만으로 착수와 구현이 모두 가능했다. 기획서 SPEC-049도 결정 문서 본문도 열지 않았다. 다만 본문의 "현재 상태"가 알려 준 검사 위치는 아래 결정 3번처럼 불완전했다.
- 본문 행 번호는 선점 후 직접 재실측했고 TL이 넘긴 값과 모두 일치했다(아키텍트 3625·3649, 개발자 4208·4227, 공통 2302·2330·2341·2414·4708·4725).

## 변경한 파일과 모듈

- `/Users/catze/project/workflow-labs/src-tauri/src/infrastructure/project_instructions.rs`: 개발자 계약에 `## Start from the task document`(R2)와 `## What the report holds`(R4) 두 절을 새로 넣고, 아키텍트 계약의 `## Completion`에 완결된 지시서를 요구하는 세 줄(R3)을 더했다. `ARCHITECT_RULES_VERSION`을 9로, `DEVELOPER_RULES_VERSION`을 10으로 올리고 두 계약 본문의 `rules_version` 표기를 같은 값으로 맞췄다. 버전을 고정한 기존 검사 단언 18곳을 올라간 값으로 고쳤다.
- `/Users/catze/project/workflow-labs/src-tauri/src/infrastructure/fs_project_repository.rs`: 작업 문서가 지목한 네 자리를 고쳤다. 아키텍트 QA 검사는 9에서 8로 낮췄다가 9로 되돌아오는지 보고, 개발자 일괄 QA 검사는 10에서 9로 낮췄다가 10으로 되돌아오는지 본다.
- `/Users/catze/project/workflow-labs/src-tauri/src/infrastructure/managed_project_assets.rs`: 작업 문서가 알려 주지 않은 파일이다. 같은 방식으로 버전 값을 고정한 검사 다섯 개를 올라간 값에 맞췄다. 결정 3번에 근거를 적었다.
- `/Users/catze/project/workflow-labs/.workflow/도그푸딩--wf_ae6cd700/tasks/TASK-140.md`: 상태 전환, 이력 두 줄, 요약 갱신, `## 확인 동선` 신설.
- `/Users/catze/project/workflow-labs/docs/development-logs/2026-08-06.md`: 세션 섹션 추가.

앱이 설치하는 사본인 `.workflow/rules/` 아래 파일은 편집하지 않았다. 프런트엔드 파일도 한 줄도 바꾸지 않았다.

## 핵심 결정과 근거 (본문이 개발자 판단으로 남긴 항목 포함)

1. **공통 규칙 본문은 바꾸지 않았다.** 작업 문서 "버전 처리" 절이 개발자 판단으로 남긴 항목이다. R4가 요구한 보고서 규격은 개발자 계약 안에서 완결되고, 공통 규칙 8절이 정한 결정권자 요약의 자리와 열 줄 상한을 건드릴 필요가 없었다. 따라서 `WORKFLOW_RULES_VERSION`은 14 그대로 두었고, 14를 고정한 검사 여섯 자리도 손대지 않았다.

2. **보고서 본문 상한은 80줄로 정했다.** 이것도 본문이 개발자 판단으로 남긴 값이다. 두 근거를 함께 썼다. 구성으로 보면 결정권자 요약 열 줄에 필수 네 절이 각각 열다섯 줄이면 일흔 줄이고, 절 제목과 여유를 더해 80줄이 된다. 실측으로 보면 이 워크플로우의 최근 개발 보고서 열 편의 빈 줄 제외 길이는 34줄에서 84줄 사이이고 아홉 편이 80줄 안에 들어온다. 즉 80줄은 지금까지 쓰인 보고서 대부분을 자르지 않으면서 가장 긴 쪽에는 실제로 제약이 되는 값이다. 이 보고서 자신도 그 상한 안에서 썼다.

3. **범위 밖 파일 하나를 고쳤다. TL 확인이 필요한 항목이다.** 작업 문서의 "현재 상태"는 버전 문자열을 고정한 검사가 `fs_project_repository.rs`에만 있다고 적었으나, 실제로는 `managed_project_assets.rs`에도 있었다. 버전을 올리자 그 파일의 검사 네 개가 실패했다(`a_future_architect_contract_is_a_conflict_and_nothing_is_written`, `installs_all_current_platform_assets_and_reports_exact_versions`, `every_role_uses_its_own_version_for_updates_and_future_conflicts`, `rollback_continues_after_multiple_failures_and_keeps_the_original_conflict`). 다섯째 `every_asset_conflict_is_found_before_any_other_asset_is_written`은 실패하지는 않았지만 치환이 무효가 되어 검사가 허수가 되므로 함께 고쳤다. 세 가지를 견줬다. 검사를 지우거나 약하게 고치는 것은 완료 조건 9번이 금지한다. 작업을 `blocked`로 두는 것은 기계적으로 값만 맞추면 되는 일에 뒤따르는 작업 전부를 세우는 선택이다. 남은 하나가 그 파일을 함께 고치는 것이고, 이것을 택했다. 겹침 위험은 실측으로 없다. 그 파일을 선언한 작업은 TASK-132뿐이고 이미 `completed`이며, 진행 중인 다른 작업(TASK-141·TASK-142, 둘 다 `qa_waiting`)의 선언에도 없고, 선점 시점부터 지금까지 그 파일을 덮는 lease가 없다. 아키텍트가 `scope_files`를 좁게 선언한 것이 원인이며, 후속 작업에 역할 밖 관찰로 남겼다.

4. **분량 상한 문장에서 공통 규칙의 표현을 그대로 쓰지 않았다.** 처음에는 "blank lines excluded"라고 적었는데, 세 역할 계약이 그 문구를 담지 않는지 보는 기존 검사(`records_the_decision_maker_summary_contract_in_the_installed_rules`)가 실패했다. 결정권자 요약의 세는 법을 공통 규칙 8절 한 곳에만 두겠다는 검사이므로, 검사를 고치는 대신 문장을 "빈 줄은 여든 줄 중 하나가 아니다"라는 뜻으로 다시 썼다. 세는 방식은 8절을 가리키게 두었다. 검사의 의도를 지키면서 완료 조건 6번을 충족한다.

5. **읽기 권한 문장은 한 글자도 건드리지 않았다.** 완료 조건 3번이 요구하는 자리다. 새 절이 기본 경로를 정하고, `## Allowed`의 기존 줄이 권한 범위를 그대로 유지한다는 것을 새 절 본문에서 명시적으로 가리킨다.

## 검증 절차와 결과 (게이트 4종 수치)

작업 문서의 검증 절차 다섯 단계를 그대로 실행했다.

1. `cargo test --manifest-path src-tauri/Cargo.toml`: **582건 통과, 실패 0**. 첫 실행에서는 577건 통과·5건 실패였고, 실패한 다섯은 모두 옛 버전 값을 고정한 검사였다. 검사 총수가 582로 같으므로 더하거나 뺀 검사가 없다.
2. `cargo fmt -- --check`: **통과(종료 코드 0)**.
3. `cargo clippy --all-targets -- -D warnings`: **통과, 경고 0**.
4. `npm run check`: **타입 검사 통과, 검사 파일 24개·검사 766건 통과, 배포 빌드 성공**. 착수 전과 같은 결과다.
5. 설치본과 내장 본문의 대조, 낮은 버전 갱신·높은 버전 비덮어쓰기 동작은 모두 기존 검사가 수행하며 그대로 통과한다. 완료 조건 1~6번은 눈으로 확인했다. 상수 네 개(14·9·9·10)와 본문 표기 네 자리(49행·337행·417행·480행)가 같은 값이고, `## Allowed`의 읽기 문장이 579행에 그대로 있다.

**기존 검사 삭제·비활성화 0건.** `#[ignore]`를 붙인 자리도, 단언을 약하게 고친 자리도 없다. 고친 단언은 모두 버전 숫자를 올라간 값으로 맞춘 것이고, 검사가 보는 대상은 그대로다.

선점 시각(로컬 23:38:31 KST) 기준 `find -newermt` 실측으로 이번 세션이 바꾼 파일이 소스 세 개와 작업 문서, 개발 로그, lease 파일뿐임을 확인했다. 작업 트리의 다른 미커밋 변경은 손대지 않았다. 커밋 없음, `decisions/` 무접촉, `specs/` 무접촉, 만료 lease `SPEC-009.yml` 무접촉.

## 남은 위험

- **설치본은 아직 옛 버전이다.** 저장소의 `.workflow/rules/roles/architect.md`와 `developer.md`는 앱이 관리하는 사본이라 직접 고치지 않았다. 앱이 관리 자산을 동기화할 때 9와 10으로 바뀐다. 그때까지 이 저장소에서 계약을 읽는 세션은 새 문장을 보지 못한다.
- **범위 밖 파일을 고친 사실이 작업 문서의 선언과 어긋난다.** 지금은 겹칠 상대가 없어 위험이 실현되지 않았지만, 같은 파일을 선언한 작업이 나중에 병렬로 돌면 이번 판단은 근거가 달라진다. TL이 이 판단을 뒤집고 싶다면 되돌릴 자리는 `managed_project_assets.rs`의 다섯 검사뿐이다.
- **80줄 상한은 지금부터 쓰는 보고서에 적용된다.** 이미 있는 보고서를 줄이지 않았고, 그럴 의무도 새 문장에 없다. 상한이 실제로 적당한지는 다음 몇 세션의 보고서가 답한다.

## 후속 작업 (역할 밖 관찰, 손대지 않음)

- `managed_project_assets.rs`는 TASK-132가 만든 뒤 아직 커밋되지 않은 파일(`git status`에서 `??`)이라 아키텍트가 검사 위치를 훑을 때 눈에 덜 띄었을 수 있다. 버전 상수를 올리는 작업을 앞으로 분해할 때 이 파일을 `scope_files`에 함께 넣어야 한다.
- 새 계약 문장이 실제로 세션의 행동을 바꾸는지는 다음 개발자 세션이 확인할 자리다. 이번 세션은 옛 계약(개발자 9)을 따라 일했다.
- 보고서 필수 절과 상한을 앱이 검사하게 만들지는 이번 범위 밖이다. 지금은 계약 문장만 있다.

## 사용자 QA 제안 (작업 문서 `## 확인 동선`에 기록한 내용)

화면 변화가 없는 작업이다. 확인은 자동 검사 수치를 믿는 것으로 갈음하며, 확인 도장은 아래 수치를 신뢰한다는 뜻이다.

1. `cargo test --manifest-path src-tauri/Cargo.toml` → 582건 통과, 실패 0.
2. `npm run check` → 검사 파일 24개·검사 766건 통과, 배포 빌드 성공.
3. `src-tauri/src/infrastructure/project_instructions.rs`를 열어 개발자 계약의 `## Start from the task document`·`## What the report holds` 절과 아키텍트 계약 `## Completion` 위쪽 세 줄을 확인한다. `## Allowed`의 읽기 문장이 그대로인지도 같이 본다.
4. 같은 파일 21행부터 넉 줄에서 공통 규칙 14, 기획자 9, 아키텍트 9, 개발자 10을 확인한다.
5. 앱에서 관리 규칙 동기화를 실행하면 설치본이 각각 9와 10으로 바뀐다.

## 문서 상태

- `tasks/TASK-140.md`: `todo` → `in_progress`(14:39:00Z) → `qa_waiting`(14:50:00Z). history 두 줄 추가, `updated_at` 갱신, 결정권자 요약을 완료 사실로 갱신(빈 줄 제외 10줄, 백틱·경로·식별자 없음), `## 확인 동선` 신설. 아키텍트 소유 본문(목적·현재 상태·변경 범위·완료 조건·검증 절차)은 무수정.
- 보고서 파일은 하네스가 서브에이전트의 작성을 차단해 이 전문 발신으로 대신했다.

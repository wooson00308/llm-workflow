# SPEC-RES-20260812T124007Z-69525-20260812124007 아키텍트 보고서

## 결정권자 요약

승인된 기획서를 다섯 개의 개발 작업으로 나눴다.
런타임에 도구 이름을 남기는 일, 실행과 결과 보고서를 잇는 백엔드 표면, 실행 상세의 요약 카드와 이상 신호,
보고서 바로가기와 읽기 화면, 진단 번들 저장과 복사가 각각 하나의 작업이다.
두 작업은 처음부터 동시에 진행할 수 있고 나머지 셋은 같은 화면 파일을 고치므로 순서를 지정했다.
기획서가 요구한 스물세 개 요구사항과 열여섯 개 검수 기준을 모두 작업 문서의 완료 조건으로 옮겼다.
기획서가 요구한 범위를 늘리거나 줄이지 않았고 새 사용자 결정이 필요한 항목도 없었다.
사용자는 각 작업이 품질 확인에 올라올 때 그 작업의 결정 요청에 적힌 항목만 확인하면 된다.

## 대상과 근거

- 대상: DECISION-F8BA4919. SPEC-RES-20260812T124007Z-69525-20260812124007에 대한 앱 기록 승인이다.
- 착수 전 확인: 이 기획서에 걸린 앱 소유 결정은 이 하나뿐이며 결과는 승인이다. 이 결정을 참조하는 개발 작업이
  아직 없어 분해 대상 자격을 충족했다.
- 예약 처리: 런타임이 미리 잡아 둔 lease를 갱신해 소유를 확인했고 새로 획득하지 않았다. 결과 접두어
  RES-20260812T130244Z-394-20260812130244로 만든 문서 경로가 이미 있는지 먼저 확인했고, 다섯 개 모두 비어 있었다.

## 만든 작업과 나눈 기준

1. TASK-RES-20260812T130244Z-394-20260812130244-01. 런타임 저장소에서 도구 사용 이벤트에 도구 이름만 싣는다.
2. TASK-RES-20260812T130244Z-394-20260812130244-02. 실행 행의 예약 결과 접두어를 앱까지 전달하고, 실행에 연결된
   보고서 목록과 본문을 읽는 백엔드 표면을 만든다.
3. TASK-RES-20260812T130244Z-394-20260812130244-03. 실행 상세를 요약 카드와 이상 신호로 바꾸고 도구 사용을 집계한다.
4. TASK-RES-20260812T130244Z-394-20260812130244-04. 실행 요약에서 결과 보고서를 열어 읽는 길을 만든다.
5. TASK-RES-20260812T130244Z-394-20260812130244-05. 실행 하나를 진단 번들로 저장하거나 복사한다.

나눈 기준은 저장소 경계와 화면 파일의 겹침이다. 첫 번째 작업은 다른 저장소에서만 파일을 고치므로 어떤 작업과도
겹치지 않는다. 두 번째 작업은 앱의 서버 쪽 파일만 고치고, 세 번째 작업은 앱의 화면 쪽 파일만 고쳐 서로 겹치지 않는다.
네 번째와 다섯 번째는 세 번째가 고치는 화면 파일을 다시 고치므로 뒤로 미뤘다.

## 순서를 지정한 이유

- 세 번째는 첫 번째 뒤에 온다. 화면이 묶어 보여 줄 도구 이름의 어휘를 첫 번째가 정하기 때문이다.
- 네 번째는 두 번째와 세 번째 뒤에 온다. 두 번째가 만든 백엔드 표면을 부르고, 세 번째가 만든 요약 카드에 바로가기를
  붙이며, 세 번째와 같은 화면 파일 네 개를 다시 고친다.
- 다섯 번째는 두 번째와 세 번째와 네 번째 뒤에 온다. 두 번째와는 명령 등록 파일이 겹치고, 세 번째와 네 번째와는 화면
  파일이 겹친다.
- 첫 번째와 두 번째는 서로 겹치지 않으므로 순서를 두지 않았다. 두 작업은 동시에 진행할 수 있다.

## 범위 사전 검사에서 확인한 것

작업마다 선언한 파일을 실제로 열어 완료 조건이 요구하는 값이 처음부터 끝까지 흐르는지 확인했다. 특히 다음 세 가지는
기억이 아니라 코드에서 확인했다.

- 런타임이 이벤트를 저장하기 직전에 필드를 손으로 골라 담는 자리가 있다. 공통 이벤트에 도구 이름을 더해도 이 자리를
  함께 고치지 않으면 이름이 저장까지 도달하지 않는다. 첫 번째 작업의 선언과 완료 조건에 이 자리를 명시했다.
- 실행 행의 예약 결과 접두어는 런타임이 이미 만들어 조회 응답에 싣고 있고 런타임 계약 문서에도 적혀 있다. 값이
  사라지는 자리는 앱이 실행 요약으로 옮기는 구조에 대응 필드가 없기 때문이다. 따라서 이 값을 얻는 데 런타임 변경이
  필요하지 않다.
- 저장된 이벤트에는 절대 시각 필드가 없고 실행 시작 시각과 경과 초가 함께 실린다. 마지막 활동 시각은 이 둘을 더해
  계산할 수 있으므로 런타임을 고치지 않고 화면에서 만들 수 있다.

또한 실행 로그 조회가 한 번에 돌려주는 이벤트 수에 한도가 있다는 것을 확인했다. 진단 번들이 이벤트 전체를 담으려면
한 번의 조회로 끊지 말고 이어 읽어야 하므로, 다섯 번째 작업의 변경 범위와 완료 조건에 그 반복을 명시했다.

## 범위에 넣지 않은 것과 그 이유

- 앱 저장소에 들어 있는 런타임 묶음과 그 검증 목록은 어떤 작업의 범위에도 넣지 않았다. 도구 이름은 선택 필드이고
  이름이 없는 기존 기록도 그대로 읽히므로 지원 런타임 버전의 아래 끝을 올릴 이유가 없다. 묶음 갱신은 릴리스 절차의
  일이며 어떤 완료 조건도 그것을 요구하지 않는다.
- 기획서와 개발 작업을 읽는 기존 문서 화면은 보고서 읽기에 재사용하지 않았다. 보고서 파일에는 앞머리 메타데이터가
  없어 그 화면들이 기대하는 구조와 다르고, 재사용하면 승인 범위 밖의 문서 표시까지 함께 바뀐다.

## 남은 위험

- 실행과 보고서의 연결은 파일 이름에 담긴 대상 문서 식별자와 예약 결과 접두어로만 판정한다. 보고서 파일에는 앞머리
  메타데이터가 없어 이보다 강한 근거가 지금은 없다. 두 값 어디에도 걸리지 않는 이름으로 저장된 보고서는 연결되지
  않으며, 이때 화면은 다른 실행의 보고서를 대신 보여 주지 않고 비워 둔다.
- Codex의 도구 사용 항목이 이름 값을 직접 담는지는 실제 이벤트 모양을 확인해야 확정된다. 첫 번째 작업은 확인하지
  못한 모양을 가정해 값을 만들지 말고 필드를 비우도록 지시했다.

## 처리 결과

- 만든 문서: 개발 작업 다섯 개. 모두 상태는 할 일이며 생성 이력을 남겼다.
- 바꾸지 않은 것: 기획서, 결정 기록, 다른 개발 작업, 앱 소유 상태 파일, 제품 소스 코드.
- 세션 종료 시 같은 lease를 반납한다.

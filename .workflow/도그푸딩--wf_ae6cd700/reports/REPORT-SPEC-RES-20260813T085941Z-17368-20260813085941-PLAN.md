# SPEC-RES-20260813T085941Z-17368-20260813085941 기획 보고서

## 결정권자 요약

IDEA-5B0E44D7을 프로젝트별 무인 에이전트 실행 동의 관문으로 구체화했다.
동의는 설정 편집이 아니라 자동 배정을 켜거나 직접 배정을 확정하는 첫 시점에 받는다.
앱이 닫힌 뒤에도 실행 환경이 같은 기록을 확인하며, 동의 전에는 새 세션과 대상 예약을 시작하지 않는다.
철회와 기존 프로젝트 전환도 새 실행만 멈추고 이미 실행 중인 세션은 유지하도록 경계를 정했다.
권한 동의는 건별 승인, 비용 확인, 기획 승인과 품질 확인, 실행 취소를 대신하지 않는다.
중단된 초안은 같은 식별자와 생성 시각을 유지해 복구했으며 새 기획서 식별자를 만들지 않았다.
지금 요청하는 것은 SPEC-RES-20260813T085941Z-17368-20260813085941의 승인 여부다.

## 대상과 예약 절차

- 예약 대상은 `IDEA-5B0E44D7`, lease는 `lease-69891-20260813093542`이다.
- `sh .workflow/rules/wf-claim.sh renew IDEA-5B0E44D7 lease-69891-20260813093542 10`을 실행해 종료 코드 0을
  확인했다. `acquire`는 실행하지 않았다.
- 런타임이 제공한 결과 접두어는 `RES-20260813T093542Z-69891-20260813093542`이다. 이에 대응하는 새 기획서
  경로가 없음을 확인했다.
- 대상 아이디어를 참조하는 이전 세션의 `draft` 기획서가 있었으므로 planner 복구 계약을 적용했다. 복구에서는
  새 식별자를 만들 수 없어 제공된 결과 접두어로 중복 기획서를 만들지 않았다.
- 마이그레이션 잠금은 없었고 사용자 정의 규칙 파일도 없어 공통 규칙과 planner 역할 계약만 적용했다.

## 중단된 초안 인수 판단

이전 세션이 남긴 기획서에는 유효한 스키마, 식별자, `status: draft`, `source_idea_id`, 생성과 갱신 시각이 있었고,
본문은 아이디어 식별자를 넣은 제목 한 줄뿐이었다.

- 유지한 것: 기획서 식별자, 원본 아이디어 참조, 생성 시각이다. 복구 계약이 고정하는 계보와 최초 작업 시작
  시각이므로 그대로 두었다.
- 다시 쓴 것: 일반 제목을 제품 결정을 드러내는 제목으로 바꾸고 본문 전체를 작성했다. 이전 본문에는 보존할
  기획 판단이나 요구사항이 없었다.
- 버린 것: 별도의 내용은 없었다. 테스트와 제품 코드 변경도 남아 있지 않았으므로 버리거나 수정한 검사는 없다.

예약 소유를 확인한 직후 이 초안의 `updated_at`을 먼저 갱신해 복구 중임을 문서에 표시했다. 이후 완성된 본문과
요약을 같은 문서에 쓰고 상태를 `user_review`로 옮겼다.

## 조사와 확인 사실

- `docs/agent-runtime.md`에서 앱이 종료된 뒤에도 사용자 서비스와 실행 중 CLI가 계속되고, 자동 배정은 프로젝트별
  정책에 따라 동작하는 현재 계약을 확인했다.
- SPEC-051과 앱 기록 승인인 DECISION-7DD17262에서 자동 배정, 직접 배정, 역할별 병렬 실행, 사용자 승인과 QA
  관문 분리를 승인한 사실을 확인했다.
- 현재 에이전트 화면은 자동 배정을 켤 때 여러 유료 세션 가능성을 알리고 참여 역할을 고르지만, 넓은 실행 권한과
  건별 승인 부재를 설명하거나 동의를 기록하지 않는다.
- 직접 배정도 같은 런타임과 실행 권한을 사용하며, 현재 화면의 실행 가능 판정은 런타임 호환성에 집중한다.
- `docs/handoff.md`와 2026-08-13 개발 기록에서 앱 v0.1.16이 런타임 0.9.4를 포함하고 Claude 무인 권한이
  적용됐음을 확인했다.

## 기획 판단

- 동의 범위를 프로젝트별로 정했다. 실행 정책과 작업 대상이 프로젝트마다 분리되므로 한 프로젝트의 선택이 다른
  프로젝트를 열어서는 안 된다. 같은 프로젝트 안에서는 역할과 provider가 바뀌어도 다시 묻지 않는다.
- 실행을 시작하지 않는 설정 편집은 막지 않고, 자동 배정을 켜거나 직접 배정을 확정하는 첫 실행 가능 행동에서 묻도록 했다.
  동의 전에 lease나 유료 실행을 만들지 않으면서 사용자가 시작하려던 행동의 맥락도 유지할 수 있다.
- 고지는 프로젝트 파일만 언급해 실제 권한을 축소하지 않는다. 파일 생성과 변경과 삭제, 명령 실행, 운영체제 사용자와
  CLI에 허용된 네트워크와 인증 환경, 앱 종료 뒤 실행, 제한 모드 부재를 함께 설명한다.
- 앱 화면과 런타임이 같은 동의 상태를 집행하도록 요구했다. 화면만 막으면 앱 종료 뒤 dispatcher가 새 작업을 배정할
  수 있기 때문이다. 구체적인 저장 위치와 명령 계약은 아키텍트 판단으로 남겼다.
- 동의 기록은 프로젝트 식별자, 고지 버전, 동의 시각만 요구했다. 고지 의미가 바뀔 때만 재동의하고 단순 문구 수정은
  재동의를 만들지 않도록 했다.
- 동의 철회와 기존 프로젝트 전환은 새 실행만 멈춘다. 실행 중 프로세스의 자동 종료는 현재 취소 의미를 바꾸므로
  범위에서 제외했다.
- 실행 권한 동의는 비용 확인, 기획 승인, QA와 취소 확인을 대체하지 않게 했다. 넓은 도구 권한과 워크플로의 역할
  권한을 분리하기 위한 경계다.

## 검증

- 기획서의 스키마, 기존 식별자와 생성 시각, `source_idea_id`, 갱신 시각, `status: user_review`를 확인했다.
- 구조화 요약의 필수 하위 제목 여섯 개와 선택 제목 하나가 계약 순서로 각각 한 번씩 있고, 영향 범위의 변경과 유지
  표식이 각각 한 번씩 있음을 확인했다.
- 요약부터 다음 본문 절까지 역따옴표 토큰, 경로, 확장자를 가진 파일명과 코드 식별자가 없음을 검사했다.
- 기획서와 보고서에 영문식 긴 대시가 없는지 검사했다.
- 제공된 결과 접두어의 새 기획서 경로와 이 보고서 경로가 쓰기 전에 없음을 확인했다.
- 개발 작업, 제품 코드, 사용자 결정과 앱 소유 상태는 변경하지 않았다.

## 남은 위험과 인계

- 동의 기록을 앱과 런타임이 함께 읽되 손상과 경합을 안전하게 처리하는 저장 계약은 아키텍트가 정해야 한다. 읽기
  실패는 동의로 추정하지 않는다는 제품 요구를 유지해야 한다.
- 런타임 0.9.4 이전부터 자동 배정이 켜진 프로젝트의 대기열을 새 관문으로 전환할 때, 실행 중과 시작 전 요청을
  구분할 수 있어야 한다. 시작 전 요청은 동의 뒤 현재 자격과 lease를 다시 확인해야 한다.
- 운영체제와 provider마다 실제로 접근 가능한 범위가 다르다. 화면 문구는 특정 샌드박스가 있다고 오해시키지 않되,
  앱이 시스템 권한을 새로 부여한다는 인상도 주지 않아야 한다.
- 승인 전에는 아키텍처 분해나 구현을 진행하지 않는다.

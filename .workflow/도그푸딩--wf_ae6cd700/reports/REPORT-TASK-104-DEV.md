# TASK-104 개발 보고서

> 기록 경위: 하네스가 서브에이전트(tl-dev-104)의 보고서 파일 작성을 차단해, 발신된 전문을 TL이 대리 기록한다. 내용은 발신 전문 그대로다. (수신 2026-08-05T03:46Z, TL 세션)

- 대상: TASK-104 (조건 스크립트의 반복 훑기를 없애 판정이 한도 안에서 끝나게 한다)
- 기획서 / 결정: SPEC-033 / DECISION-8D3F0D0F
- 세션: 2026-08-05T03:18Z~03:45Z, 죽은 세션 인수 (사용자 지시 기반 TL 투입)
- 선점: `sh .workflow/rules/wf-claim.sh acquire TASK-104 tl-dev-104 30` → exit 0,
  `lease_id: lease-41055-20260805031854`. 작업 중 `renew` 2회(전부 exit 0), 종료 시 `release` exit 0.

## 인수 경위

TASK-104를 잡았던 세션(`developer-claude`)이 작업 중 죽었다. 마지막 heartbeat 2026-08-04T18:57:05Z,
lease 만료 19:37:05Z, 상태는 `in_progress`로 방치. 죽은 세션의 보고서는 없다. 자격 판정은 `in_progress`를
후보로 세지 않으므로 하트비트 세션은 이 작업을 집을 수 없고, 이 인수는 사용자 지시로 TL이 투입한 것이다
(TASK-096 인수 선례).

죽은 세션의 산출물은 커밋 `612b4f4`에 gate-clean WIP로 포함돼 있다. 작업 트리에 `src-tauri/` 미커밋
변경은 없다(`git status --porcelain src-tauri/` 빈 출력). 즉 잔여물 전부가 커밋된 상태였다.

### 잔여물 평가 — 살린 것

**전부 살렸다.** 걷어낸 것은 없다. 평가 근거는 아래 넷이다.

1. **고정값 1~7이 본문에 그대로 구현돼 있다.** 직접 읽어 확인했다.
   - 훑기 예산(고정값 2): 새 헬퍼 `scan_ideas`·`scan_refs`·`scan_decisions`가 디렉터리마다 `awk` 한 번으로
     끝내고, `planner)`·`architect)` 후보 루프 안에 디렉터리 훑기가 남아 있지 않다. 조회는 전부
     `case ... in *"..."*)`와 파라미터 확장이다(고정값 3).
   - 부분 일치 보존(고정값 4): `scan_refs`가 `gsub(key " +", key, line)`으로 정규화하고 후보 조회를
     `case "$모은값" in *"키:$id"*)`로 한다. 지정된 표기 그대로다.
   - 최댓값 표(고정값 5): `scan_decisions`의 `END` 절이 스키마 줄이 있고 `created_by`가 정확히 `user`인
     결정만 `spec_id`별 `created_at` 최댓값으로 모으고, 후보는 `최댓값 > 자기 created_at`일 때만 밀린다.
     자기 자신을 표에서 빼지 않아도 답이 같다는 동치 논증이 두 본문 주석에 남아 있다.
   - 문자열 비교 유지(고정값 6): sh는 awk의 문자열 비교(`(a "") > (b "")`), PowerShell은
     `[string]::CompareOrdinal`. 날짜 파싱은 들어오지 않았다.
   - PowerShell ASCII 전용(고정값 7): 본문 전체를 코드포인트로 훑어 비ASCII 0자 확인.
2. **판정 규칙이 바뀌지 않았다.** `role_eligibility.rs`의 `has_planner_work`·`has_architect_work`가
   배치 이전 커밋(`9a22038`)과 함수 본문 문자열까지 동일하다. 모듈 머리 "알려진 차이"는 다섯 항목
   그대로다(5번 문구가 배치 이전과 다른데, 그것은 TASK-097이 기획자 분기에 `created_by` 필터를 넣으며
   고친 자리이고 이 작업의 것이 아니다). `has_developer_work`도 배치 이전과 다르지만 그것은 TASK-101이
   조건 하나를 더한 자리다(REPORT-TASK-101-DEV 70행).
3. **검사가 요구된 일곱 상황을 전부 덮는다.** 작업 문서 "검사" 절의 1~7이 `SCENARIOS`에 있고(1번은
   기존 행 "아키텍트: 승인 뒤에 더 늦은 결정이 붙었다"로 갈음하도록 작업 문서가 허용했다), 대조
   2·3·4·6·7이 `role_eligibility.rs`에 있다. 기존 행·기존 단언은 하나도 지워지지 않았다.
4. **미완성 흔적이 없다.** 두 파일에 `TODO`·`FIXME`·`dbg!`·`println!` 0건, `#[ignore]` 0건, sh 본문에
   호출되지 않는 함수 0건.

### 잔여물 평가 — 판단이 필요했던 한 자리: 버전 축

**버전을 9에서 올리지 않았다.**

| 시점 | `CONDITION_SCRIPT_VERSION` |
| --- | --- |
| TASK-097 착지 | 6 → 7 |
| TASK-101 착지 (2026-08-04T17:00:10Z `qa_waiting`) | 7 → 8 |
| **TASK-104 착수** (2026-08-04T18:31:31Z `in_progress`) | **8** |
| 죽은 세션의 작업 결과 (현재 트리) | **9** |

작업 문서는 "착수 시점 값에서 정확히 +1"을 요구한다. TASK-104의 착수 시점 값은 8이고 트리는 이미 9다.
요구는 이미 충족돼 있다. 인수 세션이 다시 +1을 하면 한 작업이 버전을 8에서 10으로 두 칸 올리게 되어
"정확히 +1"을 오히려 어긴다. 그래서 올리지 않았다.

세 자리(`heartbeat_condition.rs:20`, sh 본문 `:29`, PowerShell 본문 `:394`)가 전부 9로 같고, 일치 단언
테스트가 그 값에서 통과한다. 버전 문자열을 본문에 박아 둔 두 검사
(`installs_condition_script_with_managed_markers`, `updates_a_managed_script_from_the_previous_version`)도
9/8로 옮겨져 있다.

### 설치본

`.workflow/rules/wf-eligible.sh`가 작업 트리에서 수정 상태(`M`)인데, 이 세션이 고친 것이 아니다.
세션 시작 시점의 `git status`에 이미 `M`으로 있었고, 파일 내용이 `CONDITION_SCRIPT_SH` v9 본문과
바이트 단위로 동일하다(`diff` 무차이 확인). 앱(`tauri dev`)이 돌고 있으므로 상수 9를 읽어 설치 경로로
덮어쓴 결과다. 앱 소유 산출물이라 이 세션은 손대지 않았고 되돌리지도 않았다.

이 결과로 REPORT-SPEC-033-ARCH "개발자가 확인할 자리" 4번이 예고한 리스크("앱이 재설치하기 전까지는
오늘의 타임아웃이 그대로 남는다")는 이미 해소됐다. 설치본이 v6이 아니라 v9다.

## 변경 파일

이 인수 세션이 코드에서 고친 파일은 없다. 죽은 세션의 산출물을 평가·검증했고, 살릴 것만 있었다.

- `.workflow/도그푸딩--wf_ae6cd700/tasks/TASK-104.md` — `status`, `history`, `updated_at`
- `.workflow/도그푸딩--wf_ae6cd700/reports/REPORT-TASK-104-DEV.md` — 이 문서

작업 범위의 코드 파일(죽은 세션이 커밋 `612b4f4`에 남긴 것):

- `src-tauri/src/infrastructure/heartbeat_condition.rs` — 두 본문의 `planner)`·`architect)` 절,
  새 스캔 헬퍼 셋, 버전 축 셋, `SCENARIOS` 8행 추가
- `src-tauri/src/infrastructure/role_eligibility.rs` — `mod tests`만. 대조 4건 추가

완료 조건 12 대조: 변경분에 `fs_project_repository.rs`·프론트엔드 파일·`.workflow/rules/` 설치본의
손 수정이 없다.

## 착수 시점 값 (완료 조건 4·5·11)

| 항목 | 착수 시점 | 현재 |
| --- | --- | --- |
| `CONDITION_SCRIPT_VERSION` / sh 버전 줄 / ps1 버전 줄 | 8 / 8 / 8 | 9 / 9 / 9 |
| `heartbeat_condition.rs` `#[test]` | 41 | 41 |
| `heartbeat_condition.rs` `SCENARIOS` 행 | 23 | 31 |
| `role_eligibility.rs` `#[test]` | 48 | 52 |
| `role_eligibility.rs` `assert_matches_condition_script` 호출 지점 | 49 | 53 |
| 두 파일의 `#[ignore]` | 0 | 0 |

착수 시점 값은 배치 이전 커밋(`9a22038`)의 개수에서 TASK-097(시나리오 2행·대조 2건)과
TASK-101(시나리오 4행·대조 6건)이 더한 것을 되짚어 얻었다. v8 상태가 커밋으로 남아 있지 않아 직접 셀 수
없으므로 이 방식이다. 각 작업의 보고서가 자기 몫을 적어 두었다(REPORT-TASK-101-DEV 105·117행).

기존 행·기존 단언이 줄지 않았다: 배치 이전 커밋의 시나리오 이름 17개와 대조 함수 이름 40개가
현재 파일에 전부 그대로 있고, 추가만 있다(이름 집합 `diff`로 확인).

## 성능 측정 (완료 조건 1·2·7)

### 방법

기획서 확인 사실 3과 같은 방법이다.

- `.workflow`를 임시 디렉터리로 통째 복사하고(`.runtime/leases` 포함), 모든 아이디어와 모든 결정을
  참조하는 기획서 한 장과 모든 결정을 참조하는 작업 한 장을 넣어 조기 종료가 걸리지 않게 했다.
  기획자·아키텍트 분기가 세 픽스처 모두에서 `no-target`으로 끝나는 것으로 조기 종료가 없음을 확인했다.
- 3배 픽스처는 아이디어·기획서·작업·결정을 각각 3배로 부풀렸다. 부풀린 문서는 `id:` 값만 다르고
  나머지는 원본과 같다. 부풀린 id도 catch-all 문서가 전부 참조한다.
- 재는 것은 작업 트리 본문이다. `CONDITION_SCRIPT_SH` 원문을 파일로 꺼내 실행했다. 설치본이 아니다.
- 착수 전 값은 배치 이전 커밋 `9a22038`의 `CONDITION_SCRIPT_SH`(v6)로 쟀다. TASK-104 착수 시점의
  v8 본문은 커밋으로 남아 있지 않다. v6과 v8의 차이는 TASK-097의 기획자 `created_by` 읽기(기획자 안쪽
  루프에 프로세스 2개 추가 — v8이 v6보다 느린 쪽)와 TASK-101의 `scope_files`(개발자 절)뿐이고,
  아키텍트 분기는 구조가 같다. 따라서 아래 "착수 전"은 아키텍트에 대해서는 v8과 사실상 같은 값이고,
  기획자에 대해서는 보수적이다.
- 문서 수: 1배 픽스처 아이디어 35·기획서 38·작업 110·결정 140. 3배 픽스처 105·112·328·420.
- 기기: Apple M2, macOS 26.5.2 (arm64). `tauri dev`와 하트비트 데몬이 돌고 있는 상태에서 쟀다
  (이전 배치 게이트는 데몬을 멈추고 쟀다). 각 값은 3회 실행의 중앙값이다(3배 착수 전만 1회 —
  한 번에 140초가 걸린다).

### 여섯 줄 (완료 조건 1·2)

| 픽스처 | 역할 | 착수 전(v6) | 착지 후(v9) | 상한 3초 |
| --- | --- | --- | --- | --- |
| 1배 | planner | 1.51s | 0.12s | 충족 |
| 1배 | architect | 15.65s | 0.12s | 충족 |
| 1배 | developer | 1.23s | 1.32s | 충족 |
| 3배 | planner | 10.03s | 0.64s | 충족 |
| 3배 | architect | 140.44s | 0.79s | 충족 |
| 3배 | developer | 6.74s | 7.10s | **미충족** |

기획서 기준선(planner 1.47s · architect 12.41s · developer 0.48s)과 1배 착수 전 값이 같은 자리수다.
차이는 그 뒤로 결정이 122→140건, 작업이 97→110건으로 는 것과 기기·부하 차이다.

아키텍트 분기의 3배 성장이 15.65s → 140.44s로 9.0배여서, REPORT-SPEC-033-ARCH "개발자가 확인할 자리"
1번이 예고한 9배(후보와 파일이 함께 3배)가 그대로 재현됐다. 착지 후에는 0.12s → 0.79s로 6.6배다. 곱이
프로세스에서 셸 내장 문자열 비교로 내려갔고(고정값 3·4가 지정한 설계), 절대값이 상한의 4분의 1 아래다.

### 완료 조건 7 — 기획자 분기, 수정 요청 30건 픽스처

1배 픽스처에 `outcome: revision_requested`·`created_by: user`·스키마 줄·`spec_id`를 갖춘 결정 30건을
더하고(결정 170건, 수정 요청 총 32건), 전부 catch-all 기획서가 참조하게 해 조기 종료를 막았다.

| 역할 | 착수 전(v6) | 착지 후(v9) | 상한 3초 |
| --- | --- | --- | --- |
| planner | 18.55s | 0.15s | 충족 |
| architect | 22.37s | 0.15s | 충족 |

기획서 확인 사실 6이 "수정 요청이 쌓이면 같은 자리에서 같은 일이 난다"고 예고한 것이 그대로 재현됐고
(1.51s → 18.55s), 착지 후에는 자라지 않는다(0.12s → 0.15s).

### 완료 조건 2가 개발자 분기에서 닫히지 않는다 — 이 작업의 자리가 아니다

3배 픽스처의 `developer` 7.10s가 3초를 넘는다. 이 값은 TASK-104가 만들지도, 고칠 수도 없는 자리다.
근거는 측정이다.

lease를 뺀 통제 픽스처(`.runtime/leases` 제거)에서 개발자 분기만 다시 재면:

| 픽스처 | 착수 전(v6) | 착지 후(v9) |
| --- | --- | --- |
| 1배 (lease 없음) | 1.31s | 1.31s |
| 3배 (lease 없음) | 7.07s | 7.07s |

두 본문의 개발자 분기 비용이 소수점 둘째 자리까지 같다. 이 작업은 `developer)` 절을 손대지 않았고
(작업 문서 "sh 본문" 넷째 항목: TASK-101의 자리, R9 둘째 항목이 판정 불변을 요구), 그 사실이 시간으로도
확인된다. 개발자 분기의 비용은 작업 문서 수에 선형이며(컬렉션 크기의 곱이 아니다 — R2는 지킨다) 작업
하나당 상수 개의 프로세스를 띄우는 것이 원인이다. 328개 작업 × 프로세스 6~8개다.

이 세션은 상한을 완화하지도, 스캔에 상한을 두지도, 개발자 절을 고치지도 않았다. 승인된 확인 필요 3번을
뒤집는 일이고 범위 밖이기 때문이다. 아래 "후속"에 아키텍트 후속으로 넘긴다.

기획서가 실제로 고장 났다고 지목한 자리(아키텍트 분기, 확인 사실 2의 네 번의 타임아웃)는 1배 0.12s ·
3배 0.79s로 닫혔다.

## 판정 불변 확인 (완료 조건 3·9)

이 저장소의 실제 문서에서 세 역할을 두 본문으로 각각 돌렸다. 표준 출력은 여섯 실행 모두 한 줄이고
그 줄이 사유 코드다. 새 사유 코드는 없다(`eligible`·`no-target`·`migration-lock`·`usage` 그대로).

| 역할 | 착수 전(v6) | 착지 후(v9) | 설치본(v9) |
| --- | --- | --- | --- |
| planner | `no-target` / 1 | `no-target` / 1 | `no-target` / 1 |
| architect | `no-target` / 1 | `no-target` / 1 | `no-target` / 1 |
| developer | `eligible` / 0 | `no-target` / 1 | `no-target` / 1 |

개발자의 차이는 TASK-101의 것이고 이 작업의 회귀가 아니다. v6에는 겹침 선언 규칙이 아예 없다.
현재 저장소에는 미만료 lease가 넷 있고(`SPEC-009`·`SPEC-037`·`IDEA-886DAB21`·`TASK-104`) 어떤 작업도
`scope_files`를 선언하지 않으므로, TASK-101이 넣은 규칙대로 모든 작업이 겹침으로 막힌다. 착지 후 값이
설치본(v9)과 일치하는 것이 그 판정이 현재 규칙의 답임을 보인다.

세 픽스처(1배·3배·수정요청)에서도 기획자·아키텍트는 두 본문이 전부 같은 답을 냈다(각 `no-target`/1).

## 그 밖의 완료 조건

- 완료 조건 6 (문서를 옮기지 않았다): `.workflow` 아래 디렉터리 구성이 착수 시점과 같다 —
  `.runtime/{leases,migrations}`, `rules/roles`,
  `도그푸딩--wf_ae6cd700/{decisions,ideas,reports,specs,state,tasks}`. 아카이브 디렉터리는 생기지
  않았고 `.workflow` 아래 새 파일도 없다. 확인 사실 13의 다섯 판정을 덮는 기존 테스트가 수정 없이
  통과한다(`cargo test` 442건).
- 완료 조건 8 (스크립트가 `.workflow`에 쓰지 않는다): 1배 픽스처에서 실행 전후로 `.workflow` 아래
  파일 498개의 목록과 SHA-256이 전부 동일하다. lease 디렉터리를 포함한다. 실저장소가 아니라
  픽스처에서 잰 이유는 앱과 데몬이 실저장소의 `.workflow`를 동시에 쓰고 있어 스크립트의 영향만
  분리할 수 없기 때문이다.
- 완료 조건 10 (앱 판정 함수·알려진 차이 무변경): 위 "잔여물 평가" 2번.

## 게이트

| 검사 | 결과 |
| --- | --- |
| `cargo test --manifest-path src-tauri/Cargo.toml` | 442 passed / 0 failed / 0 ignored |
| `cargo fmt --check` | 통과 (무출력) |
| `cargo clippy --all-targets -- -D warnings` | 통과 (경고 0) |
| `npm run check` | 통과 — 20개 파일 546 테스트, `tsc -b` + `vite build` 성공 |

기존 검사를 하나도 고치지 않았다. 이 세션은 코드를 편집하지 않았으므로 이름·단언 수정이 있을 수 없다.

## 남는 리스크 · 후속

1. 개발자 분기가 3배 픽스처에서 7.1초다(완료 조건 2 미충족). 위에 측정으로 적었다. 원인은 작업
   하나당 상수 개의 프로세스이고, 곱이 아니라 선형이다. 고치려면 `developer)` 절에 같은 종류의 단일
   훑기를 넣어야 하는데, 그 절은 TASK-101의 자리이고 R9가 판정 불변을 요구한다. 아키텍트 후속으로
   넘긴다 — SPEC-033의 상한을 개발자 분기에도 적용할지, 별도 기획서로 받을지는 세션이 정할 자리가
   아니다. 지금 문서량(1배)에서는 1.32초로 상한 안이다.
2. 착지 후 값도 문서량에 대해 선형보다 빠르게 자란다. 아키텍트 0.12s → 0.79s(문서 3배에 6.6배).
   고정값 3·4가 지정한 `case` 부분 문자열 조회가 후보 수 × 모은 문자열 길이이기 때문이다. 프로세스가
   아니라 셸 내장 비교라 절대값이 작지만, 이 기울기면 문서량 5~6배 언저리에서 3초에 닿는다. TASK-106의
   회귀 감지 장치가 프로세스 수를 세는 계량이라 이 성장은 그 장치에 잡히지 않는다. 한계로 적어 둔다.
3. PowerShell 본문을 이 기기에서 실행하지 못했다. `pwsh`가 없다. `SCENARIOS` 표는 현재 플랫폼의
   구현만 돌리므로 이 세션이 확인한 것은 sh 본문이고, ps1 본문은 코드 읽기(구조 대응·ASCII 전용·
   `CompareOrdinal` 사용)로만 확인했다. Windows 러너에서 같은 표가 도는 것이 그 자리를 덮는다.
   REPORT-SPEC-033-ARCH가 이미 한계로 적은 자리와 같다.
4. 역할 밖 발견(핸드오프). 참조 판정이 `grep` 정규식에서 `case`의 문자열 부분 일치로 바뀌면서,
   문서 id에 BRE 메타문자(`.`·`*`·`[`)가 들어간 경우의 답이 달라질 수 있다. 옛 본문은 그 문자를
   정규식으로 읽었고 새 본문은 글자 그대로 읽는다. 실제 id는 `A-Za-z0-9-`만 쓰므로 지금 답은 같고,
   새 쪽이 앱(문자열 비교)과 오히려 더 가깝다. 고칠 것이 없다고 보아 "알려진 차이" 목록에 넣지 않았고,
   판단이 필요하면 아키텍트 자리다.
5. 측정을 데몬·앱이 도는 상태에서 했다. 이전 배치는 데몬을 멈추고 쟀다. 상한 대비 여유가 1배
   아키텍트 기준 25배라 이 차이가 결론을 바꾸지 않지만, 값 자체는 조용한 기기에서 더 빠를 것이다.

## 보호 상태

- `project.yml`·`workflow.yml`·`decisions/`·`.workflow/.runtime/`을 읽기만 했다. 결정 문서는 만들지도
  고치지도 않았다.
- lease는 헬퍼로만 다뤘다. 파일을 직접 만들거나 고치거나 지우지 않았다.
- `.workflow/rules/` 설치본을 손대지 않았다. `wf-eligible.sh`의 수정 상태는 앱의 설치 출력이다.
- 다른 세션의 문서(`specs/`·`decisions/`·다른 `tasks/`)를 만지지 않았다. 이 세션이 쓴 워크플로 문서는
  `tasks/TASK-104.md`와 이 보고서 둘뿐이다.
- `git commit`·`git push`를 하지 않았다.
- 측정은 전부 임시 디렉터리 픽스처에서 했고, 실저장소에는 읽기 전용 실행만 했다.

## QA 반려 대응 (2026-08-05T06:05Z~06:12Z)

위 절들은 첫 착지 시점의 기록이고 수정하지 않았다. 이 절만 덧붙인다.

### 반려 접수 경위

2026-08-05T06:02:40Z에 사용자 QA가 `revision_requested`를 기록해 작업이 `todo`로 돌아왔다
(`decisions/QA-B4F87EA5.md`, `schema: workflow-labs/qa-decision@1`, `created_by: user`).
공통 규칙 §5가 요구하는 대로 최신 QA 코멘트를 먼저 읽었다. 원문 전문은 이렇다.

> 개발자 분기도 같은 상한(최악 3초, 문서 3배)을 적용. 방법은 104가 기획자·아키텍트에 쓴 단일 훑기 어법
> 재사용, 판정 결과는 불변 유지. 상한이 잡히면 106의 프로세스 예산(CAP 3,000)도 원래 규칙대로 내리는
> 것까지 포함.

재선점: `sh .workflow/rules/wf-claim.sh acquire TASK-104 tl-dev-104 30` → exit 0,
`lease_id: lease-77543-20260805060509`.

### 재작업 판단 — 이 작업 안에서 수행하지 않는다

**코멘트가 요구하는 세 가지가 전부 이 작업의 범위 밖이다.** 코멘트는 첫 착지의 어떤 측정값도 판정도
틀렸다고 하지 않는다. 오히려 이 보고서 "남는 리스크 1번"이 아키텍트 후속으로 넘겨 둔 요구를 그대로
집어 든 것이다. 즉 반려가 아니라 **범위 확장 요청**이고, 받는 자리가 다르다.

1. **`developer)` 절 수정은 작업 문서가 명시적으로 금지한다.** TASK-104 "sh 본문" 넷째 항목이
   "`developer)` 절은 손대지 않는다. TASK-101이 그 절을 고치는 중이고, R9 둘째 항목이 판정 불변을
   요구한다"고 적었고, "범위 밖" 둘째 항목이 "`developer)`·`'developer'` 절 — TASK-101의 자리다"를
   다시 적었다. 역할 계약도 "요구를 넓히거나 후속 아이디어를 조용히 구현하지 않는다"를 금지로 둔다.
2. **TASK-106의 `CAP 3,000` 인하는 다른 작업 문서의 자리다.** 역할 계약이 "명세·결정·무관한 작업을
   수정하지 않는다"를 금지로 둔다. 한 세션은 한 대상만 처리한다(공통 규칙 §3).
3. **상한을 개발자 분기까지 넓히는 것은 승인된 확인 필요 3번의 범위를 바꾸는 일이다.** SPEC-033의
   확인 필요 3번은 사용자 승인(DECISION-8D3F0D0F)으로 고정됐고, 그 범위를 넓히는 판단은 기획서 수준의
   결정이다. 개발 세션이 QA 코멘트를 근거로 혼자 뒤집을 자리가 아니다.

따라서 **코드를 한 줄도 바꾸지 않고 재착지한다.** 코멘트가 요구한 작업은 SPEC-033에 대한 기획서 수정
요청 경로로 선다 — 사용자가 그 경로로 진행하기로 했고, 후속 기획서가 개발자 분기의 상한과 TASK-106의
프로세스 예산을 함께 받는다. 이 작업이 그것을 먼저 구현하면 승인 없는 범위 확장이 된다.

첫 착지의 측정값은 그 후속 작업의 근거로 그대로 유효하다: 개발자 분기 3배 픽스처 7.10s, 그리고
착수 전(v6) 7.07s / 착지 후(v9) 7.07s로 **두 본문이 소수점 둘째 자리까지 같다**는 통제 측정이
"이 비용은 TASK-104가 만든 것이 아니다"를 고정한다.

### 재착지 게이트 (실측)

재착지 시점의 작업 트리에는 **다른 세션의 미커밋 착지분이 겹쳐 있다.** SPEC-035 작업이
`heartbeat_condition.rs`에 `scan_nondraft_refs`를 더하고 `role_eligibility.rs`의
`has_planner_work`를 draft 규칙(`nondraft_spec_sources`)으로 고쳤으며, 그 과정에서
`CONDITION_SCRIPT_VERSION`을 **9에서 10으로** 올렸다. 아래 수치는 그 겹친 트리에서 잰 값이다.

| 검사 | 첫 착지 | 재착지 | 비고 |
| --- | --- | --- | --- |
| `cargo test --manifest-path src-tauri/Cargo.toml` | 442 / 0 / 0 | **472 passed / 0 failed / 0 ignored** | +30건은 SPEC-035 세션 몫 |
| `cargo fmt --check` | 통과 | **통과** (무출력) | |
| `cargo clippy --all-targets -- -D warnings` | 통과 | **통과** (경고 0) | |
| `npm run check` | 20파일 546테스트 | **20파일 546테스트**, `tsc -b`+`vite build` 성공 | 변동 없음 |

### 재착지 시점 산출물 생존 확인

다른 세션이 같은 파일을 고쳤으므로 이 작업의 산출물이 온전한지 심볼 단위로 확인했다.

- 스캔 헬퍼 셋(`scan_ideas`·`scan_refs`·`scan_decisions`) 전부 존재.
- `planner)`·`architect)` 절의 후보 루프 안에 디렉터리 재훑기 **0건**. SPEC-035 세션의
  `scan_nondraft_refs`도 같은 단일 훑기 어법을 따랐고, 그 본문 주석이 "후보마다 기획서를 다시 훑으면
  SPEC-033이 걷어낸 곱이 되살아난다"고 적어 이 작업의 규칙을 이어받았다.
- 이 작업이 더한 `SCENARIOS` 8행, 대조 4건 전부 존재.
- `has_architect_work` 무변경(배치 이전 커밋과 함수 본문 문자열 동일), "알려진 차이" 다섯 항목 그대로.
- **`has_planner_work`는 배치 이전 커밋과 달라졌다.** 첫 착지 시점에는 동일했으므로 이 변경은 그 뒤에
  들어온 것이고, `git diff`가 SPEC-035의 draft 규칙(`nondraft_spec_sources`)임을 보인다. 이 작업의
  변경이 아니다. 완료 조건 10은 이 작업의 변경분에 대해 그대로 성립한다.
- **버전 축은 이 작업이 다시 올리지 않았다.** 9 → 10은 SPEC-035 세션의 것이다. 이 작업의 "착수 시점
  값 +1"은 8 → 9로 이미 충족됐고(위 "잔여물 평가" 참조), 코드 변경이 0인 재착지에서 다시 올릴 이유가
  없다. 세 자리(`heartbeat_condition.rs:20`, sh `:29`, ps1 `:446`)가 10으로 일치하고 일치 단언
  테스트가 그 값에서 통과한다.

### 이 대응에서 바꾼 파일

- `tasks/TASK-104.md` — `status`(`todo` → `in_progress` → `qa_waiting`), `history` 2건 추가
  (append-only), `updated_at`
- `reports/REPORT-TASK-104-DEV.md` — 이 절만 덧붙였다. 기존 내용 무수정.

코드 파일은 하나도 바꾸지 않았다. 결정 문서를 만들지 않았고, 다른 세션의 문서를 만지지 않았으며,
설치본과 lease 파일에 손대지 않았고, `git commit`·`git push`를 하지 않았다.

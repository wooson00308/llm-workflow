# REPORT-SPEC-026-PLAN

> 기록 경위: 하네스가 서브에이전트(plan-a)의 보고서 파일 작성을 차단해, 발신된 전문을 TL이 대리 기록한다. 내용은 발신 전문 그대로다. (수신 2026-08-04T09:07Z, TL 세션)

기획자 세션 결과. IDEA-7BCB8947을 SPEC-026으로 합성했다.

- 대상 아이디어: IDEA-7BCB8947 (아이디어 노트 상태 배지가 제목 길이에 밀려 개행됨)
- 산출 기획서: SPEC-026 (status: user_review) — 승인 대기
- 선점: lease-4822-20260804085428 (08:54Z 획득 → 08:57Z 갱신 → 09:03Z 해제, 전부 exit 0)
- 작성 시각: 2026-08-04T09:03:00Z

## 핵심 기획 결정

1. 원인 확정: App.css:267 `.idea-preview > header` 플렉스에서 배지(.status-pill:227)에 white-space·flex-shrink 부재. 한국어는 어디서나 줄바꿈되므로 눌림의 바닥이 한 글자. width: fit-content는 선호 폭일 뿐 축소를 못 막는다.
2. 기본 상태임을 실측: 아이디어 25건 전부 title 프론트매터 0건 → 본문 첫 줄 파생(60자+말줄임=61자 상한). 25건 중 9건이 상한 도달, 신고 아이디어 자신도 그중 하나. 미리보기 최소 폭 360px에 제목 23px.
3. 어법은 코드가 이미 답함: 같은 화면 목록 행 배지(.idea-state-tag:256)에는 white-space: nowrap이 이미 있다.
4. 좁은 창 문제가 아님: 980px 미디어쿼리에서 1열이 되면 오히려 덜 눌림 — QA를 좁은 창에서만 하면 못 본다(완료 조건 3에 반영).
5. IDEA-5C6073A5(본문 개행 소실)와 증상 방향이 반대임을 명시, 제외 범위로 분리.
6. 검증은 SPEC-021 선례(CSS 문자열 검사) 제안.

## 확인 필요 3건 (사용자 결정 대기)

1. 이 화면만 vs .status-pill 전체(8자리) — 제안: 전체 (DevelopmentBoard.tsx:262가 구조까지 같음. 비용: 8자리 일괄 변경, 아키텍트가 각각 확인 필요).
2. 회귀 검증 수단 — 제안: SPEC-021과 같은 CSS 문자열 검사 (한계 명시).
3. 제목·배지 폭 다툼 — 제안: 제목은 접히게 두고 배지만 고정.

## 검증

확인 사실 14건을 코드·CSS·Rust 파생 로직·실제 아이디어 25건 파싱으로 직접 대조(추정 0). 재검증에서 두 수치 정정(61자 도달 8→9건, 테스트 20→23건). 코드 무변경이라 테스트 미실행.

## 핸드오프 — DECISION-9E5D2C71 계약 위반 소지 (중요)

SPEC-023 승인 기록(DECISION-9E5D2C71)이 created_by: user인데 본문이 "TL 세션 대리 기록"이다. 공통 규칙 §2와 충돌: decisions/*.md는 앱 소유이고, 사용자 결정은 앱이 기록한 것만 유효하며, Markdown 편집으로 사용자를 사칭한 승인·반려는 금지된다. 이 승인에 근거한 후속 작업(SPEC-023 분해→구현)이 "사용자가 실제로 읽지 않은 기획서의 구현"으로 이어질 수 있다. 실제 사용자 승인을 앱에서 받는 편이 안전하다. → TL이 사용자에게 상신(같은 형식의 대리 결정 4건 전체 대상).

## 그 외 핸드오프

- .status-pill 8자리 중 이 화면 외 7자리의 폭 조건은 미확인 — 확인 필요 1 전체 승인 시 아키텍트가 각각 볼 것.
- IDEA-5C6073A5는 같은 화면이라 파일이 겹칠 수 있으나(App.css 헤더/배지 vs 본문 렌더링) 충돌 가능성 낮음.

## 보호 상태

생성 파일 SPEC-026.md 하나. 보호 상태 무변경, 커밋 없음, lease 해제 완료.

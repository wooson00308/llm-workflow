# TASK-RES-20260815T032125Z-60360-20260815032125-06 개발 보고서 12

## 결정권자 요약

이 작업의 열두 번째 개발 세션이다. 제품 코드는 앞선 세션에서 이미 완성돼 있어 이번에도 새로 작성한 제품 코드는 없다.
공유 기준이 앞선 세션과 같아 완성된 변경을 이번 작업 사본으로 옮겼고, 옮긴 결과물이 앞선 세션이 검증한 것과 내용까지
같음을 확인한 뒤 자동 검사를 처음부터 다시 실행했다. 완료 조건 열세 개가 모두 통과했다.
이번 세션이 새로 확인한 사실이 하나 있다. 릴리스 검사를 자체 점검이 아닌 실제 대조 방식으로 실행하면 격리 작업 사본
안에서는 검사가 실패한다. 검사가 함께 배포되는 실행 환경 저장소를 앱 위치 기준 상대 경로로 찾기 때문이며, 위치를 직접
지정하면 공유 작업 공간과 같은 결과로 통과한다. 이번 변경 때문에 생긴 실패가 아니다.
반영은 이번에도 시작하지 못했다. 공유 작업 공간에 미커밋 변경 네 개가 남아 있고, 개발자 역할 계약이 그 상태에서 반영을
시작하지 못하게 한다.

## 이어받은 것과 버린 것

앞선 세션의 통합 후보 커밋 `b421940`과 보고서 11을 이어받았다. 제품 코드에서 버린 것은 없고, 문서·시험·상태 어느
쪽에서도 앞선 세션의 산출물을 폐기하지 않았다.

- 유지: `b421940`의 네 파일 변경 전부. 이번 사본에 cherry-pick해 커밋 `2665303`을 만들었고 충돌은 없었다. 두 커밋의
  트리 해시가 `a947a27`로 같아 내용이 동일함을 확인했다. 삭제되거나 약해진 시험은 없다.
- 유지: 기준 커밋 `55a7af2`. 공유 기준이 앞선 세션 이후 전진하지 않아 후보를 다른 커밋 위로 옮기지 않았다.
- 유지: 회수된 앞선 lease `lease-94165-20260815130806` 사본의 빌드 캐시. 살아 있는 리스가 이번 것 하나뿐이라 죽은
  잔여물로 판단했다. 앞선 세션과 달리 옮기지 않고 복사했다. 앱 소유 경로 안에서 이번 세션이 쓰는 자리를 자기 사본으로
  한정하기 위해서다. 복사본 안의 `tauri`와 `llm-workflow` 두 크레이트 산출물 2.2G는 빌드 스크립트 산출물이 앞선 사본의
  절대 경로를 담고 있어 `cargo clean`으로 지웠다. 남은 캐시는 2.1G, 디스크 여유 16G.

## 변경한 파일과 모듈

`2665303`이 담은 네 파일이며 이번 세션이 새로 작성한 제품 코드는 없다. 작업 문서만으로 판단이 가능해 기획서 본문과
결정 문서 본문은 열지 않았다.

- `src-tauri/src/domain/agent_runtime.rs`: 상수 `ISOLATION_CAPABLE_RUNTIME_VERSION`("0.10.0"), 지원·미지원·모름을
  구분하는 값 `IsolationSupport`, 판정 함수 `judge_isolation_support`.
- `src-tauri/src/application/agent_runtime_run_service.rs`: `plan`이 격리 판정을 인자로 더 받고, 기존 `guard` 바로
  뒤에서 개발 역할 슬롯을 검사한다. 실패 값 `RunFailure::IsolationUnsupported` 추가.
- `src-tauri/src/commands/agent_runtime.rs`: `run_gate_of`가 조회 한 번에서 호환 판정과 격리 판정을 함께 꺼낸다.
- `docs/agent-runtime.md`: 격리 지원 최소 판과 의도된 정지를 적은 절 하나.

## 검증 단계와 결과

기준 `55a7af2` 위의 커밋 `2665303`에서 전부 다시 실행했다. 반영을 시작하지 않았으므로 아래는 모두 격리 상태의 값이며, cargo 명령에는 모두 `--manifest-path src-tauri/Cargo.toml`을 붙였다.

- `cargo test ... agent_runtime`: 본체 107개, e2e 대상 21개 통과, 실패 0.
- `cargo test ... agent_runtime_run_service` 14개, `... agent_runtime_install_service` 18개, `... agent_runtime_status` 14개 통과, 실패 0(완료 조건 10).
- 완료 조건 1~3은 도메인 시험 모듈의 격리 판정 시험 네 개가, 완료 조건 4~7은 실행 서비스 시험 모듈의 관문 시험 네 개가 각각 재현하며 모두 통과했다.
  시험 본문을 직접 읽어 조건이 지정한 판 값과 역할 구성을 실제로 다루는지 확인했다. 막힌 요청이 실행 환경에 아무것도 보내지 않는 사실은 호출 기록이 비어 있는지 보는 단언이 고정한다.
- `cargo fmt --check` 종료 코드 0. `cargo check`는 오류와 경고를 합쳐 0건(완료 조건 8의 컴파일 확인).
- 완료 조건 8: 후보의 diff에서 실행 서비스의 삭제된 줄은 import 2줄과 `plan` 호출 4자리뿐이고, `start`·`cancel`·`retry`의 시험은 한 줄도 바뀌지 않았다.
- 완료 조건 9: 세 상수의 정의를 직접 읽어 계약 주 버전 1, 최소 판 "0.9.0", 최대 판 "1.99.99"로 기준과 같음을 확인했다. 시험 `the_existing_support_range_is_left_where_it_was`가 같은 값을 고정한다.
- 완료 조건 11: `docs/agent-runtime.md`의 새 절이 최소 판 0.10.0, 그 아래에서 개발 역할 요청이 거부된다는 사실, 함께
  배포되는 실행 환경이 0.9 계열이라 지원 판이 나오기 전까지 개발 실행을 시작할 수 없다는 사실 세 가지를 담고 있다.
- 완료 조건 12: `npm run agent-runtime:verify -- --self-test` 통과("정상 fixture 1건과 불일치 fixture 3건"). 이번에는
  실제 대조도 실행했다. 작업 사본에서 그대로 실행하면 "Python 실행 파일을 찾지 못했습니다"로 실패한다.
  `scripts/verify-agent-runtime-contract.mjs` 216행이 실행 환경 저장소를 앱 위치 기준 상대 경로로 계산하는데 작업
  사본에는 그 경로가 없기 때문이다. `AGENT_RUNTIME_REPOSITORY`로 위치를 지정하면 "0.9.2 / API 1 / macos-universal"로
  통과하고, 이는 공유 작업 공간에서 기준 커밋 그대로 실행한 결과와 같다. 새 상수 이름이 정규식 판정을 가로채지 않았다.
- 완료 조건 13: `git diff --name-only 55a7af2 HEAD -- src/`가 0건이다.

## 반영에서 멈춘 지점

격리 검사 결과와 반영 결과는 별개의 사실이다. 위 검사는 모두 통과했고 반영은 시작하지 않았다. 반영 직전에 다시 대조한
공유 기준 `55a7af2`는 후보의 기준과 같고, 스테이지된 변경도 진행 중인 기준 변경도 없다. 그러나 추적 파일 네 개에 미커밋
변경이 있고, 개발자 역할 계약은 이 상태에서 자동 반영을 시작하지 않으며 남의 미커밋 변경을 커밋하거나 되돌리지도 않는다.

네 개는 앞선 세션이 보고한 것과 같다. `.workflow/rules/wf-eligible.sh`와 `.workflow/rules/wf-reserve.sh`는 앱이 설치하는
사본이며 실제 기능 변경이 더해진 판이라 커밋 여부는 사용자가 정할 일이다. 나머지 둘은 이 워크플로의 작업 문서 `-03`과
`-06`이며, 공통 규칙 4절이 작업 시작 전에 이력 항목부터 쓰도록 요구하므로 반영을 수행할 세션 자신이 반영을 시도하기 전에
반드시 제어 문서를 더럽힌다. 이번 세션도 이력 항목을 쓰는 순간 그 상태를 다시 만들었다.

통합 후보를 갱신해 둔다. 커밋 `2665303`(가지
`wf-iso/TASK-RES-20260815T032125Z-60360-20260815032125-06/lease-61678-20260815132202`), 기준 `55a7af2`, 변경 파일은 위
네 개다. 작업 상태는 `in_progress`로 두었다. 반영 대기는 계약이 따로 이름 붙인 상태라 `blocked`로 두지 않았고, 반영 후
재검사를 마치지 않아 `verified`로도 두지 않았다.

## 남은 위험

- 반영 전까지 승인된 기획서가 요구한 정지가 공유 기준에 없다. 격리를 지원하지 않는 실행 환경에서 개발 역할 실행이 열려 있다.
- 관문은 실행 계획 단계에만 있다. 자동 배정은 이 경로를 지나지 않으며 작업 문서가 범위 밖으로 명시했다.
- 공유 기준이 다시 전진하면 후보를 그 커밋 위로 옮겨 검사를 다시 실행해야 한다.
- 릴리스 검사의 실제 대조는 작업 사본에서 위치 지정 없이는 실행되지 않아, 이 검사를 완료 조건으로 삼는 다음 작업이 같은
  자리에서 멈출 수 있다. 이번에 복사한 빌드 캐시는 이 사본에 있어 사본이 회수되면 다음 세션은 전체 재빌드를 치른다.

## 후속 작업

- 사용자 확인 필요: 미커밋 변경 네 개를 한 번에 커밋해야 반영이 열린다. 에이전트의 대리 정리는 계약이 금지하므로 이
  커밋은 사용자만 할 수 있다.
- 아키텍트 인계(이번 세션이 새로 확인): 릴리스 검사가 실행 환경 저장소를 앱 위치 기준 상대 경로로 계산하므로 격리 작업
  사본 안에서는 실제 대조가 실행되지 않는다. 검사에 저장소 위치를 넘기는 방법을 정하거나, 격리 세션에서 이 검사를 자체
  점검으로만 요구할지 결정해야 한다.
- 아키텍트 인계: 반영 대기 작업을 개발자 판정에서 빼는 관문이 읽는 `integration_waiting` 값을 저장소의 어느 자리도 쓰지
  않는다. 값을 적는 주체가 별도 저장소의 실행 환경이라 범위 밖이며, 그 저장소가 고치기 전까지 같은 작업이 계속 다시
  배정된다. 읽는 자리와 쓰는 자리의 행 번호는 보고서 11에 적혀 있다. 열두 번째 기록이다.
- 아키텍트 인계: 제어 문서가 추적 대상인 한 반영 전제 조건인 깨끗한 공유 작업 공간을 어떤 개발 세션도 구조상 만족시킬 수
  없다. 추적에서 뺄지, 앱이 커밋할지, 반영 판정에서 제외할지는 설계 결정이므로 개발 세션이 정할 수 없다. 보고서 8부터
  11까지와 같은 항목이며 아직 열려 있다.
- 아키텍트 인계: 회수된 사본의 빌드 캐시를 다음 세션이 재사용해도 되는지 정해야 한다. 그 경로는 앱 소유인
  `.workflow/.runtime/` 아래이고, 규칙은 빌드 산출물과 제어 상태를 구분해 적고 있지 않다.
- 아키텍트 인계: 관리 스크립트 원본을 바꾸는 작업의 완료 조건에 설치 사본 갱신을 넣을지 정해야 한다.
- 아키텍트 인계: `docs/agent-runtime.md` 버전 표가 앱 판 0.1.11을 적어 릴리스 0.1.18과 어긋난다(열두 번째 기록).
- 이번 예약의 결과 접두사는 RES-20260815T132203Z-61678-20260815132202이다.

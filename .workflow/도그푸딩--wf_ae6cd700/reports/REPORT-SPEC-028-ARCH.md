# SPEC-028 아키텍트 핸드오프

> 기록 경위: 하네스가 서브에이전트(arch-b)의 보고서 파일 작성을 차단해, 발신된 전문을 TL이 대리 기록한다. 내용은 발신 전문 그대로다. (수신 2026-08-04T09:44Z, TL 세션)

- 대상: DECISION-2A9D7B31 (SPEC-028 승인)
- 산출 작업: TASK-086, TASK-087, TASK-088 (todo)
- 세션: 09:19Z~09:29Z / lease 취득→반납 exit 0
- id 경합 실발생: SPEC-024 분해 세션과 번호 경합 → noclobber 배타 생성으로 086~088 원자 선점 후 내용 작성 (RESERVED 잔여 0, 타 세션 문서 무손상)

## 의존 지도

TASK-086(아키텍트 자격 판정: 최신 검사 + created_by 필터, R4·R5) ← depends_on TASK-076(in_progress, 같은 두 본문·버전 상수). TASK-087(앱 재가 경로 테스트 고정, R3) — 선행 없음, 086과 병행 가능(파일 불겹침). TASK-088(계약 문언 + rules_version 7→8, R1·R2·R6) ← [086, 087].

## 유의 4건 반영

1. SPEC-023 겹침: TASK-076 in_progress 실측 → depends_on 확정. 버전 상수는 "착수 시점 값+1"로 명시(076이 4→5, 086이 5→6). TASK-077은 불겹침이라 순서 없음.
2. 규칙 자산 단독: TASK-088이 project_instructions.rs 유일 작업(TASK-041 선례 인용). WORKFLOW_RULES_VERSION+본문 7→8. 역할 계약 3파일·ROLE_RULES_VERSION 무변경(구현을 문언에 맞추는 방향).
3. 문언이 구현을 앞지르지 않는 순서: 088 마지막 — "재가는 이중 분해를 안 만든다"는 086 후에만 참, "앱 도장 가능"은 087 후에 적음 (SPEC-022 선례 원칙).
4. 수기 17건 소급 준수·앱 8건 불가침: 결정 문서 무접촉, 해석·판정 결과 불변을 완료 조건화.

## 아키텍트 확정

- created_by 값 = `user-delegate` 확정 (세 작업이 같은 문자열 공유 필요). 구현이 다른 값을 골라야 하면 세 작업 동시 변경 조건.
- 스크립트 어긋남 정산은 TASK-086: 아키텍트 분기가 created_by가 user인 결정만 세게(값 전체 비교 — 접두 일치는 user-delegate가 안 걸러짐).

## 분해 중 발견 결함 (R4 네 번째의 실제 모양)

created_at 문자열 비교에서 두 표기(앱 마이크로초+00:00 vs 수기 Z)가 같은 초 안에서 역전됨 — `.`(0x2E) < `Z`(0x5A)라 앱 기록이 더 이르다고 판정. 재가가 대리 결정과 같은 초면 최신 판정이 뒤집힘. 표기 정규화는 제외 범위(두 형식이 판별 신호)라 금지, 처리 판단을 TASK-086 보고서 조건으로 위임.

## 후속 / 리스크

- (사용자·TL 판단 필요) 수기 17건은 새 재가 경로에 닿지 않는다 — created_by: user인 채라 앱이 승인으로 읽어 거부가 그대로. 새 규칙은 앞으로의 대리 결정에만 적용. 부트스트랩 결정 DECISION-2A9D7B31 자신도 그 17건에 포함 — "소급 재가 대상"이라 적었으나 현 설계로 도달 불가. 소급 재가 실행은 기획서 제외 범위라 작업 미생성.
- 기획자·개발자 분기의 created_by 필터 비대칭은 범위 준수로 미처리, 후속 상신.
- QA 결정 읽기 경로가 같은 필터 공유 — QA 대리 기록은 제외 범위, 사실만 TASK-087 보고서에 기록 예정.
- TASK-088 문언 난이도 최고: "기존 17건 판정을 문언만으로 가능하게" 요구 — 17건 중 1건 대입 판정을 보고서 조건으로 강제.

## 상태

보호 상태 무변경, 제품 코드 무변경(읽기만), git 커밋 없음. 작업 셋 todo + created history. lease 반납 완료.

## 부록 — 보충 유의 2건 반영 (2차 세션, 09:31Z~09:36Z, lease 재취득·반납)

1. git diff 의존 검증 문구 제거: TASK-087 조건 5(다섯 심볼 단위 확인으로), TASK-088 조건 11(버전 값 확인으로), TASK-086 조건 10(착수·현재 값 병기로) 교체. 세 작업에 "검증 문구 규칙" 절 신설(다중 세션 미커밋 트리에서 그 판정이 실패하거나 오인 통과하는 이유 명기). 계약 본문 확장은 하지 않음 — SPEC-028 포함 범위 여섯에 작업 문서 작성 규범이 없어 승인 범위 초과가 됨. 별도 아이디어 권고(TASK-088 착지 후 — 규칙 자산 단독 원칙).
2. DECISION-4E8C1D67을 TASK-088 문언 설계의 시험 반례로 편입: (a) 실패 모드가 둘 — 위임 없는 날조뿐 아니라 "진짜 위임이 결정 문서가 되지 못한 채 지시로만 도는" 경우도 문언이 겨눠야 함, (b) 그 거부는 새 문언 아래서도 옳아야 함("TL이 지시했으면 따른다"로 읽히면 실패). 완료 조건 6 강화: 대입 시험 두 건 중 하나는 반드시 4E8C1D67.
3. 덤: 수기 결정 17→21건 증가 실측(기획서 작성 중에도 4건 증가 — SPEC-028 근거 강화). 상수를 판별 규칙(초 단위 Z vs 마이크로초+00:00)으로 교체, 앱 형식 8건 불변·불가침 재확인.


# TASK-078 개발자 핸드오프

> 기록 경위: 하네스가 서브에이전트(dev-078=dev-071)의 보고서 파일 작성을 차단해, 발신된 전문을 TL이 대리 기록한다. 내용은 발신 전문 그대로다. (수신 2026-08-04T09:40Z, TL 세션)

- 대상: TASK-078 (워크플로별 아이디어 초안을 기억하는 브라우저 저장소를 만든다)
- 근거: SPEC-025, DECISION-5B7E9F14 / 상태: `qa_waiting` (lease-51088-20260804091720, 전부 exit 0)

## 변경한 파일 (신설 둘, 기존 무변경)

- src/features/projects/infrastructure/browserIdeaDraftStore.ts — 신설
- src/features/projects/infrastructure/browserIdeaDraftStore.test.ts — 신설, 테스트 15개

기존 저장소 넷·컴포넌트·훅·src-tauri 무변경, 보호 상태 무변경, git 커밋 없음, 새 추상화 없음(TASK-061 답습).

## 계약

키 `workflow-labs.idea-draft.v1` (기존 4키와 불충돌 실측), 형태 Record<워크플로 디렉터리, 초안>, 동작 load/save/clear (기존 모양 동일). 읽기는 없으면 "" 무예외.

## 키 축 판단 — 프로젝트 축 불추가 (작업 문서 확인 항목)

디렉터리 식별자가 uuid 유래(fs_project_repository.rs:159~:160 `wf_{uuid[..8]}`)라 프로젝트별 카운터 충돌 경로가 없음. 폴더 복사·이중 clone은 "같은 워크플로가 두 자리"인 경우로, R3 취지상 초안이 이어지는 쪽이 맞음 — 축을 더하면 오히려 R3이 깨짐.

## 실패 삼킴 주석 — 선례 문장 미복사

"표시 상태라 알릴 가치 없다"는 이 자산(사용자 저작물)에서 거짓 — 승인된 실제 근거(읽기 실패에도 화면 글·제출 정상 / 쓰기는 글자마다라 오류 반복이 더 나쁨)를 새로 적고, 선례와 결이 다름을 모듈 주석에 기록.

## 구현 판단

clear가 save(directory, "")에 위임 — "빈 저장=지움" 두 경로가 갈릴 수 없는 구조. save는 빈 초안을 항목 삭제로 다뤄 맵에 빈 문자열 미축적.

## 게이트 수치

npm run check 통과: tsc 0, vitest 17 files / 389 passed / 0 실패 (기준선 374 → 내 몫 +1파일 +15), build 성공. 완료 조건 1~11 전부 대조. 헛돌기 검증: 빈 값 삭제 로직 제거 시 해당 테스트 1개만 실패 확인 후 원상 복구.

## 사용자 QA 제안

배선 전이라 화면 변화 없음 — 회귀 부재만: 최근 프로젝트 목록, 카드 접힘 기억, 설치 가이드 접힘, 잡 값 기억, 아이디어 제출 정상 (localStorage 저장소가 다섯이 됨).

## 리스크와 후속

- 초안 무기한 축적(승인된 한계), 워크플로 삭제 후 항목 잔존(선례와 같은 성질).
- 저장소 다섯이나 공통 계층 미수립(범위 밖 명시) — 여섯 번째에서 판단.
- 다중 창 동기화 없음(나중 쓴 창이 이김).
- TASK-079 배선 주의: save는 글자마다 호출 가능하나 매번 getItem+parse — 부하 시 디바운스는 배선 쪽에서 (저장소에 넣으면 "지운 직후 읽기"가 어긋남).

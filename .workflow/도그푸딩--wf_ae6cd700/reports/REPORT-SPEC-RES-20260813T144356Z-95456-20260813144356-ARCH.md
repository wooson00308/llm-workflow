# SPEC-RES-20260813T144356Z-95456-20260813144356 아키텍트 보고서

## 결정권자 요약

승인된 집중형 품질 확인 작업대를 여섯 개의 개발 작업으로 나눴다.
결과가 섞인 제출을 받는 앱 통로를 만드는 일, 기능 묶음 판정과 임시 저장을 화면 밖으로 옮기는 일,
전용 화면에 대기열을 세우는 일, 항목을 하나씩 확인하게 하는 일, 검토 뒤 한 번에 제출하는 일,
그리고 오늘 화면과 개발 화면의 입구를 그 작업대로 모으는 일이다.
가장 큰 판단은 화면을 붙이는 순서였다. 결정을 남기는 길이 한순간도 사라지지 않도록, 기존 개발 화면의
확인 양식은 새 작업대가 제출까지 갖춘 뒤에야 없애도록 마지막 작업에 순서를 걸었다.
앞의 두 작업은 서로 겹치지 않아 동시에 진행할 수 있고, 나머지 넷은 앞뒤로 순서를 지정했다.
기획서의 요구사항 열두 개와 완료 조건 열일곱 개를 모두 작업 문서의 완료 조건으로 옮겼다.
기획서가 정한 범위를 늘리거나 줄이지 않았고, 새 사용자 결정이 필요한 항목도 없었다.
사용자는 각 작업이 품질 확인에 올라올 때 그 작업의 결정 요청에 적힌 항목만 확인하면 된다.

## 대상과 예약 절차

- 대상은 DECISION-4607A607이다. SPEC-RES-20260813T144356Z-95456-20260813144356에 대한 앱 기록 승인이며
  작성자가 사용자로 기록돼 있다.
- 착수 전 확인: 이 기획서에 걸린 결정은 이 하나뿐이고 결과는 승인이다. 이 결정을 참조하는 개발 작업이
  아직 없어 분해 대상 자격을 충족했다.
- 예약 처리: 런타임이 미리 잡아 둔 lease를 갱신해 소유를 확인했고 새로 획득하지 않았다. 실행한 명령은
  `sh .workflow/rules/wf-claim.sh renew DECISION-4607A607 lease-92948-20260813150633`이며 종료 코드 0을
  확인했다. 앱이 설치한 자격 판정 도구를 함께 돌려 이 대상이 후보로 잡히고 지금은 이 세션의 lease가
  걸린 상태임을 확인했다.
- 결과 접두어는 RES-20260813T150633Z-92948-20260813150633이다. 이 접두어로 만들 작업 문서 경로 여섯 개가
  쓰기 전에 모두 비어 있음을 확인했다.
- 마이그레이션 잠금은 없었고 사용자 정의 규칙 파일도 없어 공통 규칙과 아키텍트 역할 계약만 적용했다.

## 아키텍처 판단

기획서가 아키텍트에게 남긴 판단은 넷이었다. 섞인 결과를 어디서 받을지, 기능 묶음 판정을 어디에 둘지,
화면을 어떤 순서로 붙일지, 그리고 기존 입구를 언제 없앨지다.

- 섞인 결과는 새 앱 통로 하나가 받는다. 지금의 일괄 확인은 전 건을 확인 완료로만 남기고 메모도 하나를
  공유해서, 완료와 재작업 요청이 섞인 기능을 처리할 수 없다. 기존 통로의 인자를 늘려 고치는 방법도
  있었지만, 그 통로는 개발 화면이 지금도 쓰고 있어 같은 호출의 뜻이 도중에 바뀐다. 새 통로를 더하고
  기존 통로는 그대로 두는 쪽을 골랐다.
- 기록 규칙은 여전히 한 자리에 둔다. 새 통로도 지금 단건과 일괄이 함께 쓰는 기록 함수를 그대로 부른다.
  결정 문서를 쓰고 상태를 옮기고 이력을 더하는 규칙이 두 자리로 갈라지면 같은 결정이 통로에 따라 다르게
  남는다.
- 검토 시작 뒤 작업이 바뀌었는지는 앱이 대조한다. 화면만으로 막으면 화면이 값을 읽은 뒤 제출하기까지의
  틈이 남는다. 대조는 기록 함수 안이 아니라 그 앞에서 하도록 했다. 안에서 하면 기존 단건 통로의 판정까지
  함께 바뀌어, 유지하기로 한 것을 건드리게 된다.
- 기능 묶음 판정은 화면 밖으로 옮긴다. 지금 그 판정은 개발 화면 파일 안에 있고 에이전트 화면이 그중
  하나만 가져다 쓴다. 오늘 화면과 새 작업대까지 네 화면이 같은 기준으로 세어야 하므로, 판정을 도메인
  모듈 한 곳에 두고 화면은 읽기만 하게 했다. 옮기는 동안 기준은 한 줄도 바꾸지 않고, 기존 화면 검사가
  편집 없이 통과하는 것을 그 작업의 성공 기준으로 삼았다.
- 자동 근거 항목을 목록에서 빼던 동작은 없앤다. 지금은 확인 동선 발췌에 특정 한국어 문구가 있으면 그
  항목이 단계 목록에서 사라진다. 기획서가 요구한 것은 시각적 무게를 낮추는 것이지 범위에서 빼는 것이
  아니므로, 같은 판정을 목록에서 빼는 값이 아니라 접을지 정하는 값으로 바꿨다.
- 제출 전 검토는 기기에만 남긴다. 정식 상태가 아니어야 한다는 것이 기획서의 요구이고, 저장소는 같은
  폴더의 기존 화면 상태 저장소와 같은 어법을 쓰게 했다. 읽지 못하면 던지지 않고 빈 값으로 답한다.
- 기존 입구를 없애는 시점을 마지막 작업으로 미뤘다. 개발 화면의 확인 양식을 먼저 없애면 새 작업대가
  제출을 갖추기 전까지 사용자가 품질 결정을 남길 길이 사라진다. 그래서 입구 통합 작업은 파일이 겹치지
  않는 제출 작업에도 순서를 걸었다. 이 하나는 겹침 때문이 아니라 중간 상태를 막기 위한 순서다.
- 새 화면의 스타일은 전용 파일에 둔다. 공용 스타일 파일은 스타일 회귀 검사의 판독 대상이고 여러 작업이
  함께 고치는 자리라, 같은 저장소가 이미 쓰는 화면별 스타일 파일 방식을 따랐다.

## 만든 작업과 나눈 기준

1. TASK-RES-20260813T150633Z-92948-20260813150633-01. 작업마다 확인 완료와 재작업 요청을 나눠 담는 한
   번의 제출 통로를 만든다. 앱 백엔드부터 화면 계층의 동작까지가 한 작업이다.
2. TASK-RES-20260813T150633Z-92948-20260813150633-02. 기능 묶음 판정과 기기 내 임시 검토 저장을 화면 밖
   모듈로 분리한다.
3. TASK-RES-20260813T150633Z-92948-20260813150633-03. 품질 확인 전용 화면에 기능 대기열과 확인 범위
   설명을 만든다.
4. TASK-RES-20260813T150633Z-92948-20260813150633-04. 세션에서 항목을 하나씩 확인하고 중단해도 이어가게
   만든다.
5. TASK-RES-20260813T150633Z-92948-20260813150633-05. 제출 전 검토 화면에서 완료와 재작업 요청을 한 번에
   기록하고 실패를 건별로 설명한다.
6. TASK-RES-20260813T150633Z-92948-20260813150633-06. 오늘 화면과 개발 화면의 품질 확인 입구를 전용
   작업대 하나로 모은다.

나눈 기준은 사용자가 확인할 수 있는 단위다. 3번이 끝나면 메뉴에서 대기열을 열어 무엇을 확인해야 하는지
읽을 수 있고, 4번이 끝나면 항목을 하나씩 확인해 중단하고 돌아올 수 있으며, 5번이 끝나면 결과를 남길 수
있다. 1번과 2번은 눌러 볼 화면이 없어 자동 검사로 닫도록 썼고, 그 사실과 확인 방법을 각 작업의 결정
요청에 적었다.

## 병렬 안전과 순서

- 1번과 2번은 파일이 하나도 겹치지 않는다. 한쪽은 앱 백엔드와 앱 호출 계층을, 다른 쪽은 화면 도메인과
  기기 저장소를 다룬다. 두 작업은 동시에 진행할 수 있다.
- 3번은 2번이 만든 판정 모듈을 읽으므로 2번 뒤에 둔다.
- 4번과 5번은 3번이 만든 작업대 파일을 이어서 고치므로 차례로 이어진다. 5번은 1번이 만든 제출 통로도
  함께 쓰므로 두 작업 모두를 선행으로 적었다.
- 6번은 1번·2번·3번과 파일이 겹치고, 5번과는 겹치지 않지만 위에 적은 이유로 순서를 걸었다.
- 겹치는 파일 쌍은 여덟 쌍이고 모두 선행 관계로 순서가 정해진다. 선언에 순환은 없고, 없는 작업을 가리키는
  선언도 없다.
- 겹치지 않는 작업을 순서로 묶은 곳은 5번과 6번 한 쌍뿐이며, 그 근거는 위의 중간 상태 문제다.

## 범위 사전 검사에서 확인한 것

여섯 작업 모두 선언한 파일을 열어 완료 조건이 지나는 값의 경로를 끝까지 따라갔고, 그 내용을 각 작업의
범위 사전 검사 절에 적었다. 확인 과정에서 드러난 사실 중 작업 문서에 반영한 것은 다음과 같다.

- 확인 동선 발췌는 앱 백엔드가 만들어 이미 목록에 실려 있다. 그래서 항목을 구분하는 데는 새 조회가 필요
  없다. 다만 항목 본문에는 발췌가 아니라 절 전체가 필요하고, 그것은 작업 문서를 읽는 기존 통로와 본문에서
  절을 자르는 기존 함수로 충분하다. 새 앱 명령도 새 필드도 만들지 않았다.
- 작업 문서의 갱신 시각은 이미 목록 항목에 실려 화면까지 와 있다. 검토 도중 바뀐 항목을 가려내는 값을
  새로 만들 필요가 없어, 그 값을 임시 검토에 함께 담아 두고 비교하도록 했다.
- 오늘 화면의 결정 건수는 지금 앱 백엔드가 센 값 하나만 쓰고 그 값은 기획서 수만 센다. 품질 확인 건수를
  그 값에 더하면 앱 백엔드를 고쳐야 하지만, 판정 모듈과 목록이 이미 화면에 있으므로 화면에서 합치는
  쪽으로 정했다. 이 결정으로 6번 작업에서 앱 백엔드 파일이 빠졌다.
- 기존 일괄 확인 통로는 확인 완료 전용이고 메모도 하나만 받는다. 이 사실이 1번 작업을 새 통로로 만든
  직접 근거다.

## 기획서와의 대응

- R1은 6번, R2와 R3은 3번, R4와 R5는 4번, R6과 R7과 R9는 5번, R8은 4번과 5번, R10은 6번이 받는다.
- R11의 접근성은 화면마다 나눠 담고, 대기열 진입부터 최종 제출까지 이어지는 조건은 5번에 두었다. R12의
  시각 절제는 3번·4번·5번에 각각 담았다.
- 완료 조건 1과 2는 6번, 3은 3번, 4와 6은 4번, 5는 3번과 4번, 7은 4번, 8은 4번, 9부터 13까지는 5번, 14와
  16은 6번, 15는 5번이 받는다. 17은 여섯 작업의 검증 단계에 나눠 담았고, 기존 자동 검사를 지우거나 끄지
  않는 것을 각 작업의 조건으로 적었다.

## 후속과 위험

- 기획서의 완료 조건 4는 "품질 확인 대기 17건과 세 기능"이라는 2026-08-13 시점의 문서 집합을 그대로
  적고 있다. 오늘 다시 세어 보니 품질 확인 대기는 18건이고 기획서 넷에 묶이며, 그중 셋이 지금 확인할 수
  있는 상태다. 수치가 계속 움직이는 값이라 작업 문서에는 고정된 수 대신 "그 기능의 품질 확인 대기 작업이
  하나도 빠짐없이 목록과 건수에 들어간다"는 형태로 옮겼다. 요구의 내용은 그대로이고 검증 시점의 문서
  집합으로 확인할 수 있다.
- 6번 작업이 화면 쪽 배선을 정리하면서 앱 호출 계층의 선언 일부가 쓸모를 잃을 수 있다. 앱 백엔드의 기록
  명령과 그 검사는 지우지 않도록 작업 문서에 못 박았다. 완료 조건 8이 그 위에 서 있기 때문이다.
- 6번이 끝나기 전까지는 개발 화면의 기존 확인 입구와 새 작업대가 함께 존재한다. 의도한 중간 상태이며 각
  작업의 범위 밖 절에 그렇게 적었다.
- 역할 밖이라 손대지 않은 사항: 기획서의 상태가 사용자 검토로 남아 있다. 앱이 기획서 결정을 읽는 경로가
  따로 있고 상태를 바꾸는 것은 앱의 일이므로 문서를 고치지 않았다.

## 세션 종료

- 만든 문서: 위 여섯 개의 개발 작업 문서. 모두 준비 상태이고 생성 이력을 하나씩 갖는다.
- 고친 문서: 없다. 기획서와 결정 문서와 기존 작업 문서는 열어 읽기만 했다.
- 확인한 검사: 여섯 문서의 필수 항목과 결정권자 요약의 형식, 파일 범위 선언의 표기, 선행 선언의 순환
  여부와 겹치는 쌍의 순서를 모두 대조했다.
- lease는 이 보고서를 쓴 뒤 같은 값으로 반납했다.

# SPEC-021 아키텍트 핸드오프

> 기록 경위: 하네스가 서브에이전트(arch-b)의 보고서 파일 작성을 차단해, 발신된 전문을 TL이 대리 기록한다. 내용은 발신 전문 그대로다. (수신 2026-08-04T08:58Z, TL 세션)

- 역할: 프로젝트 아키텍트 (architect)
- 대상: DECISION-C9B1C1D5 (SPEC-021 승인, 2026-08-04T08:06:48Z)
- 산출 작업: TASK-074 (1건, status: todo, depends_on 없음 — 즉시 착수 가능)
- 세션: 2026-08-04T08:45Z ~ 08:52Z
- lease: lease-80574-20260804084538 취득 → 반납(exit 0), leases/SPEC-021.yml 소멸 확인

## 선점

migration.lock 없음, 선점 전·후 두 번 source_decision_id 참조 0건 확인 후 분해. 다른 lease(SPEC-009 만료, IDEA-4141EE4C, IDEA-CAB890F1) 무조치. SPEC-020 lease는 이미 반납돼 있었다(그 세션이 TASK-070~073을 냈다).

## 생성한 작업

TASK-074 — 개발 보드 카드가 컬럼 경계를 넘지 않게 하고 선언을 회귀 검사로 고정한다 (R1~R6 전부)

범위: src/App.css의 다섯 규칙(.task-column:410, .task-stack:420, .task-card:421, .task-card > strong:425, .task-card > p:426), 회귀 검사 파일 신설(src 아래 파일을 읽는 첫 프런트엔드 테스트), DevelopmentBoard.test.tsx 재현 시나리오 추가. DevelopmentBoard.tsx 마크업 무변경.

## 핵심 분해 결정

### 왜 한 작업인가
완료 조건 1~4의 검증 수단이 곧 회귀 검사라, 수정과 검사를 나누면 먼저 가는 쪽이 자기 완료 조건을 닫지 못한다. 변경분도 CSS 선언 네댓 개 수준 — 나누면 하드 의존만 늘고 병렬 이득 0. 재현→수정 순서(SPEC-022 방식)는 결정문 요구가 없고 검사 단언 대상이 수정이 쓰는 선언 그 자체라 쓰지 않았다.

### App.css 공유 16건 병행 확인 (depends_on 없음의 근거)
- 14건은 qa_waiting(편집 세션 없음). 유일한 in_progress TASK-062는 App.css를 안 만짐.
- todo인 TASK-072·073은 새 규칙 덧붙이기라 :410~:426 기존 보드 규칙과 구간이 안 겹치고, TASK-072는 의존 때문에 당장 착수 대상도 아님.
- 이 저장소는 App.css 공유만으로 직렬화한 선례가 없다(TASK-030·031·033·034·036·046 전부 선행 없음). 존재하는 depends_on은 전부 실제 코드 결합.
- 미커밋 App.css 변경분은 파일 끝 .task-dependencies 계열(TASK-038)로 보드 구간과 안 겹침. 줄 번호는 작업 트리 기준 경고 명시.

### 승인된 확인 필요 3건
결정문 본문이 비어 있어(승인만 기록) 기획서 제안 그대로: (1) 긴 토큰 접기, (2) 회귀 검증은 스타일 규칙을 문자열로 읽는 자동 검사, (3) 처방은 보드에만.

### 구현에 넘긴 기술 판단
- overflow-wrap: break-word 계열의 줄바꿈 기회는 min-content 계산에 반영되지 않고 anywhere만 반영된다 — R2를 직접 만족시키는 것은 anywhere. 저장소 기존 어법(word-break: break-word 3곳)과 다른 선언을 쓰는 이유를 보고서에 남길 것.
- 트랙 처방(minmax(0,1fr)·min-width:0)은 저장소 선례 있음(.idea-list > button:246, .search-result-copy:557).
- 재현 데이터(R6): 확인 사실 10의 33자 무공백 런 ``(`HeartbeatCard.tsx:246`~`:252`).`` 을 실측 검산(TASK-049 실제 요약문 143자, 160자 컷에 안 걸려 런 생존)해 작업 문서에 원문으로 박음. 지어낸 문자열 금지.
- 검사의 한계 명시 의무: 레이아웃을 재지 않는다는 사실을 검사 파일 머리에 남기고, jsdom에서 상자 크기를 묻지 말 것(전부 0이 돌아와 거짓 통과). 지키는 선언 + 깨지면 안 되는 선언(클램프, 보드 min-width·overflow-x, word-break: keep-all 2곳)을 함께 검사해 R4 회귀까지 잡게 함.

## 후속 / 리스크

- 검사는 CSS 재포맷에 취약(확인 필요 2번이 수용한 한계).
- 완료 조건 5("잘라 내는 방식으로 해결하지 않았다")는 자동으로 안 닫힘 — 사용자 QA 눈 확인 필요, 판단 기준을 보고서에 남기라는 조건 부여.
- "보드에만" 승인이라 새 카드 자리가 생기면 같은 버그 재발 가능 — 사실 기록만 남김.

## 상태

보호 상태 무변경, 제품 코드 무변경(읽기만), git 커밋 없음. TASK-074는 todo + history created. lease 반납 완료. 반납 후 잔존 lease: SPEC-009(만료), TASK-063, TASK-070.

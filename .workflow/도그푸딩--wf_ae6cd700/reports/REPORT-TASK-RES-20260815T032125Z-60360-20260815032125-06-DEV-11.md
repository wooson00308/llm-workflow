# TASK-RES-20260815T032125Z-60360-20260815032125-06 개발 보고서 11

## 결정권자 요약

이 작업의 열한 번째 개발 세션이다. 제품 코드는 앞선 세션에서 이미 완성돼 있어 이번에도 새로 작성한
제품 코드는 없다. 공유 기준이 앞선 세션과 같아 완성된 변경을 이번 작업 사본으로 그대로 옮겨 왔고,
옮긴 결과물의 내용이 앞선 세션이 검증한 것과 완전히 같음을 확인한 뒤 자동 검사를 처음부터 다시
실행해 완료 조건 열세 개를 모두 직접 확인했다. 전부 통과했다.
반영은 이번에도 시작하지 못했다. 공유 작업 공간에 미커밋 변경 네 개가 남아 있고, 개발자 역할 계약이
그 상태에서 반영을 시작하지 못하게 하기 때문이다. 사용자가 그 네 파일을 한 번에 커밋하면 반영이
열린다. 같은 작업이 열한 번 다시 배정되는 이유도 앞선 세션과 같으며 이번에도 직접 재확인했다.

## 이어받은 것과 버린 것

앞선 세션의 통합 후보 커밋 `ec4cdea`와 보고서 10을 이어받았고, 제품 코드에서 버린 것은 없다.

- 유지: `ec4cdea`의 네 파일 변경 전부. 이번 사본에 cherry-pick해 커밋 `b421940`을 만들었고 충돌은 없었다. 두 커밋의 트리 해시가 `a947a27`로 같아 내용이 동일함을 확인했다. 삭제되거나 약해진 시험은 없다.
- 유지: 기준 커밋 `55a7af2`. 공유 기준이 앞선 세션 이후 전진하지 않아 후보를 다른 커밋 위로 옮길 필요가 없었다.
- 유지: 회수된 앞선 lease `lease-58885-20260815125204` 사본의 빌드 캐시 3.8G. 살아 있는 리스가 이번 것 하나뿐이라 죽은 잔여물로 판단해 이번 사본으로 옮겨 재사용했다.
- 버림: 그 캐시 안의 `tauri`와 `llm-workflow` 두 크레이트 산출물 2.2G. 빌드 스크립트 산출물이 앞선 사본의 절대 경로를 담고 있어 그대로는 빌드가 실패한다. 두 크레이트만 `cargo clean`으로 지웠다. 남은 캐시는 2.1G이고 디스크 여유는 19G다.
- 버림: 없음(문서·시험·상태 어느 쪽에서도 앞선 세션의 산출물을 폐기하지 않았다).

## 변경한 파일과 모듈

`b421940`이 담은 네 파일이며 이번 세션이 새로 작성한 제품 코드는 없다.

- `src-tauri/src/domain/agent_runtime.rs`: 상수 `ISOLATION_CAPABLE_RUNTIME_VERSION`(35행, "0.10.0"), 지원·미지원·모름을 구분하는 값 `IsolationSupport`, 판정 함수 `judge_isolation_support`.
- `src-tauri/src/application/agent_runtime_run_service.rs`: `plan`이 격리 판정을 인자로 더 받고, 기존 `guard` 바로 뒤에서 개발 역할 슬롯을 검사한다. 실패 값 `RunFailure::IsolationUnsupported` 추가.
- `src-tauri/src/commands/agent_runtime.rs`: `run_gate_of`가 조회 한 번에서 호환 판정과 격리 판정 두 값을 함께 꺼낸다.
- `docs/agent-runtime.md`: 격리 지원 최소 판과 의도된 정지를 적은 절 하나(28~41행).

작업 문서만으로 판단이 가능해 기획서 본문과 결정 문서 본문은 열지 않았다.

## 검증 단계와 결과

기준 `55a7af2` 위의 커밋 `b421940`에서 전부 다시 실행했다. 반영을 시작하지 않았으므로 아래는 모두 격리 상태의 값이다. cargo 명령에는 모두 `--manifest-path src-tauri/Cargo.toml`을 붙였다.

- `cargo test ... agent_runtime`: 본체 107개 통과, e2e 대상 21개 통과, 실패 0.
- `cargo test ... agent_runtime_run_service`: 14개 통과, 실패 0.
- 완료 조건 1~3은 도메인 시험 `a_runtime_at_or_above_the_floor_supports_isolation`, `a_runtime_below_the_floor_does_not_support_isolation`, `an_unreadable_version_is_undetermined_and_never_supported`, `the_isolation_floor_does_not_collide_with_the_release_check_names`가 재현하며 모두 통과했다. 시험 본문을 직접 읽어 조건이 지정한 판 값("0.10.0", "0.11.0", "1.0.0" 대 "0.9.5", "0.9.0")과 판이 없는 경우·숫자 판 모양이 아닌 경우를 실제로 다루는지 확인했다.
- 완료 조건 4~7은 `a_runtime_that_supports_isolation_plans_developer_slots_as_before`, `a_runtime_without_isolation_refuses_only_the_requests_holding_a_developer_slot`, `an_undetermined_isolation_verdict_does_not_open_developer_slots`, `a_mixed_request_is_refused_as_a_whole`이 재현하며 모두 통과했다. 막힌 요청이 실행 환경에 아무것도 보내지 않는 사실은 같은 시험의 호출 기록이 비어 있는지 보는 단언(565행)으로 고정돼 있다.
- `cargo test ... agent_runtime_install_service` 18개, `... agent_runtime_status` 14개 통과, 실패 0(완료 조건 10).
- `cargo fmt --check` 종료 코드 0. `cargo check`는 오류와 경고를 합쳐 0건(완료 조건 8의 컴파일 확인).
- `npm run agent-runtime:verify -- --self-test`: 통과. "정상 fixture 1건과 불일치 fixture 3건"(완료 조건 12).
- 완료 조건 1: 새 상수 이름에 `MINIMUM_RUNTIME_VERSION`이 부분 문자열로 들어 있지 않음을 확인했다.
- 완료 조건 9: 세 상수의 정의 줄(18·24·25행)이 기준과 후보의 diff에 나타나지 않아 값이 그대로임을 확인했다. 계약 주 버전 1, 최소 판 "0.9.0", 최대 판 "1.99.99"다.
- 완료 조건 8: 후보의 diff에서 실행 서비스의 삭제된 줄은 import 2줄과 `plan` 호출 4자리뿐이고, `start`·`cancel`·`retry`의 시험은 한 줄도 바뀌지 않았다.
- 완료 조건 11: `docs/agent-runtime.md`의 새 절이 최소 판 0.10.0, 그 아래에서 개발 역할 요청이 거부된다는 사실, 함께 배포되는 실행 환경이 0.9 계열이라 지원 판이 나오기 전까지 개발 실행을 시작할 수 없다는 사실 세 가지를 모두 담고 있음을 확인했다.
- 완료 조건 13: `git diff --name-only 55a7af2 HEAD -- src/`가 0건이다.

## 반영에서 멈춘 지점

격리 검사 결과와 반영 결과는 별개의 사실이다. 위 검사는 모두 통과했고 반영은 시작하지 않았다.
반영 직전에 다시 대조한 공유 기준 `55a7af2`는 후보의 기준과 같다. 스테이지된 변경도 진행 중인 기준
변경도 없다. 그러나 추적 파일 네 개에 미커밋 변경이 있고, 개발자 역할 계약은 이 상태에서 자동 반영을
시작하지 않으며 남의 미커밋 변경을 커밋하거나 되돌리지도 않는다.

네 개는 앞선 세션이 보고한 것과 같다. `.workflow/rules/wf-eligible.sh`와 `.workflow/rules/wf-reserve.sh`는
앱이 설치하는 사본이며 각각 48줄과 149줄이 더해진 판이다. 공백 차이가 아니라 실제 기능 변경이므로
커밋 여부는 사용자가 정할 일이다. 나머지 둘은 이 워크플로의 작업 문서 `-03`과 `-06`이다. 공통 규칙 4절이
실제 작업을 시작하기 전에 이력 항목부터 쓰도록 요구하므로, 반영을 수행할 세션 자신이 반영을 시도하기
전에 반드시 제어 문서를 더럽힌다. 이번 세션도 이력 항목을 쓰는 순간 그 상태를 다시 만들었다.

통합 후보를 갱신해 둔다. 커밋 `b421940`(가지 `wf-iso/TASK-RES-20260815T032125Z-60360-20260815032125-06/lease-94165-20260815130806`),
기준 `55a7af2`, 변경 파일은 위 네 개다. 작업 상태는 `in_progress`로 두었다. 반영 대기는 계약이 따로
이름 붙인 상태라 `blocked`로 두지 않았고, 반영과 반영 후 재검사를 마치지 않아 `verified`로도 두지 않았다.

## 남은 위험

- 반영 전까지 승인된 기획서가 요구한 정지가 공유 기준에 없다. 격리를 지원하지 않는 실행 환경에서 개발 역할 실행이 지금처럼 열린다.
- 관문은 실행 계획 단계에만 있다. 자동 배정은 이 경로를 지나지 않으며 작업 문서가 범위 밖으로 명시했다.
- 공유 기준이 다시 전진하면 후보를 그 커밋 위로 옮겨 검사를 다시 실행해야 한다.
- 이번에 옮긴 빌드 캐시는 이 사본에 있다. 이 사본이 회수되면 다음 세션은 다시 전체 재빌드를 치른다.

## 후속 작업

- 사용자 확인 필요: 미커밋 변경 네 개를 한 번에 커밋해야 반영이 열린다. 에이전트의 대리 정리는 계약이 금지하므로 이 커밋은 사용자만 할 수 있다.
- 아키텍트 인계: 반영 대기 작업을 개발자 판정에서 빼는 관문이 읽는 `integration_waiting` 값을 저장소의 어느 자리도 쓰지 않는다는 사실을 이번 세션이 직접 재확인했다. 읽는 자리는 `.workflow/rules/wf-eligible.sh`(237·241·1141행)와 `src-tauri/src/infrastructure/heartbeat_condition.rs`(263행)이고, 기록의 `step`을 쓰는 유일한 자리인 `.workflow/rules/wf-reserve.sh`의 `write_isolation_record` 호출 다섯 곳(220·227·236·239·241행)은 `preparing`, `waiting:storage`, `failed:worktree`, `ready`, `failed:record`만 쓴다. 이번 예약의 기록도 `step: ready`다. TASK-RES-20260815T032125Z-60360-20260815032125-03이 그 값을 적는 주체를 별도 저장소의 실행 환경으로 범위 밖에 두었으므로, 그 저장소에 아이디어가 올라가기 전까지 같은 작업이 계속 다시 배정된다. 열한 번째 기록이다.
- 아키텍트 인계: 제어 문서가 추적 대상인 한 반영 전제 조건인 깨끗한 공유 작업 공간을 어떤 개발 세션도 구조상 만족시킬 수 없다. 추적에서 뺄지, 앱이 커밋할지, 반영 판정에서 제외할지는 설계 결정이므로 개발 세션이 정할 수 없다. 보고서 8·9·10과 같은 항목이며 아직 열려 있다.
- 아키텍트 인계: 회수된 사본의 빌드 캐시를 다음 세션이 재사용해도 되는지 정해야 한다. 그 경로는 앱 소유인 `.workflow/.runtime/` 아래이고, 규칙은 빌드 산출물과 제어 상태를 구분해 적고 있지 않다.
- 아키텍트 인계: 관리 스크립트 원본을 바꾸는 작업의 완료 조건에 설치 사본 갱신을 넣을지 정해야 한다.
- 아키텍트 인계: `docs/agent-runtime.md` 버전 표가 앱 판 0.1.11을 적어 릴리스 0.1.18과 어긋난다(열한 번째 기록).
- 이번 예약의 결과 접두사는 RES-20260815T130807Z-94165-20260815130806이다.

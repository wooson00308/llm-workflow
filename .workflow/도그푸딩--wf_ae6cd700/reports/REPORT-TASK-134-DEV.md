# TASK-134 개발 보고서 (developer 세션, 전문)

> 기록 경위: 하네스가 서브에이전트(tl-dev-134)의 보고서 파일 작성을 차단해, 발신된 전문을 TL이 대리 기록한다. 내용은 발신 전문 그대로다. (수신 2026-08-06T12:35Z경, TL 세션)

## 착수 판정 근거

- `sh .workflow/rules/wf-eligible.sh developer` → `eligible` (exit 0).
- TASK-133 상태 `qa_waiting` → depends_on 충족. DECISION-6650F2D7 `outcome: approved`, `created_by: user`.
- 겹침 판정: 선점 시점 활성 lease는 TASK-135(codex, `lease-80671-20260806120918`, 만료 12:54:18Z) 하나뿐이고 그 `scope_files`는 전부 `src-tauri/**`라 TASK-134의 `scope_files`와 동일 경로가 없다. `SPEC-009.yml`은 2026-08-03 만료 lease라 판정에서 제외하고 파일은 손대지 않았다.
- 선점: `sh .workflow/rules/wf-claim.sh acquire TASK-134 developer-fable 45` → `lease-84426-20260806121729` (12:17:29Z). 선점 직후 `in_progress` 기록, 중간에 renew 1회, 종료 시 release (exit 0, lease 파일 삭제 확인).

## 결정권자 요약

설정 화면에 현재 프로젝트 카드와 별도인 관리 규칙 카드를 추가했다.
공통 규칙과 기획자, 아키텍트, 개발자 계약의 설치 버전과 앱 제공 버전을 각 행에 그대로 표시한다.
현재 상태, 갱신됨, 재시도 필요, 충돌을 서로 다른 상태 이름과 후속 행동으로 나눠 적었다.
재시도 필요와 충돌, 동기화 명령 실패는 프로젝트 오류와 분리된 관리 규칙 알림으로 올린다.
알림이 있어도 사이드바 이동과 설정 화면, 다른 프로젝트 열기가 그대로 동작한다.
버전을 쓰지 않는 진입 안내에는 숫자를 만들지 않고, 설치 버전을 읽지 못한 경우도 그대로 적는다.
규칙 변경은 다음에 시작하는 에이전트 세션부터 적용된다고 안내하며 실행 중인 세션은 보장하지 않는다.
사용자 정의 규칙 편집이나 저장 기능은 추가하지 않았다.
프런트엔드 검사 728개와 타입 검사, 배포 빌드가 모두 통과했다.
사용자는 TASK-134의 확인 동선을 따라 설정 카드와 충돌 알림을 직접 확인해 주면 된다.

## 변경 파일 (절대경로, 선점 시각 기준 `find -newermt`로 확인)

- /Users/catze/project/workflow-labs/src/App.tsx
- /Users/catze/project/workflow-labs/src/App.css
- /Users/catze/project/workflow-labs/src/features/projects/components/WorkspaceShell.tsx
- /Users/catze/project/workflow-labs/src/features/projects/components/WorkspaceShell.test.tsx
- /Users/catze/project/workflow-labs/src/features/projects/components/SettingsView.tsx
- /Users/catze/project/workflow-labs/src/features/projects/components/SettingsView.test.tsx
- /Users/catze/project/workflow-labs/.workflow/도그푸딩--wf_ae6cd700/tasks/TASK-134.md
- /Users/catze/project/workflow-labs/docs/development-logs/2026-08-06.md

`src-tauri/**`, `decisions/**`, `.workflow/rules/**`는 전혀 건드리지 않았다. 커밋도 하지 않았다.
(주의: `-newermt`는 로컬시간(KST) 기준이라 UTC 선점 시각 12:17:29Z를 로컬 21:17:29로 넣어야 정확하다. UTC 문자열을 그대로 넣으면 9시간 전 기준이 되어 남의 변경까지 잡힌다.)

## 핵심 결정과 근거

1. 새 컴포넌트 파일을 만들지 않고 카드는 `SettingsView.tsx` 안, 알림 판정은 `WorkspaceShell.tsx` 안에 뒀다. 선언된 `scope_files` 밖에 파일을 만들면 겹침 판정의 근거가 무너진다.
2. `managedAssets`를 `WorkspaceShell`의 선택 prop이 아니라 필수 prop으로 넣었다. 배선이 빠지면 타입 검사에서 즉시 드러난다. 대신 기존 검사 12곳의 render 호출에 prop 한 줄씩 추가했다(기존 검사 내용은 그대로).
3. 상태 문구를 네 갈래로 완전히 분리했다. 특히 `retry_required`는 "잠시 뒤 새로 고침", `conflict`는 "파일을 정리한 뒤 새로 고침"으로 후속 행동이 다르다(완료 조건 5).
4. 버전 표기에서 두 가지 없음을 구분했다. 제공 버전이 없는 진입 안내는 `버전을 쓰지 않는 자산`, 버전을 쓰는 자산의 설치 값이 없으면 `설치 확인 못 함`. 진입 안내에 숫자를 만들어 붙이지 않는다.
5. 알림은 `current`·`updated`에서는 만들지 않는다. 할 일이 없는 상태까지 공통 영역을 차지하면 진짜 알림이 묻힌다. 껍데기가 알림을 state로 복사하지 않고 prop만 그리므로, 자동 조회가 프로젝트 요약을 교체해도 알림이 사라지지 않는다(완료 조건 9).
6. TASK-132가 화면 계층까지 내려보낸 롤백 실패·복구 위치를 카드 하단에 표시했다. 표시하지 않으면 백엔드가 남긴 복구 경로를 사용자가 볼 통로가 아예 없다.
7. 결과가 없을 때 화면이 "현재 상태"라고 추정하지 않는다. 호환성 값을 보고 왜 동기화하지 않았는지 적는다.

## 게이트 수치 (4종)

1. `npm run check`: 타입 검사 통과 / 프런트엔드 검사 파일 23개, 검사 728개 전부 통과 / 배포 빌드 성공(`vite build`, 330 modules, 1.11s).
2. 개별 실행 `npx vitest run src/features/projects/components/SettingsView.test.tsx src/features/projects/components/WorkspaceShell.test.tsx --maxWorkers=1`: 2개 파일 40개 통과.
3. cargo 게이트: **무변경으로 생략**. 이번 세션이 수정한 파일에 `src-tauri` 경로가 하나도 없다. 병행 중인 codex 세션이 러스트를 동시에 만지고 있어 지금 cargo를 돌리면 남의 중간 상태를 측정하게 된다. Rust 수치(574개 통과 등)는 TASK-135 세션 보고서를 따른다.
4. 기존 검사 삭제·비활성화 0건: `SettingsView.test.tsx`의 `it` 3 → 16, `WorkspaceShell.test.tsx`의 `it` 19 → 24. `skip`/`todo`/`xit` 0건. 두 파일 diff의 삭제 라인은 import 3줄과 헬퍼 시그니처 2줄뿐이고 삭제된 단언·검사 케이스는 없다. 전체 검사 수 710 → 728 (+18: 설정 화면 13, 껍데기 5).

## 추가한 회귀 검사 (18개)

- 관리 규칙 카드가 현재 프로젝트 카드와 분리돼 있고 프로젝트 경로를 다시 표시하지 않는다.
- 공통 규칙·세 역할 계약·선점 헬퍼가 각 행에 자기 설치·제공 버전을 표시한다.
- 진입 안내 두 개에는 숫자를 만들지 않는다. 설치 버전을 읽지 못하면 그 사실을 적는다.
- 네 결과 상태가 서로 다른 이름과 후속 행동으로 보이고, 재시도와 충돌 문구가 같지 않다.
- 충돌 자산의 이름·설치/제공 버전·백엔드 이유가 카드에서 확인된다.
- 롤백 실패의 복구 경로가 카드에 남는다.
- 호환성 제한 프로젝트에서 결과가 없는 이유를 설명하고 현재 상태로 추정하지 않는다.
- 마지막 동기화 계기(프로젝트 열기 / 수동 새로 고침)를 표시한다.
- 명령 실패가 프로젝트 오류와 다른 알림으로 보이고 프로젝트 카드는 계속 보인다.
- 충돌 알림이 있는 상태에서 설정 이동과 다른 프로젝트 열기 버튼이 동작한다(완료 조건 6).
- 프로젝트 요약만 교체하는 재렌더 뒤에도 알림이 남는다(완료 조건 9).
- `current`·`updated`에서는 알림을 만들지 않는다.
- 다음 세션 안내 문구가 있고 실행 중 세션 보장 문구는 없다(완료 조건 7·8).
- 카드에 버튼과 입력 요소가 없어 커스텀 규칙 편집이 추가되지 않았다(완료 조건 11).

## 리스크와 후속

- 병행 세션(codex)이 `src-tauri`를 수정 중이라 작업 트리에 이 작업과 무관한 변경이 함께 있다. 이번 수치는 프런트엔드 게이트만의 결과다.
- 세션 도중 앱 제공 버전이 공통 규칙 14, 기획자 9, 아키텍트 8, 개발자 9로 올라갔다(codex의 TASK-135 변경). TASK-134 본문이 예시로 적은 8·7·8은 작업 문서 작성 시점의 값이다. 화면은 백엔드가 준 값을 그대로 표시하므로 숫자가 바뀌어도 동작은 같고, 확인 동선에는 현재 값을 적었다. 참고로 세션 중 `.workflow/rules/*.md`의 설치 버전도 13/8/7/8에서 14/9/8/9로 바뀌었는데 이는 실행 중인 앱이 동기화한 결과이고 내 변경이 아니다.
- 번들 크기 경고는 이전부터 있던 빌드 경고이며 이번 변경과 무관하다.
- SPEC-048에서 파생되는 사용자 정의 규칙 화면 작업(TASK-137 계열)은 이 카드와 같은 화면·같은 스타일 파일을 수정한다. TASK-134가 QA를 통과한 뒤 시작하는 것이 안전하다.

## 사용자 QA 제안 (확인 동선, 작업 문서에도 동일하게 기록함)

1. 개발 앱에서 저장소를 열고 사이드바 `설정`을 누른다. `현재 프로젝트` 카드와 별도로 `관리 규칙` 카드가 보이고 상태 줄에 `현재 상태` 또는 `갱신됨`이 표시된다.
2. 자산 목록에서 역할 계약 세 줄이 각자의 숫자를 보여주는지 확인한다. 현재 앱 제공 값은 공통 규칙 14, 기획자 9, 아키텍트 8, 개발자 9다. `AGENTS 진입 안내`·`CLAUDE 진입 안내`는 숫자 대신 `버전을 쓰지 않는 자산`으로 나와야 한다.
3. 상단 새로 고침을 누른다. `마지막 동기화` 값이 `수동 새로 고침`으로 바뀐다.
4. 충돌 재현: `.workflow/rules/roles/architect.md`의 `rules_version: 8`을 `99`로 바꾸고 저장한 뒤 새로 고침을 누른다. `관리 규칙 충돌: 아키텍트 역할 계약` 알림이 프로젝트 오류 배너와 다른 모양으로 뜨고, 알림이 있어도 사이드바 이동과 `다른 프로젝트 열기`가 동작하며, 카드의 아키텍트 행이 `충돌`과 이유를 보여준다. 10초 이상 그대로 둬도(자동 조회 4회 이상) 알림이 사라지지 않아야 한다. 확인 후 값을 `8`로 되돌린다.
5. 카드 아래 안내가 "다음에 시작하는 에이전트 세션부터 적용된다"고 말하고, 실행 중인 세션에 적용됐다는 문장은 없는지 확인한다.

## 문서 상태

- `tasks/TASK-134.md`: `todo` → `in_progress`(12:17:29Z) → `qa_waiting`(12:29:45Z). history 두 줄 추가, `updated_at` 갱신, 결정권자 요약을 완료 사실로 갱신(10줄), `## 확인 동선` 신설. 본문의 목적·화면 계약·완료 조건·검증 절차는 아키텍트 소유라 손대지 않았다.
- `docs/development-logs/2026-08-06.md`: `## 개발자 세션: TASK-134 관리 규칙 상태 화면` 섹션 추가(한국어, 공통 규칙 §9 문체).
- `decisions/`는 읽기만 했고 아무것도 만들거나 고치지 않았다.

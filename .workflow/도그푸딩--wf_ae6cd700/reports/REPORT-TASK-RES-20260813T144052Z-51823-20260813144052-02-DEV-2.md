# TASK-RES-20260813T144052Z-51823-20260813144052-02 개발 보고서 2

## 결정권자 요약

앞 세션이 막혔던 이유가 아키텍트 교정으로 풀려, 남아 있던 구현을 마쳤다.
실행 시작 경로 세 곳의 동의 확인은 앞 세션이 이미 구현했고, 이번에 남은 일은 출시 관문 검사 세 개가
유효한 동의를 준비하지 않아 실패하던 것을 바로잡는 것이었다.
그 검사 파일의 준비 코드에 동의를 남기는 줄만 더했고, 각 검사가 확인하는 사실과 단언은 그대로 두었다.
저장소 전체 검사가 실패 0건으로 통과했다. 직전 기록인 3건 실패 360건 통과가 363건 통과로 바뀌었고,
늘어난 3건이 막혀 있던 바로 그 검사들이다.
이로써 이 작업의 완료 조건 11개가 모두 확인됐다.
사용자가 지금 확인할 것은 작업 문서의 확인 동선에 적은 네 명령의 숫자다. 눈으로 볼 화면은 없으며,
확인 도장은 그 숫자를 신뢰한다는 뜻이다.

## 변경 파일과 모듈

- `../../Git/claude-heartbeat/tests/test_agent_end_to_end.py`: 실행을 시작하는 검사 세 개
  `test_limits_choose_two_targets_and_fake_clis_leave_no_prompt_in_state`,
  `test_migration_lock_and_no_target_start_no_provider`,
  `test_competing_dispatchers_start_one_provider_for_one_target`의 준비 코드에
  `store.save_consent`를 요구 고지 버전으로 부르는 줄을 각각 더했다. 요구 버전 상수를 계약 모듈에서
  가져오는 import 한 줄을 함께 더했다. 단언, 검사 이름, 검사가 확인하는 사실은 바꾸지 않았다.

이 세션이 고친 제품 코드와 문서는 없다. 변경 범위 1번부터 6번까지와 완료 조건 1번부터 10번까지는 앞
세션이 이미 구현했고, 이번 세션의 몫은 변경 범위 7번과 완료 조건 11번뿐이었다.

## 검증 절차와 결과

- 검증 단계 1 `python -m pytest tests/test_agent_dispatch.py`: 39 passed. 완료 조건 1·2·5·6·7·8·9.
- 검증 단계 2 `python -m pytest tests/test_agent_cli.py`: 30 passed. 완료 조건 3·4·6.
- 검증 단계 4 `python -m pytest tests/test_agent_end_to_end.py`: 4 passed, 1 skipped. 건너뛴 하나는
  환경 변수가 없으면 실행하지 않는 실제 서비스 smoke다. 앞 세션이 판정을 임시로 무력화하고 측정한
  4 passed, 1 skipped와 같은 결과이므로, 세 검사는 동의를 갖춘 상태에서 이전과 같은 사실을 확인한다.
- 검증 단계 5 `python -m pytest`: 363 passed, 8 skipped, 실패 0건. 완료 조건 11을 충족한다.
- 완료 조건 10은 계약 문서의 "실행 경로의 동의 확인" 절을 직접 읽어 확인했다. 경로별 확인 지점, 사유
  이름, 동의 부족이 실패가 아니라 대기라는 사실이 모두 적혀 있다.
- 검사를 약하게 만들지 않았다는 사실은 `git diff tests/test_agent_end_to_end.py`로 확인했다. 추가된
  줄은 동의 기록 세 줄과 import 한 줄뿐이고, 삭제되거나 수정된 단언은 없다.
- `python -m ruff check tests/test_agent_end_to_end.py`: All checks passed.

## 남은 위험

- 유효한 동의가 없는 프로젝트에서 자동 배정이 멈출 때 실행 환경은 아무 기록도 남기지 않는다. 사용자가
  앱을 열기 전에는 배정이 멈춘 이유를 알 방법이 없다. 승인된 범위 밖이라 이번에도 손대지 않았다.
- 실행 시작 전 확인 함수가 저장소를 인자로 받는다. 이 함수를 부르는 경로가 나중에 생기면 저장소를 함께
  넘겨야 하고, 넘기지 않으면 호출 자체가 실패한다.
- 실행을 시작하는 검사 파일은 이제 준비 코드에서 동의를 남겨야 한다. 이 규칙을 모르는 채 새 검사를
  추가하면 그 검사만 조용히 대기 상태를 관찰하게 된다. 지금은 이 파일과 다른 두 검사 파일이 모두
  갖췄으므로 실패로 드러나지만, 새 파일이 생길 때는 같은 누락이 다시 날 수 있다.

## 후속 작업

- 역할 밖 관찰: 동의를 읽고 남기고 철회하는 명령 세 개는 표준 입력 JSON 계약에는 구현돼 있고 계약이
  알리는 구현 명령 목록에도 실려 있으나, 셸 하위 명령으로 연결돼 있지 않다.
  `src/heartbeat/cli.py`의 명령 대응표에 동의 항목이 없어 `heartbeat agent consent read`는 인자 오류로
  거절된다. 직접 실행해 확인했다. 그 파일은 이 작업과 앞 작업
  TASK-RES-20260813T144052Z-51823-20260813144052-01 어느 쪽의 `scope_files`에도 없어 두 작업 모두 고칠
  수 없다. 앱이 런타임을 어떤 경로로 부르는지에 따라 실제 문제인지가 갈리므로, 아키텍트가 확인해
  필요하면 별도 작업으로 나누는 것이 좋겠다. 이 작업의 완료 조건은 이 값과 무관하므로 막지 않았다.
- 역할 밖 관찰: 작업 문서의 `## 막힌 사유` 절은 저장소 전체 검사를 "검증 단계 4"라고 적고 있으나,
  아키텍트 교정으로 단계가 하나 늘어 지금은 5번이다. 규칙상 기록된 그대로 두어야 해서 고치지 않았다.
  아키텍트 보고서도 같은 사실을 이미 적어 두었다.
- 런타임 예약 인계로 받은 결과 접두사 RES-20260813T154830Z-49423-20260813154830은 쓰지 않았다. 이
  세션은 새 기획서나 새 작업 문서를 만들지 않았고, 그 접두사로 시작하는 문서 경로가 없다는 것도
  확인했다.
- 앞 세션의 잔여물 판정: 작업 문서의 상태와 이력, `## 막힌 사유` 절, 앞 세션의 보고서, 그리고 작업
  저장소에 남아 있던 제품 코드와 검사 코드 변경을 모두 그대로 이어받았다. 앞 세션이 기록한 실패
  세 건이 이번에 그대로 재현되고 준비 코드 수정만으로 사라지는 것을 확인했으므로, 그 변경이 보고된
  내용과 일치한다고 판단했다. 버리거나 다시 쓴 것은 없다.

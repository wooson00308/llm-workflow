# TASK-RES-20260813T144052Z-51823-20260813144052-02 개발 보고서

## 결정권자 요약

실행 환경의 새 실행 경로 세 곳이 시작 직전에 같은 동의 판정 하나를 지나도록 구현을 마쳤다.
동의가 없거나 철회됐거나 고지 버전이 낮으면 대상 예약도 실행 도구 시작도 하지 않고 대기로 남으며,
그 사유는 일반 실패와 구분되는 이름 하나로 돌아온다. 이미 실행 중이던 세션과 회복 절차는 손대지 않았다.
완료 조건 10개 중 9개를 검사로 확인했고, 두 검사 파일은 각각 통과한다.
남은 하나가 저장소 전체 검사이며, 여기서 막혔다.
이 작업이 고쳐도 된다고 적힌 파일 목록에 검사 파일 하나가 빠져 있어, 그 파일의 검사 세 개가 계속 실패한다.
목록을 어기고 고치면 두 세션이 같은 파일을 동시에 건드릴 수 있어 고치지 않고 멈췄다.
지금 사용자가 확인할 것은 없다. 파일 목록을 바로잡는 것은 아키텍트의 몫이고, QA는 그 뒤에 요청한다.

## 변경 파일과 모듈

- `../../Git/claude-heartbeat/src/heartbeat/agent_dispatch.py`: 동의 판정 함수 `consent_is_valid`와
  프로젝트 단위 확인 `project_may_execute`, 사유 이름 `EXECUTION_CONSENT_REQUIRED`를 더했다.
  `_preflight_target`에 저장소 인자를 받아 함수 맨 앞에서 판정하고, 세 호출 지점
  `start_one_run`·`queue_one_run`·`_start_run_row`가 저장소를 넘기게 했다. `tick_project`에서는 회복
  절차 뒤, 배정 루프 앞의 조기 반환 자리에 같은 판정을 두었다.
- `../../Git/claude-heartbeat/src/heartbeat/agent_cli.py`: 동의 응답의 유효 판정이 위 함수를 부르게 바꿔
  규칙이 한 곳에만 있게 했다. 요구 고지 버전은 계약 모듈에서 그때그때 읽도록 참조 방식을 바꿨다.
  `run.retry`는 동의 부족일 때 사유와 단계를 응답 데이터 최상위에 실어 봉투의 사유 코드가
  `execution_consent_required`가 되게 했다.
- `../../Git/claude-heartbeat/docs/agent-runtime-contract.md`: "실행 경로의 동의 확인" 절을 더해 경로별
  확인 지점, 사유 이름, 동의 부족이 실패가 아니라 대기라는 사실, 실행 중 세션과 계획 조회가 대상이
  아니라는 사실을 적었다.
- `../../Git/claude-heartbeat/tests/test_agent_dispatch.py`: 공용 준비 함수 `configure`가 기본으로 동의를
  남기게 하고, 그 함수를 쓰지 않는 기존 검사 두 곳에 동의 기록을 더했다. 완료 조건 1·2·5·6·7·9에
  대응하는 검사 8개를 더했다.
- `../../Git/claude-heartbeat/tests/test_agent_cli.py`: 완료 조건 3·4·6에 대응하는 검사 3개를 더했다.
  요구 고지 버전을 올리는 기존 검사의 대체 지점을 규칙이 있는 계약 모듈로 옮겼다.

## 검증 절차와 결과

- `python -m pytest tests/test_agent_dispatch.py` 통과: 39 passed. 완료 조건 1·2·5·6·7·8·9를 확인한다.
- `python -m pytest tests/test_agent_cli.py` 통과: 30 passed. 완료 조건 3·4·6을 확인한다.
- `python -m pytest` 실패: 3 failed, 360 passed, 8 skipped. 실패는 모두
  `tests/test_agent_end_to_end.py`이며 `test_limits_choose_two_targets_and_fake_clis_leave_no_prompt_in_state`,
  `test_migration_lock_and_no_target_start_no_provider`,
  `test_competing_dispatchers_start_one_provider_for_one_target` 셋이다.
- 실패 원인을 저장소를 고치지 않고 분리했다. 임시 pytest 플러그인으로 새 판정 함수만 무력화하고 같은
  파일을 돌리면 4 passed, 1 skipped로 모두 통과한다. 세 실패는 그 파일이 유효한 동의를 준비하지 않는
  탓이며, 이번 변경의 다른 부분에서 온 것이 아니다. 플러그인은 저장소 밖에 두었고 확인 뒤 지웠다.
- 완료 조건 10은 계약 문서 절을 직접 읽어 확인했다.

## 남은 위험

- 유효한 동의가 없는 프로젝트에서는 자동 배정이 아무 기록도 남기지 않고 조용히 멈춘다. 사용자가 앱을
  보지 않으면 왜 배정이 멈췄는지 알 방법이 지금은 화면뿐이다. 이 작업의 범위 밖이라 손대지 않았다.
- `_preflight_target`은 이제 저장소를 받는다. 이 함수를 부르는 다른 경로가 나중에 생기면 저장소를 함께
  넘겨야 하며, 넘기지 않으면 호출 자체가 실패한다.
- 저장소 전체 검사가 통과한 상태를 확인하지 못했다. 위 세 검사를 고치기 전까지 회귀 여부는 열려 있다.

## 후속 작업

- 아키텍트가 `scope_files`에 `../../Git/claude-heartbeat/tests/test_agent_end_to_end.py`를 더해야 한다.
  그 파일은 자체 `configuration` 헬퍼로 설정을 만들고 다른 검사 파일의 준비 함수를 부르지 않으며, 세
  검사 중 둘은 저장소에 설정을 저장하지도 않고 `start_one_run`에 설정 객체를 직접 넘긴다. 그래서
  범위 안의 어떤 파일을 고쳐도 그 세 검사에 닿지 않는다.
- 목록이 고쳐진 뒤 남은 일은 그 파일의 준비 코드에 동의 기록 한 줄을 더하고 저장소 전체 검사를 다시
  돌리는 것뿐이다. 이번 세션에서 검사를 지우거나 약하게 만든 것은 없다.
- 역할 밖 관찰: 이 작업의 완료 조건은 실행 경로를 바꾸므로, 실행을 시작하는 모든 검사 파일이 준비
  코드에서 동의를 남겨야 한다. 앞으로 실행 경로를 바꾸는 작업을 나눌 때 `tests/test_agent_end_to_end.py`와
  `tests/test_agent_recovery.py`를 함께 살피면 같은 누락이 반복되지 않는다. 이번에는
  `tests/test_agent_recovery.py`가 `tests/test_agent_dispatch.py`의 준비 함수를 부르고 있어 범위 안에서
  해결됐다.
- 런타임 예약 인계로 받은 결과 접두사 RES-20260813T150635Z-93550-20260813150635은 이 작업에서 새 문서
  식별자를 만들 일이 없어 쓰지 않았다.

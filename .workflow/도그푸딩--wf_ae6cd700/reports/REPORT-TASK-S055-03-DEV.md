# TASK-S055-03 개발 보고서

## 결정권자 요약

이 작업은 구현하지 못했고 작업 정의 오류로 막았다. 제품 코드는 한 줄도 바꾸지 않았다.
완료 조건 8과 9는 앱 판정도 고른 대상의 종류를 알리고 그 값이 판정 도구와 같은지 대조하기를 요구한다.
그 종류를 담을 자리가 앱 판정 결과 구조인데, 그 구조가 사는 파일이 이 작업의 선언 범위에 없다.
기존 두 값으로는 종류를 표현할 수 없고, 제외 사유 자리에 실으면 같은 조건이 요구하는 대조가 깨진다.
선언 범위를 넓히는 판단은 아키텍트 몫이라 제품 파일을 손대지 않고 막았다.
나머지 완료 조건과 검증 절차는 지금 선언만으로 충족할 수 있으므로 추가 선행은 필요하지 않다.
사용자는 이 작업에 수정 요청을 남겨 선언 범위를 넓히도록 요청하면 된다.

## 변경 파일과 모듈

제품 파일 변경은 없다. 작업 문서 한 건만 상태와 이력과 막힌 사유 절을 갱신했다.

- `.workflow/도그푸딩--wf_ae6cd700/tasks/TASK-S055-03.md`: 상태를 `blocked`로, 차단 분류를
  `definition_error`로 기록하고 `blocked` 이력 항목과 `## 막힌 사유` 절을 같은 편집에서 남겼다.

## 검증 절차와 결과

- `cargo test --manifest-path src-tauri/Cargo.toml` 통과: 629 passed, 0 failed. 착수 전 값과 같다.
- `git status`로 `src-tauri`와 `src`와 `docs` 아래에 이번 세션의 변경이 없음을 확인했다.
- 구현 전 대조에서 확인한 사실은 아래 셋이다.
  - 누락된 경로: `src-tauri/src/domain/project.rs`. 대안 경로를 택하면
    `src-tauri/src/infrastructure/fs_project_repository.rs`가 대신 필요하다.
  - 그 파일을 필요로 하는 직접 참조: 범위 안의 `role_eligibility.rs`가 판정 결과 타입
    `RoleWorkVerdict`와 `WorkCandidate`를 `crate::domain::project`에서 가져다 쓴다. 두 타입의 필드는
    대상 id와 후보별 판정 코드 둘뿐이라 대상의 종류를 담을 자리가 없다. `pending_role_work`의 반환
    형태를 바꾸는 대안은 유일한 호출자인 `fs_project_repository.rs`의 `summary_from_manifest`가 함께
    움직여야 한다.
  - 그대로 두면 실패할 검증: 완료 조건 8(판정 결과가 식별자와 종류를 함께 알린다)과 완료 조건 9(앱
    판정과 판정 도구가 같은 종류를 낸다), 그리고 검증 절차 4(같은 픽스처를 양쪽에 넣어 대조). 두 판정을
    대조하는 검사는 `role_eligibility.rs`의 `assert_matches_condition_script`이며 지금은 대상과 후보
    목록만 비교한다. 앱 쪽에 종류가 없으면 이 검사에 종류 비교를 더할 수 없다.
- 후보로 검토했다가 버린 우회를 함께 남긴다. 후보별 판정 코드 문자열에 종류를 섞는 방법은 그 값이 판정
  도구의 사유 코드와 어휘를 맞추는 자리라 완료 조건 9의 제외 사유 대조를 깨뜨린다. 대상 식별자의 접두사로
  종류를 유추하는 방법은 앱이 다른 근거로 추측하지 않는다는 이 승인의 원칙과 어긋난다.

## 남은 리스크

- 이 판단이 틀렸다면 비용은 한 세션이다. 완료 조건 8의 "판정 결과"를 판정 도구의 기계 출력만으로 읽으면
  선언 범위 안에서 구현할 수 있다. 다만 완료 조건 9가 앱과 도구가 같은 종류를 낸다고 적고 있어 그 읽기는
  9와 어긋난다고 보았다.
- 조건 스크립트 본문과 자산 버전은 손대지 않았다. 절반만 구현해 도구만 종류를 내면 두 판정이 갈라진
  상태로 남으므로 아무것도 바꾸지 않는 편을 택했다.

## 후속 작업

- 아키텍트가 이 작업의 선언 범위에 `src-tauri/src/domain/project.rs`를 더하고, 대상 종류를 어느 값으로
  담을지 작업 문서에 적으면 곧바로 구현할 수 있다. 그 뒤 필요한 변경은 판정 결과에 종류 값 하나, 앱
  아키텍트 분기의 후보 두 종류 구분, 조건 스크립트 본문과 자산 버전, 그리고 두 판정 대조 검사의 종류
  비교다.
- 앞 작업(TASK-S055-02)이 남긴 인계 둘은 이 작업에서 처리하지 않았고 그대로 유효하다. 요청 기록의 스키마
  이름을 규칙 본문에 실을지는 규칙 파일을 범위로 가진 작업의 몫이고, 한 작업이 두 번 고쳐질 때 연결 필드가
  마지막 요청만 가리키는 한계도 그대로다. 두 번째 사항은 이 작업의 후보 판정과 맞닿아 있으므로, 범위를
  넓혀 재개할 때 함께 판단하면 좋다.

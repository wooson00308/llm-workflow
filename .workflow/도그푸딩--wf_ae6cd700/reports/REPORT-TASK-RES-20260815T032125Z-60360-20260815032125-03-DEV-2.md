# TASK-RES-20260815T032125Z-60360-20260815032125-03 개발 보고서 2

## 결정권자 요약

정의가 고쳐져 다시 열린 이 작업을 구현하고 격리 사본에서 검사까지 마쳤다. 격리 검사를 끝냈는데도
사용자의 미커밋 변경 때문에 반영하지 못한 작업은, 그 상황이 그대로인 동안 다시 시작되지 않는다.
사용자가 자기 변경을 정리하거나 공유 기준이 앞으로 나가면 그 작업은 곧바로 다시 진행 대상이 되고,
범위가 겹치지 않는 다른 작업은 그동안 정상적으로 진행된다. 세 곳에 나뉘어 있던 같은 판정 규칙을
함께 고쳐, 세 구현이 같은 대상과 같은 사유를 내는 것을 자동 대조로 확인했다. 격리 사본에서 검사
840건이 모두 통과했다. 다만 공유 작업 공간에 다른 세션의 미커밋 변경이 남아 있어 반영은 시작하지
않았고, 반영과 반영 뒤 재검사가 남아 있으므로 이 작업은 아직 완료로 기록하지 않았다. 그래서 이
근거는 GROUP-RES-20260815T032125Z-60360-20260815032125의 자동 검증 결과에 아직 들어가지 않는다.
판정이 실제로 동작하는 것은 별도로 배포되는 실행 환경이 기록에 반영 대기 단계를 적기 시작한 뒤다.

## 변경한 파일과 모듈

선언된 네 파일만 고쳤고 범위를 넓히지 않았다.

- `src-tauri/src/domain/project.rs`: 제외 사유 공개 상수 `INTEGRATION_WAITING`을 `OVERLAP` 옆에 더했다.
- `src-tauri/src/infrastructure/fs_project_repository.rs`: 준비 기록을 읽어 통합 대기 집합을 만드는
  `integration_waiting_tasks`와 보조 함수 둘(`read_integration_waiting_base`,
  `shared_workspace_is_dirty`)을 더하고, `summary_from_manifest`가 `lease_ids`와 같은 자리에서 집합을
  한 번 만들어 `pending_role_work`에 넘기도록 배선했다. 현재 커밋 조회는 TASK-...-05가 만든
  `QaBasePin::current`를 재사용해, 이 파일에 Git을 읽는 경로가 두 벌로 갈라지지 않게 했다.
- `src-tauri/src/infrastructure/role_eligibility.rs`: `pending_role_work`가 집합을 인자로 받고
  `developer_verdict`가 겹침 판정 뒤·원천 결정 판정 앞에서 제외한다. 이 모듈에는 파일 시스템 접근도
  Git 실행도 더하지 않았다.
- `src-tauri/src/infrastructure/heartbeat_condition.rs`: 두 판정 스크립트 본문에 같은 규칙을 넣었다.
  POSIX 본문은 `scan_shared_base`와 `integration_waiting_blocks`, PowerShell 본문은 `Invoke-GitRead`,
  `Update-SharedBase`, `Test-IntegrationWaiting`이다. 판 번호를 21로 올렸다.

집합을 `WorkflowInput`의 새 필드가 아니라 `pending_role_work`의 새 인자로 준 것은, 이 값이 워크플로우
하나가 아니라 프로젝트 하나의 값이기 때문이다. 이미 같은 성질인 미만료 lease 집합과 전달 방식을
맞췄다. 기록의 키는 `target_id` 필드가 아니라 파일 이름으로 잡았다. 스크립트 본문이 작업 id로 기록
파일을 직접 조회하므로, 두 값이 어긋난 기록에서도 세 구현이 같게 판정한다.

이전 세션이 남긴 격리 사본(`lease-28280-20260815050358`)에는 변경 커밋도 브랜치 갱신도 없어 인수할
제품 코드가 없었다. 그 사본을 삭제하거나 되돌린 것은 없다.

## 검증 단계와 결과

모두 격리 사본에서 실행했다. `cargo`가 셸 실행 경로에 없어 `~/.cargo/bin`을 경로에 넣고 실행했다.

1. `cargo test --manifest-path src-tauri/Cargo.toml heartbeat_condition` 55건 통과, 실패 0건. 완료 조건
   1~5와 7을 재현하는 검사 다섯 개를 더했다. 기존 `developer_exit_code`는 그대로 두고, 준비 기록과 Git
   저장소를 함께 준비하는 `developer_run_with_isolation`을 옆에 두어 확장했다. 저장소 픽스처는
   `reservation_helper.rs` 시험의 `git_project` 방식을 따랐다.
2. `cargo test --manifest-path src-tauri/Cargo.toml role_eligibility` 93건 통과, 실패 0건. 완료 조건 11은
   기존 `assert_matches_condition_script`를 새 픽스처 네 개에 적용해 확인했다. 이 보조 함수가
   `FileSystemProjectRepository::inspect`를 지나 앱 배선까지 고정하고, `planner`와 `architect`를 함께
   대조하므로 완료 조건 10의 근거이기도 하다.
3. 완료 조건 9는 `role_eligibility.rs`의 시험 모듈 앞 구간에 `std::fs`, `std::path`,
   `std::process::Command`가 하나도 없음을 확인해 검사했다. 모듈 주석 4행의 규정은 그대로 참이다.
4. 완료 조건 6은 Git 저장소가 아닌 임시 프로젝트를 쓰는 기존 시험들이 하나도 바뀌지 않고 통과한 것이
   근거다. `heartbeat_condition.rs`와 `role_eligibility.rs`의 기존 `project` 보조 함수가 만드는 프로젝트가
   그 상태이며, 두 모듈의 기존 검사 전부가 통과했다.
5. `cargo test --manifest-path src-tauri/Cargo.toml fs_project_repository` 195건 통과, 실패 0건.
6. `cargo fmt --manifest-path src-tauri/Cargo.toml --check` 통과, `cargo check` 통과.
7. 라이브러리 전체 820건과 통합 20건 모두 통과, 실패 0건.
8. PowerShell 본문은 이 기기에서 실행할 수 없으므로 두 본문을 나란히 놓고 정적으로 대조했다. 기록
   존재·단계 일치·기준 커밋 존재·조회 실패·작업 공간 청결·기준 커밋 일치의 여섯 분기가 같은 차례로 같은
   답을 내고, 사유 값은 양쪽 모두 `integration-waiting`이며 위치도 겹침 뒤·원천 결정 앞으로 같다. 더한
   코드는 전부 ASCII라 BOM 뒤 ASCII 관례 검사가 통과한다.

완료 조건 12의 판 번호 리터럴 검사는 태스크가 적은 두 자리 외에 한 곳이 더 있었다. 상한 초과 오류
문구를 대조하는 검사가 앱이 아는 판 번호를 문자열에 박아 두고 있어 함께 21로 맞췄다.

## 남은 위험

- 반영과 반영 뒤 재검사가 남았다. 격리 사본에서 통과한 것이 공유 기준에서도 통과한다는 근거는 아직
  없다. 다만 후보 커밋의 기준(`febbfee`)과 지금 공유 기준(`adb194d`) 사이의 차이는 개발 로그 문서
  한 파일뿐이라, 리베이스가 이 변경의 네 파일과 겹치지 않는다.
- 판정은 기록에 반영 대기 단계가 적히기 전까지 어떤 작업도 새로 제외하지 않는다. 그 값을 적는 주체는
  별도로 배포되는 실행 환경이므로, 이 저장소만으로는 실동작을 확인할 수 없다.
- Git 조회 실패를 대기 유지로 다루므로, Git 저장소가 아닌 프로젝트에 반영 대기 기록이 잘못 남으면 그
  작업은 계속 후보에서 빠진다. 기록을 지우거나 단계를 바꾸면 풀린다.
- 저장 공간. 검사 도중 디스크가 가득 차 링크가 실패했다(errno 28). 이번 세션 사본의 증분 빌드 캐시를 지워 진행했고 다른 사본은 건드리지 않았다.

## 후속 작업

- 통합 인계 하나. 공유 작업 공간에 이 세션의 것이 아닌 미커밋 변경이 남아 있어 반영을 시작하지 않았다.
  변경된 추적 파일은 `.workflow/rules/wf-reserve.sh`와 TASK-...-06의 작업 문서다. 그 변경이 정리되면
  개발자 세션이 아래 통합 후보 기록에서 이어받아, 현재 기준 위로 올린 뒤 격리 검사를 다시 돌리고 반영과
  재검사를 수행하면 된다.
- 통합 후보 기록: 변경 커밋 `ff246e1`, 시작 기준 `febbfee`, 브랜치
  `wf-iso/TASK-RES-20260815T032125Z-60360-20260815032125-03/lease-28307-20260815072625`. 변경 파일은 위
  네 개이고, 실행한 검사와 결과는 위 검증 절과 같다.
- 아키텍트 인계 하나. 완료 조건 12가 판 번호 리터럴 검사를 두 자리로 적었으나 실제로는 세 자리였다.
  다음에 판을 올릴 때를 위해 완료 조건에 반영해 두면 좋다. 범위 사전 검사의 줄 번호는 TASK-...-05가
  반영되면서 어긋났으나, 가리키는 대상은 모두 찾을 수 있어 판단에 지장은 없었다.
- 운영 인계 하나. 완료된 작업의 격리 사본이 남아 저장 공간을 크게 차지한다. 회수는 앱이 소유하는
  실행 상태를 다루는 일이라 이 세션이 하지 않았다.
- 원천 기획서와 결정 문서는 자격 판정에 필요한 범위에서만 열었다. 구현 판단은 태스크 문서만으로
  가능했고, 부족해서 밖을 찾아본 곳은 없다.
- 런타임 예약 인계로 받은 결과 접두사는 RES-20260815T072626Z-28307-20260815072625이며, 이 세션은 새
  문서 식별자를 만들지 않아 사용하지 않았다.

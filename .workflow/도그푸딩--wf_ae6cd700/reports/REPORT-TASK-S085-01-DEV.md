# TASK-S085-01 개발 보고서

## 결정권자 요약

구현하지 않고 막았다. 이유는 셋이다. 첫째, 이 작업이 고치라고 지정한 파일은 이 프로젝트가 아니라
런타임 저장소에 있는데 런타임이 준비해 준 격리 사본은 이 프로젝트의 사본이라 그 파일이 하나도 없다.
다음 개발 세션도 같은 사본을 받으므로 시간이 지난다고 풀리지 않는다. 둘째, 작업 문서가 적은 코드
위치는 그 저장소의 커밋이 아니라 사용자가 아직 커밋하지 않은 작업 트리에서만 맞고, 지정된 파일 셋이
모두 그 미커밋 변경을 안고 있어 통합도 충돌로 막힌다. 셋째, 이 작업이 요구하는 동작을 사용자가 이미
그 저장소에 직접 구현해 병합을 기다리는 중이다. 판 상태 문서에 사용자 지시로 SPEC-085 대신 직접
구현했고 기획서 폐기는 복귀 후 사용자가 처리한다고 적혀 있다. 제품 코드는 한 줄도 바꾸지 않았고
시험을 지우거나 약화한 편집도 없다. 다음 처리는 아키텍트가 GROUP-085의 처분을 정하는 것이고, 이
작업이 막혀 있는 동안 그 그룹의 자동 확인은 성립하지 않는다.

## 보고 없이 끝난 실행 기록 확인

`.workflow/.runtime/silent-runs/`에 이 대상의 기록이 하나 있었다(`6458b29d…`). 개발자 역할,
2026-08-19T18:23:22Z 시작, 18:23:24Z 종료, 사유 `supervisor_identity_unverified`. 2초 만에 결과 없이
끊긴 실행이며, 사유값 자체가 SPEC-085이 다루는 허위 즉살이다.

인수한 잔여물은 없다. 작업 문서는 `todo` 상태에 `created` 항목 하나뿐이었고 보고서도 없었다. 격리
사본은 기준 커밋 `948043a`에서 변경 0건이었다. 버리거나 다시 쓴 것이 없고 버린 시험도 없다.

## 변경한 파일과 모듈

제품 코드는 없다. 선언한 파일 셋 가운데 어느 것도 열어 고치지 않았다.

이 프로젝트에서 고친 문서는 둘이다.

- `tasks/TASK-S085-01.md`: `todo` → `in_progress` → `blocked` 전이와 각각의 `history` 항목,
  `blocked_kind: definition_error`, `## 막힌 사유` 절. 본문의 범위와 완료 조건은 아키텍트의 것이므로
  손대지 않았다.
- `reports/REPORT-TASK-S085-01-DEV.md`: 이 보고서.

## 검증 절차와 결과

작업의 검증 절차(런타임 저장소 pytest)는 실행하지 않았다. 구현이 없어 확인할 대상이 없고, 그
저장소에는 사용자의 미커밋 변경이 올라가 있어 지금 상태의 결과가 이 작업의 결과가 되지 못한다.

막힌 사유를 이루는 사실은 다음과 같이 확인했다.

- 소유 확인: `wf-claim.sh renew TASK-S085-01 lease-53900-20260819182230 45` 종료 코드 0.
  `acquire`는 부르지 않았다.
- 자격: 그룹 GROUP-085은 `active`, 개정 1이고 작업의 `work_group_revision`도 1이다. SPEC-085을
  대상으로 한 결정은 DECISION-2D078F50 하나뿐이고 `outcome: approved`, `created_by: user`다.
  `depends_on`이 없고, 다른 두 유효 lease(TASK-S080-03, TASK-S081-02)의 선언 경로와 겹치지 않는다.
- 격리 사본: `.../worktrees/TASK-S085-01/lease-53900-20260819182230`에 `src/heartbeat`가 없다.
  브랜치는 `wf-iso/TASK-S085-01/...`이고 기준 커밋은 workflow-labs의 `948043a`다.
  `wf-reserve.sh`는 등록된 프로젝트 루트에서만 `git worktree`를 만든다.
- 기준 불일치: 런타임 저장소 HEAD(`02b0a0c`)의 `_device_remaining`은 309행, 작업 트리에서는 335행이다.
  작업 문서가 적은 335·340·345·322·383행은 작업 트리 쪽과 맞는다. 선언한 파일 셋 셋 모두가
  `git status`에 수정으로 잡힌다(합계 14개 파일 800줄).
- 중복 구현: 같은 저장소의 `fix/slot-accounting-live-processes` 브랜치에 `203012b`(점유 집계)와
  `f838143`(재입양)이 있고 원격에도 올라가 있다. 계획 산출물 이름이 `deviceOccupied`·
  `projectOccupied`, 제외 사유가 `slots_held_by_unrecovered_runs`로 이 작업 문서의 `occupied`·
  `occupied_slots`와 다르며, TASK-S085-02가 맡을 정리 수행까지 함께 담고 있다.
- 사용자 지시: `docs/handoff.md`와 `docs/development-logs/2026-08-20.md`에 "사용자 지시로 SPEC-085
  대신 다이렉트 구현", "복귀 후 PR #40 병합 → 런타임 릴리스 컷 → SPEC-085 폐기 도장"이 적혀 있다.

작업 문서 밖을 읽은 곳: SPEC-085과 DECISION-2D078F50과 GROUP-085을 열었다. 결정의 유효성 확인은
자격 판단이 요구하는 것이라 작업 문서의 부족과는 무관하다. 다만 작업 문서에는 이 구현이 프로젝트 밖
저장소에서 어떤 사본으로 수행되는지가 적혀 있지 않아, 격리 사본과 선언 경로의 대응을 문서 밖에서
직접 확인해야 했다. 그 대응이 없다는 것이 이 블록의 첫 번째 사유다.

## 남은 위험

- 사용자의 미커밋 변경과 병합 대기 브랜치는 이 세션이 건드리지 않았다. 다만 그 둘이 같은 파일을 고치고
  있어, 병합 순서는 여전히 사용자 몫으로 남는다.
- 이 대상에 `supervisor_identity_unverified` 기록이 다시 남았다. 런타임 #38이 그대로면 이 작업을 다시
  예약해도 세션이 같은 방식으로 끊길 수 있다.
- 점유 집계가 배포 런타임에 아직 없으므로, 배정이 재개된 지금은 한도 초과 스폰이 재발할 수 있다.
  판 상태 문서가 이미 적어 둔 위험이며 이 세션이 바꾼 것은 없다.

## 후속 작업

- 아키텍트 핸드오프: GROUP-085과 TASK-S085-01·02의 처분을 정해야 한다. 기획서 폐기 도장은 사용자
  몫이므로, 문서만으로 판단이 서지 않으면 `configuration_unresolved_revision`과
  `## 사람의 판단이 필요한 이유`로 사용자 판단 사항을 남기는 편이 맞다.
- 아키텍트 핸드오프: 프로젝트 밖 저장소를 대상으로 하는 작업은 지금 구조에서 격리 사본을 받을 수 없다.
  TASK-S085-02도 같은 선언을 갖고 있어 같은 자리에서 막힌다. 작업을 만들 때 사본과 통합 지점이 정해질
  수 있는지 먼저 확인하는 편이 낫다.
- 이 세션은 통합을 기다리는 상태가 아니므로 `wait-integration`을 부르지 않았다.

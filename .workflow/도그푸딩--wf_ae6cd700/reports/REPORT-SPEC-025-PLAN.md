# REPORT-SPEC-025-PLAN

> 기록 경위: 하네스가 서브에이전트(plan-b)의 보고서 파일 작성을 차단해, 발신된 전문을 TL이 대리 기록한다. 내용은 발신 전문 그대로다. (수신 2026-08-04T09:04Z, TL 세션)

기획자 세션 결과. IDEA-19843535를 SPEC-025로 합성했다.

- 대상 아이디어: IDEA-19843535 (아이디어 인박스·오늘 화면 입력 유실)
- 산출 기획서: SPEC-025 (status: user_review) → 이후 DECISION-5B7E9F14로 위임 대리 승인됨
- 선점: lease-92385-20260804085157 (acquire → release, 둘 다 exit 0)
- 작성 시각: 2026-08-04T09:02:00Z

## 대상 선정

migration.lock 없음, 미답변 수정 요청 없음. 미처리 아이디어 셋 중 가장 이른 IDEA-19843535 — TL 배정과 계약 판정 일치, 경합 없음. SPEC-025는 noclobber 배타 생성.

## 조사에서 확인한 것

- 원인 한 줄: 입력창의 글은 IdeaComposer의 지역 상태(IdeaComposer.tsx:12 useState("")), 저장 코드 없음.
- 두 화면 모두 조건부 렌더(WorkspaceShell.tsx:312·:369) — 메뉴 이동 시 언마운트로 소멸.
- 같은 컴포넌트가 두 곳(오늘 :328 compact/quick-idea, 인박스 IdeaInbox.tsx:114 기본형/idea-inbox-input)이라 초안이 둘.
- 선례: SPEC-019의 browserSetupGuideCollapseStore(:19~:42, 실패 전부 기본값·저장 실패 삼킴). 단 그 규칙의 근거는 "표시 상태"라 사용자 저작물 적용 여부는 별개 판단 → 확인 필요로.
- 초안의 목적지는 워크플로(addIdea가 workflow.directory로 씀, WorkspaceShell.tsx:141~:143).
- 현행 불일치: 인박스는 key={workflow.directory}(:373)라 워크플로 전환 시 초안 소멸, 오늘 화면은 key 없어 잔존.
- 제출 실패 시엔 지금도 글이 남는다(IdeaComposer.tsx:17, onAdd 참일 때만 초기화) — "쓴 글을 지키는" 규칙의 확장이 이번 일.
- 두 입력창을 다루는 자동화 테스트 전무.

## 핵심 기획 결정

R1(떠나도 남는다)~R6(현행 유지), 확인 사실 15항, 완료 조건 17항. 인용 줄 번호 전부 작업 트리 대조.

## 확인 필요 4건과 처리

1. 두 초안 통합 — 제안대로 승인 (동시 표시 없음, 분리 시 신고 증상 잔존).
2. 워크플로 단위 — 제안대로 승인 (오제출 방지, 인박스 현행 근접).
3. 저장 실패 미통지(선례 동일) — 제안대로 승인, 사용자 저작물 한계 인지.
4. 초안 수명 없음 — 제안대로 승인.

## 아키텍트에게 넘기는 것

- 저장 수단(선례 저장소 확장 vs 새 키)은 구현 위임. R4는 실패 시 동작만 정한다.
- 확인 필요 1·2의 결론이 저장 키 구조를 직접 결정 (승인됨: 통합·워크플로 단위).
- 확인 사실 8의 현행 불일치를 어느 쪽으로 맞출지가 R3의 실질.
- 두 입력창 테스트 전무 — 완료 조건 대부분이 새 테스트, 회귀 기준선 없음.

## 역할 밖 관찰 (수정 안 함)

- 뷰 선택 미기억(WorkspaceShell.tsx:107~:109), 인박스 선택 아이디어·본문 마운트마다 초기화 — 같은 계열이나 원천 요구 밖, 제외 범위 명시.
- SPEC-009.yml 만료 lease 잔존(무조치).
- 세션 종료 시점 IDEA-7BCB8947은 다른 세션 선점, 남은 미처리는 IDEA-5C6073A5 하나.

## 세션 정리 상태

변경 파일 SPEC-025.md 하나. 보호 상태 무변경, git 커밋 없음, lease 해제 완료.

---
schema: workflow-labs/work-group@1
id: GROUP-DECISION-6723CEE0
title: 막힌 작업의 사유와 재개 조건을 작업 패널에 표시한다
status: active
revision: 1
qa_mode: user
source_spec_id: SPEC-053
source_decision_id: DECISION-6723CEE0
created_at: 2026-08-14T09:08:07.880257+00:00
updated_at: 2026-08-14T09:08:07.880257+00:00
---

# 막힌 작업의 사유와 재개 조건을 작업 패널에 표시한다

## 기능 설명

기존 개발 작업을 기획서 기준의 작업 그룹으로 전환했습니다.

### QA-01 · 막힌 사유 문서 계약을 관리 규칙에 반영한다

이 작업에는 눈으로 볼 화면이 없다. 바뀐 것은 앱이 설치하는 규칙 문언과 사용자·개발자용 계약 문서라
자동 검사가 그 문언을 대조한다. 확인 도장은 아래 수치를 신뢰한다는 뜻이다.


문언을 직접 읽고 싶다면 이 저장소의 설치본이 이미 갱신돼 있다. 개발 중인 앱이 관리 자산을 동기화하면서
새 계약을 내려받았고, 그 결과가 그대로 남아 있다.

1. `.workflow/rules/workflow.md`의 frontmatter가 `rules_version: 18`이고, 5절 안에
   `### Recording why a task is blocked`가 있다. 그 아래에 `## 막힌 사유` 제목과 네 라벨(막힌 지점,
   필요한 해결, 재개 조건, 관련 대상)이 예시 블록으로 한 번씩 정의돼 있다.
2. 같은 절에서 네 규칙을 읽을 수 있다. 값을 비우거나 라벨을 빠뜨리면 계약이 요구하는 절이 아니라는 것,
   관련 대상이 없으면 `없음`을 쓰고 여럿이면 쉼표와 공백으로 나열한다는 것, 사유 문구만 고치는 편집은
   상태 전이가 아니라 이력을 늘리지 않는다는 것, 차단에서 벗어나도 절을 지우지 않고 재차단 시 현재
   사유로 교체한다는 것.
3. `.workflow/rules/roles/developer.md`는 `rules_version: 13`이고 `## Blocking a task` 절에서 자기
   의무만 적은 뒤 공통 규칙 `§5`를 가리킨다. 라벨 목록을 다시 적지 않는다.
4. `.workflow/rules/roles/planner.md`와 `architect.md`는 `rules_version: 11` 그대로다. 본문이 바뀌지
   않은 계약은 버전이 오르지 않는다.

같은 내용이 `docs/file-contract.md`의 `### 막힌 사유 절`에 한국어로 적혀 있다.

기존 문서는 바뀌지 않는다. 이 절이 없거나 형식이 불완전한 기존 작업 문서를 앱이 고치지 않고, 그 때문에
목록·상세 화면의 표시나 자격 판정이 달라지지도 않는다.

### QA-02 · 막힌 사유 절을 추측 없이 읽는 로컬 파서를 만든다

이 작업은 화면 변경이 없는 순수 로컬 파서 작업이다. 저장소 루트에서
빌드가 모두 성공해야 한다. 사용자 확인 도장은 이 자동 검사 수치와 개발 보고서의 남은 위험을
검토하고 신뢰한다는 뜻이다.

### QA-03 · 막힌 사유와 관련 대상을 작업 우측 패널에 표시한다

1. 앱의 개발 작업 화면에서 `blocked` 상태이며 유효한 `## 막힌 사유` 절이 있는 작업을 연다.
2. 오른쪽의 `진행이 막혔습니다` 패널에서 문서 갱신 시각, 막힌 지점, 필요한 해결, 재개 조건과 관련
   대상이 작성 순서대로 보이는지 확인한다.
3. 실제 작업과 일치하는 관련 대상은 식별자, 제목과 현재 상태가 보이는 버튼인지 확인하고, 키보드로
   실행했을 때 같은 상세 화면에서 그 작업이 열리는지 확인한다.
4. 없는 작업이나 외부 의존은 버튼 없이 작성 문구만 보이고, 관련 대상이 `없음`이면 대체 링크가 생기지
   않는지 확인한다.
5. 구조화 사유가 없는 막힌 작업에서는 결정권자 요약 또는 원문 확인 안내가 나오며 원인을 추측하지
   않는지 확인한다.
6. 창 너비를 980픽셀 이하로 줄여 패널이 문서 아래 한 열로 쌓이고 긴 문장과 식별자가 가로로 잘리지
   않는지 확인한다. 다시 넓힌 뒤에도 정보 순서가 같은지 확인한다.
7. `qa_waiting` 작업을 열어 기존 확인 동선, 메모 입력, 완료와 개발 준비로 되돌리기 조작이 그대로인지
   확인한다. 이 확인 결과를 TASK-145 품질 확인 메모에 기록한다.

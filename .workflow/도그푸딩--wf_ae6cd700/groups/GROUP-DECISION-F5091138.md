---
schema: workflow-labs/work-group@1
id: GROUP-DECISION-F5091138
title: 구조화된 결정권자 요약 보드
status: active
revision: 1
qa_mode: user
source_spec_id: SPEC-052
source_decision_id: DECISION-F5091138
created_at: 2026-08-14T09:08:07.880257+00:00
updated_at: 2026-08-14T09:08:07.880257+00:00
---

# 구조화된 결정권자 요약 보드

## 기능 설명

기존 개발 작업을 기획서 기준의 작업 그룹으로 전환했습니다.

### QA-01 · 새 기획서와 개발 작업이 구조화된 요약 계약을 따른다

이 작업에는 눈으로 볼 화면이 없다. 바뀐 것은 앱이 설치하는 규칙 문언과 사용자·개발자용 계약 문서라
자동 검사가 그 문언을 대조한다. 확인 도장은 아래 수치를 신뢰한다는 뜻이다.


문언을 직접 읽고 싶다면 두 곳을 보면 된다.

1. 임시 폴더를 프로젝트로 열면 앱이 `.workflow/rules/workflow.md`를 새로 설치한다. 그 파일의
   frontmatter가 `rules_version: 17`이고, `## 8.` 절 안에 `### The structured summary`가 있다. 일곱
   하위 제목이 `### 제안`부터 `### 결정 요청`까지 이 순서로 한 번씩 정의되고, `### 영향 범위`가
   `- 변경:`과 `- 유지:` 두 표식을 요구하며, `### 비용과 위험`은 선택이라고 적혀 있다.
2. `.workflow/rules/roles/`의 세 계약은 각각 `rules_version: 11`(planner), `11`(architect),
   `12`(developer)이고, 자기 문서의 의무만 적은 뒤 공통 규칙 `§8`을 가리킨다. 제목 목록을 다시 적지
   않는다.

같은 내용이 `docs/file-contract.md`의 `#### 구조화된 요약` 절과 기획서·개발 작업 예시에 한국어로 적혀
있다. 개발 보고서 예시와 설명은 평문 요약과 열 줄 상한을 그대로 유지한다.

이미 있는 문서는 바뀌지 않는다. 기존 기획서·작업의 평문 요약이나 요약 없는 문서를 앱이 고치지 않고,
그 때문에 목록·활동 화면의 표시나 자격 판정이 달라지지도 않는다.

### QA-02 · 구조화된 요약을 추측 없이 판정하고 값으로 분리한다

이 작업은 화면 대신 자동 검사로 확인한다.
45개 검사가 모두 통과하는지 확인한다.
두 결과가 같으면 사용자 QA 확인은 자동 검사 결과를 신뢰하는 것으로 완료한다.

### QA-03 · 구조화된 요약이 반응형 결정 보드로 열린다

기획서 화면에서 SPEC-052를 열면 `제안`부터 `결정 요청`까지 결정 보드가 기본 화면에 나타난다.
창 너비를 700픽셀보다 넓게 두면 변화 흐름 세 칸과 영향 범위 두 칸이 보이고, 더 좁히면 같은 제목
순서가 한 열로 쌓여야 한다. 긴 문장이 카드 밖으로 잘리거나 가로 스크롤을 만들지 않아야 한다.
`비용과 위험`은 제목과 문장이 있는 경고 영역으로 보인다. `원문 전문 보기`를 키보드로 누르면
SPEC-052의 `결정권자 요약`부터 본문 마지막까지 나타나고 `aria-expanded`가 참으로 바뀐다.
`요약만 보기`를 누르면 결정 보드로 돌아온다. 평문 요약 문서는 기존 Markdown 요약으로 열려야 한다.

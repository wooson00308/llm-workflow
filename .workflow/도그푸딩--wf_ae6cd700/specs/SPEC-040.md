---
schema: workflow-labs/spec@1
id: SPEC-040
title: 개발자 분기도 같은 상한 안에 들어오고, 프로세스 예산이 약속대로 내려간다
status: user_review
source_idea_id: IDEA-B828F62D
created_at: 2026-08-05T06:32:00Z
updated_at: 2026-08-05T06:40:00Z
---

# 개발자 분기도 같은 상한 안에 들어오고, 프로세스 예산이 약속대로 내려간다

## 결정권자 요약

이전에 승인한 "판정이 3초 안에 끝난다"는 목표가 세 역할 중 둘에서만 닫혔다. 개발자 몫 하나가 남았고, 문서가 세 배로 늘어난 상태를 흉내 낸 시험에서 7.1초가 걸린다. 데몬은 10초에서 판정을 끊고 끊긴 판정을 "일감 없음"으로 처리하므로, 이대로 문서가 더 쌓이면 개발 잡이 조용히 뜨지 않는 날이 온다.

이 문서를 승인하면 남은 하나를 앞선 둘과 같은 방식으로 고치고, 그때 함께 세운 자동 감시 장치의 기준값도 원래 약속했던 값으로 내린다. 지금 그 기준값은 "오늘의 실측 바로 위"라 사실상 개발자 몫 하나만 감시하고 있다.

승인하지 않으면 개발자 몫은 7.1초에 남고, 감시 장치의 기준값도 느슨한 채로 남는다. 이미 고쳐진 둘은 그대로 유효하다.

결정할 것은 셋이다. 시간 상한을 자동 검사로 못 박을지 아니면 착지 보고의 실측으로 닫을지, 감시 기준값을 하나로 둘지 역할별로 가를지, 윈도우용 구현의 상한을 어디까지 확인할지다. 셋 다 아래 "확인 필요"에 제안과 대가를 적었다.

## 기획 내용

SPEC-033이 판정 비용에 상한을 걸었다 — 최악 경로 3초, 그리고 지금 문서량의 3배 픽스처에서도 그 3초. 그 기획서가 실제로 고장 났다고 지목한 자리(아키텍트 분기, 네 번의 데몬 타임아웃)는 닫혔다. TASK-104가 반복 훑기를 없애 아키텍트 분기를 1배 0.12초·3배 0.79초로 내렸고, 기획자 분기도 같은 어법으로 접혔다. 남은 것은 개발자 분기 하나다. 3배 픽스처에서 7.10초이고, TASK-104의 보고서가 그것을 완료 조건 2 미충족으로 명기한 뒤 아키텍트 후속으로 넘겼다(확인 사실 1).

그 미달은 TASK-104가 만든 것이 아니다. lease를 뺀 통제 픽스처에서 착수 전(v6) 7.07초와 착지 후(v9) 7.07초가 소수점 둘째 자리까지 같다(확인 사실 2). 개발자 분기는 그 작업이 손대지 말라고 명시적으로 금지한 자리였고 — TASK-101이 같은 절에 겹침 판정을 넣는 중이었다 — 비용도 그 사실대로 움직이지 않았다. 원인은 컬렉션 크기의 곱이 아니라 작업 문서 하나당 상수 개의 외부 프로세스이고, 그래서 작업 수에 선형이다(확인 사실 3·5). SPEC-033의 R2("곱으로 늘지 않는다")는 이미 지키고 있고 R1(절대 상한)만 못 지킨다.

사용자가 이 자리를 직접 지목했다. 원문은 이렇다(2026-08-05, IDEA-B828F62D에 실린 지시 전문).

> 개발자 분기도 같은 상한(최악 3초, 문서 3배)을 적용. 방법은 104가 기획자·아키텍트에 쓴 단일 훑기 어법 재사용, 판정 결과는 불변 유지. 상한이 잡히면 106의 프로세스 예산(CAP 3,000)도 원래 규칙대로 내리는 것까지 포함.

세 문장이 이 기획서의 뼈대다. 그리고 셋째 문장이 가리키는 약속은 코드 안에 이미 적혀 있다. TASK-106이 회귀 감지 장치를 세울 때 아키텍트가 고정한 규칙은 "예산은 3초 상당의 절반 이하"였고 그 값이 약 777개인데, 그때 개발자 분기가 이미 3배에서 2,743개(약 5.3초)라 777로는 첫날부터 실패한다는 이유로 3,000이 됐다. 그 상수 주석에 "개발자 분기가 목표 안으로 들어오면 이 값을 777 이하로 내린다"가 적혀 있다(확인 사실 4). 이 기획서가 그 날을 만든다.

주의할 자리는 하나다. 개발자 분기는 판정이 제일 두꺼운 절이다. 후보 상태 판정(TASK-110이 `in_progress` 회수를 넣었다), 선행 판정(순환 선언 검출 포함), 겹침 판정(TASK-101의 `scope_files`)이 전부 그 안에 산다(확인 사실 10). 사용자가 "판정 결과는 불변 유지"를 지시에 넣은 것이 이 자리다. 다행히 그것을 고정할 장치는 이미 서 있다 — sh·PowerShell·앱 세 구현의 답을 대조하는 시나리오 표와 대조 테스트다(확인 사실 9). 새로 만들 것이 아니라 이미 있는 그물이 새 본문에서도 통과하면 된다.

한편 이 변경은 회귀 감지 장치에 구멍을 하나 더 낸다. TASK-106의 계량은 외부 프로세스 수이고, 단일 훑기 어법은 비용을 프로세스에서 셸 내장 문자열 비교로 옮긴다. 그래서 고치고 나면 개발자 분기의 성장도 그 계량에 잡히지 않게 된다 — 지금 계량이 실제로 무언가를 보고 있는 유일한 분기가 개발자 분기다(1배 919 · 3배 2,743, 나머지 둘은 3과 2). 그 성장이 상상이 아니라는 근거도 있다. 아키텍트 분기는 착지 후에도 문서 3배에 6.6배로 자라고, "이 기울기면 문서량 5~6배 언저리에서 3초에 닿는다"가 이미 한계로 기록돼 있다(확인 사실 8). 확인 필요 1번이 이 자리를 사용자에게 묻는다.

마지막으로 범위를 하나 잘라 둔다. 이 아이디어가 기획서 수정 요청이 아니라 아이디어 경로로 들어온 것은, 앱이 승인된 기획서에 후속 결정을 보낼 길을 주지 않기 때문이다. 그 갭 자체는 IDEA-986E5DBF로 따로 등록돼 있고 이 기획서가 다루지 않는다. 그 직전에 같은 의도의 도장이 작업 QA 반려로 잘못 들어가 TASK-104가 한 번 `todo`로 되돌아간 일도 있었는데, 그 대응은 코드 변경 없이 재착지하는 것으로 이미 끝났다(확인 사실 13).

### 확인 사실

2026-08-05 기준이다. 성능 수치는 전부 기존 보고서에서 인용했고 이 세션이 새로 측정한 것은 없다 — 인용 출처를 항목마다 적었다. 코드 위치는 작업 트리 기준이다.

1. **SPEC-033의 완료 조건 2가 개발자 분기에서 닫히지 않았다.** REPORT-TASK-104-DEV "성능 측정" 절의 여섯 줄 표에서 3배 픽스처 `developer`가 착지 후 7.10초이고, 같은 보고서가 그것을 "미충족"으로 표기했다. 같은 표의 1배 `developer`는 1.32초로 상한 안이다. 같은 픽스처의 기획자·아키텍트는 3배에서 0.64초·0.79초다.

2. **그 비용은 TASK-104가 만든 것이 아니다.** 같은 보고서의 통제 측정 — `.runtime/leases`를 제거한 픽스처에서 개발자 분기만 다시 재면 1배가 착수 전(v6) 1.31초 / 착지 후(v9) 1.31초, 3배가 7.07초 / 7.07초다. 두 본문이 소수점 둘째 자리까지 같다. 그 작업은 `developer)` 절을 손대지 않았고, 작업 문서가 그것을 명시적으로 금지했다(TASK-101이 같은 절을 고치는 중이었고 R9가 판정 불변을 요구했다).

3. **원인은 곱이 아니라 작업 문서당 상수 개의 프로세스다.** REPORT-TASK-106-DEV의 계량이 개발자 분기 1배 919개 · 3배 2,743개로 비율 2.98이다(선형 상한 3.5배 안). 3배 픽스처의 명령별 내역은 `sed` 866 · `grep` 865 · `head` 722 · `date` 145 · `tr` 145이고, REPORT-TASK-110-DEV가 `in_progress` 회수를 넣은 뒤에도 919 / 2,743과 그 내역이 그대로임을 다시 확인했다. 같은 픽스처의 기획자는 3개, 아키텍트는 2개다.

4. **예산 3,000은 규칙에서 역산한 값이 아니고, 내린다는 약속이 코드에 적혀 있다.** `heartbeat_condition.rs:2598`~`:2617`의 `CAP` 주석이 근거를 담고 있다 — 같은 픽스처를 계량 없이 재면 1배 1.478초 / 3배 5.299초라 초당 518~623개이고, 목표 3초는 대략 1,550~1,870개, 아키텍트가 고정한 "그 절반 이하" 규칙은 약 777개다. 개발자 분기가 이미 2,743개(약 5.3초)라 777로는 첫날부터 실패하므로 "오늘의 최댓값 바로 위"인 3,000을 썼고, 주석이 "개발자 분기가 목표 안으로 들어오면 이 값을 777 이하로 내린다. 통과시키려고 올리는 자리가 아니다"로 끝난다. 같은 모듈의 `GROWTH_NUMERATOR`·`GROWTH_DENOMINATOR`(`:2595`~`:2596`)가 비율 상한 3.5배다.

5. **프로세스가 뜨는 자리가 개발자 분기 본문에 그대로 있다.** `heartbeat_condition.rs`의 sh 본문 `developer)` 절(`:395`~)이 작업 파일 하나마다 후보 상태 검사(`:405`의 `grep`), 문서 id 읽기(`sed`·`head`)를 하고, 이어서 `lease_blocks`(`:57`, lease 파일이 있으면 `sed`·`head`·`tr`·`date`), `deps_of`(`:69`, `grep -c`와 `sed`·`head`, 토큰마다 `sed` 하나 더), `task_file`(`:150`, 선행마다 `grep -ls`), `dep_satisfied`(`:155`, `grep` 한둘), `reaches`(`:160`, 같은 헬퍼들을 다시), `overlap_blocks`(`:124`)를 부른다. `overlap_blocks`는 다시 `scope_of`(`:96`)를 자기 문서와 lease가 잡은 문서에 대해 부른다. 곱이 아니라 후보 수 × 상수인 구조가 여기서 나온다. PowerShell 본문의 같은 절은 `:809`다.

6. **기획자·아키텍트 분기는 이미 상수다.** REPORT-TASK-110-DEV의 계량 표에서 착지 후 기획자 3 / 3, 아키텍트 2 / 2로 1배와 3배가 같다. TASK-104가 만든 단일 훑기 어법의 결과다.

7. **재사용할 어법이 이미 넷 있다.** sh 본문의 `scan_ideas`·`scan_refs`·`scan_decisions`(TASK-104)와 `scan_nondraft_refs`(TASK-110)다. 디렉터리마다 `awk` 한 번으로 끝내고 후보별 조회는 `case`와 파라미터 확장으로만 한다. 본문 주석이 그 규칙을 적어 두었고("후보마다 같은 디렉터리를 다시 읽으면 판정 비용이 컬렉션 크기의 곱이 된다"), SPEC-035 세션도 새 헬퍼를 그 어법으로 썼다. 개발자 분기만 아직 이 어법을 쓰지 않는다.

8. **계량은 프로세스 수라서 셸 내장 비교의 성장을 보지 못한다.** REPORT-TASK-106-DEV "남는 리스크" 5번과 REPORT-TASK-104-DEV "남는 리스크" 2번이 같은 자리를 적는다. 후자는 아키텍트 분기의 착지 후 값이 0.12초 → 0.79초로 문서 3배에 6.6배 자랐고 "이 기울기면 문서량 5~6배 언저리에서 3초에 닿는다"고 적었다. REPORT-TASK-106-DEV "남는 리스크" 4번은 그 반대편도 적는다 — 기획자·아키텍트는 프로세스가 3개·2개뿐이라 절대 상한이 사실상 놀고 있고, 상수 몇 개가 느는 회귀는 그 두 분기에서 잡히지 않는다.

9. **세 구현이 같은 답을 내는 것을 고정하는 장치가 이미 서 있다.** `role_eligibility.rs`의 `assert_matches_condition_script`가 시나리오마다 세 역할 전부에서 앱 판정과 설치된 스크립트의 종료 코드를 대조하고, `heartbeat_condition.rs`의 `SCENARIOS` 표(현재 43행)가 sh와 PowerShell 두 본문을 같은 상황에 돌리며, 버전 일치 단언이 상수와 두 본문의 버전 줄을 함께 본다. SPEC-032 R3이 요구한 계열이고, TASK-110 보고서가 "세 판정이 갈라진 자리가 하나도 없다"를 그 장치로 확인했다.

10. **개발자 분기가 담고 있는 판정은 셋이다.** 후보 상태(`todo`와 `in_progress`, `blocked`은 후보가 아니다 — SPEC-035 R1), 선행 충족(`qa_waiting`·`completed`만 충족, 순환 선언은 미충족 — SPEC-013 계열), 겹침(`scope_files` 문자열 완전 일치, 선언이 없거나 형식 오류면 막는다 — SPEC-032). 세 규칙 전부 이 기획서가 건드리지 않는 자리다.

11. **지금 이 파일들을 잡고 있는 미완료 작업이 없다.** 미완료 작업은 TASK-112·113·114가 `qa_waiting`, TASK-115·116·117·118이 `todo`이고, 그중 `heartbeat_condition.rs`나 `role_eligibility.rs`를 `scope_files`에 적은 것은 하나도 없다. TASK-115는 `heartbeat_status.rs`, TASK-118은 `project_instructions.rs`·`docs/file-contract.md`다.

12. **설치본과 작업 트리의 버전이 갈라져 있다.** `.workflow/rules/wf-eligible.sh`의 버전 줄이 9이고 `heartbeat_condition.rs:20`의 상수는 10이다. 데몬이 실제로 도는 것은 설치본이므로, 이 기획서의 작업이 착지해도 앱이 자산을 다시 설치하기 전까지 데몬의 판정 비용은 옛 값 그대로다. 설치·갱신 동선은 SPEC-037이 다루고 있다.

13. **이 아이디어가 아이디어 경로로 들어온 경위.** 사용자가 SPEC-033에 범위 확장 수정 요청을 보내려 했으나 앱이 승인된 기획서에 후속 결정을 지원하지 않아 아이디어로 등록했고, 그 갭 자체는 IDEA-986E5DBF로 따로 서 있다. 그 직전에 같은 의도가 작업 QA 반려 다이얼로그로 잘못 들어가(`QA-B4F87EA5`) TASK-104가 `todo`로 되돌아간 오발이 있었고, 그 대응은 REPORT-TASK-104-DEV "QA 반려 대응" 절대로 코드 변경 없이 재착지하는 것으로 끝났다.

### 포함 범위

- 조건 스크립트의 개발자 분기가 조기 종료가 걸리지 않는 최악 경로에서 3초 안에 끝나고, 문서량 3배 픽스처에서도 그 상한을 지킨다.
- 방법은 TASK-104가 기획자·아키텍트 분기에 쓴 단일 훑기 어법의 재사용이다.
- 세 구현(sh 본문·PowerShell 본문·앱의 `has_developer_work`)이 지금과 같은 답을 계속 낸다.
- TASK-106이 세운 프로세스 예산을 아키텍트가 원래 고정한 규칙(3초 상당의 절반 이하)으로 내린다.
- 상한 달성이 사람의 눈이 아니라 검증 가능한 형태로 남는다. 형태는 확인 필요 1번에서 정한다.

### 제외 범위

- 자격 규칙의 변경. 후보 상태 집합, 선행 충족 판정, 순환 선언 검출, 겹침 판정, lease 만료 기준, 조기 종료와 사유 코드가 전부 지금 그대로다. 같은 답을 더 싸게 낼 뿐이다.
- 데몬의 10초 한도를 바꾸거나 설정 가능하게 만드는 일. SPEC-033 제외 범위 그대로이고, claude-heartbeat 저장소의 몫이다.
- 완료 문서 아카이브와 파생 인덱스. SPEC-033 확인 필요 1·2번의 승인된 결정을 그대로 유지한다.
- 앱 조회 재읽기(2.5초 폴링)의 추가 정리. SPEC-033 R7이 정한 범위는 TASK-105로 이미 닫혔다.
- 기획자·아키텍트 분기의 재작업. 확인 사실 6대로 이미 상수이고, 이 기획서가 그 둘의 본문을 고칠 이유가 없다.
- 승인된 기획서에 후속 결정을 보내는 앱 동선. IDEA-986E5DBF의 몫이다.
- 설치본 갱신 경로. 확인 사실 12의 버전 격차는 SPEC-037이 다룬다.
- 문서 스키마 변경·마이그레이션.

## 요구사항 명세

### R1. 개발자 분기가 같은 상한 안에서 끝난다

- 조건 스크립트의 개발자 분기가, 조기 종료가 걸리지 않는 최악 경로에서 3초 안에 끝난다. 상한 값과 배수는 SPEC-033 확인 필요 3번이 정한 것을 그대로 쓴다.
- 문서량 3배 픽스처에서도 그 상한을 지킨다.
- 기준선은 확인 사실 1·2의 인용값이다 — 1배 1.32초, 3배 7.10초, lease를 뺀 통제에서 1.31초 / 7.07초. 같은 방법으로 잰 전후 값을 보고서에 적는다.
- 기획자·아키텍트 분기의 값이 나빠지지 않는다.

### R2. 방법은 이미 있는 어법의 재사용이다

- 확인 사실 7의 단일 훑기 어법을 개발자 분기에 재사용한다. 새 어법을 발명하지 않는다. 사용자 지시가 방법까지 지정한 자리다.
- 후보별 조회는 프로세스를 띄우지 않는 수단으로 한다. 조회마다 프로세스를 띄우면 비용이 자리만 옮긴다.
- 개발자 분기에는 기획자·아키텍트에 없던 성질이 있다 — 후보 하나가 다른 문서(선행 작업, lease가 잡은 문서)를 찾아가고, 순환 선언 검출이 그 탐색을 반복한다. 그 자리를 같은 어법으로 어떻게 접을지는 아키텍트가 정한다. 이 요구는 "이미 있는 어법을 쓴다"까지이고 자료 구조를 지정하지 않는다.
- 두 플랫폼 본문 모두에 적용한다. 한쪽만 고치면 확인 사실 9의 표가 두 답을 다르게 만든다.

### R3. 판정 결과가 달라지지 않는다

- 확인 사실 10의 세 판정이 전부 지금과 같은 답을 낸다. 후보 상태 집합도, 선행 충족 기준도, 순환 선언이 미충족이라는 것도, 겹침의 문자열 완전 일치도, 선언이 없거나 형식 오류인 쪽이 막힌다는 것도 그대로다.
- 만료 lease가 대상을 막지 않는 규칙, 읽을 수 없는 표기를 선점으로 세지 않는 규칙, 판정이 lease 파일을 읽기만 하는 규칙이 그대로다.
- 조기 종료가 지금과 같은 자리에서 걸린다. 사유 코드는 표준 출력의 첫 줄이자 유일한 줄로 남고 새 코드가 생기지 않는다.
- 판정은 조회 시점 파생이고 결과를 문서에 되쓰지 않는다. 스크립트가 `.workflow` 아래에 아무것도 쓰지 않는다.

### R4. 세 구현이 계속 같은 답을 낸다

- 확인 사실 9의 대조 테스트와 시나리오 표가 기존 행·기존 단언 수정 없이 통과한다. 판정 규칙을 바꾸지 않으므로 기대값이 뒤집히는 검사가 있어서는 안 된다.
- 두 플랫폼 본문을 함께 고치고 `CONDITION_SCRIPT_VERSION`과 두 본문의 버전 줄을 함께 올린다.
- 앱 판정은 지금처럼 파일을 만지지 않는다. 필요한 값은 읽는 쪽이 모아서 넘긴다.
- 두 구현의 알려진 차이 목록에 새 차이가 생기면 그 자리에 적는다. 적지 않은 차이를 남기지 않는다.

### R5. 프로세스 예산이 약속대로 내려간다

- 확인 사실 4의 규칙 — 예산은 3초 상당의 절반 이하 — 으로 예산을 내린다. 그 규칙이 지금 가리키는 값은 약 777개다.
- 실제 값은 착지 후 실측의 최댓값 위에 여유를 두되 그 규칙의 상한을 넘지 않는다. 값을 정한 근거(환산에 쓴 측정, 여유의 크기, 무엇이 늘면 걸리는지)를 주석과 보고서 두 자리에 적는다.
- 통과시키려고 예산을 올리지 않는다. 올려야 할 이유가 보이면 올리지 말고 그 사실을 보고서에 적는다.
- 비율 상한(3배 픽스처가 1배의 3.5배 이하)은 그대로 둔다. 확인 사실 3대로 개발자 분기는 이미 그 안에 있고, 이 기획서가 바꾸는 것은 절대 상한 쪽이다.

### R6. 상한이 사람의 눈에 다시 의존하지 않는다

- SPEC-033 R8이 세운 요구가 개발자 분기에서도 성립한다. 판정 비용이 다시 한도에 다가가는 것을 사람이 로그나 보고서에서 발견하는 방식이 아니어야 한다.
- 확인 사실 8의 구멍이 이 변경으로 넓어진다. 비용이 프로세스에서 셸 내장 비교로 옮겨 가면 지금 계량이 개발자 분기의 성장도 보지 못하게 되고, 그때 계량이 실제로 보고 있는 분기는 하나도 남지 않는다.
- 그 구멍을 이번에 어떻게 다룰지는 확인 필요 1번에서 정한다. 어느 쪽으로 정해지든, 남는 한계는 코드 주석과 보고서에 적혀 다음 세션이 읽을 수 있어야 한다.

### R7. 지금 되는 것이 그대로 된다

- 세 역할의 자격 판정, 선행 판정, 아이디어 상태 판정, QA 이벤트 병합이 지금과 같은 값을 낸다.
- 기존 자동 테스트가 삭제되거나 비활성화되지 않는다. 기존 시나리오 행과 기존 대조 단언이 줄지 않는다.
- 조건 스크립트가 여전히 사유 코드를 표준 출력 첫 줄이자 유일한 줄로 내고, 데몬이 그 줄을 옮기고 앱이 문장으로 옮기는 경로가 그대로다.
- 규칙 자산 파일(`.workflow/rules/*`)을 세션이 직접 고치지 않는다. 앱이 설치하는 자산이다.
- 확인 사실 11 때문에 파일 충돌 위험은 지금 낮지만, 이 기획서의 작업은 `heartbeat_condition.rs`·`role_eligibility.rs`를 잡는 다른 작업이 생기면 그것과 병행되지 않도록 겹침 선언과 선행 선언으로 순서를 적는다.

## 확인 필요

사용자 결정이 필요한 항목이다. 승인 시 아래 제안대로 진행한다.

1. **벽시계 3초를 자동 검사로 못 박을지, 착지 보고의 실측으로 닫을지.**

   제안: 자동 검사는 프로세스 예산(확인 필요 2번의 값)까지로 두고, 벽시계 3초는 착지 보고서의 실측으로 닫는다. 근거는 둘이다. 첫째, 프로세스 수는 기기와 무관하게 결정적이라 여유를 몇 퍼센트까지 좁혀도 러너 편차로 소진되지 않는 반면, 벽시계는 같은 기기에서도 앱과 데몬이 도는지에 따라 흔들린다 — 지금 있는 측정들이 그 조건을 매번 따로 적고 있는 이유다. 둘째, 벽시계 검사를 넣으면 그 검사가 느린 러너에서 간헐적으로 실패하고, 그때 사람이 가장 먼저 하는 일은 상한을 올리는 것이다. 그러면 상한이 있다는 사실만 남고 상한은 사라진다.

   비용: R6이 지목한 구멍이 그대로 남는다. 셸 내장 문자열 비교의 성장은 어느 자동 장치에도 잡히지 않고, 확인 사실 8의 "문서량 5~6배에서 3초"라는 기울기가 사실이면 그 날을 다시 사람이 발견한다. 이번 변경이 개발자 분기까지 그 사각지대로 옮기므로 사각지대는 넓어진다.

   대안 A: 벽시계 검사도 만든다. 3초를 그대로 단언하지 말고 넉넉한 상한(예: 데몬 한도의 절반)으로 두어 플레이키를 피하고, 정밀한 3초는 보고서가 맡는다. 뒤집기로 하면 검사 시간이 늘고(지금도 계량 검사 하나가 나머지 전부보다 오래 걸린다), 어느 환경에서 재는 값이 기준인지 검사가 스스로 정해야 한다.

   대안 B: 계량 자체를 바꾼다. 프로세스 수 대신 셸 내장 비교의 양(모은 문자열 길이 × 후보 수 같은 결정적 값)을 함께 세어 사각지대를 없앤다. 가장 근본적이지만 계량 장치를 새로 설계하는 일이라 이 기획서보다 크고, TASK-106이 세운 장치의 재작성이 된다.

2. **프로세스 예산을 하나로 둘지, 역할별로 가를지.**

   제안: 지금처럼 하나로 두고 규칙대로 내린다. 사용자 지시가 "원래 규칙대로 내리는 것"이라 적었고, 그 규칙(3초 상당의 절반 이하)은 값 하나를 가리킨다. 상수가 하나면 근거도 한 자리에만 적히고, 어느 분기가 어느 예산을 쓰는지 읽는 사람이 헷갈릴 자리가 없다.

   비용: 확인 사실 8이 적은 "절대 상한이 놀고 있다"가 더 심해진다. 지금은 개발자 분기가 2,743개로 예산 3,000에 붙어 있어 그 하나만 실질적으로 감시받는데, 개발자 분기가 내려오면 세 분기 전부가 예산에서 멀어진다. 그러면 후보당 상수가 몇 개 느는 회귀는 비율 단언(3.5배)에만 걸리고, 세 분기가 모두 상수인 상태에서는 비율도 1.00 근처라 그것마저 무디다.

   대안: 역할별로 가른다. 각 역할의 착지 후 3배 실측 위에 여유를 둔 값을 따로 두고, 셋 다 규칙의 상한 아래에 둔다. 세 분기 각각에서 "상수가 늘었다"가 잡히므로 감시가 실제로 일한다. 뒤집기로 하면 상수가 셋이 되고 값마다 근거를 적어야 하며, 역할 하나를 고칠 때마다 그 역할의 예산을 다시 정하는 절차가 생긴다.

3. **PowerShell 본문의 상한을 어디까지 확인할지.**

   제안: 두 본문에 같은 어법을 요구하되, 상한 검증은 sh 본문으로만 하고 PowerShell 쪽은 확인 사실 9의 시나리오 표가 판정 동일성을 덮는 것으로 둔다. 근거는 현행 한계 그대로다 — 계량 장치는 유닉스 전용이고, PowerShell 본문은 파일마다 프로세스를 띄우지 않는 구조라 같은 계량이 성립하지 않으며, 지난 세 작업 전부가 로컬에 `pwsh`가 없어 그 본문을 실행하지 못했다고 적었다.

   비용: PowerShell 본문의 판정 비용은 아무도 재지 않는다. 그 본문이 느려지는 회귀는 Windows에서 데몬을 돌리는 사람이 타임아웃으로 발견한다.

   대안: Windows 러너에서 도는 상한 검사를 새로 만든다. 사각지대가 닫히지만, 그쪽 구현에 맞는 계량을 새로 설계하고 러너에서 재는 값의 안정성을 따로 확보해야 한다. 확인 필요 1번의 대안 B와 성격이 같은 크기의 일이다.

## 기대효과

- SPEC-033이 세운 상한이 세 분기 모두에서 닫힌다. 지금은 "아키텍트는 고쳤고 개발자는 남았다"가 완료 조건 미충족으로 문서에 적혀 있는 상태다.
- 개발 잡이 조용히 뜨지 않는 날이 밀린다. 지금 문서량에서 개발자 분기는 1.32초로 한도 안이지만 3배에서 7.10초이고, 확인 사실 3의 구조상 그 값은 작업 문서 수에 그대로 비례한다. 3배는 이 워크플로우의 성장 속도로 며칠 치다.
- 예산이 약속대로 내려가 감시가 다시 조인다. 3,000은 "오늘의 최댓값 바로 위"라 개발자 분기 하나만 보고 있었다.
- 세 분기가 같은 비용 모델을 갖는다. 다음 규칙 확장이 어느 분기에 들어와도 "후보마다 디렉터리를 다시 읽지 않는다"는 같은 문장이 적용되고, 지금처럼 한 분기만 옛 구조로 남는 상태가 없어진다.
- 데몬이 같은 프로젝트의 세션을 여럿 띄우는 날의 전제가 선다. 병렬은 판정 빈도를 곱하므로, 가장 비싼 분기가 남아 있는 상태로 병렬을 켜면 타임아웃이 먼저 온다.

## 완료 조건

1. 조건 스크립트의 개발자 분기가 3배 픽스처에서 3초 안에 끝난다. 검증: REPORT-TASK-104-DEV와 같은 방법(임시 디렉터리 픽스처, catch-all 문서로 조기 종료를 막고 세 역할이 모두 `no-target`으로 끝나는 것을 확인)으로 재고 전후 값을 보고서에 적는다. 기준선은 확인 사실 1·2의 인용값이다. (R1)
2. 1배 픽스처에서도 상한 안이고, 기획자·아키텍트 분기의 값이 나빠지지 않는다. 검증: 같은 측정의 여섯 줄을 보고서에 적는다. (R1)
3. 개발자 분기의 프로세스 계량이 착수 전(1배 919 · 3배 2,743)보다 내려가고, 비율 단언이 그대로 통과한다. 검증: TASK-106이 세운 계량 검사. (R2)
4. 프로세스 예산이 확인 사실 4의 규칙 안으로 내려가고, 값의 근거가 주석과 보고서 두 자리에 적힌다. 통과시키려고 올린 자리가 없다. 검증: 상수 값과 그 주석, 그리고 보고서. (R5)
5. 이 저장소의 실제 문서에서 세 역할의 사유 코드와 종료 코드가 착수 전과 착지 후에 같다. 검증: 두 본문을 각각 실행해 대조하고 보고서에 적는다. 값이 달라졌다면 그 차이가 이 변경의 것이 아님을 근거와 함께 적는다. (R3)
6. 앱과 스크립트를 대조하는 기존 시나리오가 기대값 수정 없이 통과한다. 판정 규칙을 바꾸지 않았으므로 기대값이 뒤집힌 검사가 하나도 없어야 한다. 검증: `role_eligibility.rs`의 대조 테스트. (R3·R4)
7. 두 플랫폼 본문이 같은 규칙을 담고, `CONDITION_SCRIPT_VERSION`과 두 본문의 버전 줄이 함께 오른다. 기존 시나리오 표가 기존 행 수정 없이 통과한다. 검증: 버전 일치 단언 테스트와 시나리오 표. (R4)
8. 두 구현의 알려진 차이 목록에 적지 않은 차이가 없다. 검증: 목록과 변경분 대조를 보고서에 적는다. (R4)
9. 조건 스크립트가 `.workflow` 아래에 아무것도 쓰지 않는다. 검증: 판정 전후로 워크플로우 트리와 lease 디렉터리의 (경로, 내용)이 같음을 고정하는 기존 검사가 통과한다. (R3)
10. 사유 코드가 여전히 표준 출력의 첫 줄이자 유일한 줄이고 새 코드가 없다. 검증: 세 역할 실행의 표준 출력이 한 줄인지 확인한다. (R3)
11. 확인 필요 1·3번에서 정해진 대로 검증 장치가 서거나 서지 않고, 남는 사각지대가 주석과 보고서에 적힌다. 검증: 변경분과 보고서. (R6)
12. 기존 자동 테스트가 삭제되거나 비활성화되지 않는다. 기존 시나리오 행 이름과 대조 함수 이름이 줄지 않는다. 검증: 이름 집합 대조를 보고서에 적는다. (R7)
13. 이 기획서의 작업이 `heartbeat_condition.rs`·`role_eligibility.rs`를 잡는 다른 작업과 병행되지 않도록 겹침 선언과 선행 선언이 적힌다. 검증: 분해 결과의 `scope_files`와 `depends_on`. 확인 사실 11이 근거다. (R7)
14. `npm run check`와 `cargo test --manifest-path src-tauri/Cargo.toml`이 통과한다.

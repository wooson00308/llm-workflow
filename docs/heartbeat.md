# 하트비트 가이드

LLM Workflow는 LLM을 실행하지 않는다. 앱은 프로젝트의 Markdown/YAML 파일로 상태를 관리할 뿐이고, "누가 언제 역할 세션을 깨우는가"는 전적으로 사용자 환경(스케줄러, 반복 실행 명령, CI 잡)의 책임이다. 이 문서는 그 주기적 기동을 하트비트라고 부르고, 세 역할을 각각 어떤 잡으로 돌리는지, 처리할 대상이 있는지 어떻게 판정하는지, 그 판정이 선점 프로토콜과 어떤 관계인지를 정리한다.

## 하트비트의 기본 구조

모든 역할 잡은 같은 세 단계를 갖는다.

1. 조건 검사: `scripts/wf-eligible.sh`로 그 역할이 처리할 대상이 있는지 확인한다.
2. 역할 세션 실행: 대상이 있을 때만 LLM 세션을 한 번 실행한다.
3. 종료: 세션이 끝나면 잡은 아무것도 하지 않는다. 다음 주기에 1번부터 다시 검사한다.

잡은 세션의 출력을 해석하지 않고 후처리도 하지 않는다. 상태 전이는 전부 세션이 문서에 기록하며, 잡이 결정하는 것은 기동 여부뿐이다.

아래 예시의 `<llm-cli>`는 자리표시자다. 사용자가 쓰는 LLM CLI 실행 명령으로 그대로 교체하면 되고, 특정 제품 전용 설정을 전제하지 않는다. 프롬프트 문자열을 인자로 받는 CLI든 표준 입력으로 받는 CLI든 "조건 검사 명령 + 역할 세션 실행 명령"이라는 조합은 동일하다.

## 역할 잡 3종

### 기획자

깨어나는 대상은 어떤 기획서도 참조하지 않은 미처리 아이디어, 그리고 `revision_requested` 결정에 따른 기획서 재작업이다. 다만 후자는 현재 조건 스크립트가 감지하지 못한다. [현재 한계](#현재-한계)를 참조한다.

```sh
cd /path/to/project || exit 1
sh scripts/wf-eligible.sh planner || exit 0
<llm-cli> "기획자 역할로 진행해줘. .workflow의 공통 규칙과 planner 역할 계약을 따르고, 처리할 대상이 없으면 NO_ELIGIBLE_WORK만 보고하고 멈춰."
```

프롬프트에는 세 가지가 들어가야 한다. 역할을 기획자 하나로 고정할 것, 공통 규칙 `.workflow/rules/workflow.md`와 역할 계약 `.workflow/rules/roles/planner.md`를 읽고 따를 것, 처리할 대상이 없으면 파일을 바꾸지 말고 `NO_ELIGIBLE_WORK`만 보고한 뒤 정지할 것.

권장 주기는 30분이다.

### 프로젝트 아키텍트

깨어나는 대상은 최신 결정이 `approved`인 기획서다.

```sh
cd /path/to/project || exit 1
sh scripts/wf-eligible.sh architect || exit 0
<llm-cli> "프로젝트 아키텍트 역할로 진행해줘. .workflow의 공통 규칙과 architect 역할 계약을 따르고, 처리할 대상이 없으면 NO_ELIGIBLE_WORK만 보고하고 멈춰."
```

프롬프트 요지는 기획자와 같고 역할 계약만 `.workflow/rules/roles/architect.md`로 바뀐다.

권장 주기는 30분이다. 아키텍트가 깨어나는 조건인 승인은 사용자가 앱에서 내리는 것이라 하루에 몇 번 일어나지 않는다. 더 짧게 잡아도 대부분의 기동이 헛도는 검사로 끝난다.

### 개발자

깨어나는 대상은 `status: todo`인 개발 작업이다.

```sh
cd /path/to/project || exit 1
sh scripts/wf-eligible.sh developer || exit 0
<llm-cli> "개발자 역할로 진행해줘. .workflow의 공통 규칙과 developer 역할 계약을 따르고, 처리할 대상이 없으면 NO_ELIGIBLE_WORK만 보고하고 멈춰."
```

프롬프트 요지는 동일하고 역할 계약은 `.workflow/rules/roles/developer.md`다.

권장 주기는 15분이다. 아키텍트 세션 한 번이 `todo` 작업을 여러 건 만들기 때문에, 세 역할 중 대상이 가장 자주 쌓이는 쪽이 개발자다.

### 주기를 정하는 근거

역할 세션 한 건은 문서를 읽고 코드를 고치고 검증까지 하므로 대체로 수 분에서 수십 분이 걸린다. 주기가 그 소요 시간보다 짧으면, 앞선 세션이 아직 lease를 쥔 채 작업하는 동안 새 세션이 계속 깨어난다. 새로 깨어난 세션은 대상이 이미 선점되어 있으니 `NO_ELIGIBLE_WORK`로 끝나고, 진행되는 일 없이 중복 기동 비용만 늘어난다. 반대로 주기를 너무 늘리면 승인이나 작업 생성 이후의 대기 시간만 길어진다.

그래서 권장값의 기준은 "한 건을 처리하는 데 걸리는 시간과 비슷하거나 그보다 긴 주기"다. 위 숫자는 출발점이고, 실제 세션 소요 시간을 재서 조정한다.

## 조건 스크립트 사용법

`scripts/wf-eligible.sh`는 역할별로 지금 처리 가능한 대상이 있는지만 판정한다. 파일을 만들거나 고치지 않는 읽기 전용 스크립트다.

- 실행 위치: 프로젝트 루트. 스크립트가 `.workflow/...` 상대 경로를 쓰므로 다른 위치에서 실행하면 판정이 틀어진다.
- 호출: `sh scripts/wf-eligible.sh <role>`. 파일 모드에 실행 권한이 없으므로 `sh`로 실행한다.
- 인자: `planner` | `architect` | `developer` 중 하나.

종료 코드는 세 가지다.

| 코드 | 의미 |
| --- | --- |
| `0` | 처리 가능한 대상이 있다 |
| `1` | 없다 |
| `2` | 잘못된 사용법. 인자를 생략한 경우도 포함한다 |

`.workflow/.runtime/migration.lock`이 있으면 역할 인자를 해석하기 전에 `1`을 반환한다. 마이그레이션 중에는 기동을 억제하는 것이 목적이라, 이때는 잘못된 역할 이름을 줘도 `2`가 아니라 `1`이 나온다.

위 예시들이 쓴 `|| exit 0` 형태는 `1`과 `2`를 구분하지 않는다. 잡을 처음 설정할 때는 인자 오타가 조용히 묻히지 않도록 종료 코드를 직접 비교해 한 번 확인하는 편이 낫다.

판정 단위는 개별 워크플로우가 아니라 프로젝트 전체다. 스크립트는 `.workflow/*/` 아래 등록된 모든 워크플로우 디렉터리를 훑고, 그중 하나라도 대상이 있으면 `0`을 반환한다.

### 역할별 판정 기준

세 역할 모두 "이미 후속 문서가 참조하는 대상"과 "이미 lease가 있는 대상"을 제외한다.

- `planner`: 아이디어의 `id`에 대해, 그 id를 `source_idea_id`로 참조하는 기획서가 있으면 제외하고, `leases/<아이디어-id>.yml`이 있으면 제외한다.
- `architect`: `outcome: approved`인 결정의 `id`에 대해, 그 id를 `source_decision_id`로 참조하는 작업이 있으면 제외하고, 그 결정의 `spec_id`로 만든 `leases/<기획서-id>.yml`이 있으면 제외한다. 아키텍트가 선점하는 대상은 결정이 아니라 기획서이므로 lease 이름도 기획서 id를 쓴다.
- `developer`: `status: todo`인 작업의 `id`에 대해 `leases/<작업-id>.yml`이 있으면 제외한다.

### 현재 한계

이 문서는 이후 스크립트 변경의 계약 기준점이다. 역할 계약의 이상적 서술과 스크립트의 실제 판정이 어긋나는 지점을 아래에 남겨 둔다.

- lease는 파일 존재 여부만 본다. 스크립트가 `expires_at`을 읽지 않으므로, 만료된 lease 파일이 남아 있으면 그 대상은 계속 제외된다. 세션이 비정상 종료해 lease를 남긴 경우 해당 대상은 파일을 치울 때까지 하트비트에 잡히지 않는다.
- `planner` 판정은 미처리 아이디어만 훑는다. `revision_requested` 결정에 따른 기획서 재작업은 현재 감지하지 못하므로, 수정 요청 재작업은 사용자가 직접 세션을 띄워야 한다.
- `architect` 판정은 결정 파일 단위로 `approved`를 찾을 뿐, 그것이 해당 기획서의 최신 결정인지는 확인하지 않는다.

## 선점 프로토콜과의 관계

조건 스크립트는 "지금 깨울 가치가 있는가"를 판단하는 사전 필터이며, 선점 그 자체가 아니다. 실제 클레임은 세션이 `.workflow/.runtime/leases/<문서-id>.yml`을 배타적으로 생성해서 수행한다. 파일 계약의 상세는 [파일 계약](file-contract.md)을 참조한다.

검사 시점과 세션이 실제로 lease를 만드는 시점 사이에는 시간 간격이 있다. 그 사이에 다른 세션이 같은 대상을 먼저 잡을 수 있으므로, **스크립트가 `0`을 반환해도 세션이 `NO_ELIGIBLE_WORK`로 끝날 수 있다.** 이것은 버그가 아니라 정상 동작이다. 잡은 이 결과에 대해 재시도하지 않고 다음 주기를 기다린다.

하트비트 잡이 절대 하면 안 되는 것이 두 가지 있다.

- 스크립트 결과를 근거로 세션 대신 lease를 만드는 것. 클레임은 실제로 작업을 수행하는 세션만 한다. 잡이 미리 만들어 두면 세션이 자기 lease에 막히거나, 세션이 뜨지 않은 채 대상이 잠긴다.
- 만료되지 않은 다른 세션의 lease를 지우거나 갱신하는 것. 만료된 lease만 교체 대상이며, 그 판단과 교체도 세션의 몫이다.

여러 역할 잡을 동시에 돌려도 된다. 역할별 대상 문서가 다르고, 클레임이 파일시스템의 배타적 생성으로 보호되기 때문이다. 두 세션이 같은 문서를 동시에 선점하려 하면 한쪽은 반드시 실패하고 `NO_ELIGIBLE_WORK`로 끝난다. 같은 역할의 잡을 여러 개 돌리는 것도 같은 이유로 안전하지만, 서로 파일이 겹치는 작업을 병렬로 진행하지 않도록 작업 문서의 의존성 기술을 지켜야 한다.

# 2026-08-07 개발 로그

## TL 세션 — 독스 워크플로우 로그 규격 개정 (사용자 위임)

- 배경: 2026-08-06 로그 감사에서 병렬 세션 충돌 흔적 확인 — TASK-131 원본(18:00)·재작업(18:10) 섹션 순서 뒤집힘 1건, 산출물은 있는데 로그 섹션이 없는 세션 2건(SPEC-048 분해, SPEC-050 기획). 하루 파일 240KB(약 11.7만 토큰) 실측.
- 결정(사용자가 규격 설계 위임): ① LLM Workflow 역할 세션은 개발 로그 면제 — 기록 정본은 reports/·작업 문서 ② 로그 쓰기는 셸 append 전용, 전체 재작성 금지 ③ 팔로업은 docs/handoff.md를 로그보다 먼저 읽기.
- 변경: `~/.claude/rules/docs-workflow.md` 개정(글로벌 룰), TL 메모리 갱신. 하트비트 세션은 다음 기동부터 새 룰을 읽는다. codex는 이 룰의 구속 대상이 아니며 저장소 관례가 줄면 자연 수렴 예상.
- 효과 추정: 역할 세션당 로그 작성 1-2천 토큰 제거 + 하루 파일 크기 대폭 축소 + 충돌 표면 소멸. 관측은 오늘 이후 로그 파일 크기로.

## TL 직접 수정 — 일괄 QA 전 건 성공 시 다이얼로그 자동 닫힘

- 사용자 결함 보고: 레인 일괄 QA 확인을 2단계 무장으로 실행해도 다이얼로그가 안 닫힘. 원인은 결함이 아니라 설계 — 건별 결과 표시(SPEC-031 R4)를 위해 실행 후 열어 두는 구조였는데, 전 건 성공일 때는 읽을 실패가 없어 붙들 이유가 없었다.
- 변경: `DevelopmentBoard.tsx` LaneQaDialog `run()` — 전 건 기록 성공이면 결과 화면 대신 닫는다. 실패 1건 이상 또는 호출 실패는 기존대로 열어 두고 건별 표시(R4 보존). 테스트 2건의 기대값을 새 동작으로 수정(전 건 성공 → 다이얼로그 소멸), 삭제·비활성화 0건.
- 게이트: DevelopmentBoard 80/80, npm run check 통과. 사용자 직접 지시의 파이프라인 밖 수정.

## TL 세션 — v0.1.10 배포 진행 (GitHub 장애로 일시 중단)

- 사용자 지시로 브랜치·PR 없이 main 직행 배포. 컷 게이트 4종 로컬 그린 → 내용 커밋(land QA batch) + 범프 커밋(0.1.10) 푸시.
- 1차 CI 빨강: 윈도우 clippy — custom_rules.rs 테스트의 root 바인딩이 유닉스 전용 블록에서만 사용됨. _root 리네임 5줄(ffba525)로 수정, 2차 CI 전 갈래 그린. SPEC-044 규범이 첫 컷에서 빨간 커밋 태그를 실제로 막았다.
- v0.1.10 태그 푸시(ffba525) → 릴리스 빌드 맥·윈도우 성공, 리눅스만 GitHub Actions 부분 장애(Set up job에서 Service Unavailable, 상태 페이지 partial outage 확인)로 2회 실패. SPEC-044 승인 범위대로 인프라 실패 한정 재실행 1회 수행.
- dev 브랜치 삭제(로컬+원격, main에 완전 병합·50커밋 뒤처짐). claude-heartbeat group 디스패치도 그쪽 main에 커밋·푸시(99969ec).
- 사용자 결정으로 야간 감시 종료. 남은 절차: ① Actions 복구 확인(githubstatus.com) ② `gh run rerun 31116563398 --failed` ③ 그린 후 자산 3종+latest.json 확인 ④ `gh release edit v0.1.10 --draft=false`. 게시는 사용자가 이미 위임("배포까지 부탁할게").

## 팔로업 세션 — v0.1.10 배포 마무리 (게시 완료)

- 목표: 전 세션 인수인계분 배포 잔여 절차 완료. handoff.md 우선 독해 규격 첫 적용.
- 진행: githubstatus 전 시스템 정상 확인 → `gh run rerun 31116563398 --failed` → 리눅스 포함 3 잡 전부 그린 → 자산 14종(리눅스 rpm/AppImage/deb, 맥 dmg/app.tar.gz, 윈 exe/msi, 각 sig, latest.json) 확인 → latest.json version 0.1.10·11개 플랫폼 키 전부 서명 포함 확인 → `gh release edit v0.1.10 --draft=false` 게시(2026-08-07T04:19Z). 게시 위임은 전 세션 사용자 지시.
- 부수 작업(사용자 지시): `~/.codex/AGENTS.md` 독스 워크플로 절이 7/10 미러 구버전이라 개정 3종(역할 세션 로그 면제·handoff 우선·append 전용)을 영어로 반영. 코덱스 내 다른 사본 없음 확인.
- 후속: 배포 트랙 잔여 없음. QA 도장 4건(TASK-139~142)은 사용자 관문으로 유지.

## 팔로업 세션 — QA 도장 반영 (정정)

- 사용자가 TASK-139~142 QA 확인을 직접 완료(QA-9CB7026D). 보드 실측 138/138 completed, qa_waiting 0.
- handoff.md 보드·사용자 관문 갱신. 직전 섹션의 "QA 도장 4건 대기 유지" 문장은 본 섹션으로 대체.

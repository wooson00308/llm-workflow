# 2026-08-05 개발 로그

## TL 인수인계 세션 (00:00~00:15 KST) — 전판 상태 실측

### 목표 / 요청 범위
- 이전 세션(2026-08-04 TL 웨이브·커밋 컷) 팔로업, TL 인수인계 접수. 상태 실측만 하고 지휘 행동은 하지 않음.

### 실측 결과 (판 상태)
- 작업 보드: completed 89 / qa_waiting 3(TASK-094·095·097) / in_progress 1(TASK-096) / todo 1(TASK-098) / blocked 0. TASK-022는 grep 오탐(본문 예시 문자열) — 실제 completed.
- 라이브 세션: 하트비트 아키텍트가 SPEC-031 분해 중 (lease-21454, heartbeat 15:00Z, 만료 15:45Z). 데몬 가동 중(PID 26886, 21:37 KST 기동).
- 승인: SPEC-031(DECISION-1FAA8251)·SPEC-032(DECISION-0D79A7F0) 앱 도장 완료(13:17~18Z, created_by: user). SPEC-032는 분해 대기 — 아키텍트가 031 다음에 집을 것.
- 아이디어 인박스 2건: IDEA-455EC49A(문서 스케일·스캔 타임아웃), IDEA-FCDDD4DE(데몬 업데이트 안내 카드). 기획자 대상.
- git: 현 브랜치 claude/fix-windows-clippy-tempdir는 origin/main과 내용 동일(PR #18 머지 완료, 클리피 2커밋 포함). 미커밋 변경 = TASK-097 착지분(heartbeat_condition.rs·role_eligibility.rs) + TASK-096 진행분(DevelopmentBoard.tsx/.test.tsx·App.css) + 워크플로 문서.

### 발견한 문제 (인수인계 핵심)
1. TASK-096 세션 사망. lease 만료(13:40:52Z, 마지막 heartbeat 13:10:52Z), 상태 in_progress 방치, 보고서 없음. 진행분이 트리에 남았는데 DevelopmentBoard.test.tsx 44건 전원 실패 — 원인은 테스트 33행 `afterEach(() => localStorage.clear())`가 jsdom에서 TypeError(localStorage.clear is not a function), 일부 테스트는 본문에서도 "사용자 QA 버튼 다중 매치" 실패. 이 상태로는 npm run check 게이트를 쓰는 모든 후속 세션(당장 TASK-098)이 막힌다.
2. claude-heartbeat 재시작 시한폭탄. dream-heartbeat는 editable 설치(~/Git/claude-heartbeat/src 직결)인데 21:46 KST에 체크아웃이 v0.8.0-jobsd → main으로 이동 + pull. 현 데몬 프로세스(21:37 기동)만 v0.8.0 코드를 메모리에 들고 있고, 디스크는 jobs.d 미지원 main. HEARTBEAT.md는 부재, 잡은 전부 jobs.d(workflow-labs + mech-arena). KeepAlive라 크래시·재부팅·bootout 어느 쪽이든 재기동 순간 잡 0개로 조용히 전체 정지. 서브프로세스로 새로 뜨는 heartbeat CLI도 이미 main 코드를 읽는다.
3. claude/v0.8.0-jobsd 브랜치는 push 완료·PR 미생성. main 대비 +3 커밋(6042cc3·2fa0a7d·25e372e[문서 4줄])·-1 커밋(behind).

### 후속 / 리스크 (사용자 관문 포함)
- 긴급: v0.8.0-jobsd PR/머지(또는 최소한 체크아웃 복귀) — 위 2번 해소 전까지 데몬 재시작 금지.
- 긴급: TASK-096 처리 방침(TL 워커 인수 재개 vs 되돌리고 재배정) — 트리 레드 해소 전 TASK-098 착수 위험.
- 사용자 도장: QA 3건(094·095·097), 수기 결정 21건 소급 재가 방법, 만료 lease SPEC-009.yml 정리.
- v0.1.8 릴리스 결정, claude-heartbeat 0.8.0 릴리스 결정.

### 변경한 파일
- docs/development-logs/2026-08-05.md (이 문서). 코드·워크플로 문서 무변경, lease 무접촉.

## TL 지휘 세션 (00:30~00:50 KST) — TASK-096 인수 완주

### 목표 / 요청 범위
- 사용자 지시: "QA 확인했어, 96 재시동, 필요한 권한 위임". 죽은 세션의 TASK-096을 워커로 인수 완주시킨다.

### 실행
- 착수 전 실측: 사용자 QA 도장으로 TASK-094·095·097·098 completed 확인. 그 사이 SPEC-031 분해로 TASK-099·100 신설(둘 다 `depends_on: TASK-096` — 096이 병목) 확인.
- 오퍼스 워커 tl-dev-096-r2 스폰(개발자 계약, 만료 lease 인수, 타 세션 파일 무접촉·결정 문서 금지·커밋 금지 명시, 종료 시 SendMessage 보고 필수).
- 워커 착지(15:44Z qa_waiting, lease 반납 15:45Z). TL은 문서 단위 검증(상태·history·lease)만 수행, 게이트는 보고 수치 채택(운영 규범).

### 결과 (워커 보고 수치)
- 원인 확정: 죽은 세션이 더한 `afterEach(() => localStorage.clear())` 3줄이 44건 전멸의 단일 원인(검사 환경 전역 localStorage는 메서드 없는 빈 객체). "이중 렌더 의심"은 cleanup 누락의 2차 증상이었음.
- 죽은 세션 잔여물 평가: 제품 코드 배선 대부분 살림(#unassigned 상수 통합 포함), 깨진 3줄만 걷어냄, 작업 문서의 검사 7건은 전부 새로 작성.
- 게이트: DevelopmentBoard.test.tsx 51/51(기존 44건 무수정 통과 + 신규 7), 저장소 테스트 포함 67/67, npm run check exit 0(19파일 508건, vite build 326 modules). Rust 무변경으로 cargo 생략.
- 보고서: 서브에이전트 파일 쓰기 차단으로 TL 대리 기록 — `reports/REPORT-TASK-096-DEV.md` (기록 경위 머리말 형식, 발신 전문 그대로).

### 후속 / 리스크
- 트리 레드 해소 → npm 게이트 쓰는 후속 세션 정상화. TASK-099·100 선행 충족 — 하트비트 자연 소화 예상.
- 하트비트 병행 산출 관측: 기획자가 IDEA-455EC49A 처리 완료(lease 소멸), 아키텍트가 SPEC-032 분해 착수(lease 생성). 개입 불요.
- 사용자 관문(변동 없음): TASK-096 QA(레인 접기 — 보고서 "사용자 QA 제안" 6항이 동선), claude-heartbeat v0.8.0-jobsd PR/머지(데몬 재시작 시한폭탄 — 머지 전 재시작 금지), 커밋 컷 타이밍, 수기 결정 21건 소급 재가, 만료 lease SPEC-009.yml.

## 아키텍트 세션: SPEC-031 분해 (00:00~00:10 KST / 15:00~15:08Z)

### 목표 / 요청 범위
- 프로젝트 아키텍트 역할 하나, 대상 하나. 승인된 기획서를 구현 가능한 개발 작업으로 분해.
- 자격 후보: 최신 앱 소유 결정이 `approved`인데 그 결정을 참조하는 작업이 0건인 기획서 둘 — SPEC-031(DECISION-1FAA8251, 13:17:51Z), SPEC-032(DECISION-0D79A7F0, 13:18:02Z). 한 세션 한 대상 규칙대로 승인이 이른 SPEC-031을 집었다.
- 선점 `lease-21454-20260804150000` 취득(exit 0) → 갱신(exit 0) → 반납(exit 0). 클레임 직후 자격 재검증에서 참조 작업 0건 재확인.

### 핵심 결정과 근거
- **둘로 나눴다.** TASK-099(확인 전용 일괄 QA 명령 + 게이트웨이·훅), TASK-100(레인 헤더 액션 + 확인 화면 + 건별 결과). 승인된 확인 필요 1번이 "앱 명령 하나"를 정해 쓰기 경로와 화면이 서로 다른 파일 무리로 갈린다.
- **화면을 더 쪼개지 않았다.** "액션·확인 화면"과 "실행·결과"로 나누면 눌러도 아무 일이 없는 반쪽이 남고, 같은 두 파일(`DevelopmentBoard.tsx`·`App.css`)을 다시 직렬로 만지게 되어 이득이 없다.
- **명령 이름을 `confirm_task_qa_batch`로, `outcome` 파라미터는 만들지 않기로.** 일괄 반려가 기획서 제외 범위라 파라미터로 열어 두면 다음 사람이 뚫린 길로 읽는다.
- **건 하나 처리부를 private 함수로 뽑아 단건·일괄이 공유.** 확인 필요 1번이 스스로 적은 비용("QA 기록 규칙이 지켜지는 자리가 둘이 된다")을 가장 싸게 줄이는 방법. 단건 경로의 외부 동작 무변경을 조건으로 달았다.
- **훅 반환은 boolean이 아니라 건별 결과 배열 또는 null.** R4의 건별 보고가 이 값을 타고 화면으로 간다. 확인 사실 8의 전역 에러 문구에 얹지 않는다.
- **TASK-099에도 `depends_on: [TASK-095, TASK-096]`을 달았다.** 그 작업 범위에는 겹치는 파일이 하나도 없어 역할 계약이 경계하는 "이유 없는 직렬화"에 가깝지만, 승인된 완료 조건 14가 이 기획서의 작업 전체를 그 뒤에 세우라고 문언으로 요구한다. 아키텍트가 승인 조건을 자기 판단으로 좁히지 않았고, 대신 그 값(파일이 안 겹치는데도 함께 막힌다)을 보고서에 적었다.

### 변경한 파일
- `.workflow/도그푸딩--wf_ae6cd700/tasks/TASK-099.md` 신규 (`todo`, history `created`)
- `.workflow/도그푸딩--wf_ae6cd700/tasks/TASK-100.md` 신규 (`todo`, history `created`)
- `.workflow/도그푸딩--wf_ae6cd700/reports/REPORT-SPEC-031-ARCH.md` 신규
- `docs/development-logs/2026-08-05.md` (이 섹션)
- 제품 코드·기획서·결정 문서 무변경. lease는 헬퍼로만 취득·갱신·반납.

### 검증 단계와 결과
- 결정 122건 전수 스캔으로 기획서별 최신 앱 소유 결정 판정 → 미분해 `approved` 둘 확인.
- 작업 96건의 `source_decision_id` 전수 대조 → DECISION-1FAA8251·0D79A7F0 참조 0건.
- 분해 근거는 실제 코드에서 읽었다: `record_task_qa`(`fs_project_repository.rs:374`~`:427`), `summary_from_manifest` 1회 호출 자리, `generate_handler!`(`lib.rs:14`~`:28`), `recordTaskQa` 훅(`useProjectWorkspace.ts:262`~`:290`), `SpecLaneBoard`·`buildSpecLanes`·`laneSignal`(`DevelopmentBoard.tsx`), 가짜 게이트웨이(`useProjectWorkspace.test.ts:69`), 다이얼로그 어법(`ProjectSearchDialog.tsx:93`~`:106`).
- 코드는 읽기만 했다. 테스트는 돌리지 않았다 — 이 역할의 산출물이 문서뿐이다.

### 후속 / 리스크
- **두 작업 모두 지금 착수 불가.** 선행 TASK-096이 `in_progress`이고(선행은 `qa_waiting`·`completed`에서만 충족) 그 세션은 리스 만료·보고서 부재 상태다. TASK-096 정리가 이 파이프라인의 관문이다. 위 인수인계 세션의 후속 1번과 같은 자리.
- **트리 레드도 같은 관문에 걸린다.** TASK-100이 `DevelopmentBoard.tsx`·`App.css`를 만지므로, 인수인계 기록이 적은 `DevelopmentBoard.test.tsx` 44건 실패가 남아 있으면 "기존 검사가 수정 없이 통과한다"를 먼저 만족시킬 수 없다.
- **SPEC-032가 아직 분해 대기.** 다음 아키텍트 세션의 대상.
- **카드가 0장이라 목록에서 빠진 레인은 일괄 액션에 닿지 않는다.** SPEC-029가 정한 표시 규칙이고 SPEC-031 R8이 그대로 두라고 해서 범위 밖으로 남겼다. 값이 문제되면 새 아이디어가 필요하다.
- **역할 밖 관찰(무수정)**: 만료 lease `SPEC-009.yml`·`TASK-096.yml` 잔존. 판정은 lease를 고치지 않는다는 §4대로 손대지 않았다.

## 세션: TASK-098 개발 (계약 임시 불릿 제거 + 규칙 자산 버전 인상)

### 목표 / 요청 범위
- 개발자 역할 1건. `.workflow` 공통 규칙 §1~§7과 developer 계약을 따라 자격 있는 작업 하나만 처리.
- 자격 판정 결과 대상은 TASK-098 (SPEC-030 R4·R5). TASK-099·TASK-100은 선행 TASK-096이 `in_progress`라 미충족.

### 핵심 결정과 근거
- **앞 문장을 `Every judgement ignores it:`으로.** 원문 `Two judgements ignore it and one does not:`은 두 절 모두 거짓이 됐다 — 항목이 셋에서 둘로 줄었고 TASK-097 이후 기획자 판정까지 셋 다 무시한다. 셈을 다시 세는 대신 성격을 말하는 문장으로 바꿨다.
- **기획자 판정을 새 불릿으로 되살리지 않았다.** 적을 내용이 "이제 차이 없다"뿐이고, 없어진 결함을 계약이 계속 말하지 않는 것이 R4의 요구다.
- **`outcome` 값 목록 차이는 계약에 새로 적지 않았다.** 변경 전 본문에 원래 없던 내용이고 R4 셋째 항목이 그대로 두라고 적었다. 코드 주석(알려진 차이 5)에 남아 있어 찾을 자리는 유지된다.
- **착수 시점 값 8 → 9.** 고정값 가정 금지 요구대로 착수 시점에 읽어 +1. `WORKFLOW_RULES_VERSION`과 본문 `rules_version` 두 자리.

### 변경한 파일
- `src-tauri/src/infrastructure/project_instructions.rs` — 불릿 1개 삭제, 앞 문장 1줄 교체, 버전 두 자리 8→9, 테스트 단언 7건 8→9.
- `.workflow/도그푸딩--wf_ae6cd700/tasks/TASK-098.md` — `todo` → `in_progress` → `qa_waiting`, history 2건 추가.
- `.workflow/도그푸딩--wf_ae6cd700/reports/REPORT-TASK-098-DEV.md` 신규.
- `docs/development-logs/2026-08-05.md` (이 섹션).
- 역할 계약 세 본문·`ROLE_RULES_VERSION`·`heartbeat_condition.rs`·`role_eligibility.rs` 무변경. lease는 헬퍼로만 취득·갱신·반납.

### 검증 단계와 결과
- `cargo test --manifest-path src-tauri/Cargo.toml` → 404 passed / 0 failed / 0 ignored.
- `project_instructions` 필터 → 14 passed (설치·검증 경로 포함).
- `cargo fmt --check` exit 0, `cargo clippy --all-targets -- -D warnings` 경고 0건. 확인 사실 13이 적었던 타 세션 위반 2건(`heartbeat_status.rs` fmt, `heartbeat_process.rs:216` clippy)은 이미 사라졌다.
- `npm run check` **실패**. typecheck 3건 전부 `DevelopmentBoard.test.tsx`의 TS6133이고, `npm run test`는 44 failed / 457 passed(실패 전부 같은 파일). 이 작업 diff에 `src/` 파일이 0건이라 귀속 대상이 아니다 — 미커밋 상태인 TASK-096 세션 산출물이다. TASK-097 보고서(12:51Z)는 같은 게이트를 501 passed로 기록했고 TASK-096 착수가 13:05Z다.
- 무변경 확인은 파일·심볼 단위로 했다(작업 문서의 검증 문구 규칙). `ROLE_RULES_VERSION`=5, planner 5, architect 4, developer 4 — 착수 시점과 동일.

### 후속 / 리스크
- **프런트엔드 게이트는 TASK-096이 마무리돼야 초록으로 돌아온다.** TASK-096은 `in_progress`인데 lease가 13:40:52Z에 만료돼 멈춘 상태다. TASK-098 QA에서 이 게이트를 기준으로 삼으면 남의 작업에 막힌다.
- 설치본 `.workflow/rules/workflow.md`는 아직 `rules_version: 7`. 상수가 9가 됐으니 앱이 다음 설치를 돌릴 때 따라 올라간다. 설치본 직접 수정은 범위 밖이라 손대지 않았다.
- **역할 밖 관찰(무수정)**: 만료 lease `SPEC-009.yml`·`TASK-096.yml` 잔존. §4대로 판정은 lease를 고치지 않는다.

## 기획자 세션: IDEA-455EC49A → SPEC-033 (00:21~00:40 KST / 15:21~15:40Z)

### 목표 / 요청 범위
- 기획자 역할 하나, 대상 하나. 미처리 아이디어 또는 미답변 수정 요청 하나를 `user_review` 기획서로 만든다.
- 미답변 수정 요청 0건 확인(`revision_requested` 2건 모두 SPEC-013·SPEC-015가 응답 완료). 미처리 아이디어 2건은 `created_at` 동률(`2026-08-04T12:53:07Z`)이라 문서 id 순으로 IDEA-455EC49A 선택.
- 선점 `lease-34800-20260804152117` 취득(exit 0) → 갱신(exit 0) → 반납(exit 0). 취득 직후 SPEC-033을 `draft`로 만들고 본문을 썼다.

### 핵심 결정과 근거
- **아이디어의 본안(완료 문서 아카이브)을 이번 범위에서 뺐다.** 측정이 전제를 뒤집었다. 결정 문서 122→30건이면 아키텍트 분기가 13.37s→6.08s로 줄지만, 작업 문서 97→20건은 13.12s→12.24s로 거의 안 변한다. 가장 큰 디렉터리 `reports/` 144건은 조건 스크립트가 읽지도 않고 앱은 목록만 센다 — 판정 비용 0. 즉 완료 문서를 치워도 오늘의 타임아웃은 안 사라진다.
- **진짜 원인은 후보마다 같은 디렉터리를 다시 훑는 곱.** `wf-eligible.sh:144`~`:174`가 승인 결정 31건 각각에 대해 `decisions/` 122건을 재스캔하고, 안쪽 한 바퀴마다 프로세스를 최대 4개 띄운다. 판정 한 번에 프로세스 6천 개 이상. 기획자 분기(`:124`~`:132`)도 같은 모양인데 바깥 루프가 지금 2건이라 아직 싸다 — 확인 사실 6에 적어 두었다.
- **아카이브가 깨뜨리는 참조 판정을 다섯 개 세었다.** 아이디어가 든 "아키텍트 이중 분해"보다 개발자 선행 판정이 더 직접적이다 — 완료 작업을 `tasks/`에서 치우면 `task_file`이 문서를 못 찾아 미충족이 되고 후행 작업이 전부 닫힌다.
- **앱 조회(2.5초 폴링)는 "고장 난 자리"로 취급하지 않았다.** 폴링 한 번에 파일 본문 읽기 534회이고 세 디렉터리를 각각 두 번 훑지만, 측정된 실패도 사용자 증상도 없다. 이유가 사라진 중복만 걷어내는 선까지 제안하고 캐시·증분은 확인 필요 4번으로 올렸다.
- **파생 인덱스를 제안에서 뺀 근거.** 역할 세션은 앱을 거치지 않고 문서를 직접 쓰므로 앱만 갱신하는 인덱스는 판정이 돌아야 하는 바로 그 순간 가장 어긋난다. 그리고 `docs/file-contract.md:79`·`:172`가 "조회 시점 파생, 어디에도 저장하지 않는다"를 네 자리에서 같다고 못박고 있다.

### 검증 단계와 결과
- 실패 실물 확인: `~/.claude/heartbeat/heartbeat_20260804.log`에 `condition 타임아웃 (10s) → 안전을 위해 skip` 4건, 전부 `wf-architect-...` 잡(20:46:17·21:38:05·21:54:05·22:12:11 KST). 기획자·개발자 0건, 8/2·8/3 로그 0건.
- 한도 출처 확인: `~/Git/claude-heartbeat/src/heartbeat/core.py:247`의 `timeout=10` 하드코딩. 잡 문법의 `timeout`은 세션용(`:355`·`:416`)으로 별개. 초과 시 fail-closed(`:251`·`:373`).
- 재현: `.workflow`를 `/tmp` 픽스처로 복사하고 모든 대상을 처리 완료로 만들어 조기 종료를 없앤 뒤 측정. 설치본 v6 planner 1.35s / architect 12.67s / developer 0.47s, 작업 트리 v7 1.47s / 12.41s / 0.48s.
- 스케일 측정: 결정 30·60·90·122건 → 6.08s·8.73s·10.51s·13.37s. 작업 97→20건 → 13.12s→12.24s.
- 저장소 파일은 읽기만 했다. 측정은 전부 `/tmp` 픽스처였고 정리했다. 제품 코드·테스트 무변경이라 게이트는 돌리지 않았다.

### 변경한 파일
- `.workflow/도그푸딩--wf_ae6cd700/specs/SPEC-033.md` 신규 (`user_review`, 확인 사실 17건·요구사항 R1~R9·확인 필요 4건·완료 조건 14건)
- `.workflow/도그푸딩--wf_ae6cd700/reports/REPORT-SPEC-033-PLAN.md` 신규
- `docs/development-logs/2026-08-05.md` (이 섹션)
- 아이디어·결정·작업·앱 소스·규칙 자산 무변경. lease는 헬퍼로만 취득·갱신·반납.

### 후속 / 리스크
- **사용자 결정 4건 대기.** ① 아카이브를 넣을지(제안: 뺀다) ② 파생 인덱스 vs 반복 훑기 제거(제안: 후자) ③ 목표 상한(제안: 최악 3초, 문서량 3배에서도 유지) ④ 앱 조회 범위(제안: 이유 사라진 중복만).
- **SPEC-032와 파일이 겹친다.** 둘 다 `heartbeat_condition.rs`·`role_eligibility.rs`를 손댄다. 둘 다 승인되면 나중에 서는 쪽이 선행 선언으로 순서를 적어야 한다(SPEC-033 완료 조건 13).
- **SPEC-032 확인 사실 22가 낡았다.** TASK-097·098을 `todo`로 적었는데 지금 둘 다 `completed`다. 그 기획서는 결정 대기 중이라 이 세션은 손대지 않았다.
- **미처리 아이디어 1건 남음**: IDEA-FCDDD4DE(연동 카드 데몬 갱신 안내). 다음 기획자 세션 대상.
- **아키텍트 대기 물량 있음**: 착수 시점 `wf-eligible.sh architect`가 `eligible`/0. 다음 아키텍트 세션의 자리다.
- **역할 밖 관찰(무수정)**: 만료 lease `SPEC-009.yml`·`TASK-096.yml` 잔존.

## 아키텍트 세션 (00:40~00:55 KST) — SPEC-032 분해

### 목표 / 요청 범위
- 프로젝트 아키텍트 역할로 승인 결정 하나를 개발 작업으로 분해. `wf-eligible.sh architect` exit 0.
- 대상은 DECISION-0D79A7F0(SPEC-032 승인, `created_by: user`, 2026-08-04T13:18:02Z). 미파생 승인 결정은 이 하나뿐이었다.
- 선점 `lease-42307-20260804154000` 취득(exit 0) → 갱신(exit 0) → 반납(exit 0). 클레임 직후 파생 작업 0건 재확인.

### 핵심 결정과 근거
- **작업 셋으로 쪼갰다.** TASK-101(판정 구현) → TASK-102(계약 문언) → TASK-103(화면). 세 작업의 범위 파일은 교집합이 없다.
- **TASK-101을 더 쪼개지 않았다.** R3이 조건 스크립트 두 본문과 앱 판정의 동일 답을 요구하고 `assert_matches_condition_script`가 그것을 고정한다. 한쪽만 먼저 착지하면 새 픽스처에서 두 판정이 갈린다. 선언 파서도 sh·PowerShell·Rust 셋이 같은 결론을 내야 한다. TASK-097이 같은 이유로 두 본문을 한 작업에 묶은 선례를 따랐다.
- **계약이 구현보다 뒤에 선다.** TASK-102가 TASK-101을 선행으로 적는다. 판정이 필드를 읽지 않는 동안 계약이 그것을 요구하면 계약을 지킨 세션이 아무 보호도 못 받는다. TASK-097 → TASK-098의 순서 근거 그대로다.
- **아키텍트가 필드 이름과 표기를 고정했다.** 기획서가 "범위 파일 목록"까지만 정하고 이름·표기를 남겨 뒀는데, 세 구현과 계약 문언이 같은 이름을 써야 한다. `scope_files`, `depends_on`과 같은 열 0 한 줄 흐름 시퀀스, 허용 문자는 `depends_on` 집합 + `.` `/`, 비교는 문자열 완전 일치(정규화·글롭·대소문자 접기 없음).
- **승인된 확인 필요 2번을 넓은 쪽으로 읽었다.** "선언 없는 작업은 활성 lease가 하나라도 있으면 제외"에서 lease가 잡은 것이 작업 문서가 아니어도 센다. 근거는 기획서 자신의 비용 문단("다른 문서를 잡은 lease가 살아 있는 동안"). 좁은 읽기(작업 lease만)는 버렸고 그 사실을 보고서에 적었다. 대칭성은 파생 — lease가 잡은 작업 쪽 선언이 없거나 형식 오류여도 후보를 막는다.
- **이번 분해 자체에는 `scope_files`를 쓰지 않았다.** 계약이 없는 필드를 아키텍트가 먼저 쓰면 TASK-102의 순서 근거가 뒤집히고, TASK-101 완료 조건 12가 쓰는 회귀 전제("이 저장소에 선언이 하나도 없다")도 깨진다.
- **기획서가 몰랐던 겹침 셋을 선행 선언으로 막았다.** 기획서 확인 사실 22가 쓰인 뒤 SPEC-031에서 TASK-099·100이 나왔다. 101↔099(`fs_project_repository.rs`·`domain/project.rs`), 103↔100·096(`DevelopmentBoard.tsx`·테스트·`App.css`), 103↔099(`types.ts`, 100을 거쳐 비켜남). 확인 사실 4가 기술한 실패 모양이 정확히 이 자리다.

### 변경한 파일
- `.workflow/도그푸딩--wf_ae6cd700/tasks/TASK-101.md`·`TASK-102.md`·`TASK-103.md` 신규 (셋 다 `status: todo`, `created` history 1줄, `source_spec_id`·`source_decision_id` 기입)
- `.workflow/도그푸딩--wf_ae6cd700/reports/REPORT-SPEC-032-ARCH.md` 신규
- `docs/development-logs/2026-08-05.md` (이 섹션)
- 앱 소스·기획서·결정·기존 작업 무변경. lease는 헬퍼로만 취득·갱신·반납.

### 검증 단계와 결과
- 분해 후 `wf-eligible.sh`: architect `no-target`/1(승인이 파생됨), planner `eligible`/0, developer `eligible`/0.
- 선행 대상 실재 확인: TASK-096 `qa_waiting`, 097·098 `completed`, 099·100 `todo`. 미존재 id 0건.
- 순환 없음: 101→{097,098,099}, 102→{098,101}, 103→{100,101}. 기존 099→{095,096}, 100→{095,096,099}.
- 표기 검사: 세 문서 모두 `^depends_on:` 1줄, 열 0 흐름 시퀀스, 토큰 문자 위반 0.
- 제품 코드·테스트 무변경이라 `cargo test`·`npm run check`는 돌리지 않았다. 세 작업 문서가 각각 그것을 자기 완료 조건으로 갖는다.

### 후속 / 리스크
- **전이 구간이 보수적이다.** TASK-101 착지 직후에는 저장소에 선언을 가진 작업이 하나도 없어, 활성 lease가 하나라도 있으면 모든 `todo`가 개발자 자격에서 빠진다. 활성 lease가 없으면 판정은 지금과 완전히 같다. 기획서 확인 필요 2번의 비용 문단이 예고한 승인된 대가다.
- **SPEC-033과 두 파일이 겹친다.** SPEC-033(조건 스크립트 성능)이 `user_review`라 파생 작업이 없고 이 분해가 먼저 섰다. 승인되면 그쪽 분해가 TASK-101을 선행으로 적을 자리다. 승인 안 된 기획서를 근거로 순서를 만들지 않았다.
- **`scope_files` 누락 검사 장치가 없다.** 빠뜨리면 안전한 쪽으로 기울 뿐이라 사고는 아니지만, 계속 비면 병렬 여지가 열리지 않는다. 후속 아이디어 감.
- **역할 밖 관찰(무수정)**: 설치본과 상수가 다시 어긋나 있다 — `CONDITION_SCRIPT_VERSION` 7 대 설치본 6, `WORKFLOW_RULES_VERSION` 9 대 설치본 8, `ARCHITECT_RULES` 4. 이 세션이 읽고 따른 계약은 설치본이다.
- **역할 밖 관찰(무수정)**: 만료 lease `SPEC-009.yml` 잔존. §4대로 손대지 않았다. `TASK-096.yml`은 이 세션 중 그쪽 세션이 반납했다.

## 개발자 세션: TASK-099 개발 (확인 전용 일괄 QA 명령 + 게이트웨이·훅) (00:54~01:05 KST / 15:54~16:04Z)

### 목표 / 요청 범위
- 개발자 역할 1대상 1클레임. `wf-eligible.sh developer` exit 0 → `todo` 5건 중 선행이 충족된 유일한 작업 TASK-099.
- SPEC-031 R3·R4·R6·R7, 완료 조건 4·5·7·9·10·12·13·14·15의 기록 몫. 화면은 범위 밖(TASK-100).
- 선점 `lease-69300-20260804155436`(acquire/renew/release 전부 exit 0), `in_progress`(15:54:40Z) → `qa_waiting`(16:04:25Z).

### 핵심 결정과 근거
- **건 처리 본체를 free function `record_one_task_qa`로 뽑아 단건·일괄이 공유.** 작업 문서가 지목한 대로 "QA 기록 규칙이 지켜지는 자리가 둘이 되는" 비용을 없애는 가장 싼 방법. 판정 순서·에러 종류·기록 형식은 한 글자도 안 바꿨고, 뽑기 전엔 없던 반환값(작업 id)만 더했다. `record_task_qa`는 그 함수를 부르는 한 줄이 됐다.
- **실패 건의 `task_id`는 두 번째 읽기로 채운다(`task_id_of`, 4줄).** 에러에 id를 실어 나르는 구조(전용 실패 타입 + `From` 구현)도 검토했지만 단건 경로에 `map_err` 배관이 늘어난다. 실패 경로에서만 도는 4줄이 더 싸고, "문서를 읽지 못하면 `None`"이라는 계약도 함수 모양 그대로 표현된다.
- **`outcome` 파라미터를 안 만들었다.** 승인된 확인 필요대로 확인 전용. 이름(`confirm_task_qa_batch`)이 그것을 말한다. 일괄 반려를 파라미터로 열어 두면 다음 사람이 뚫린 길로 읽는다.
- **건별 실패는 `setError`로 안 올린다.** 전역 문구는 다음 호출이 덮는 자리라 R4의 건별 보고를 담지 못한다. 훅은 `TaskQaBatchEntry[] | null`을 돌려주고, `null`은 호출 자체의 실패다.
- **기존 훅 검사는 이름도 내용도 안 고쳤다.** `gatewayFor`에 가짜 메서드 한 줄만 늘었다.

### 변경한 파일
- `src-tauri/src/domain/project.rs` — `TaskQaBatchResult`·`TaskQaBatchEntry` (+19 -0)
- `src-tauri/src/infrastructure/fs_project_repository.rs` — `record_one_task_qa`·`task_id_of` 추출, `confirm_task_qa_batch` 신설, 검사 7건 (+373 -31, 삭제 31줄은 전부 추출로 옮겨 간 본체 29줄 + import 줄바꿈 2줄)
- `src-tauri/src/application/project_service.rs`·`commands/projects.rs`·`lib.rs` — 위임 한 줄, 명령 하나, `generate_handler!` 항목 하나
- `src/features/projects/domain/types.ts`·`infrastructure/tauriProjectGateway.ts`·`application/useProjectWorkspace.ts` — 결과 타입 둘, 게이트웨이 메서드, 훅 콜백
- `src/features/projects/application/useProjectWorkspace.test.ts` — 검사 2건 (+91 -0)
- `.workflow/도그푸딩--wf_ae6cd700/tasks/TASK-099.md` (`qa_waiting`, history 2줄 추가), `reports/REPORT-TASK-099-DEV.md` 신규
- `docs/development-logs/2026-08-05.md` (이 섹션)

### 검증 단계와 결과
- `cargo test` 착수 전 기준선 **404 passed** → 최종 **411 passed / 0 failed**. 기존 404건은 하나도 안 고치고 통과(완료 조건 6). 작업 문서가 적은 "89건"은 기획 시점 수치라 실측값을 썼다.
- `npm run check` → 19 파일 **510건** 통과, `vite build` 326 modules.
- `cargo fmt --check` 위반 0(중간에 `cargo fmt` 1회 적용), `cargo clippy --all-targets -- -D warnings` 경고 0.
- 앱 호출 1회 확인: 훅 검사가 `toHaveBeenCalledTimes(1)`로 고정 + 명령 본체의 `summary_from_manifest` 호출 수를 코드로 세어 **1건**(완료 조건 5).
- 삭제 라인 전수 확인: 내가 만진 9개 파일 중 삭제가 있는 건 3개뿐이고 전부 이동·줄바꿈. 검사 파일에서 지워진 줄 0.
- 금지 4파일(`DevelopmentBoard.tsx`·`App.css`·`WorkspaceShell.tsx`·`App.tsx`) 미포함. `git diff --stat`에 앞 둘이 보이는 것은 **세션 시작 시점 `git status`에 이미 있던 미커밋 변경**이고 이 작업 것이 아니다.

### 후속 / 리스크
- **TASK-100·TASK-101이 열렸다.** 둘 다 TASK-099를 선행으로 갖고 이제 `qa_waiting`이라 충족.
- **TASK-100 인계**: 훅은 요청 순서 그대로의 건별 결과 배열을 준다. 건별 실패 문구는 전역 에러로 안 가므로 화면이 그 배열을 직접 그려야 한다. 대상 범위 판단(확인 필요 3번)도 TASK-100 몫.
- **선행이 되돌아오는 자리**: TASK-096은 아직 `qa_waiting`. 사용자 QA가 `todo`로 되돌리면 작업 문서 지시대로 고쳐서 진행하지 말고 아키텍트 후속으로 넘긴다.
- **화면이 없어 사람이 눌러 볼 자리가 없다.** 이 작업의 QA는 자동 검사로 닫는 설계다(새 9건 + 기존 404건 무수정 통과).
- **역할 밖 관찰(무수정)**: 만료 lease `SPEC-009.yml` 잔존. §4대로 손대지 않았다. 작업 중 `TASK-096.yml` lease가 다른 세션에 의해 새로 잡혔다.

## TL 세션 이어서 (01:00~01:10 KST) — TASK-096 반려 재투입 + 아이디어 등록

### 실행
- 사용자 QA 반려(QA-F25FD89E, 15:57:50Z) 접수: "화면 비율을 줄이면 레인 안 내용물(완료·QA대기 등)이 오른쪽 바깥으로 빠져나감". TASK-096 `todo` 복귀 확인.
- tl-dev-096-r2 워커를 SendMessage로 재투입(컨텍스트 유지 재사용). 지시: 재선점 최우선 → QA 원문 독해 → 묶기 끈 보드의 좁은 폭 보장을 레인 안에서도 성립 → CSS 선언 단위 회귀 검사(TASK-089 선례) → 게이트 그린 → qa_waiting 재착지 → SendMessage 보고. 위 TASK-099 세션이 관측한 "TASK-096.yml lease 신규 취득"이 이 재투입이다.
- 사용자 질문("중단된 세션 재개 수단이 이미 있나")에 대한 실측 답변: 없음. wf-eligible.sh 개발자 분기는 `status: todo`만 봄(:182), SPEC-012가 회수 정책을 명시적으로 제외해 둔 상태. 오늘 TASK-096 2.5시간 방치가 실증.
- 사용자 지시로 IDEA-7EBEDE40 등록: 중단 세션 회수 정책 — 역할별 죽음 시그니처(in_progress+만료 lease / draft+만료 lease / 부분 분해), 판정 편입, 인수 세션의 잔여물 평가 의무. SPEC-012의 제외 범위를 정면으로 받는 내용.

### 후속 / 리스크
- 병행 관측: SPEC-032 분해 완료(TASK-101~103, scope_files 도입), SPEC-033(조건 스크립트 성능) user_review, TASK-099 착지로 TASK-100·101 선행 열림(100은 096 반려로 다시 대기).

### 096 재작업 착지 (01:05~01:10 KST, 워커 보고 수치)
- 원인: `.task-board`의 `min-width: 950px`(SPEC-021 소유)이 좁은 레인 상자를 뚫고 나감. 묶기 끈 보드는 워크스페이스 전체 스크롤이 흡수해 안 보였을 뿐. 재현 조건은 묶기 켬 + 모든 상태(5열) + 창 폭 ~1300px 이하.
- 처방: 레인 안 보드에만 스크롤 상자(`.task-lane-scroll { min-width: 0; overflow-x: auto }`)를 씌움. 헤더(집계·신호·접기 버튼)는 상자 밖이라 밀리지 않음 — R2·R3·R6 유지. `.task-board` 최소 폭 무접촉(SPEC-021 결정 존중).
- 회귀 고정: specLaneOverflow.test.ts 신설 3건(CSS 선언 단위, TASK-089 어법) + 보드 마크업 검사 1건. 변이 확인 통과.
- 게이트: 보드+신설 55/55, SPEC-021 검사 포함 62/62, npm run check exit 0(20파일 514건 — 내 몫 +4, 나머지 +2는 병행 세션의 useProjectWorkspace.test.ts 추가분으로 구분 기재). qa_waiting 재착지(16:04:46Z), lease 반납, 잔존 lease는 SPEC-009.yml뿐.
- 첫 착지 보고의 테스트 파일 증분 수치 정정(+175→+188, 삭제 0줄 사실 불변)이 재작업 절에 포함됨. REPORT-TASK-096-DEV.md에 재작업 절 대리 추기 완료(기록 경위 머리말).
- 사용자 QA 재확인 필요: 좁힌 창(모든 상태 필터)에서 레인 테두리 안 가로 스크롤, 헤더 고정, 레인별 독립 스크롤, 묶기 끈 보드 무변화.

## 기획자 세션 (01:08~01:25 KST) — IDEA-FCDDD4DE → SPEC-034

### 목표 / 요청 범위
- 공통 규칙 `.workflow/rules/workflow.md` + `roles/planner.md` 한 역할·한 대상.
- 산출: `specs/SPEC-034.md`(`status: user_review`), `reports/REPORT-SPEC-034-PLAN.md`.

### 핵심 결정과 근거
- **대상 선택**: 수정 요청 둘(`DECISION-FEB99DAB`→SPEC-013, `DECISION-2F71D20D`→SPEC-015) 모두 답변 완료. 미참조 아이디어 둘 중 `created_at`이 이른 IDEA-FCDDD4DE(12:53:07Z) 선점. 남은 IDEA-7EBEDE40(16:06:11Z)은 다음 기획자 세션 몫.
- **아이디어의 버전 원천 전제가 둘 다 없다.** `heartbeat --version` 플래그 부재(`main`·`claude/v0.8.0-jobsd` 양쪽 `cli.py`), `state.json` 데몬 버전 항목 부재. 앱의 실행 파일 후보(PATH·`~/.local/bin`)도 이 기기의 pyenv 경로에 닿지 못함. → 아이디어 제안 2번(선제적 "업데이트 필요" 표시)을 확인 필요 2번으로 올리고 제안은 "이번엔 뺀다".
- **버전 답이 셋이고 셋이 다르다**(실측): pip 메타데이터 0.5.1 / 체크아웃 `pyproject.toml` 0.7.0 / 도는 코드는 미푸시 `claude/v0.8.0-jobsd`. SPEC-024 확인 필요 3번의 "버전 문자열은 거짓말할 수 있다"가 우려가 아니라 실측 사실이 됨.
- **`pip install claude-heartbeat`가 지금 실패한다**: PyPI 404(2026-08-05 실측). 앱 설치 마법사 1단계가 그 명령이므로, 같은 어법을 갱신 안내에 복사하면 앱이 안 되는 명령을 내밀게 됨 → 확인 필요 1번(절차 형태)·3번(마법사 1단계 동반 수정 여부).
- **재시작 명령을 앱이 조립할 수 없다**: 이 기기 launchd 라벨이 `com.catze.dream-heartbeat`(표준 아티팩트 부재). 표준화는 데몬 저장소 P2 항목 → R4는 "라벨을 지어내지 않는다"로 제약.

### 변경한 파일
- `.workflow/도그푸딩--wf_ae6cd700/specs/SPEC-034.md` 신규 (확인 사실 16 / R1~R8 / 확인 필요 4 / 완료 조건 15)
- `.workflow/도그푸딩--wf_ae6cd700/reports/REPORT-SPEC-034-PLAN.md` 신규
- `docs/development-logs/2026-08-05.md` (이 섹션)

### 검증 단계와 결과
- `sh .workflow/rules/wf-eligible.sh planner` → `eligible`(exit 0). 대상 판정은 계약대로 직접 수행.
- lease `lease-87895-20260804160826` acquire(16:08Z) → renew 1회 → 세션 종료 시 release.
- 코드·앱 상태 무변경. 이 세션이 만든 것은 문서 셋뿐.

### 후속 / 리스크
- **지금 장전된 사고(무수정, 데몬 저장소 몫)**: 도는 데몬은 `jobs.d`를 읽는 v0.8.0 코드를 메모리에 들고 있고 작업 트리는 `jobs.d`를 모르는 `main`. `~/.claude/HEARTBEAT.md`는 빈 파일이고 역할 잡 여섯은 `jobs.d/`에만 있음. 데몬이 재시작하면(재부팅·launchctl·크래시, `KeepAlive` 참) 여섯 잡이 조용히 전부 멈춤. 복구는 `~/Git/claude-heartbeat`를 `claude/v0.8.0-jobsd`로 되돌리거나 그 브랜치를 `main`에 병합하는 일.
- **핸드오프**: (1) 설치 마법사 1단계 명령이 실패 상태 — 확인 필요 3번이 부결되면 별도 아이디어 등록 필요. (2) 아이디어가 범위 밖 근거로 든 `docs/planning/2026-08-04-productization.md`에 배포 채널 항목이 없음 — "그쪽 백로그"가 실제로는 비어 있음.
- **역할 밖 관찰(무수정)**: 만료 lease `SPEC-009.yml` 여전히 잔존(`expires_at` 2026-08-03T01:20Z). §4대로 손대지 않음.

## 아키텍트 세션 (01:20~01:40 KST) — DECISION-8D3F0D0F(SPEC-033) 분해

### 목표 / 요청 범위
- 공통 규칙 `.workflow/rules/workflow.md` + `roles/architect.md` 한 역할·한 대상.
- 산출: `tasks/TASK-104~106`(전부 `todo`), `reports/REPORT-SPEC-033-ARCH.md`.

### 핵심 결정과 근거
- **대상**: 미파생 승인 결정은 `DECISION-8D3F0D0F`(SPEC-033, `created_by: user`, 15:58:40Z) 하나뿐. 스크립트 아키텍트 분기를 손으로 재현해 후보를 열거해 확인.
- **결정 본문이 비어 있음** → 기획서 "확인 필요" 머리글("승인 시 아래 제안대로 진행한다")대로 네 항목 모두 제안 채택으로 읽음. 아카이브 제외, 파생 인덱스 제외, 상한 3초/3배, 앱 폴링은 "이유가 사라진 중복만".
- **분해 3건**: 104(조건 스크립트 두 본문 반복 훑기 제거 + 버전 축), 105(조회 재읽기 중복 정리, R7), 106(회귀 감지 장치, R8). R6은 확인 필요 2번이 뒤집히지 않아 미적용, R5는 "아무것도 옮기지 않음"으로 자동 충족.
- **곱이 하나가 아니라 둘**: 기획서 확인 사실 5가 짚은 최신 결정 판정 외에, 후보마다 `tasks/*.md` 97건을 다시 읽는 분해 여부 판정도 곱(31×97=3,007회 읽기, 3배 픽스처에서 9배). 둘 다 없애지 않으면 완료 조건 2를 못 지킴 → TASK-104 작업 내용에 명시.
- **앱 판정에는 곱이 없음**: `latest_approvals`가 결정을 한 번 읽고 끝냄. TASK-104의 `role_eligibility.rs` 범위를 검사로만 한정하고, 판정 함수를 고쳐야 하면 그것이 판정 분기 신호이므로 보고 후 중단하게 함.
- **R8 형태 고정(기획서가 아키텍트에게 위임)**: 벽시계 시간이 아니라 외부 프로세스 수를 센다. `PATH`를 shim 디렉터리 하나로 두고 1배·3배 픽스처에서 선형(≤3.5배)+절대(CAP) 두 단언. 시간은 러너마다 달라 단언이 될 수 없고 실제 비용은 프로세스 수(확인 사실 5).
- **순서**: TASK-104·105는 TASK-101(SPEC-032) 뒤 — 파일 겹침에 더해 **둘 다 `CONDITION_SCRIPT_VERSION`을 "착수 시점 값 +1"로 올리는** 버전 축 충돌이 진짜 겹침. 106은 104 뒤. 104와 105 사이에는 겹침이 없어 순서를 두지 않음.
- **세 작업에 `scope_files`를 쓰지 않음**: 계약에 아직 없는 필드이고, TASK-101 완료 조건 12가 "이 저장소 작업에 `scope_files`가 하나도 없다"를 회귀 판정 전제로 씀.

### 변경한 파일
- `.workflow/도그푸딩--wf_ae6cd700/tasks/TASK-104.md`·`TASK-105.md`·`TASK-106.md` 신규 (전부 `status: todo`, `history` created 1건)
- `.workflow/도그푸딩--wf_ae6cd700/reports/REPORT-SPEC-033-ARCH.md` 신규
- `docs/development-logs/2026-08-05.md` (이 섹션)

### 검증 단계와 결과
- `sh .workflow/rules/wf-eligible.sh architect` → 착수 전 `eligible`(exit 0), 착지 후 `no-target`(exit 1). 세 역할 착지 후: planner `eligible`(0) / architect `no-target`(1) / developer `eligible`(0).
- lease `lease-28381-20260804162149` acquire(16:21Z) → renew 1회(16:27Z) → release(16:35Z, exit 0). 잔존 lease는 만료된 `SPEC-009.yml`뿐.
- 제품 코드·기획서·결정 무변경. 읽기만 함.

### 후속 / 리스크
- **데몬은 설치본을 돌린다**: `.workflow/rules/wf-eligible.sh`가 `condition_script_version: 6`, 작업 트리 상수는 7. TASK-104가 착지해도 앱이 재설치하기 전까지 오늘의 타임아웃은 그대로. 기획서가 다루지 않은 자리 → 아키텍트 후속 아이디어 후보.
- **3초에 곱 제거만으로 못 닿을 수 있음**: 기획서 확인 필요 3번 비용 문단이 예고한 리스크. 그 경우 TASK-104는 값만 적고 멈추게 했다(상한 완화·스캔 상한은 승인 뒤집기).
- **TASK-106 검사가 느릴 수 있음**: 3배 픽스처가 파일 1,000개 언저리. 배수 3은 승인된 값이라 세션이 줄이지 못하게 하고 보고 후 후속으로 넘기게 함.
- **역할 밖 관찰(무수정)**: 만료 lease `SPEC-009.yml` 잔존, `.workflow/rules/workflow.md`(rules_version 9)와 설치 계약 파일들에 미커밋 변경 있음. §4·§2대로 손대지 않음.

## 세션: TASK-101 개발 (겹침 선언 `scope_files` 도입)

### 목표 / 요청 범위
- 공통 규칙 `.workflow/rules/workflow.md` + `roles/developer.md` 한 역할·한 대상.
- 대상: TASK-101 (SPEC-032 본체, DECISION-0D79A7F0 승인). 산출: 제품 코드 4파일 + `reports/REPORT-TASK-101-DEV.md`.

### 핵심 결정과 근거
- **대상 선정**: 착수 시점 `todo` 7건 중 선행 충족은 TASK-100·TASK-101 둘. TASK-101을 골랐다 — 나머지 5건(102·103·104·105·106)이 전부 이것을 기다린다. TASK-100과는 범위가 겹치지 않는다.
- **lease 대상 id는 파일 stem**: `AgentLeaseSummary.task_id`(파일 내용)가 아니라 `lease_ids()`의 stem을 쓴다. 조건 스크립트가 `$leases/$id.yml`로 찾는 값과 같아야 두 판정이 갈라지지 않는다. `summary_from_manifest`에서 `lease_ids`를 위로 끌어올려 `PreparedWorkflow::read`와 `pending_role_work`가 같은 집합을 본다.
- **`overlap_block`이 규칙의 단일 정의**: `Option<Vec<String>>`을 돌려 자격 판정(집합)과 상세 payload(근거)가 같은 함수를 쓴다. `unsatisfied_dependencies`/`task_dependencies` 짝과 같은 구조.
- **그래프 값이 구조체로**: `(status, DependencyDeclaration)` 3튜플 대신 `TaskNode { status, dependencies, scope }`. 두 선언이 한 번의 읽기에서 나오고 `tasks/` 훑기 횟수는 늘지 않았다.
- **파서를 합치지 않음**: `scope_files`의 허용 문자에 `.`과 `/`가 더 있어 `parse_dependency_declaration`을 일반화하지 않고 같은 어법의 함수를 하나 더했다(작업 문서 지시).
- **버전은 착수 시점 값 +1**: 상수·sh·ps1 셋 다 7이었고 8로 올렸다. 설치본은 6인 채로 두었다 — 앱이 덮어쓰는 산출물이다.
- **잡힌 lease가 없으면 파일을 열지 않음**: 두 스크립트 본문의 겹침 헬퍼가 자기 선언을 "막을 lease를 처음 만났을 때" 읽는다. 활성 lease 0건인 흔한 경우의 판정 비용이 그대로다.

### 변경한 파일
- `src-tauri/src/domain/project.rs` — `TaskOverlapBlock`, `TaskDocument.overlap_blocks`
- `src-tauri/src/infrastructure/fs_project_repository.rs` — `ScopeDeclaration`·`parse_scope_declaration`·`TaskNode`·`overlap_block`·`overlap_blocked_task_ids`·`task_overlap_blocks`, `PreparedWorkflow`/`read_task`/`summary_from_manifest` 배선, 검사 10건
- `src-tauri/src/infrastructure/role_eligibility.rs` — `WorkflowInput.overlap_blocked`, `has_developer_work` 조건 하나, 대조 검사 6건
- `src-tauri/src/infrastructure/heartbeat_condition.rs` — sh `scope_of`·`overlap_blocks`, ps1 `Get-Scope`·`Test-Overlapped`, 버전 8, 시나리오 표 4행
- `.workflow/도그푸딩--wf_ae6cd700/tasks/TASK-101.md`(`qa_waiting`), `reports/REPORT-TASK-101-DEV.md` 신규
- `docs/development-logs/2026-08-05.md`(이 섹션)

### 검증 단계와 결과
- `cargo test` 427 passed / 0 failed (착수 시점 411). `npm run check` 통과(테스트 514, 빌드 성공). `cargo fmt --check`·`cargo clippy --all-targets -- -D warnings` 통과.
- 실저장소 세 역할 판정: 활성 lease가 없는 동안 새 본문 v8과 설치본 v6이 같은 답(planner 0 / architect 0 / developer 0). 회귀 없음. 세션 중 앱이 기록한 `DECISION-3ECEDCA1` 때문에 architect가 1→0으로 바뀌었으나 두 본문이 함께 바뀌었다.
- sh 본문은 형식 오류·중복 키·빈 목록·블록 표기·공백 든 경로·겹침 6가지를 임시 픽스처로 손수 돌려 Rust 파서와 같은 답을 확인. PowerShell 본문은 이 기계에서 실행 불가 — CI Windows 러너가 시나리오 표로 대조한다.
- lease `lease-55572-20260804163813` acquire(16:38Z) → renew 1회(16:52Z) → release(17:00Z, exit 0). 잔존은 만료된 `SPEC-009.yml`뿐.

### 후속 / 리스크
- **TASK-102 착지 전까지 활성 lease 하나가 개발자 자격을 닫는다**: 지금 저장소의 어느 작업에도 `scope_files`가 없어 전부 "선언 없음"이고, 승인된 확인 필요 2번대로 잡힌 lease 하나에 막힌다. 계약 문언(TASK-102)이 서고 작업들이 선언을 갖기 시작하면 풀린다.
- **기존 94건 소급 기입은 기획서 제외 범위**. 위 기간을 줄이려면 필요한데 새 기획서의 몫 → 아키텍트 후속.
- **기존 검사 1건의 픽스처를 고쳤다**: `a_finished_dependency_satisfies_the_declaration`이 선행을 lease로 후보에서 빼는 픽스처라 새 규칙에 걸렸다. 이름·단언·기대값은 그대로 두고 두 작업에 겹치지 않는 선언을 줬다. 삭제도 `#[ignore]`도 아니다.
- **화면은 TASK-103**: payload 이름은 `overlapBlocks`, 원소는 `{ leaseTargetId, sharedFiles }`. `types.ts`에는 아직 없다.
- **sh `overlap_blocks`의 비용**: 잡힌 lease마다 `task_file`로 작업 문서를 훑는다. lease는 보통 0~2건이라 작지만 SPEC-033(TASK-104)이 판정 비용을 손볼 때 같이 보면 좋다.

## 개발자 세션 — TASK-100 (02:27~02:39 KST)

### 목표 / 요청 범위
- 개발자 역할 한 건. `.workflow` 공통 규칙과 developer 계약을 따라 자격 있는 개발 작업 하나만 처리.
- 선택: **TASK-100** (레인 헤더에 일괄 QA 확인 화면을 붙이고 건별 결과를 보인다). SPEC-031 /
  DECISION-1FAA8251(`approved`, `created_by: user`).
- 자격 판정: `wf-eligible.sh developer` exit 0. `todo` 9건 중 선행 충족은 TASK-100·102·104·105·107
  다섯이었고 그중 가장 앞선 하나만 잡았다.

### 핵심 결정과 근거
- **대상 집합은 `allTasks` 순회에서 모은다.** `SpecLane.qaWaiting`을 `visibleTasks` 쪽에서 채우면 필터에
  가려진 건이 빠져 액션 문구의 건수와 목록의 건수가 어긋난다 — 승인된 확인 필요 3번을 정면으로 어기는
  자리라 작업 문서가 "가장 틀리기 쉬운 자리"로 지목했다. `laneSignal`·`counts`·완료 절단은 무변경.
- **열린 레인을 키가 아니라 레인 스냅샷으로 든다.** 작업 문서는 `useState<string | null>`을 적었지만,
  상태 필터가 걸린 채 전 건이 `completed`가 되면 그 레인은 카드 0장이 되어 `buildSpecLanes`가 목록에서
  빼고, 키로 다시 찾는 구조였다면 건별 결과를 읽기 전에 화면이 사라진다. 같은 문단이 요구한 R4
  ("결과를 읽기 전에 화면이 사라지면 R4가 무의미해진다")를 우선했다. 보고서에 벗어난 자리로 명시.
- **콜백은 한 번만 부른다.** 승인된 확인 필요 1번대로 선택 건수만큼 반복 호출하지 않는다. 검사 4가 그것을
  `toHaveBeenCalledTimes(1)`로 고정한다.
- **프롭을 선택 필드로 만들지 않았다.** 필수 필드라 기존 render 호출에 프롭 한 줄씩이 붙는다. 화면이 이
  콜백 없이 동작하는 상태를 만들지 않기 위해서다(작업 문서 요구).

### 변경한 파일
- `src/features/projects/components/DevelopmentBoard.tsx` — `SpecLane.qaWaiting`, 레인 헤더 액션,
  `LaneQaDialog` 컴포넌트(목록·개별 제외·공통 코멘트·2단계 무장·실행 중 잠금·건별 결과), 프롭 하나
- `src/features/projects/components/DevelopmentBoard.test.tsx` — 검사 10건 추가(52→62)
- `src/features/projects/components/WorkspaceShell.tsx` / `.test.tsx` — 프롭 배선 / 프롭 한 줄씩 12곳
- `src/App.tsx` — `workspace.confirmTaskQaBatch` 한 줄
- `src/App.css` — `.task-lane-batch`·`.lane-qa-*` 규칙 신설(기존 규칙 무변경, 새 색 없음)
- `.workflow/도그푸딩--wf_ae6cd700/tasks/TASK-100.md`(`qa_waiting`, history 2줄),
  `reports/REPORT-TASK-100-DEV.md` 신규
- `docs/development-logs/2026-08-05.md`(이 섹션)

### 검증 단계와 결과
- `npx vitest run` 착수 514건 → 마침 **524건 통과**(20파일). `DevelopmentBoard.test.tsx` 52→62.
- `npm run check`(typecheck + test + build) 통과. `vite build` 326 modules.
- `cargo test --manifest-path src-tauri/Cargo.toml` → **427 passed / 0 failed**. Rust 무변경, 회귀 없음.
  (`cargo`가 PATH에 없어 `~/.cargo/bin` 추가해 실행 — 전 세션과 같은 상황)
- CSS 중복 선택자 검사: `cssRules.ts`와 같은 파싱으로 전수 확인. 검출 15건은 전부 미디어 쿼리 안의
  기존 선언이고 새로 더한 선택자 중 중복 0.
- 기존 검사 변경분: `WorkspaceShell.test.tsx` 12줄 추가·삭제 0. `DevelopmentBoard.test.tsx`의 삭제 라인은
  전부 `render(<DevelopmentBoard …/>)` 호출 줄과 타입 import 한 줄이며 단언·검사 이름 변경 0.
- lease `lease-79876-20260804172705` acquire(17:27:05Z) → renew 1회(17:33Z) → release(17:38Z, exit 0).
  잔존은 만료된 `SPEC-009.yml`뿐.

### 후속 / 리스크
- **TASK-103 선행이 풀린다.** `depends_on: [TASK-100, TASK-101]`이고 둘 다 `qa_waiting`이 됐다.
  TASK-103도 `DevelopmentBoard.tsx`·`App.css`를 만지므로 병행하지 않는다.
- **한계(무수정)**: 카드가 0장이라 목록에서 빠진 레인에는 헤더가 없어 액션도 없다. SPEC-029가 정한 표시
  규칙이고 SPEC-031 R8이 그대로 두라고 했다. 검색어로 신호 레인의 카드가 전부 가려지면 액션이 사라진다.
- **한계(무수정)**: 확인 화면이 떠 있는 동안 2.5초 갱신으로 새로 생긴 `qa_waiting`은 목록에 들어오지
  않는다. 사용자가 체크하는 동안 목록이 흔들리지 않게 한 판단이고, 닫았다 열면 새 목록이 온다.
- **역할 밖 관찰(무수정)**: `SpecLaneBoard`의 프롭이 아홉 개가 됐다. 묶을 여지가 있으나 요청과 직접
  연결되지 않아 두었다.
- **역할 밖 관찰(무수정)**: 만료된 `SPEC-009.yml` lease 여전히 잔존. §4대로 손대지 않았다.

## 개발자 세션 — TASK-103 (03:12~03:30 KST / 18:12~18:30Z)

### 목표 / 요청 범위
- 개발자 역할 한 건. `.workflow` 공통 규칙과 developer 계약을 따라 자격 있는 개발 작업 하나만 처리.
- 선택: **TASK-103** (겹침으로 막힌 작업이 화면에서 시작 가능으로 보이지 않게 한다).
  SPEC-032 R7 / DECISION-0D79A7F0(`approved`, `created_by: user`, SPEC-032의 유일한 결정).
- 자격 판정: `wf-eligible.sh developer` exit 0. `todo` 7건 중 선행 충족은
  TASK-103·104·105·107 넷이었고 그중 가장 앞선 하나만 잡았다.
- 선점 `lease-21358-20260804181230` 취득(exit 0) → 갱신(exit 0) → 반납(exit 0).

### 핵심 결정과 근거
- **겹침을 `permanent` 판정에 넣지 않았다.** R7 넷째 항목이 요구한 구분이다. 겹침은 lease가 만료·해제되면
  풀리는 일시 상태라 `missing`·`cyclic`·형식 오류의 붉은 경고 톤이 아니라 대기 톤을 쓴다. 문구 상수도
  따로 뒀다(`OVERLAP_NOTE`, `OVERLAP_UNDECLARED_NOTE`).
- **조기 반환에 겹침을 더한 것이 첫 줄.** 선행 선언이 없는 작업이 겹침으로 막히면 지금 구조에서는 화면에
  그 사실을 담을 자리가 아예 없었다(`!formatError && dependencies.length === 0` → `null`).
- **빈 `sharedFiles`와 공유 경로를 다른 문구로 갈랐다.** 근거는 `overlap_block`
  (`fs_project_repository.rs:1597`)이 두 선언 중 하나가 없거나 형식 오류일 때 `Some(Vec::new())`을
  돌려주는 자리다. 문구가 같으면 사용자가 "무엇을 고쳐야 열리는가"를 읽지 못한다.
- **선행 목록 `<ul>`에 길이 가드를 더했다.** 겹침만 있는 작업에서 빈 `<ul>`이 그려지는 것을 막는다.
  겹침이 없을 때의 렌더 결과는 가드 전후가 같다.
- **payload를 그대로 읽기만 했다.** 화면에서 lease를 다시 조회하지 않았고 목록 payload에도 값을 더하지
  않았다(R7 셋째 항목). 게이트웨이는 `invoke<TaskDocument>` 통과라 변환 자리가 없어 손댈 것이 없었다.
- **기존 검사는 이름도 본문도 고치지 않았다.** 테스트 헬퍼 둘에 기본값 `[]`인 인자 하나만 더해 기존
  호출부 동작을 그대로 뒀다.

### 변경한 파일
- `src/features/projects/domain/types.ts` — `TaskOverlapBlock` 신설, `TaskDocument.overlapBlocks?` 추가
- `src/features/projects/components/DevelopmentBoard.tsx` — `TaskDependencies`의 조기 반환·`startable`
  판정·겹침 블록, 호출부 프롭 한 줄. `permanent` 판정 무변경
- `src/features/projects/components/DevelopmentBoard.test.tsx` — 검사 6건 추가(62→68)
- `src/App.css` — `.task-overlaps`·`.task-overlap-note`·`.task-overlap` 규칙 신설(기존 규칙 무변경)
- `.workflow/도그푸딩--wf_ae6cd700/tasks/TASK-103.md`(`qa_waiting`, history 1줄),
  `reports/REPORT-TASK-103-DEV.md` 신규
- `docs/development-logs/2026-08-05.md`(이 섹션)

### 검증 단계와 결과
- `npx vitest run src/.../DevelopmentBoard.test.tsx` → **68 passed / 0 failed**(착수 62 → +6).
- `npm run check` → vitest **530 passed / 20 파일**, `tsc -b` + `vite build`(326 modules) 성공.
- `cargo test --manifest-path src-tauri/Cargo.toml` → **428 passed / 0 failed / 0 ignored**
  (`cargo`가 PATH에 없어 `~/.cargo/bin` 추가해 실행). 이 세션은 Rust를 만지지 않았다.
- 완료 조건 7(변경분에 Rust·`ActivityView.tsx`·`WorkspaceShell.tsx` 없음)은 두 방향으로 확인.
  `find src src-tauri/src -newermt <착수 시각>` → 네 파일, `grep -rln`(새 심볼) → 같은 네 파일.
  이 트리엔 타 세션 미커밋 변경이 겹쳐 있어 "`git diff`가 비어 있다"는 쓰지 않았다.

### 후속 / 리스크
- **payload에 모자란 것은 없었다.** TASK-101이 실은 `overlapBlocks`만으로 R7 네 요구가 전부 그려졌다.
- **아직 이 저장소의 작업 문서에 `scope_files`가 하나도 없다.** 실제 화면에서 겹침 블록이 뜨려면 선언과
  활성 lease가 먼저 필요하고, QA는 추가한 검사 6건이 그 상태를 대신 세운다.
- 겹침 상대의 **제목**은 그리지 않았다. payload가 싣는 것은 `leaseTargetId`뿐이고 제목을 얻으려면 새
  조회를 만들어야 하는데 R7 셋째 항목이 금지한다.
- 목록·보드 카드의 겹침 배지는 작업 문서가 후속 아이디어로 돌렸다. 상세를 열기 전에는 겹침이 보이지 않는다.
- **역할 밖 관찰(무수정)**: 만료된 `SPEC-009.yml` lease 여전히 잔존. §4대로 손대지 않았다.

## 세션: TASK-107 개발 (갱신 안내 명령 원문 payload)

### 목표 / 요청 범위
- 개발자 역할 1건. `.workflow` 공통 규칙 §1~§7과 developer 계약(rules_version 4)을 따라 자격 있는
  작업 하나만 처리.
- 자격 판정 결과 대상은 TASK-107 (SPEC-034 R3·R4). 착수 시점 `todo` 4건 중 TASK-106은 선행
  TASK-104가 `in_progress`라 미충족, TASK-108·109는 각각 107·108을 기다린다. `depends_on`이 없는
  TASK-107 하나만 남았다.
- 선점 `lease-37051-20260804194051` 취득(exit 0) → 갱신 1회(exit 0) → 반납(exit 0).

### 핵심 결정과 근거
- **값을 섹션 공통 영역에 두었다.** 두 카드가 같은 값을 같은 문구로 보여야 한다는 R7 때문이다
  (`managed_block_failure`·`jobs_file_path`가 같은 이유로 이미 공통이다). `heartbeat.setup_stages`에
  얹지 않았다 — 그 목록은 설치 마법사의 것이고 dream 카드는 읽지 않는다.
- **재시작 두 값은 macOS에서만 `Some`.** `heartbeat_setup.rs`의 `service_stage`와 같은 어법으로
  `cfg!(target_os = "macos")`에 갈랐다. Linux·Windows 재시작 절차는 이 저장소가 확인한 적이 없고,
  확인하지 않은 명령을 싣는 것이 R2가 막는 일이다.
- **`<라벨>`을 빈자리로 남겼다.** 표준 라벨을 골라 넣으면 이 기기의 `com.catze.dream-heartbeat`에서
  바로 틀린다(확인 사실 11). 비었다는 것을 문구로 말하는 일은 TASK-108의 몫이다.
- **판정을 하나도 만들지 않았다.** 파일 읽기·명령 실행·버전 조회가 새 모듈에 0건이다. 조립 함수는
  입력 자체가 없어 조회 상태에 따라 값이 갈리지 않는다.
- **마법사 1단계는 손대지 않았다.** 승인된 확인 필요 3번. `pip install claude-heartbeat`는 지금
  PyPI 404로 실패하지만, 해소는 데몬 저장소의 배포이지 앱 문구 수정이 아니다.

### 변경한 파일
- `src-tauri/src/infrastructure/heartbeat_update.rs` 신규 — 상수 5, 조립 함수 1, 검사 3건
- `src-tauri/src/infrastructure/mod.rs` — 모듈 등록 1줄
- `src-tauri/src/domain/project.rs` — `HeartbeatUpdateGuide` 구조체(`HeartbeatSetupState` 뒤)
- `src-tauri/src/application/heartbeat_service.rs` — `IntegrationsSnapshot.update_guide` 필드,
  import 2줄, 조립 1줄, 검사 3건(98 → 101)
- `.workflow/도그푸딩--wf_ae6cd700/tasks/TASK-107.md`(`todo` → `in_progress` → `qa_waiting`,
  history 2줄), `reports/REPORT-TASK-107-DEV.md` 신규
- `docs/development-logs/2026-08-05.md`(이 섹션)
- `heartbeat_setup.rs`·프론트엔드 일체 무변경. lease는 헬퍼로만 취득·갱신·반납.

### 검증 단계와 결과
- `cargo test --manifest-path src-tauri/Cargo.toml` → **442 passed / 0 failed / 0 ignored**
  (신규 6건 전부 통과, `~/.cargo/bin`을 PATH에 추가해 실행).
- `cargo fmt` 적용, `cargo clippy --all-targets -- -D warnings` 경고 0건.
- `npm run check` → typecheck 통과, vitest **530 passed / 20 파일**, `vite build`(326 modules) 성공.
  이 세션은 `src/`를 만지지 않았다.
- 기존 검사 `reading_the_setup_stages_does_not_touch_either_home`이 **수정 없이** 통과(완료 조건 7).
- 무변경 확인은 파일·심볼 단위로 했다 — `heartbeat_setup.rs`는 `git diff --stat` 출력 0줄,
  `PACKAGE_COMMAND`(`:29`) 그대로. 이 트리엔 타 세션 미커밋 변경이 겹쳐 있어 "`git diff`가 비어
  있다"는 쓰지 않았다.

### 후속 / 리스크
- **화면은 아직 이 값을 읽지 않는다.** `updateGuide`가 payload에만 있고 TASK-108(역할 잡 카드)·
  TASK-109(dream 카드)가 받아 간다. 두 작업은 이제 선행이 충족됐다.
- **macOS 밖은 재시작 걸음이 비어 있다.** Linux·Windows 사용자는 갱신 명령까지만 본다. 그 플랫폼의
  절차가 확인되면 상수 둘을 늘리는 것이 후속 자리다.
- **`pip install -U claude-heartbeat`는 지금 실패하는 명령이다**(확인 사실 3). 승인된 확인 필요
  3번에 따라 그대로 실었다.
- **역할 밖 관찰(무수정)**: TASK-104가 `in_progress`인데 lease가 2026-08-04T19:37:05Z에 만료됐다.
  개발자 자격 판정은 `todo`만 보므로 그 작업은 누구에게도 잡히지 않는 상태로 남아 있다. 만료된
  `SPEC-009.yml`도 여전히 잔존. §4대로 둘 다 손대지 않았다.

## 세션: TASK-108 개발 (084 경고 자리의 갱신 안내 — 공용 컴포넌트와 역할 잡 카드)

### 목표 / 요청 범위
- 개발자 역할 1건. `.workflow` 공통 규칙 §1~§7과 developer 계약(rules_version 4)을 따라 자격 있는
  작업 하나만 처리.
- 대상은 TASK-108 (SPEC-034 R1~R6·R8). 착수 시점 `todo` 3건 중 TASK-106은 선행 TASK-104가
  `in_progress`라 미충족, TASK-109는 108을 기다린다. 선행(TASK-107)이 `qa_waiting`인 108 하나만
  남았고 `wf-eligible.sh developer`도 exit 0 / `eligible`이었다.
- 선점 `lease-59958-20260804200017` 취득(exit 0) → 갱신 1회(exit 0) → 반납(exit 0).

### 핵심 결정과 근거
- **안내를 두 카드가 함께 쓸 컴포넌트로 뽑았다.** `HeartbeatUpdateGuide.tsx` 신규.
  `IntegrationCard.tsx`에 넣지 않았다 — 그 파일은 연동 골격이고 하트비트 전용 블록을 두면 층이
  섞인다. `JobChanges.tsx`가 같은 자리의 선례다.
- **문구를 props로 받지 않는다.** 두 카드가 다른 문구를 넘길 자리를 만들면 R7("두 카드가 같은 것을
  같은 문구로")이 화면 테스트로 내려간다. props는 payload 값 하나다.
- **복사 상태를 컴포넌트 인스턴스가 든다.** 084 경고가 역할별이라 한 화면에 안내가 여럿 뜬다. 카드가
  상태를 들면 한 번의 복사로 여러 자리에 "복사됨"이 함께 뜬다. 검사 7이 그 분리를 고정한다.
- **표시 조건을 새로 만들지 않았다.** `missingRunEvidence(Boolean(job), run)`가 이미 그 자리의
  조건이고 안내는 그 안에 들어간다 → 상시 표시가 구조적으로 불가능하다(승인된 확인 필요 4번).
  `bodyWarning`과 어떤 버튼의 `disabled`도 건드리지 않았다.
- **안내가 원인을 단정하지 않는다.** 084는 "앱은 하트비트 버전을 판정하지 않으므로 둘 중 어느 쪽인지
  알지 못합니다"로 끝난다. 안내 첫 문장을 "설치한 방법 그대로 갱신합니다 / 앱은 이 기기의 설치 방법을
  알지 못합니다"로 두어 같은 어법을 잇는다. "갱신하면 해결됩니다" 류의 판정 문구를 만들지 않았다.
- **명령을 화면이 조립하지 않는다.** 다섯 값 전부 `guide.*`를 그대로 JSX에 넣는다. 파일의 템플릿
  리터럴은 `aria-label`과 `className` 둘뿐이고 둘 다 명령이 아니다.
- **재시작 두 값이 `null`이면 명령을 지어내지 않는다.** "앱은 이 플랫폼에서 재시작하는 방법을 알지
  못합니다"로 대신 말한다. 마법사 3단계가 같은 상태를 같은 어법으로 말한다.

### 변경한 파일
- `src/features/projects/components/integrations/HeartbeatUpdateGuide.tsx` 신규 — 문구 상수 9,
  걸음 조립 2함수, 복사 상태 1
- `src/features/projects/domain/types.ts` — `HeartbeatUpdateGuide` 인터페이스,
  `IntegrationsSnapshot.updateGuide` 필드
- `src/features/projects/components/integrations/HeartbeatCard.tsx` — import 1줄,
  084 경고 안 렌더 1줄(+주석). 그 밖은 무변경
- `src/App.css` — `.heartbeat-update-*` 11줄 신규(기존 선택자 무수정)
- `src/features/projects/components/integrations/IntegrationsView.test.tsx` — 픽스처 `updateGuide`,
  `describe("IntegrationsView 갱신 안내")` 검사 9건(188 → 197), 기존 검사 2건의 선택자 2줄
- `src/features/projects/components/integrations/DreamCard.test.tsx` — 픽스처만
- `src/features/projects/application/useProjectWorkspace.test.ts` — 픽스처만
- `.workflow/도그푸딩--wf_ae6cd700/tasks/TASK-108.md`(`todo` → `in_progress` → `qa_waiting`,
  history 2줄), `reports/REPORT-TASK-108-DEV.md` 신규
- `docs/development-logs/2026-08-05.md`(이 섹션)
- `DreamCard.tsx`·`src-tauri/`·`tauriProjectGateway.ts` 무변경. lease는 헬퍼로만 취득·갱신·반납.

### 검증 단계와 결과
- `npm run check` → typecheck 통과, vitest **539 passed / 20 파일 / 0 failed**,
  `vite build`(327 modules) 성공.
- `cargo test --manifest-path src-tauri/Cargo.toml` → **442 passed / 0 failed / 0 ignored**
  (`~/.cargo/bin`을 PATH에 추가해 실행). 이 세션은 `src-tauri/`를 만지지 않았다.
- 마법사 복사 버튼 넷을 세는 기존 검사가 **수정 없이** 통과(완료 조건 11).
  `.skip`·`.todo` 신규 0건.
- 무변경 확인은 파일·심볼 단위로 했다 — `heartbeat.setupStages`를 읽는 자리는 착수 시점과 같은
  세 곳(`remaining`·`requiredStages`·마법사 `.map`)이고 이 세션이 셋 다 만지지 않았다. 이 트리엔
  타 세션 미커밋 변경이 겹쳐 있어 "`git diff`가 비어 있다"는 쓰지 않았다.

### 후속 / 리스크
- **작업 지시에서 벗어난 자리 1건 — 기존 검사 2줄 수정.** `IntegrationsView 하트비트 잡 지금 실행`의
  두 검사가 `row.querySelector("pre code")`로 실행 실패 명령을 집었는데, 그 픽스처는 084 경고도 함께
  뜨는 상태라 안내의 첫 `<pre>`가 앞으로 왔다. 선택자를
  `.heartbeat-run-failure-command code`로 좁혀 원래 겨냥하던 요소를 이름으로 못 박았다. 이름·기대값·
  나머지 단언은 그대로이고 검사를 약화시키지 않았다. 보고서에 별도 절로 남겼다.
- **dream 카드는 아직 이 안내를 그리지 않는다.** TASK-109의 몫이고, SPEC-034 완료 조건 10(두 카드가
  같은 조건·같은 문구)은 그 작업에서 닫힌다. 이 세션은 `DreamCard.test.tsx` 픽스처만 채웠다.
- **macOS 밖은 재시작 명령이 없다.** 화면이 "앱이 방법을 알지 못한다"로 말한다. 검사 6이 고정.
- **역할 밖 관찰(무수정)**: TASK-104가 여전히 `in_progress`이고 lease는 2026-08-04T19:37:05Z에
  만료됐다. 개발자 자격 판정은 `todo`만 보므로 그 작업은 누구에게도 잡히지 않는 상태로 남아 있고,
  TASK-106은 그 때문에 계속 선행 미충족이다. 만료된 `SPEC-009.yml`도 잔존. §4대로 손대지 않았다.

## TL 커밋 컷 세션 (08:20~08:50 KST)

### 착수 시점 실측
- 데몬 정지 확인(launchctl 등록 없음, 프로세스 없음). 라이브 세션 0. 잔존 lease는 만료된 SPEC-009.yml·TASK-104.yml뿐.
- 밤사이 착지: TASK-100·102·103·105(SPEC-031~033), SPEC-034 승인(DECISION-3ECEDCA1)·분해·107~109 착지, SPEC-033 승인(DECISION-8D3F0D0F). TASK-096 QA 확정(QA-634EB74E, confirmed).
- SPEC-035(중단 세션 회수 정책, source: IDEA-7EBEDE40) user_review 도달 — 사용자 도장 대기.
- TASK-104: 죽은 세션(마지막 heartbeat 18:57Z, lease 만료 19:37Z), in_progress 방치. 잔여물은 게이트를 깨지 않음(아래 수치). IDEA-7EBEDE40이 지목한 구멍의 재발 사례 — TASK-106이 선행 미충족으로 막혀 있음.
- 어제의 하트비트 체크아웃 시한폭탄은 컷 전에 해체 완료(v0.8.0-jobsd 복귀, 17:00Z경). 데몬 재가동 안전.

### 컷 게이트 실측 (데몬 정지·lease 무활동 상태)
- cargo test 442 passed / 0 failed / 0 ignored
- cargo fmt --check exit 0, cargo clippy --all-targets -D warnings 경고 0
- npm run check exit 0 (typecheck·vitest·vite build 327 modules)
- TASK-104 잔여물 포함 상태의 수치다. 커밋에 포함하고 PR 본문에 WIP임을 명기.

### 커밋
- 브랜치 claude/qa-batch-20260805, 전체 트리 커밋(TASK-096~109 산출 + 워크플로 문서 + 로그). push 후 PR 생성은 사용자 몫(본문 파일 전달).

### 후속 / 리스크
- 사용자 관문: SPEC-035 도장(회수 정책 — 104 같은 고아의 구조적 해법), TASK-104 처리(SPEC-035 대기 vs TL 워커 즉시 인수), QA 대기 9건, PR 머지, 데몬 재가동, claude-heartbeat v0.8.0 PR/릴리스.

# 2026-08-04 개발 로그

## 세션: 하트비트 동시성 문서 팔로업 → 사고 대응 → 데몬 P0 구현 → 전수검수

### 목표 / 요청 범위
- 하트비트 병렬 실행 가능 여부 확인(문서 팔로업)
- 개발 보드 카드 오버플로우 스크린샷 원인 분석
- 크로스 프로젝트 잡 증발 사고 대응
- claude-heartbeat 프로덕트화 계획 수립·P0 구현
- 양 저장소 전수검수

### 핵심 결정과 근거
- 하트비트 데몬(claude-heartbeat)은 프로젝트 간·내 모두 사실상 직렬. 병렬 관련 워크플로 작업(SPEC-010/013 계열)은 "병렬을 켜는" 것이 아니라 전제 조건(depends_on·선점 헬퍼) 구축이었음을 스펙 원문으로 확인.
- 카드 오버플로우: `.task-stack`(App.css:420) 암시적 auto 트랙이 excerpt 속 줄바꿈 불가 토큰의 min-content에 밀려 컬럼을 뚫는 버그. Chrome·WKWebView 양쪽에서 실데이터로 재현 확정. 수정은 `minmax(0,1fr)` + `overflow-wrap:anywhere` 2줄이나, App.css가 qa_waiting 작업들과 충돌하므로 아이디어 등록 경로로.
- 15:15 크로스 프로젝트 잡 증발 사고: 앱 `merge_block`(heartbeat_service.rs:425)이 자기 slug 잡 + dream만 보존해 타 프로젝트 잡이 병합에서 탈락. HEARTBEAT.md 직접 편집으로 workflow-labs 잡 3개 복원(이전 값 그대로).
- 데몬 개선을 워크플로가 아닌 claude-heartbeat 저장소 별건으로 진행하기로 결정. 계획: `docs/planning/2026-08-04-productization.md` (claude-heartbeat 저장소).

### 변경한 파일·모듈 (claude-heartbeat 저장소)
- core.py: jobs.d/<slug>.md 병합 파싱(P0-A), 스케줄러 배리어 제거 + _inflight 가드(P0-B), condition stdout 사유 통로(last_condition_output)
- cli.py: `heartbeat migrate` (--dry-run)
- tests: test_jobs_d.py, test_dispatch.py, test_migrate.py 신규 + 기존 테스트 튜플 대응
- docs/config-contract.md 신규, CHANGELOG 0.8.0 후보 항목
- workflow-labs 저장소는 이 세션에서 코드 변경 없음

### 검증 단계와 결과
- pytest 99 passed / 7 skipped (claude-heartbeat)
- migrate dry-run 실파일 검증 통과
- 15:58 workflow-labs 기획자 재가동 확인 → IDEA-6588A18A 선점, SPEC-019 드래프트 생성

### 전수검수 결과 (탐색 에이전트 2 + 런타임 실측)
- workflow-labs: 미커밋 +10,587줄 = qa_waiting 17건 구현분(HEAD는 문서 커밋뿐). 증발 버그 테스트 0(단일 SLUG 픽스처). HeartbeatCard.test.tsx 부재. 판정 4벌(sh 원본·ps1·role_eligibility.rs·scripts 잔재). wf-claim.sh 설치본 부재.
- claude-heartbeat: 쓰기 경로(cmd_install, heartbeat-register 스킬) jobs.d 미전환. 문서 3종 스테일. WIP 2건(model·cwd) CHANGELOG 무기록. launchd 라벨 코드(com.claude-heartbeat) vs 실설치(com.catze.dream-heartbeat) 불일치 → 앱이 서비스 미등록으로 오판 중. state.json write_text 비원자적. 코드 지적 5건(cwd 체크 순서, quota 락 밖 변이, 그룹 내 now 고정, max_workers 큐잉 시 due 스킵, once의 inflight 미확인).

### 이어진 작업 (같은 세션 후반)
- 검수 지적 수정: state.json 원자적 쓰기, CWD 확인 순서, quota 락, 잡별 due 시각, once 경고, jobs.d 파일 필터, init의 jobs.d 생성, 계약 문서 보강 (테스트 101 → 104 그린)
- P1 전환: `heartbeat install` 잡 등록을 jobs.d로, heartbeat-register 스킬·README/setup/ko 문서 전환
- claude-heartbeat `claude/v0.8.0-jobsd` 브랜치에 커밋 2개 (6042cc3, 2fa0a7d). push 미실행
- workflow-labs 아이디어 4건 등록: 증발 버그 수정, 카드 오버플로우, 판정 단일화+사유 표시, jobs.d 전환
- 기획자 하트비트가 기존 아이디어 2건을 SPEC-019·SPEC-020(user_review)으로 처리 완료
- 16:26 기획자 세션 종료 확인 후 launchctl bootout으로 하트비트 데몬 비활성화 (사용자 체크리스트)

### 후속 / 리스크 (오전 시점 — 아래 TL 세션에서 대부분 해소)
- 구데몬 재시작 완료(16:56, 새 코드 v0.8.0 가동), migrate 2회 실행 완료(양 프로젝트 jobs.d 이전).
- 아이디어 4건 등록 완료 → 전부 스펙·구현 라인 진입.

## TL 병렬 지휘 세션 (저녁, 2일차) — 인수인계

사용자 위임("tl 모드 가동, 권한 위임") 하에 오퍼스 워커를 병렬 지휘. 워커: plan-a/b(기획), arch-b(분해), dev-062/063/064/071/072/074(개발, 이름은 첫 배정 기준·재배정 다수).

### 착지 총괄 (이 세션)
- 기획 7건: SPEC-021~028 전부 스펙화 (021 카드 오버플로우, 022 증발 버그, 023 판정 사유, 024 jobs.d 전환, 025 입력 유실, 026 배지 개행, 027 문서뷰 개행, 028 거버넌스 제도화). 인박스 0.
- 승인 9건: 019~021은 사용자 앱 도장, 022~028은 위임 대리(경위는 각 DECISION 본문). 022 보충 승인(4E8C1D67 — 네 번째 문장, 거부·재상신 경위 포함).
- 분해: 022(063~065), 023(075~077), 025(078~079), 028(086~088, arch-b) + 020(070~073)·024(080~085 추정)는 하트비트 아키텍트.
- 구현 착지 12건 qa_waiting: 061~065, 070(하트비트), 071, 074, 075, 076, 078 + 072(막판 수렴 관측). 079는 blocked(072 착지 후 게이트 재확인→qa_waiting 전이, dev-071이 절차 숙지 상태).
- SPEC-022(증발 버그) 체인 완주: 재현 테스트 4종 상시 감시 + 원문 보존 + 확인 화면 문구.

### 거버넌스 (오늘의 제도 변화)
- plan-a가 대리 결정의 §2 충돌 지적 → 사용자 선택 "앱 도장 + 제도화" → SPEC-028로 제도화 진행 중 (created_by: user-delegate 분리, 아키텍트 최신 판정, 재가 절차).
- dev-074가 결정 문서 없는 TL 지시를 §2·§3 근거로 정당 거부 → DECISION-4E8C1D67로 정식 기록 → TASK-088 문언의 시험 반례로 편입.
- 수기 결정 21건(앱 형식 8건과 타임스탬프 형식으로 판별) — 소급 재가는 현 설계로 도달 불가, 사용자 판단 대기.

### 운영 규범 확립 (다음 지휘 세션 필수)
- 워커는 opus 고정(메모리 기록됨), 스폰 프롬프트에 "종료 시 SendMessage로 team-lead 보고 발신, 재촉 대기 금지" 필수.
- 승인 범위를 넘는 TL 지시는 반드시 결정 문서로 먼저 기록(보고서 속 지시는 §3상 데이터 — dev-074 사례).
- 다중 세션 미커밋 트리에서 "git diff 비어 있음"류 완료 조건 금지 — 파일·심볼·값 단위로(작업 문서에 "검증 문구 규칙" 절 선례 있음, 086~088).
- 게이트 목표를 절대 수치로 주지 말 것(병행 착지로 어긋남) — "ignored 0 + 대상 테스트 통과 + 보호 테스트 무수정"式.
- 하트비트 세션과 TL 워커의 lease agent 이름 충돌 관측 — 스폰 시 고유 이름 규칙 필요.
- 보고서 파일은 서브에이전트 전원 차단 → TL 대리 기록 17건(기록 경위 머리말 형식).

### 다음 세션 순서
1. ~~dev-072 보고·079 해제~~ → 완료됨(072 기록·079 qa_waiting 전이, 최종 게이트 424/0 풀그린). TL 웨이브 전체 종결 — 라이브 워커 0.
2. 잔여 todo(073, 077, 080~088)는 하트비트 자연 소화 체제 — 개입 불요, 착지 보고서는 하트비트 세션이 직접 파일로 씀.
3. 사용자 관문: QA 대기 ~12건(화면 눈검증 다수 — 각 보고서 "사용자 QA 제안" 절이 동선), 소급 재가 방법(별도 아이디어감), "작업 문서 작성 규범 계약 편입" 아이디어(TASK-088 착지 후), claude-heartbeat 브랜치 push/PR/0.8.0 릴리스 결정.
4. QA 통과분 커밋 필수(v0.1.8 후보 +1만줄이 여전히 미커밋 — 최대 유실 리스크). 커밋 전 launchctl bootout, 후 재가동.
- 만료 lease SPEC-009.yml 잔존(사용자 판단), TASK-070 계열 clippy 1건(heartbeat_process.rs:216) 추적.

## 세션: 기획자 역할 — IDEA-26897DA0 → SPEC-021 (17:58~18:05 KST)

### 목표 / 요청 범위
- `.workflow` 공통 규칙 + planner 역할 계약을 따라 처리 대상 하나만 기획서로 만든다.

### 자격 판정
- 미답변 수정 요청 결정: 없음. `DECISION-2F71D20D`는 SPEC-015가, `DECISION-FEB99DAB`는 SPEC-013이 `source_decision_id`로 답변 완료.
- 미처리 아이디어 4건: IDEA-26897DA0 / IDEA-4141EE4C / IDEA-A61F6A81 / IDEA-CAB890F1 (`created_at` 전부 동일 → id 사전순 선두 채택).
- `.workflow/.runtime/leases/SPEC-009.yml`은 만료(2026-08-03T01:20Z)라 아무것도 덮지 않음.

### 핵심 결정과 근거
- 대상: IDEA-26897DA0(개발 보드 카드 오버플로우). 선점 `lease-59057-20260804075815` → 작성 후 해제.
- 아이디어의 원인 진단을 코드에서 독립 검증함. `.task-stack`(App.css:420)에 트랙 정의 없음 → 암시적 `auto` 트랙의 min이 카드 min-content. `.task-card`·`.task-column` 모두 `min-width` 해제 없음. 보드 트랙 `minmax(178px, 1fr)`의 min이 고정값이라 트랙이 안 넓어지고 아이템이 밖으로 넘침.
- 실제 재현 데이터 확인: TASK-049(`qa_waiting`) 요약문에 `` (`HeartbeatCard.tsx:246`~`:252`). `` 33자 무공백 런.
- 한글은 기본 줄바꿈으로 접히므로(저장소가 `word-break: keep-all`을 따로 거는 것이 반증) 원인은 ASCII 런으로 한정.
- 같은 excerpt를 쓰는 리스트·캘린더·인박스·검색은 이미 막혀 있고 보드만 무방비임을 확인 → 범위를 보드로 한정.
- 검증 가능성 문제를 요구사항으로 승격: jsdom은 레이아웃 미계산 + `App.css`는 `App.tsx`만 import → 컴포넌트 테스트에 스타일시트 자체가 없음. 회귀 검증 수단을 확인 필요 2번으로 올림.
- 구현 처방(CSS 두 줄)은 아키텍트 몫이라 요구사항에 넣지 않고 확인 사실·확인 필요로만 기록.

### 변경한 파일
- `.workflow/도그푸딩--wf_ae6cd700/specs/SPEC-021.md` 신규 (`status: user_review`, `source_idea_id: IDEA-26897DA0`)
- 확인 사실 18항 / 요구사항 R1~R6 / 확인 필요 3건 / 완료 조건 14항

### 검증 단계와 결과
- App.css 줄 번호 전수 대조 (407·408·410·420~428·442·477·246·557·648·50·572) — 전부 일치.
- `git diff src/App.css`에 보드 관련 규칙 변경 없음 확인 (줄 번호는 작업 트리 기준으로 유효).
- 자격 판정을 `wf-eligible.sh planner`로 교차 확인 (exit 0).

### 후속 / 리스크
- 남은 미처리 아이디어 3건: IDEA-4141EE4C(판정 단일화+사유 표시), IDEA-A61F6A81(크로스 프로젝트 잡 증발), IDEA-CAB890F1(jobs.d 전환).
- IDEA-A61F6A81은 데이터 손실 계열이라 우선순위 재고 여지 있음. 현행 계약은 `created_at` 순서만 정의하므로 동률일 때의 우선순위 규칙이 없다는 점이 드러남.
- SPEC-021은 확인 필요 2번(회귀 검증 수단)이 승인되어야 완료 조건 1~4의 검증 방법이 확정됨.

## 세션: 기획자 역할 — IDEA-A61F6A81 → SPEC-022 (17:17~17:35 KST)

### 목표 / 요청 범위
- `.workflow` 공통 규칙 + planner 역할 계약을 따라 처리 대상 하나만 기획서로 만든다.

### 자격 판정
- 미답변 수정 요청 결정: 없음(DECISION-FEB99DAB→SPEC-013, DECISION-2F71D20D→SPEC-015로 답변 완료).
- 미처리 아이디어 6건: IDEA-4141EE4C / IDEA-A61F6A81 / IDEA-CAB890F1(동률 `created_at` 07:23:55) + IDEA-19843535 / IDEA-7BCB8947 / IDEA-5C6073A5(08:08~08:09).
- `wf-eligible.sh planner` exit 0으로 교차 확인. 미만료 lease 없음(SPEC-009.yml은 2026-08-03T01:20Z 만료).

### 핵심 결정과 근거
- 대상: IDEA-A61F6A81(크로스 프로젝트 잡 증발). 선점 `lease-68229-20260804081808` → 작성 후 해제.
- 동률 3건 중 사전순 선두(IDEA-4141EE4C)가 아니라 데이터 손실 계열을 택함. 근거는 실제 사고 발생분이라는 점과 직전 세션이 남긴 우선순위 재고 노트(이 파일 77행). 계약은 `created_at` 순서만 정의하므로 동률 처리는 여전히 세션 판단.
- 아이디어의 원인 진단을 코드에서 독립 검증: `merge_block`(heartbeat_service.rs:425)의 두 인자가 모두 자기 slug 산출물이고, `preserved_role_jobs`(:493)·`preserved_dream_job`(:514)의 보존 범위도 자기 slug. 쓰기는 `install_managed_jobs`가 블록을 통째 교체(heartbeat_jobs.rs:141·140 주석).
- 아이디어에 없던 사실 3건 추가: (1) 확인 화면의 "지워짐" 목록도 자기 slug만 봐서 예고가 없음(HeartbeatCard.tsx:668), (2) 재설정 확인 문구가 "같은 블록의 다른 잡은 그대로 둡니다"로 사실과 반대(:1021), (3) 전 역할 해제 시 블록 자체가 제거되어 남의 잡이 함께 소멸(테스트 :2358~:2374가 그 동작을 고정).
- 현재 기기 상태를 확인 사실로 기록: `~/.claude/heartbeat/HEARTBEAT.md` 부재, `jobs.d/-Users-catze-project-workflow-labs.md`만 존재, 앱은 jobs.d 미인지(src-tauri에 참조 0) → 지금 이 기기의 피해 반경은 0에 가까움. 이 사실이 IDEA-CAB890F1과의 범위 충돌을 만들어 확인 필요 1번(지금 고칠지 / jobs.d 전환에 맡길지)으로 승격.
- 보존 방식(원문 그대로 vs 앱 렌더러 재작성)은 사용자 눈에 보이는 보장이 갈리므로 확인 필요 2번으로. 구현 처방은 아키텍트 몫이라 요구사항에 넣지 않음.
- 남의 잡 검증 실패가 이 프로젝트의 저장을 막는 교착 가능성(validate_preserved, :433)을 R2로 승격.

### 변경한 파일
- `.workflow/도그푸딩--wf_ae6cd700/specs/SPEC-022.md` 신규 (`status: user_review`, `source_idea_id: IDEA-A61F6A81`)
- 확인 사실 18항 / 요구사항 R1~R5 / 확인 필요 3건 / 완료 조건 13항

### 검증 단계와 결과
- 인용 줄 번호 전수 대조 (heartbeat_service.rs·heartbeat_jobs.rs·heartbeat_roles.rs·heartbeat_dream.rs·heartbeat_status.rs·HeartbeatCard.tsx·DreamCard.tsx) — 초안의 HeartbeatCard.tsx:1096→1094, DreamCard.tsx:836→835 2건 교정.
- `merge_block`이 HEAD(:370)와 작업 트리(:425) 동일함을 확인 — 미커밋 변경분이 만든 결함이 아님.
- 기존 문서 중복 확인: `merge_block`/증발 언급은 TASK-054(순서 주석)·REPORT-TASK-012-DEV뿐, 이 결함을 다루는 기획서 없음.

### 후속 / 리스크
- 남은 미처리 아이디어 5건: IDEA-4141EE4C, IDEA-CAB890F1, IDEA-19843535, IDEA-7BCB8947, IDEA-5C6073A5.
- SPEC-022 확인 필요 1번이 뒤집히면 R1~R2가 IDEA-CAB890F1(jobs.d 전환) 기획서로 넘어가고 이 기획서는 R3~R4만 남음. 두 기획서가 같은 실패를 다루므로 승인 순서가 곧 범위 결정.
- 확인 사실 18의 중복 정의 실패 모드(앱 미전환 상태에서 migrate 실행)는 이 기획서 범위 밖 — jobs.d 전환 쪽에서 다뤄야 함.

## TL 병렬 지휘 세션 (저녁, 3일차) — 잔여 todo 소진 웨이브

### 목표 / 요청 범위
- 사용자 위임("tl 작업 이어서, 권한 위임, 남은 것 달리자") 하에 잔여 todo 11건(TASK-073·077·081~089)을 오퍼스 워커 병렬 지휘로 소진한다.

### 착수 시점 상태 실측
- todo 11건: 073, 077, 081~089 (TASK-089는 SPEC-026 파생으로 어제 인수인계 이후 추가, TASK-080은 하트비트가 소화해 qa_waiting).
- lease는 만료된 SPEC-009.yml 하나뿐, migration.lock 없음. wf-claim.sh 설치본 존재(오늘 16:34).
- 근거 결정 전부 승인 확인: SPEC-020(53577F93)·023(9E5D2C71)·024(3C8F1A42)·026(8D4A2E96)·028(2A9D7B31).
- 하트비트 데몬 가동 중(com.catze.dream-heartbeat). launchctl bootout은 세션 권한 분류기에 차단되어 데몬은 켠 채 진행 — 충돌 쌍은 lease 선점으로 방어.

### 웨이브 설계 (파일 겹침 분석 결과)
- 073↔077이 HeartbeatCard.tsx·IntegrationsView.test.tsx에서 충돌(077 문서의 병행 분석은 065 미착지 시점 기준이라 낡음) → 한 워커(tl-dev-077)에 직렬 배정, 시작 시 두 lease 선claim으로 하트비트 세션 끼어들기 차단.
- 웨이브 1 (병렬 6기, 오퍼스): 077→073 체인, 081, 085, 086, 087, 089.
- 웨이브 2 이후 게이트: 082(081+077 착지 후), 088(086+087 착지 후) → 083(082+073 착지 후) → 084(083 착지 후).

### 착지 총괄 (웨이브 종결, 20:12 KST)
- TL 워커 착지 10건: 077, 081, 082, 083, 084, 085, 086, 087, 088, 089 (재배정 사슬: tl-dev-081→082→084, tl-dev-087→088, tl-dev-077→083). 073은 하트비트 세션(developer-claude)이 선점 경합에서 이겨 직접 착지(09:58Z) — lease 방어가 설계대로 동작, 산출물 공존 실측 확인(077 정정 보고).
- 하트비트 병행 착지: 090·091(SPEC-027), 092(SPEC-022 보충). 웨이브 종료 시점 전판 todo 0 / in_progress 0 / blocked 0 — qa_waiting 42건으로 수렴. 라이브 워커 0, 잔존 lease는 만료된 SPEC-009.yml뿐.
- 웨이브 최종 게이트(084 워커 완주): cargo test 401/0/0, cargo fmt --check 클린, npm run check 474/0 + 빌드 성공. clippy --all-targets는 heartbeat_process.rs:216(cloned_ref_to_slice_refs, 타 세션 미추적 파일) 1건만 잔존 — 해당 lint 제외 시 경고 0. 웨이브 산출 코드의 clippy 지적 0.
- 대리 기록 10건(기록 경위 머리말 형식): REPORT-TASK-077·081·082·083·084·085·086·087·088·089-DEV.md. 077·087 보고서에는 후속 정정 절 포함.
- 스펙 완주: SPEC-024(jobs.d 전환, 080~085) R1~R7 전부 닫힘 — 앱이 더 이상 ~/.claude/HEARTBEAT.md를 쓰지 않음(증발 사고 구조 원인 종결). SPEC-028(거버넌스, 086~088) 완주 — user-delegate 대리 결정 계약 명문화, rules_version 8. SPEC-020·023 카드 표시 사슬 완주.
- 거버넌스 산출: 086의 상신(완료 조건 1이 판정으로 못 닫힘, 실존 반례 DECISION-4E8C1D67)을 088이 실측으로 받아 거짓 문언 없이 부분 착지. 기획자 분기 created_by 비대칭은 계약에 자기 결함으로 명시(임시 문언) + 후속 아이디어 IDEA-ACAE7F01 등록.
- 사용자 아이디어 등록 1건: IDEA-A11E3E67(개발 보드 기획서별 그룹 보기·그룹 QA 신호 — QA 단위 어긋남 동기).
- launchctl bootout은 세션 권한 분류기 차단으로 미실행 — 데몬 켠 채 lease 경합으로 완주했고 실충돌 0. 커밋 전 bootout은 여전히 사용자 몫.

### 다음 관문 (전부 사용자)
1. QA 42건 — 스펙 17개 전부 사슬이 닫혀 있어 어느 그룹이든 통째 QA 가능. 권장 순서: SPEC-022+024(저장 경로·증발·전환 상호작용, 한 세션에서), SPEC-020+023(카드 실행 표시 계열), SPEC-028(앱 기동 시 rules_version 8 설치 확인 겸), SPEC-021·025·026·027(화면 계열), 이후 구배치(009·011·012·013·015·016·017·018).
2. QA 통과분 커밋(+1만 줄 미커밋 — 최대 유실 리스크). 커밋 전 launchctl bootout, 후 재가동.
3. claude-heartbeat claude/v0.8.0-jobsd push/PR/릴리스 결정, heartbeat_process.rs:216 clippy 1건(소유 세션의 std::slice::from_ref 한 줄 수정).
4. 수기 결정 21건 소급 재가 방법(새 계약상 created_by: user인 채로는 재가 경로가 닿지 않음 — REPORT-TASK-088 참조), 만료 lease SPEC-009.yml 정리.

### 커밋 컷 실행 (같은 세션 후반, 21:00~21:30 KST)
- 사용자 QA 도장 42건 완료(077은 사유 두 경로 — no-target 대응 문장·데몬 타임아웃 원문 통과 — 실물 확인 후 마지막 도장). 컷 직전 하트비트가 093~095 착지·096~098 분해·SPEC-029~032 기획까지 추가 산출.
- 컷 절차: 잔존 기획자 세션 2기 드레이닝 감시 → 사용자 launchctl bootout → 풀 게이트 실측 → 커밋.
- clippy 잔존 1건(heartbeat_process.rs:216, cloned_ref_to_slice_refs)은 소유 작업(TASK-070 계열)이 completed로 닫혀 주인이 사라진 상태라, 보고서 3건의 공통 처방대로 TL이 `std::slice::from_ref(&script)` 한 줄로 정리 — 이 웨이브 최초의 clippy --all-targets 완전 클린.
- 컷 게이트(데몬 정지·lease 무활동): cargo test 401/0/0, fmt 클린, clippy 클린, npm run check 474/0 + 빌드 성공.
- 커밋 994c87b `claude/qa-batch-20260804` — 351파일, +46,306/−1,180. push 완료. PR 생성은 세션 권한 차단으로 사용자 직접 실행(본문 파일 전달).
- 잔여: 데몬 재가동(사용자), v0.1.8 버전 범프·릴리스 결정, claude-heartbeat 저장소 push/PR/릴리스, 문서 용량(아카이브·스캔 인덱스) 아이디어 등록 대기 — 오늘 "condition 타임아웃 (10s)" 실측이 근거.

## SPEC-027 작업 분해 (아키텍트 세션)

### 목표 / 요청 범위
- 프로젝트 아키텍트 역할로 승인된 기획서 하나를 개발 작업으로 분해한다. 대상이 없으면 NO_ELIGIBLE_WORK만 보고.
- `wf-eligible.sh architect` exit 0. 파생 작업 없는 승인 결정 2건 — DECISION-6F1B8C53(SPEC-027, 09:31Z), DECISION-4E8C1D67(SPEC-022 보충 승인, 09:38Z).
- 이른 것부터 처리하는 직전 세션(REPORT-SPEC-026-ARCH)의 순서 규칙을 따라 SPEC-027 선택. 선점 `lease-10674-20260804100100` 취득 → 갱신 → 반납.

### 핵심 결정과 근거
- **분해 2건**: TASK-090(뷰어에 옵트인 프롭 + 의존성) → TASK-091(아이디어 호출부에서만 켜고 양쪽을 검사로 고정). 파일이 하나도 겹치지 않고, 순서는 충돌이 아니라 프롭 의존에서 나온다.
- **구현 수단(기획서 확인 사실 14가 아키텍트에게 넘긴 항목)**: `remark-breaks` 의존성 + `preserveLineBreaks` 프롭. 대안이던 지역 mdast 플러그인은 `mdast-util-newline-to-break`를 손으로 다시 짜는 일이고, CSS `pre-wrap`은 R3 위반이라 탈락.
- 호환성은 읽기만으로 확인: remark-breaks 4.0.0의 의존(`unified: ^11`, `@types/mdast: ^4`)과 설치본(unified 11.0.5, @types/mdast 4.0.4, react-markdown 10.1.0, remark-gfm 4.0.1)이 맞물린다. 실제 설치는 하지 않음 — 아키텍트는 제품 코드를 고치지 않는다. 해석된 버전 보고를 TASK-090 완료 조건으로 넘김.
- **SPEC-008 "세 뷰 동일 렌더"와의 관계(결정 문서가 명시를 요구)**: 어긋나는 범위는 소프트 개행 하나뿐이고, 기존 테스트 이름 `renders markdown formatting like the other document views`는 고치지 않는다(그 검사가 확인하는 불릿·링크·강조는 세 화면에서 여전히 같고, 기획서 완료 조건 4가 "수정 없이 통과"를 요구). 예외는 새 검사 두 개의 이름으로 남긴다.
- **TASK-079와의 순서(결정 문서가 판단을 요구)**: 착지 확인(`qa_waiting`, 변경분이 작업 트리에 있음). 그럼에도 `depends_on: [TASK-090, TASK-079]`로 적음 — `qa_waiting`은 선행 충족이라 지금 막는 것이 없고, QA가 `todo`로 되돌릴 때만 발동해 같은 두 파일의 동시 편집을 막는다.
- 개발 작업 상세의 "켜지지 않았음" 검사는 TASK-090 이전에도 통과한다. 그래서 빨간불 확인 절차(호출부에 프롭을 임시로 넣어 실패를 보고 되돌리기)를 완료 조건에 넣음 — TASK-089가 CSS 검사에 쓴 선례.

### 변경한 파일
- `.workflow/도그푸딩--wf_ae6cd700/tasks/TASK-090.md` 신규 (`status: todo`, 선행 없음)
- `.workflow/도그푸딩--wf_ae6cd700/tasks/TASK-091.md` 신규 (`status: todo`, `depends_on: [TASK-090, TASK-079]`)
- `.workflow/도그푸딩--wf_ae6cd700/reports/REPORT-SPEC-027-ARCH.md` 신규

### 검증 단계와 결과
- 기획서 확인 사실 재실측 — 어긋난 값 0건, 줄 번호까지 동일. `MarkdownBody.tsx:1`~`:19`(플러그인 remarkGfm 하나), 호출부 셋(`IdeaInbox.tsx:236`·`SpecWorkspace.tsx:144`·`DevelopmentBoard.tsx:274`), `.markdown-body`에 `white-space` 선언 없음, remark-breaks 미설치.
- 테스트 개수 실측(10:05Z): IdeaInbox 34건, DevelopmentBoard 32건, MarkdownBody 1건. 셋 다 소프트 개행 입력을 쓰지 않음(기획서 확인 사실 12 확인).
- 병행 안전: `todo`·`in_progress`·`blocked` 작업 전수 확인 결과 MarkdownBody 계열·package.json·DevelopmentBoard.test.tsx를 범위에 둔 미완료 작업 0건. TASK-089는 CSS만 범위로 두고 `.tsx`·이 테스트 파일을 명시적으로 제외.
- 선점 뒤 자격 재확인(파생 작업 0건), `depends_on` 대상 문서 존재 확인, 순환 없음.
- 실행 명령은 읽기뿐: `npm ping`, `npm view remark-breaks`, `node -p`로 설치본 버전, `wf-eligible.sh architect`. `node_modules`·제품 코드 무변경.

### 후속 / 리스크
- **아키텍트 대상 1건 미처리: DECISION-4E8C1D67(SPEC-022 보충 승인).** lease 없음. 그 분해는 "SPEC-024 분해가 흡수할 수 있으면 별도 작업을 만들지 말 것"을 요구하므로 REPORT-SPEC-024-ARCH와 SPEC-024 파생 작업 범위를 먼저 읽어야 한다.
- 문단 간격(28px)과 줄 간격(21px) 차이가 사용자 QA에서 약하다는 판정을 받을 수 있음. CSS 조정은 기획서 제외 범위라 그 경우 후속 아이디어 감으로 넘긴다고 TASK-091에 명시.
- 재현 데이터가 저장소에 없다(아이디어 26건 중 문단 내 개행 0건). 검증 입력도 QA도 새로 쳐야 한다.
- 기획서 완료 조건 11은 해당 없음(확인 필요 1번이 아이디어 뷰 한정으로 승인 → R6 둘째 항목 미발동).

## 세션: 기획자 역할 — IDEA-ACAE7F01 → SPEC-030 (19:55~20:05 KST)

### 목표 / 요청 범위
- 기획자 역할로 대상 하나만 처리. 없으면 NO_ELIGIBLE_WORK 보고 후 정지.
- 자격 판정: `wf-eligible.sh planner` exit 0. 수정 요청 2건(DECISION-FEB99DAB→SPEC-013, DECISION-2F71D20D→SPEC-015)은 모두 후속 기획서가 있어 처리 완료 상태. 미처리 아이디어는 IDEA-ACAE7F01 1건뿐이라 그것을 선택(역할 계약의 "수정 요청 우선" 규칙 적용 결과 대상 없음 → 아이디어로 이동).
- 선점 `lease-42085-20260804105615` 취득 → 갱신 → 반납(전부 exit 0).

### 핵심 결정과 근거
- **아이디어 처방을 그대로 수용**: 조건 스크립트 기획자 분기에 `created_by` 필터 추가 + `CONDITION_SCRIPT_VERSION` +1, 공통 규칙의 임시 문장 삭제 + `WORKFLOW_RULES_VERSION` +1.
- **범위를 한 자리 넓혀 제안(확인 필요 1번)**: 아이디어는 비교 루프만 지목했으나, 후보 선택부에도 같은 필터가 없어 반대 방향 갈림(대리 `revision_requested` 단독 → 스크립트만 eligible)이 성립하는 것을 실측으로 확인. 원인이 하나라 한 자리만 고치면 방향만 바뀐 채 남는다. 아이디어 문언을 넘는 부분이므로 사용자 결정 항목으로 올리고, 뒤집을 때 어떤 완료 조건이 빠지는지 명시.
- **버전 값을 고정 숫자로 안 적음**: 상수(6/8)와 설치본(4/7)이 어긋나 있고 미착지 작업이 여럿이라, TASK-088 선례대로 "착수 시점 값 +1"로 요구.
- **제외**: `outcome` 값 목록 차이(알려진 차이 5의 나머지 절반), 앱 판정 변경, 소급 정리. 각각 사유를 제외 범위에 기재.

### 변경한 파일
- `.workflow/도그푸딩--wf_ae6cd700/specs/SPEC-030.md` 신규 (`status: user_review`, `source_idea_id: IDEA-ACAE7F01`)

### 검증 단계와 결과
- 아이디어의 재현을 임시 디렉터리 픽스처로 직접 실행(저장소 파일은 읽기만). 설치본 v4: `exit 0` → 대리 승인 추가 후 `exit 1`. 작업 트리 상수 v6 본문: `eligible` → `no-target`. 두 버전 모두에서 갈림 확인.
- 반대 방향 실측: 대리 `revision_requested` 단독 → `planner exit 0`(eligible), 같은 픽스처에서 `architect exit 1`(아키텍트 분기는 이미 닫힘).
- 앱 필터 확인: `read_spec_decisions`(`fs_project_repository.rs:1560`~`:1590`)가 `created_by: user` + outcome 3종으로 거르고 `latest_revision_requests`(`:1653`)는 그 목록 안에서만 비교.
- 저장소 실측: 결정 문서 75건 전부 `created_by: user`, `user-delegate` 0건 → 실저장소 판정 불변이 완료 조건(11번).
- 병행 안전: `heartbeat_condition.rs`·`project_instructions.rs`를 범위에 둔 `todo`·`in_progress` 작업 0건(미완은 전부 `qa_waiting`). QA 반려 시 겹칠 수 있다는 점을 확인 사실 11에 기재.

### 후속 / 리스크
- 확인 필요 1번의 사용자 결정에 따라 완료 조건 1번과 R1 첫 항목, R3 둘째 항목이 함께 빠진다.
- `outcome` 값 목록 차이는 이 기획서 밖. 후속 아이디어 등록 권장(제외 범위에 기재).
- 기존 게이트에 타 세션 소유 위반 2건(`heartbeat_status.rs` fmt, `heartbeat_process.rs:216` clippy)이 남아 있어, 이 기획서 파생 작업의 게이트 판정 시 구분 필요(확인 사실 13).

## SPEC-030 작업 분해 (아키텍트 세션)

### 목표 / 요청 범위
- 프로젝트 아키텍트 역할로 승인된 기획서 하나를 개발 작업으로 분해한다. 대상이 없으면 NO_ELIGIBLE_WORK만 보고.
- `wf-eligible.sh architect` exit 0. 파생 작업 없는 승인 결정은 DECISION-4B917B03(SPEC-030, 11:16:56Z) 1건뿐 — 나머지 28건은 전부 참조 작업 있음.
- 선점 `lease-71036-20260804112626` 취득(exit 0) → 갱신(exit 0) → 반납(exit 0).

### 핵심 결정과 근거
- **분해 2건**: TASK-097(`heartbeat_condition.rs` + `role_eligibility.rs` — 판정 구현·버전·시나리오 표) → TASK-098(`project_instructions.rs` — 계약 임시 불릿 삭제·`rules_version`).
- **순서의 근거는 파일 충돌이 아니라 문언과 사실의 선후**: TASK-098이 지울 불릿이 말하는 상태는 TASK-097 착지 뒤에야 거짓이 된다. 먼저 지우면 계약이 "닫혔다"는 침묵으로 거짓말을 한다. SPEC-030 R4 첫 항목이 직접 지시하고 TASK-088이 SPEC-028에서 세운 선례.
- **`role_eligibility.rs`를 TASK-097에 붙임**: 그 파일에서 만지는 것은 모듈 머리 주석("알려진 차이 5")과 대조 테스트뿐이고 판정 함수는 무변경. 따로 떼면 TASK-097에 매달릴 수밖에 없어 병렬 여지가 0인데 문서만 는다. TASK-086이 같은 두 파일을 한 작업으로 묶은 선례. 기획서 완료 조건 14가 제한하는 두 파일은 각각 하나씩으로 유지.
- **버전 값을 고정 숫자로 안 적음**: 분해 시점 값(6/8)을 보고서에 적되 두 작업 모두 "착수 시점 값을 읽어 +1, 그 값을 보고서에 기재"로 요구. 상수와 설치본(4/7)이 어긋나 있고 미착지 작업이 여럿이다.
- **픽스처 헬퍼 방향 지정**: 기존 `write_decision_document`(outcome 고정)·`write_later_revision_request`(created_by·created_at 고정)로는 확인 사실 4·5의 두 행을 못 세운다. 시그니처 변경 대신 헬퍼 하나 추가로 지정 — 기존 행 불변이 완료 조건.
- **사유 코드 불증설**: `eligible`·`no-target`으로 두 행이 다 표현된다(SPEC-023 규약은 제외 범위).

### 변경한 파일
- `.workflow/도그푸딩--wf_ae6cd700/tasks/TASK-097.md` 신규 (`status: todo`, 선행 없음)
- `.workflow/도그푸딩--wf_ae6cd700/tasks/TASK-098.md` 신규 (`status: todo`, `depends_on: [TASK-097]`)
- `.workflow/도그푸딩--wf_ae6cd700/reports/REPORT-SPEC-030-ARCH.md` 신규

### 검증 단계와 결과
- 선점 뒤 자격 재확인: `source_decision_id: DECISION-4B917B03` 참조 작업 0건.
- 기획서 확인 사실 재확인(읽기만): 기획자 분기 두 자리에 `created_by` 없음, 아키텍트 분기 두 자리에 있음(sh `[ "$cb" = "user" ]`, ps1 `-cne 'user'`), `CONDITION_SCRIPT_VERSION` 6 / 설치본 4, `WORKFLOW_RULES_VERSION` 8 / 설치본 7, "알려진 차이 5" 문언, 지울 불릿 문언.
- `the_installed_script_matches_the_scenario_table`이 임시 프로젝트에 상수 본문을 설치해 돌리는 것을 확인 — 설치본 파일에 의존하지 않으므로 설치본 직접 수정을 두 작업 모두의 범위 밖으로 명시.
- 병행 안전 전수 확인: 세 파일을 범위에 둔 미완료 작업이 `heartbeat_condition.rs` 8건 / `role_eligibility.rs` 7건 / `project_instructions.rs` 2건이고 **전부 `qa_waiting`**, `todo`·`in_progress` 0건.
- `depends_on` 대상 문서 존재 확인, 순환 없음.
- 착지 후 세 역할 판정: `planner exit 1`, `architect exit 1`(승인 소진), `developer exit 0`(TASK-097 픽업 가능, TASK-098은 선행 미충족으로 제외).

### 후속 / 리스크
- **id 경합 실발생**: 분해 시점에 TASK-095·096이 `RESERVED` 본문만 있는 상태(SPEC-029 분해 세션이 lease 보유, 만료 11:45:09Z). 그 둘을 건드리지 않고 `set -C` 배타 생성으로 097·098을 원자 선점 후 내용 작성. RESERVED 잔여 0, 타 세션 문서 무손상.
- **QA 반려 리스크**: 위 미완료 작업 중 하나가 `todo`로 되돌아오면 같은 파일에 두 세션이 붙는다. 두 작업 문서 모두에 "착수 직전 재확인, 겹치면 `blocked` 후 상신"을 명시.
- **CI 세 플랫폼 실행은 로컬에서 안 남**: PowerShell 본문 실행 검증은 CI에서만 난다. TASK-097 완료 조건 5에 "한 플랫폼만 돈 경우 그 사실과 플랫폼을 보고서에 적는다"를 넣어 한계를 감추지 못하게 했다.
- `outcome` 값 목록 차이는 이 기획서 밖(제외 범위). 후속 아이디어 등록은 기획자 역할이라 이 세션에서 하지 않음.
- 게이트의 타 세션 위반 2건(`heartbeat_status.rs` fmt, `heartbeat_process.rs:216` clippy) 구분 기재를 두 작업 완료 조건에 포함.

## 개발자 세션: TASK-093 (SPEC-029 목록 payload에 source_spec_id)

### 목표 / 요청 범위
- 개발자 역할로 자격 있는 개발 작업 하나를 처리. `wf-eligible.sh developer` exit 0.
- `todo` 3건 중 선행 충족은 TASK-093·094·097. TASK-093 선택 — 후행 TASK-095를 열어 주고 범위가 필드 하나로 닫힌다.
- 선점 `lease-76126-20260804113609` 취득(exit 0) → 갱신(exit 0) → 반납(exit 0).

### 핵심 결정과 근거
- **테스트 이름을 안 바꿨다.** 작업 문서가 이름 변경/유지 둘 다 허용하고 이유를 보고서에 남기라고 요구했다. `REPORT-TASK-029-DEV.md:106`이 `reads_the_source_decision_of_a_task_and_leaves_it_empty_elsewhere`를 이름으로 인용하고 있어, 바꾸면 그 인용이 갈 곳을 잃는다. 지나간 보고서는 다른 작업의 산출물이라 이 세션이 고칠 자리가 아니다. 이름이 두 필드 중 하나만 말하게 되는 값은 치렀다.
- **판정도 파생도 없음.** `source_decision_id`와 완전히 같은 모양으로 `yaml_text` 한 줄. 값이 없으면 `None`이고 그것이 TASK-095의 미분류 레인 근거가 된다.
- **`depends_on`은 안 실었다.** DECISION-DD348ED0이 확인 필요 2번을 제안대로 승인 — payload 추가 필드는 `source_spec_id` 하나가 상한.
- **확인 사실 6의 주석 두 곳 무수정.** 두 주석의 대상은 의존 선언이지 프론트매터 전반이 아니라 이 변경 뒤에도 참이다.

### 변경한 파일
- `src-tauri/src/domain/project.rs:117`~`:119` — `WorkflowItemSummary`에 `source_spec_id` + 주석 2줄
- `src-tauri/src/infrastructure/fs_project_repository.rs:1180`·`:1192` — 값 추출·리터럴 각 1줄, `:2825`~`:2830` 기존 테스트에 단언 3줄
- `src/features/projects/domain/types.ts:48`~`:49` — `sourceSpecId?: string | null` + 주석 1줄
- `.workflow/도그푸딩--wf_ae6cd700/tasks/TASK-093.md` (`qa_waiting`, history 2줄 추가), `reports/REPORT-TASK-093-DEV.md` 신규

### 검증 단계와 결과
- `~/.cargo/bin/cargo test --manifest-path src-tauri/Cargo.toml` → 401 passed / 0 failed / 0 ignored. (`cargo`가 PATH에 없어 절대경로 실행)
- `keeps_the_declaration_out_of_the_list_payload` **수정 없이** 통과 — 확인 필요 2번의 상한을 지키는 테스트.
- `npm run check` (typecheck → test → build) 전부 통과. 18 파일 474건, `vite build` 325 modules.
- 범위 판정은 `git diff`가 아니라 심볼 검색으로. 병행 세션 미커밋 변경이 커서 HEAD 대비 diff에 무관한 필드가 섞인다. `source_spec_id` 전수 검색 결과 이 세션이 만든 자리는 6곳뿐이고 `sourceSpecId`는 `types.ts` 1곳(=읽는 코드 없음).

### 후속 / 리스크
- **TASK-095가 딛고 설 자리**: `sourceSpecId`가 `null`인 미분류 레인 경로는 오늘 실데이터로 안 밟힌다(작업 문서 88건 전부 값 있음). 화면 검사는 픽스처 필요.
- **역할 밖 관찰(무수정)**: `fs_project_repository.rs:770` 괄호 문구가 문장만 떼어 읽으면 넓게 읽힌다. 작업 문서가 주석 정리를 범위 밖으로 못 박아 그대로 뒀다.
- **역할 밖 관찰(무수정)**: `.workflow/.runtime/leases/SPEC-009.yml`이 2026-08-03T01:20:00Z 만료된 채 잔존. 판정이 만료 lease를 안 세므로 막는 것은 없다.
- 사용자 QA: 화면 변화 없음. 보드·문서 목록 회귀 없음만 확인하면 된다.

## 개발자 세션: TASK-095 (SPEC-029 기획서별 스윔레인과 그룹 QA 신호)

### 목표 / 요청 범위
- 개발자 역할로 자격 있는 개발 작업 하나를 처리. `wf-eligible.sh developer` exit 0.
- `todo` 5건(TASK-094~098) 중 선행 충족은 094·095·097. 판정 직후 다른 세션이 TASK-094를 선점(lease 20:52 생성, 상태 `in_progress`)해서 제외 → TASK-095 선택.
- 선점 `lease-95371-20260804115338` 취득(exit 0) → 갱신(exit 0) → 반납(exit 0).

### 핵심 결정과 근거
- **집계·신호는 `workflow.items.tasks` 전체, 카드만 `filteredTasks`.** DECISION-DD348ED0의 확인 필요 1번. `buildSpecLanes(allTasks, visibleTasks, specs)`가 두 집합을 이름으로 갈라 받아 섞이지 않게 했다. 필터에 가려진 `todo` 하나로 "통째로 QA 가능"이 켜지는 것을 막는 자리다.
- **빠진 레인 고지 문구를 필터 유무로 갈랐다.** 승인된 문구 `완료만 있어 표시하지 않은 기획서 N개`는 필터 없는 상태를 전제로 쓰였다. 검색으로 빠진 레인까지 "완료만 있어"라고 하면 R5가 금지한 거짓말이라, 참인 자리에만 승인 문구를 쓰고 아니면 `조건에 맞는 카드가 없어…`로 갈아 끼웠다. 판정은 기존 `hasFilters` 재사용.
- **규격 밖 상태를 신호의 "막는 쪽"에 셌다.** 무엇인지 모르는 상태를 "QA 대기만 남았다"의 근거로 삼을 수 없다. 헤더 수치 이름은 열 제목 `확인 필요`와 겹치지 않게 `규격 밖`(헤더는 `statusLabels` 어휘, 열은 `taskColumns` 어휘로 이미 갈려 있다).
- **`SpecLaneBoard`를 별도 컴포넌트로.** 묶기를 켰을 때만 마운트돼 꺼진 보드에서는 파생 비용이 0이고, `useMemo`가 2.5초 주기로 목록이 갱신될 때만 다시 센다.
- **`BoardView`에 `label?` 프롭 하나.** 기본값이 지금 문자열이라 기존 33건이 무수정 통과. region 이름은 제목이 아니라 레인 키로 지어 같은 제목의 기획서 둘이 겹치지 않게 했다.
- **`depends_on` 안 읽음.** 확인 필요 2번이 잘랐다. 접기 버튼도 안 만듦(TASK-096 몫).

### 변경한 파일
- `src/features/projects/components/DevelopmentBoard.tsx` — 파생 함수 4 + `emptyLane`/`SpecLane`, `SpecLaneBoard`, 상수 4, `laneGrouping` 상태·토글, `BoardView`의 `label` 프롭
- `src/features/projects/components/DevelopmentBoard.test.tsx` — 헬퍼 4 + 검사 11건 추가 (기존 33건 무수정)
- `src/App.css` — `/* Development spec lanes */` 규칙 13개 추가 (기존 `.task-board`·`.task-column`·`.task-card` 무수정)
- `.workflow/도그푸딩--wf_ae6cd700/tasks/TASK-095.md` (`qa_waiting`, history 2줄 추가), `reports/REPORT-TASK-095-DEV.md` 신규

### 검증 단계와 결과
- `npm run check` (typecheck → test → build) 전부 통과. 19 파일 501건, `vite build` 325 modules.
- `npx vitest run …/DevelopmentBoard.test.tsx` → 44건 통과. 착수 시점 33건 → 11건 추가, 기존 33건 **수정 없이** 통과.
- 검사 6번(`keeps a todo hidden by the status filter from lighting the lane signal`)이 확인 필요 1번을 고정한다. 집계를 표시 집합으로 바꾸면 이 검사가 깨진다.
- `git diff --stat` 세 파일만. 검사 파일 삭제 1줄은 앞선 세션의 import 변경이고 이 세션 편집은 전부 추가. Rust·`types.ts` 무변경.

### 후속 / 리스크
- **TASK-096이 `#unassigned` 상수 둘을 하나로 합쳐야 한다.** 지금 `DevelopmentBoard.tsx`의 `UNASSIGNED_LANE_KEY`와 TASK-094가 만든 `browserSpecLaneCollapseStore` 쪽에 같은 값이 따로 있다. 작업 문서 둘이 그렇게 나눠 놨고, 합치기 전까지 한쪽만 고치면 접힘 상태가 어긋난다.
- 레인 순서가 신호에 따라 바뀌므로 2.5초 갱신 중 레인이 위아래로 움직일 수 있다. 승인된 확인 필요 3번의 결과라 이 작업 범위 밖이고, 거슬리면 별도 아이디어 감이다.
- **역할 밖 관찰(무수정)**: `SPEC-009.yml` 만료 lease 여전히 잔존. §4가 판정은 lease를 안 고친다고 정해 손대지 않았다.
- 사용자 QA: 묶기를 끈 보드가 지금과 완전히 같은지, 레인 안 가로 스크롤(jsdom이 못 보는 자리), 실데이터에서 `todo` 섞인 레인에 신호가 안 켜지는지.

## 기획자 세션: SPEC-031 (스펙 그룹 단위 QA 일괄 확인)

### 목표 / 요청 범위
- 기획자 역할로 자격 있는 대상 하나를 처리. `wf-eligible.sh planner` exit 0.
- 미답변 수정 요청 0건(DECISION-2F71D20D → SPEC-015, DECISION-FEB99DAB → SPEC-013으로 둘 다 답변됨). 미처리 아이디어 1건 = IDEA-0E45508A → 그것을 선택.
- 선점 `lease-97692-20260804115648` 취득(exit 0) → 갱신(exit 0) → 반납(exit 0).
- 산출물: `specs/SPEC-031.md` (`status: user_review`). PLAN 보고서는 안 씀 — SPEC-029·030 선례대로 근거를 기획서 확인 사실에 전부 넣었다.

### 핵심 결정과 근거
- **아이디어의 실사례를 파일에서 실측했다.** 오늘 11:42:09~11:45:39에 QA 확인 42건, 전부 `confirmed`, 코멘트는 42건 모두 0자. 기획서 18개에 흩어져 있고 2건 이상 묶이는 레인이 11개(35건). 레인 단위 액션이면 도장 입력 42회 → 18회. 아이디어가 "41건"이라 적은 것의 실제 값이다.
- **빈 코멘트 42건이 확인 필요 2번(공통 코멘트만)의 근거가 됐다.** 건별 코멘트 수요가 실측으로 안 나타난다. 단건 경로도 확인은 빈 코멘트를 허용한다(`validate_task_qa`).
- **쓰기 경로를 확인 필요 1번으로 올렸다.** 단건 명령 한 번이 프로젝트 전체를 다시 읽어(문서 414건 + `tasks/` 94건 재훑기 ≈ 508건) 요약을 돌려준다. 42건을 프론트에서 그대로 부르면 ≈21,000건 읽기 + 화면 갱신 42회. 게다가 실패가 전역 에러 문구 하나라 "42건 중 20번째 실패"를 사용자가 알 방법이 지금 구조에 없다 → 앱 일괄 명령 신설을 제안, 프론트 N회 호출을 대안으로 뒀다.
- **필터와 대상 범위를 확인 필요 3번으로 올렸다.** 신호·수치는 필터·완료 절단 이전 전체 기준(SPEC-029 승인분)인데 대상만 표시 집합으로 좁히면 "9건 확인" 눌러 3건 찍히는 상태가 된다. 제안은 전체 기준 대상 + 확인 화면이 전 건 나열(실행 직전에 사용자 눈앞에 오게).
- **아이디어가 이미 그은 선은 요구사항으로 내렸다.** 건별 기록(R3), 신호 켜진 레인만(R1), 개별 제외(R2), 일괄 반려 제외 — 확인 필요로 되묻지 않았다.
- **리스 검사를 제외 범위에 명시했다.** `record_task_qa`에 리스 검사가 없다. 일괄만 리스를 보기 시작하면 카드로 열면 찍히고 레인에서 찍으면 안 찍히는 상태가 된다(지금 TASK-094가 정확히 그 상태 — `qa_waiting` + 만료 전 리스).
- **착수 순서를 완료 조건 14번에 박았다.** SPEC-029의 TASK-095(`in_progress`)·TASK-096(`todo`)이 `DevelopmentBoard.tsx`·`App.css`를 잡고 있어 병행 불가.

### 변경한 파일
- `.workflow/도그푸딩--wf_ae6cd700/specs/SPEC-031.md` 신규 (확인 사실 20 / 요구사항 R1~R8 / 확인 필요 3 / 완료 조건 15)
- `docs/development-logs/2026-08-04.md` (이 섹션)

### 검증 단계와 결과
- `wf-eligible.sh planner` → `eligible` exit 0. 판정 근거는 손으로 재확인: 기획서 30건의 `source_idea_id`와 아이디어 30건을 대조해 미참조 1건(IDEA-0E45508A) 확인, `decision@1` 31건에서 `revision_requested` 2건을 뽑아 둘 다 후속 기획서가 `source_decision_id`로 물고 있는 것 확인.
- 선점 헬퍼 3회 호출 전부 exit 0. 반납 후 `leases/`에 `SPEC-009.yml`만 남음.
- 기획서의 코드 인용은 전부 작업 트리에서 직접 읽어 줄 번호까지 확인(`fs_project_repository.rs`, `DevelopmentBoard.tsx`, `useProjectWorkspace.ts`, `useArmedConfirm.ts`, `lib.rs`, `tauriProjectGateway.ts`). 추정 없음.
- 코드·앱 상태 무변경. 테스트는 안 돌림(기획자 세션이라 코드 변경 0).

### 후속 / 리스크
- **지금 당장은 값이 안 난다.** 남은 `qa_waiting`이 2건(TASK-077·TASK-094)이고 서로 다른 레인이라 일괄 대상이 되는 레인이 없다. 값은 다음 배치에서 난다 — 기획서 기대효과에도 그대로 적었다.
- **확인 필요 1번을 뒤집으면 완료 조건 4·12번의 검증 방식이 갈린다.** 앱 명령이면 파일 단언, 프론트 루프면 호출 인자 단언. 승인 시 한쪽으로 정해진다.
- **역할 밖 관찰(무수정)**: `SPEC-009.yml` 만료 lease가 아직 남아 있다. 앞선 세션들이 같은 것을 기록했고 §4가 판정은 lease를 안 고친다고 정해 손대지 않았다.
- **역할 밖 관찰(무수정)**: `REPORT-TASK-088-DEV` 후속 3번의 "수기 결정 21건 소급 재가"는 이 기획서 제외 범위로 뒀다. 아이디어가 "같은 모양의 문제"로 지목했고 이 기획서의 일괄-입력·건별-기록 원칙이 선례가 되지만, 그 자체는 별도 아이디어 감이다.

## 기획자 하트비트 세션 (12:07Z~12:19Z) — IDEA-9B175442

### 목표 / 요청 범위
- 기획자 역할로 자격 있는 대상 하나를 처리. 대상 없으면 `NO_ELIGIBLE_WORK`만 보고.
- 미답변 수정 요청 0건(DECISION-FEB99DAB → SPEC-013, DECISION-2F71D20D → SPEC-015). 미처리 아이디어 1건 = IDEA-9B175442를 선택. `IDEA-0E45508A`는 미만료 lease가 있었지만 SPEC-031이 이미 참조하므로 애초에 대상이 아니었다.
- 선점 `lease-5443-20260804120701` 취득(exit 0) → 갱신 2회(exit 0) → 반납(exit 0).
- 산출물: `specs/SPEC-032.md`(`status: user_review`), `reports/REPORT-SPEC-032-PLAN.md`.

### 핵심 결정과 근거
- **아이디어의 데몬 전제가 사실과 달랐다.** `~/Git/claude-heartbeat`를 직접 읽었다. `_run_slug_group`이 같은 slug의 잡을 순차 실행하고(`core.py:555`~`:584`), `_inflight` 가드가 실행 중 그룹을 건너뛰며(`:589`~`:621`), 잡 문법에 동시 실행 수 필드가 없고 파서가 모르는 키를 버린다(`:253`~`:270`). slug는 `_slug_to_cwd`로 프로젝트 경로가 되므로 한 프로젝트에 두 slug를 만들 수 없다. 0.8.0이 연 것은 프로젝트 사이 병렬이고 같은 프로젝트 안은 여전히 직렬이다(`docs/planning/2026-08-04-productization.md:110`). → 아이디어 제안 3번(동시 세션 수 옵트인)을 확인 필요 3번으로 올리고 제안은 "이번엔 뺀다"로 뒀다.
- **판정만 고치면 절반이라는 것을 요구사항으로 분리했다(R4).** 조건 스크립트가 답하는 것은 역할별 불리언 하나이고 어느 대상을 잡을지는 세션이 정한다. 개발자 계약 문언이 새 선언을 판정 근거로 지목하지 않으면 세션이 여전히 겹치는 작업을 고른다.
- **선언 형태를 확인 필요 1번으로 올리되 R2로 제약했다.** 실측된 실패(TASK-073/TASK-077)는 서로 다른 승인에서 나온 두 작업 사이의 것이라, "병행 금지 작업 id 목록"은 지목할 대상이 아직 없어 구조적으로 못 막는다. 제안은 범위 파일 목록.
- **개발자 계약에 이미 규칙이 있다는 것이 출발점이 됐다.** `developer.md:15`가 "No unexpired lease may cover overlapping work."를 자격 조건으로 적는데, lease는 `task_id`만 싣고 범위는 산문에만 있어 실행 불가능한 문장이다. 기획 내용의 첫 문단이 이 자리다.
- **선언 없는 옛 작업의 취급을 확인 필요 2번으로 올렸다.** 아이디어가 "이 취급 방식 자체도 기획 판단"이라 남긴 자리. 제안은 보수적 취급(활성 lease가 있으면 제외).
- **착수 순서를 완료 조건 14번에 박았다.** TASK-097이 `heartbeat_condition.rs`·`role_eligibility.rs`를, TASK-098이 `project_instructions.rs`를 잡고 있어 병행 불가.

### 변경한 파일
- `.workflow/도그푸딩--wf_ae6cd700/specs/SPEC-032.md` 신규 (확인 사실 23 / 요구사항 R1~R9 / 확인 필요 3 / 완료 조건 15)
- `.workflow/도그푸딩--wf_ae6cd700/reports/REPORT-SPEC-032-PLAN.md` 신규
- `docs/development-logs/2026-08-04.md` (이 섹션)

### 검증 단계와 결과
- 대상 판정을 손으로 재확인: 아이디어 30건과 기획서 31건의 `source_idea_id`를 대조해 미참조 1건(IDEA-9B175442) 확인, `decision@1` 31건에서 `revision_requested` 2건을 뽑아 둘 다 후속 기획서가 `source_decision_id`로 물고 있음을 확인.
- 선점 헬퍼 4회 호출(acquire·renew·renew·release) 전부 exit 0. 반납 후 `leases/`에 만료된 `SPEC-009.yml`만 남음.
- 반납 후 `sh .workflow/rules/wf-eligible.sh planner` → `no-target` exit 1. 기획자 대기 물량 소진 확인.
- 기획서의 인용은 전부 직접 읽어 줄 번호까지 확인(`wf-eligible.sh`, `role_eligibility.rs`, `heartbeat_condition.rs`, `heartbeat_jobs.rs`, `heartbeat_service.rs`, `fs_project_repository.rs`, `project_instructions.rs`, `ActivityView.tsx`, `DevelopmentBoard.tsx`, `types.ts`, `~/Git/claude-heartbeat`의 `core.py`·`config-contract.md`·`CHANGELOG.md`). 추정 없음.
- 코드·앱 상태 무변경. 테스트는 안 돌림(기획자 세션이라 코드 변경 0).

### 후속 / 리스크
- **역할 밖 관찰(무수정)**: `docs/heartbeat.md`의 "현재 한계" 절(`:125`~`:131`)과 `:21`이 조건 스크립트 v6과 어긋난다. 문서가 한계로 적은 셋(만료 lease 미판정·수정 요청 미감지·아키텍트 최신 미확인)을 스크립트는 이미 전부 처리한다. 문서 갱신은 별도 아이디어 감.
- **역할 밖 관찰(무수정)**: 만료된 `SPEC-009.yml` lease가 여전히 남아 있다. §4대로 판정은 lease를 고치지 않으므로 손대지 않았다.
- **확인 필요 1번과 2번이 서로를 좁힌다.** 1번에서 작업 id 목록을 고르면 R2를 만족시킬 장치가 따로 필요해지고, 2번의 보수적 취급이 그 빈자리를 임시로 메우는 모양이 된다. 두 항목을 함께 읽고 결정하는 편이 낫다.
- **claude-heartbeat 쪽 후속.** 잡별 동시 실행 필드는 그 저장소의 계약 변경이라 이 저장소에서 할 일이 아니다. 확인 필요 3번을 뒤집으려면 그쪽 아이디어 등록이 먼저다.

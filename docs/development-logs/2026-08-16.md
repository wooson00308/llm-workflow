# 2026-08-16

## 새벽 마감: codex 전권·서류 자기잠금 해소·격리 스위트 완주 (02:30 KST경)

- codex 커밋 불가의 뿌리: `--sandbox workspace-write`가 `.git` 쓰기를 금지 — mech-arena 미커밋
  100건 누적과 격리 커밋 차단("Operation not permitted")의 공통 원인. 사용자 결정으로
  `danger-full-access` 전환(런타임 0.9.8, PR #31). 직후 격리 브랜치에 codex 첫 커밋
  c258d0b("test: verify integrated shop and assembly flows") 실측.
- 서류 자기잠금: 통합 청결 검사가 세션 자신이 쓰는 제어 문서(.workflow)를 사용자 작업으로 취급해,
  검증 통과한 후보가 자기 보고서에 막혔다. 개발자 계약 20 — 제어 문서는 문서 전용 커밋으로 먼저
  정리, 사용자 보호는 그 밖 전부에 유지(872b1b3). 두 프로젝트 규칙 사본 수동 동기화.
- 격리 스위트 완주: -06(격리 미지원 런타임 차단)까지 통합 — 6종 전부 완료. -06이 세운 관문
  (ISOLATION_CAPABLE_RUNTIME_VERSION=0.10.0)에 맞춰 런타임 0.10.0 게시(PR #32, 코드 변화 없는
  판 선언). 설치·재기동 완료, dispatcher 0.10.0.
- 유령 dispatcher 발견: `agent project resume` CLI가 launchd 밖 dispatcher를 스폰해 kickstart가
  안 닿았다 — 직접 종료 후 서비스 재기동으로 해소. "실행 중 버전 오보"(디스크 기준 보고)와 함께
  런타임 후속 과제로 남김.
- 판 상태: workflow-labs 격리 6종 완료·오류 0, mech-arena -004는 후보 커밋 보유 상태로 통합
  재시도 대기(계약 20 적용 후 서류 자기잠금 재발 없음 확인 필요), 릴리스 체인은 PR 검사→병합→
  태그→게시→설치·재기동까지 전 과정 무인 수행 확인.

## v0.1.19 릴리스 컷 준비 (15:34 KST경)

- 사용자 요청으로 릴리스 컷 시작. 컷 절차는 v0.1.18과 동일 — dev push 검사 네 갈래
  통과 확인 후 main fast-forward, 같은 커밋에 태그.
- 버전 범프: 앱 0.1.18 → 0.1.19 (`package.json`, `Cargo.toml`, `Cargo.lock`,
  `tauri.conf.json`), 번들 런타임 핀 runtime-v0.9.6 → runtime-v0.10.0.
- 런타임 핀 상향 근거: 격리 게이트(8dc7efe)가 `ISOLATION_CAPABLE_RUNTIME_VERSION=0.10.0`
  미만 런타임의 개발자 실행을 거부하므로, 0.9.6을 그대로 실으면 앱이 자기 번들 런타임을
  거부한다. runtime-v0.10.0 게시물(자산 3종: linux-x86_64·windows-x86_64·macos-universal)
  존재 확인. 0.10.0은 앱 허용 범위(0.9.0~1.99.99) 안.
- 실릴 것(v0.1.18 이후 10커밋): 통합 대기 태스크의 개발자 판정 제외, 실행 중 dispatcher의
  다운로드 런타임 자동 인계, 파이프라인 서류 자기잠금 해소(계약 20), 격리 미지원 런타임
  차단 게이트, 디스크 포화 사건 기록.
- 작업 트리의 `.workflow` dirty 3건(developer.md, wf-eligible.sh, wf-reserve.sh)은 다른
  세션 소유로 보존 — 컷 커밋에 포함하지 않는다.

## v0.1.19 릴리스 컷 (15:53 KST경)

- 권한: 기부여된 릴리스 컷 주도 진행 권한(2026-08-15)으로 진행. 게시와 워크플로 도장은 계속 사람 몫.
- 근거 실행: 컷 대상 커밋 e6f1f35의 push 검사 네 갈래(Frontend·Rust 3-OS) 전부 통과 —
  CI 실행 31931788371. 이 실행을 확인한 뒤 15:42 KST에 main을 e6f1f35로 fast-forward하고
  v0.1.19 태그 생성. 로컬 게이트 4종(cargo test·fmt·clippy·npm check)도 사전 통과.
- 릴리스 워크플로 31932055217 — prepare + 3-OS 빌드 전부 성공, 드래프트 릴리스
  "LLM Workflow v0.1.19" 생성(자산 14종: dmg·exe·msi·deb·rpm·AppImage·latest.json·서명).
  번들 런타임 runtime-v0.10.0.
- 후속: 드래프트 게시는 설치 확인 후 사용자가 수행.

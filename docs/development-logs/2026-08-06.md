# 2026-08-06 개발 로그

## TL 세션 (13:57 KST) — v0.1.9 게시, 릴리스 빚 2건 아이디어 등록

### 목표 / 요청 범위
- 사용자 승인("ㄱㄱㄱ")으로 릴리스 마무리 두 건 실행.

### 실행
- v0.1.9 드래프트 릴리스 게시: `gh release edit v0.1.9 --draft=false`, draft=false 실측 확인. latest.json 활성화로 자동 업데이트 열림. https://github.com/wooson00308/llm-workflow/releases/tag/v0.1.9
- 아이디어 등록 2건 (v0.1.9 릴리스 세션 빚 목록 유래):
  - IDEA-22F8EA3F — 리눅스 간헐 실패 테스트 2건(ETXTBSY 경합)의 결정화. 같은 커밋 fail→pass 실측 근거·원인 가설·처방 재료 수록.
  - IDEA-040FF2C1 — 컷·릴리스 절차에 "PR CI 전 매트릭스 그린 후 병합" 규범 편입. 로컬 그린≠CI 그린 3계열 실증(OS cfg·node 버전·플랫폼 자산)과 규범이 앉을 자리 질문 수록.
- created_at 1초 차로 테스트 결정화가 먼저 기획되게 순서 부여.

### 후속
- 기획자 파이프라인이 두 아이디어를 순차 소화 예정. 릴리스 관련 잔여 사용자 관문 없음.

## 문서 한국어 가독성 개선 아이디어 등록

- 범위: 에이전트가 작성하는 아이디어·기획서·개발 작업·보고서의 한국어 문체 규칙.
- 결정: 구체적인 주체·행동·결과를 쓰고, 기존 문서를 문체 예시로 복제하지 않는 방향을 아이디어로 등록했다.
- 변경: IDEA-825A67C4 신규. 역할 규칙·파일 계약·정본 버전과 예시를 후속 기획 범위로 제안했다.
- 검증: 아이디어 스키마·ID·시간 형식과 기존 문서 미변경을 확인했다.
- 후속: 기획자가 기획서를 `user_review`로 올리면 사용자 승인 후 규칙 정본을 수정한다.

## 기획자 세션 (16:14~16:32 KST) — SPEC-043 (IDEA-22F8EA3F)

### 목표 / 요청 범위
- 기획자 역할 1대상 처리. 공통 규칙과 planner 계약 순서로 판정 후 기획서 1건 산출.

### 대상 판정
- 회수 대상 0(기획서 42건 전부 `user_review`), 미답변 수정 요청 0(`revision_requested` 3건 모두 후속 기획서 도달), 미처리 아이디어 2건 → `created_at` 이른 IDEA-22F8EA3F 선점(`lease-4885-20260806071521`, acquire·renew·release 전부 exit 0).

### 핵심 결정과 근거
- **아이디어의 실측 주장이 서지 않음을 확인.** 인용된 CI 실행 3건 + 병합 후 main 실행까지 리눅스 4회 모두 `529 passed; 0 failed`, 지목된 두 테스트 전부 `ok`. 같은 창에서 뒤집힌 실패 2건은 윈도우(`fs_project_repository.rs:4787`)였고 원인은 커밋 `07b6a68`이 고친 `sh`/`.sh` 하드코딩 — 재실행이 아니라 수정 후 통과. 저장소 CI 62건 중 리눅스 테스트 단계 실패는 0건.
- **그래도 남는 구조적 노출만 다루는 문서로 세움.** 방금 쓴 파일을 앱 실행 경로가 직접 띄우는 자리 26곳 + 판정 비용 shim. 지목된 두 테스트는 그중 5회일 뿐이라 범위를 어법 전체로 잡음.
- **아이디어의 처방 방향 3개 중 1개 폐기.** "판정 비용 shim처럼 안정적인 자리와 통일"은 그 자리도 동일 어법이라 본보기가 없음 — 대상으로 편입.
- **구현 방식 미선택.** R3에서 성질만 고정(고정 대기 금지·상한 존재·다른 이유의 실패는 재시도 없음), 장치 선택은 아키텍트 몫.

### 변경한 파일·모듈
- 신규 `.workflow/도그푸딩--wf_ae6cd700/specs/SPEC-043.md` (`user_review`), `reports/REPORT-SPEC-043-PLAN.md`. 제품 소스·아이디어·결정·작업 문서 무변경.

### 검증
- CI 실행 로그 4건 직접 열람(`gh run view --log`/`--log-failed`), 실행 62건 결론·실패 작업 API 집계, 인용 코드 자리 전수 열람, 실행 갈래 고정 설정 부재 확인, 결정권자 요약 절 10줄·금칙 토큰 0 확인.

### 후속 / 리스크
- SPEC-043 확인 필요 3건(작업 진행 여부·상한 재시도 허용 여부·재현 확인 상시화) 사용자 결정 대기. 1번이 뒤집히면 나머지는 무의미.
- 다음 기획자 대상은 IDEA-040FF2C1(재실행 허용 기준이 SPEC-043 제외 범위와 맞닿음), 그다음 IDEA-825A67C4.
- 역할 밖 관찰: `fs_project_repository.rs:2821`이 아직 `sh` + `.sh` 하드코딩이고 `#[cfg(unix)]`도 없음 — 윈도우에서 대조가 비어도 통과하는 상태. 이번 범위 밖이라 보고서 핸드오프로만 남김.

## 기획자 세션 (16:41~17:05 KST) — SPEC-044 (IDEA-040FF2C1)

### 목표 / 요청 범위
- 기획자 역할 1대상 처리. 공통 규칙과 planner 계약 순서로 판정 후 기획서 1건 산출.

### 대상 판정
- 회수 0(기획서 43건 전부 `user_review`), 미답변 수정 요청 0, 미처리 아이디어 2건 → `created_at` 이른 IDEA-040FF2C1 선점(`lease-19907-20260806074423`, acquire·renew·release 전부 exit 0). `wf-eligible.sh planner` → `eligible`/0.

### 핵심 결정과 근거
- **아이디어의 3계열 실측은 전부 섰다.** 직전 세션이 SPEC-043에서 아이디어 실측을 뒤집었기에 이번에도 인용 근거를 되짚었는데, PR #19의 윈도우 clippy 4건·프런트 3건·대조 헬퍼 2건이 실행 로그와 코드에 그대로 있었다.
- **아이디어가 모르던 값이 더 컸다.** ① 같은 사고가 v0.1.6에도 있었다(PR #12, 검사 시작 73초 뒤 병합, Rust 3갈래 빨강 → PR #13 수습). ② **v0.1.8은 그 빨간 커밋에 태그가 붙어 게시됐다** — `v0.1.8` → `0899245`(윈도우 Rust 실패), 게시 12:55:35Z, 수습 `ead3ea6`에는 태그 없음. ③ 태그 워크플로가 검사 매트릭스를 안 돌린다(버전 대조 + 번들 빌드만) → PR 검사가 산출물 앞 마지막 그물.
- **정확성 보정**: v0.1.8의 빨강은 정적 검사 한 건이고 앱 동작을 바꾸지 않았다. 그래서 근거를 피해 크기가 아니라 절차에 뒀다 — 무엇이 빨갰는지는 그날의 운.
- **"규범이 앉을 자리"에 결정적 사실.** `docs/releasing.md`는 개발 로그 여드레치에서 인용 0건, 링크는 `README.md:55` 하나. 반면 로컬 게이트는 로그에 51번. 후보 넷을 판정 기준으로만 갈라 적고 선택은 사용자에게 넘김.
- **저장소 설정을 실질 선택지로 올림.** main 브랜치 보호 없음·규칙 집합 0건·계정 `admin: true`. 문서와 달리 "안 읽어서 못 지킨다"를 원천 제거하지만 사용자 계정으로만 조작 가능해 에이전트 완료 조건으로 검증 불가 → 확인 필요 2.
- **재실행 기준은 측정 후 좁게.** 검사 실행 51건 중 같은 커밋 다중 실행 2건, 둘 다 결론 동일. 빨강→초록 뒤집힘 0건. 허용 범위를 인프라 실패 한정으로 제안.
- **구현 방식 미선택.** R1~R6을 결과의 성질로만 고정, 문장이 앉을 파일·절·정본 구조는 아키텍트 몫.

### 변경한 파일·모듈
- 신규 `.workflow/도그푸딩--wf_ae6cd700/specs/SPEC-044.md` (`user_review`), `reports/REPORT-SPEC-044-PLAN.md`. 제품 소스·아이디어·결정·작업 문서 무변경.

### 검증
- PR #12·#17·#19 생성·병합 시각과 검사 실행 작업별 결론, main 푸시 검사 15건, 태그 10건의 대상 커밋, v0.1.8 게시 시각, 릴리스 워크플로 실행 6건을 저장소 API로 직접 열람.
- 검사 실행 51건을 커밋 해시로 묶어 재실행 결론 분포 집계(성공 35·실패 13·취소 3).
- 브랜치 보호·규칙 집합·계정 권한 조회, 두 워크플로 파일 단계 전수, 설치기 관리 블록 상수와 보존 테스트 열람.
- 결정권자 요약 9줄(공백 제외)·금칙 토큰 0건·H1 직후 위치를 스크립트로 확인.

### 후속 / 리스크
- SPEC-044 확인 필요 3건(규범 자리·저장소 설정 도입 여부·재실행 허용 범위) 사용자 결정 대기. 1번과 2번은 서로 물려 함께 보는 편이 낫다.
- 다음 기획자 대상은 IDEA-825A67C4 하나.
- 역할 밖 관찰: `docs/releasing.md` 브랜치 흐름 3번의 버전 파일 목록에 `Cargo.lock`이 빠져 있다(v0.1.9 컷에서는 함께 갱신). 범위 밖이라 미수정.

## 기획자 세션 (17:00~17:20 KST) — SPEC-045 (IDEA-825A67C4)

### 목표 / 요청 범위
- 기획자 역할 1대상 처리. 공통 규칙과 planner 계약 순서로 판정 후 기획서 1건 산출.

### 대상 판정
- 회수 0(기획서 44건 전부 `user_review`), 미답변 수정 요청 0(`revision_requested` 3건 모두 SPEC-013·SPEC-015·SPEC-041이 `source_decision_id`로 수신), 미처리 아이디어 1건 → IDEA-825A67C4 선점(`lease-27281-20260806080138`, acquire·renew·release 전부 exit 0). `wf-eligible.sh planner` → `eligible`/0, 세션 종료 후 `no-target`/1.

### 핵심 결정과 근거
- **아이디어 주장을 문서 367편에서 실측.** 기획서 45·작업 125·보고서 197 기준 "자리" 279편, "몫" 190편, "닫다" 172편, "어법" 104편, "착지" 83편, "원천" 72편, "표면" 24편. 몇몇 세션의 습관이 아니라 저장소 전체 문체임을 수치로 고정.
- **가장 강한 근거는 요약 절이었다.** `## 결정권자 요약` 보유 문서 29편 중 28편이 같은 낱말을 쓴다. 기획서 5편은 5/5. 사용자용 절을 만든 것만으로 문장이 안 바뀐다는 증거라 요약과 R2 근거로 사용.
- **이 변경은 문서 편집이 아니라 앱 개발 작업.** 공통 규칙·역할 계약 본문은 `project_instructions.rs` 문자열 상수이고, 함께 움직이는 것이 넷이다 — 상수 본문, `WORKFLOW_RULES_VERSION`(12)·`ROLE_RULES_VERSION`(7), 버전 문자열 단언 테스트, 설치기가 다시 쓰는 저장소 규칙 파일. 이걸 모르면 마크다운만 고쳤다가 다음 설치에서 덮인다. 기획 내용과 R7에 명시.
- **규칙은 영어, 대상 문서는 한국어.** 규칙 자산에 한국어는 `## 결정권자 요약`·`## 확인 동선` 리터럴 둘뿐. 낱말 대응표를 영어 규칙에 넣을지 한국어 파일 계약에 둘지가 실제 결정이라 아이디어의 3번 확정 항목과 합쳐 확인 필요 3으로 냄.
- **아이디어 6번 항목은 두 읽기가 갈려 혼자 정하지 않음.** (가) 계약 문서끼리의 중복 / (나) 에이전트 작성 문서끼리의 중복. 뒷문장 근거로 (나)를 택해 R6에 넣되, 두 읽기를 기획 내용에 나란히 적어 수정 요청으로 바로잡을 수 있게 함.
- **"문체는 판정에 영향 없음" 전제를 코드에서 확인.** 조건 스크립트가 읽는 키 10개, `role_eligibility` 모듈은 값만 받고 파일을 안 연다. R8이 추정이 아님.
- **권장 분량은 "적지 않는다"로 제안.** 문제는 길이가 아니라 낱말이고(실측), 하드 게이트가 아닌 분량 권장은 준수 확인 방법이 없음. 대안은 확인 필요 2에 적음.
- **문체 자기 적용.** 기획서·보고서를 제안 규칙대로 작성. 모호어는 인용 시에만 사용하고 서술에는 미사용. 초안에서 걸린 2건(`요약 절이라는 자리`, `그 자리를 빈 채로`) 수정.

### 변경한 파일·모듈
- 신규 `.workflow/도그푸딩--wf_ae6cd700/specs/SPEC-045.md` (`user_review`), `reports/REPORT-SPEC-045-PLAN.md`. 제품 소스·아이디어·결정·작업 문서 무변경.

### 검증
- 기획서 44건 상태·원천 참조 전수, 아이디어 42건 대조, 결정 172건의 스키마·`created_by`·`outcome`·`spec_id` 집계.
- 문서 367편 낱말 7종 출현 집계 + `## 결정권자 요약` 절만 추출한 2차 집계(29편/28편).
- `.workflow/rules/workflow.md` §8 전문, 역할 계약 3종 `## Completion`, `project_instructions.rs` 상수·버전·`plan_rules_file` 분기·버전 단언 테스트, `wf-eligible.sh` 전문, `role_eligibility.rs` 머리 주석 확인.
- 결정권자 요약 9줄(공백 제외)·백틱/경로/식별자 0건·H1 직후 위치를 스크립트로 확인.

### 후속 / 리스크
- SPEC-045 확인 필요 3건(문체 절 위치·권장 분량 포함 여부·대응표 위치) 사용자 결정 대기. 3번은 승인 후 작업 범위를 바꾼다 — 한국어 계약 문서에 두면 앱 자산 밖이라 타 프로젝트 설치본에 대응표가 안 따라간다.
- 미처리 아이디어 0건. 다음 기획자 세션은 새 아이디어·수정 요청이 없으면 `NO_ELIGIBLE_WORK`.
- 승인 시 규칙 버전이 올라가 저장소 규칙 파일이 다음 설치에서 재작성된다. 진행 중 세션의 규칙 읽기 시점과 겹칠 수 있음.
- 역할 밖 관찰(신규): 같은 문체가 앱 소스 주석에도 있다(`role_eligibility.rs` 머리 주석). 이번 범위 밖이라 미수정, 필요 시 별도 아이디어.
- 역할 밖 관찰(승계): `docs/releasing.md` 브랜치 흐름 3번의 `Cargo.lock` 누락, `fs_project_repository.rs:2821`의 `sh`/`.sh` 하드코딩 + `#[cfg(unix)]` 부재. 둘 다 미수정.

## 기획자 세션 (17:17~17:25 KST) — SPEC-046 (DECISION-0A67218F 수정 요청 반영)

### 목표 / 요청 범위
- 기획자 역할 1대상 처리. 공통 규칙과 planner 계약 순서로 판정 후 기획서 1건 산출.

### 대상 판정
- 회수 0(기획서 45건 전부 `user_review`), 미답변 수정 요청 1건 → DECISION-0A67218F(SPEC-045) 선점(`lease-34688-20260806081827`, acquire·renew·release 전부 exit 0). 미처리 아이디어 0(아이디어 42건 전부 참조됨). 계약 순서상 수정 요청이 아이디어보다 우선이나, 이번엔 아이디어 후보 자체가 없었다. `wf-eligible.sh planner` → `eligible`/0.
- `revision_requested` 4건 중 앞의 3건(FEB99DAB·2F71D20D·89468FD0)은 SPEC-013·SPEC-015·SPEC-041이 `source_decision_id`로 수신해 답변 완료. 0A67218F만 미수신이고 SPEC-045의 유일·최신 결정.

### 핵심 결정과 근거
- **측정 서술을 실제 측정 범위에 맞춘 것이 최대 변경.** 집계는 낱말 문자열 포함 여부라 "제자리"의 "자리", "닫히다"의 "닫"까지 포함되고 비유적 용례 판정은 하지 않았다. 요약·확인 사실·기대효과 세 곳을 "검토 대상 표현이 포함됐다"로 고치고, 측정 방법과 한계(용례 비율은 표본 검토 필요)를 확인 사실 머리에 명시.
- **수치 재측정.** SPEC-045 이후 문서가 늘어 다시 셌다. 368편(기획서 45·작업 125·보고서 198) 중 343편 포함, 요약 절 보유 30편 중 29편 포함. 아이디어 42편 중 22편은 이번에 새로 추가 — 완료 조건에 아이디어를 넣으라는 요구에 근거 수치도 4종을 덮게 함.
- **확인 필요 3건 전부 소멸.** 독립 상위 절 / 권장 분량 미기재 / 대응표 분할 배치로 사용자가 확정. 확인 필요 절은 "없다"로 남기고 어디서 정해졌는지만 표시 — 이전 문서와 대조할 위치를 유지하기 위함.
- **대응표는 제안도 대안도 아닌 하이브리드로 확정.** 설치되는 공통 규칙에 핵심 원칙+최소 예시 3쌍, `docs/file-contract.md`에 확장 대응표+전후 예문. SPEC-045가 적었던 비용 2개 중 "타 프로젝트에 안 따라감"은 해소되고 "두 문서 불일치"는 남아, R7에 대조 조건(공통 규칙 3쌍이 확장표에 같은 대응으로 존재)을 명시.
- **R6 해석 확정.** 에이전트 작성 문서의 종류별 초점 구분(둘째 읽기)이 맞다고 사용자가 확인. 두 읽기 병기를 제거하고 "계약 문서끼리의 중복 축소가 아니다"를 R6에 못박음.
- **완료 조건을 즉시 확인 12건 / 후속 관찰 3건으로 분리.** 즉시 확인 쪽 표본은 전후 예문의 "후" 문장과 이 변경 수행 중 생성되는 개발 작업·보고서. 후속 관찰 쪽에 새 문서의 문체 추이, 그 관찰 방법의 한계, 미준수 시 자동 검사 재판단을 배치.
- **문서 자체의 비유 표현 수정.** "네 가지가 함께 움직인다"→"네 항목을 함께 수정해야 한다", "원칙이 문서에 걸린다"→"원칙을 문서에 적용한다", "그 값을 치르는 조항"→"불일치 위험을 줄이기 위한 조건". 같은 기준으로 본문 전체 재작성.

### 변경한 파일·모듈
- 신규 `.workflow/도그푸딩--wf_ae6cd700/specs/SPEC-046.md` (`user_review`), `reports/REPORT-SPEC-046-PLAN.md`. SPEC-045는 보존(무변경). 제품 소스·아이디어·결정·작업 문서 무변경.

### 검증
- 기획서 45건 상태·출처 참조 전수, 아이디어 42건 대조(미참조 0), 결정 173건에서 스키마·`outcome`·`spec_id`·`created_at` 집계로 수정 요청 4건과 답변 상태 확인.
- 낱말 7종 재집계: 문서 368편 → 343편 포함, 아이디어 42편 → 22편 포함, 요약 절 30편 → 29편 포함.
- 규칙 자산 재확인: `workflow.md:234`와 `project_instructions.rs:278` 동일 문장, `WORKFLOW_RULES_VERSION` 12(`:21`)·`ROLE_RULES_VERSION` 7(`:24`), 테스트의 문자열 단언과 상수 단언(`:1020`·`:1021`), 역할 계약 버전 7·6·7.
- SPEC-046 결정권자 요약 10줄(공백 제외, 상한 준수)·백틱/경로/식별자/줄번호 0건·H1 직후 위치를 스크립트로 확인. 본문의 대상 낱말 7줄은 전부 인용(측정 예시·집계·대응표)으로 확인.

### 후속 / 리스크
- SPEC-046 사용자 결정 대기. 확인 필요 항목 없음 — 결정은 "이 범위대로 개발 시작할지" 하나.
- 승인 시 아키텍트가 R7의 5개 대상(규칙 본문 상수, 버전 값 2곳, 버전 단언 테스트, 저장소 규칙 파일, 한국어 파일 계약 문서)을 한 묶음으로 봐야 함. 마크다운만 고치면 다음 설치에서 덮인다.
- 미처리 아이디어 0건, 미답변 수정 요청 0건. 다음 기획자 세션은 새 입력이 없으면 `NO_ELIGIBLE_WORK`.
- 역할 밖 관찰(승계): `docs/releasing.md` 브랜치 흐름 3번의 `Cargo.lock` 누락, `fs_project_repository.rs:2821`의 `sh`/`.sh` 하드코딩 + `#[cfg(unix)]` 부재, `role_eligibility.rs` 머리 주석의 동일 문체. 셋 다 미수정.

## 아키텍트 세션 (17:39~17:47 KST) — SPEC-046 분해 (TASK-130, TASK-131)

### 목표 / 요청 범위
- 프로젝트 아키텍트 역할 1대상 처리. 공통 규칙과 architect 계약 순서로 판정 후 승인 기획서 1건을 `todo` 작업으로 분해.

### 대상 판정
- `wf-eligible.sh architect` → `eligible`/0. 결정 173건 직접 훑어 `approved`+`created_by: user`+최신 결정 40건 중 작업 미참조 1건 = DECISION-19BE9FD5(SPEC-046) 확정.
- SPEC-046 선점(`lease-41387-20260806083936`, acquire·release 전부 exit 0). 선점 후 재확인: `grep -l DECISION-19BE9FD5 tasks/*.md` 0건.
- 작업 생성 후 재실행 → `no-target`/1. 승인 소비 확인. `wf-eligible.sh developer` → `eligible`/0 (TASK-130만 대상, TASK-131은 선행 미충족).

### 핵심 결정과 근거
- **2분할.** TASK-130 = 앱 소스의 규칙 자산(공통 규칙 §9, §8 문장 교체, 세 역할 계약, 버전 5값, 새 검사). TASK-131 = `docs/file-contract.md`의 확장 대응표·전후 예문. TASK-118이 같은 두 파일을 1작업으로 처리한 전례가 있으나 이번 변경량이 커서 나눔. 검토 방법도 다름(`cargo test` vs 사람이 예문 읽기).
- **`scope_files` 무겹침인데 `depends_on` 부여.** 파일 충돌이 아니라 R7·완료 조건 5의 대조 기준 때문. 확장 대응표의 3쌍은 "기획서가 적은 값"이 아니라 "실제 설치된 규칙 본문"과 맞아야 하고, TASK-130이 표기를 다듬으면 먼저 끝난 TASK-131이 어긋난 채 도장까지 통과함. 이 근거를 TASK-131 본문과 아키텍트 보고서에 명시.
- **새 절은 §9, 맨 뒤 덧붙임.** `§` 상호참조가 앱 소스에 §4 11·§5 2·§6 3·§8 7건, `file-contract.md`에 §4 2·§8 1건 = 23건. 중간 삽입 시 전부 밀림. TASK-118의 §8 처리와 동일 판단.
- **§8에서 두 문장 중 뒤의 것만 삭제.** 확인 사실 4가 두 문장을 인용하며 "마지막 문장"이라 적어 범위가 한 가지로 안 읽힘. 앞 문장("These conditions reach the summary section and nothing else.")은 요약 절 금지 조건의 적용 범위만 정하므로 삭제 시 R2 3번 항목이 깨짐. R2 세 항목을 모두 만족시키는 유일한 읽기라 고정하고, 개발자가 다른 읽기로 판단하면 인계 사항으로 보고하도록 작업 문서에 명시.
- **버전: 공통 12→13, planner 7→8, developer 7→8, architect 6→7, `ROLE_RULES_VERSION` 7→8.** architect를 8이 아닌 7로 두는 것이 핵심 — `a_role_contract_below_the_version_constant_is_not_rewritten_every_time`이 "아키텍트 계약 버전 < 상수"를 전제로 서 있음. 7 < 8로 전제 유지(TASK-118의 6 < 7을 한 칸 이동).
- **설치본은 `scope_files` 제외.** `.workflow/rules/*.md`는 앱이 다시 설치. 기획서 완료 조건 10은 QA 도장 이후 확인 항목이라 TASK-130 완료 조건 9에 "개발자가 `## 확인 동선`에 확인 방법을 적는다"로 배치.
- **확장 대응표 추가 낱말 4종 확정: 자리·몫·어법·표면.** 확인 사실 1의 7종 중 최소 3쌍에 없는 것들. 각 대응어는 기획서 미정이라 개발자가 문맥 근거와 함께 정하도록 위임.

### 변경한 파일·모듈
- 신규 `.workflow/도그푸딩--wf_ae6cd700/tasks/TASK-130.md`, `tasks/TASK-131.md` (둘 다 `status: todo`, `history` `created` 1건), `reports/REPORT-SPEC-046-ARCH.md`.
- 제품 소스·기획서·결정·기존 작업 문서 무변경.

### 검증
- 결정권자 요약 절: 두 작업 모두 공백 제외 9줄(상한 10), 백틱·경로·`snake_case`·`파일:줄번호` 0건 스크립트 확인.
- `depends_on`을 조건 스크립트의 `deps_of` 이식본으로 파싱 → TASK-130 `[]`, TASK-131 `[TASK-130]` 정상. `scope_files` 키 각 1회, 경로 허용 문자만 사용.
- `wf-eligible.sh` architect → `no-target`/1, developer → `eligible`/0.
- lease 파일 잔여 없음(`SPEC-009.yml`은 2026-08-03 만료분으로 무관).

### 후속 / 리스크
- TASK-130 → TASK-131 순으로 개발자 세션 2회 필요. TASK-131은 TASK-130이 `qa_waiting` 이상이 되어야 대상이 됨.
- 확인 사실 4의 인용 범위 모호성은 아키텍트 판단으로 고정. 개발자가 다른 읽기가 맞다고 보면 기획자 단계로 회귀해야 함.
- 확장 대응표 4종 대응어는 기획서 미정 → QA에서 수정 요청으로 돌아올 여지 있음.
- 기획서 완료 조건 11(작업 문서·보고서가 R1 준수)은 자동 검사가 아니라 세션 자기 점검. 기획서가 자동 검사를 제외 범위로 정한 결과.
- 공통 규칙 현재 251줄. §9가 커지면 매 세션 읽기 비용이 늘어 TASK-130 검증 절차 5에 확인 항목으로 배치.
- 역할 밖 발견 없음.

## 개발자 세션 (17:49 KST~) — TASK-130

### 목표 / 요청 범위
- 승인된 SPEC-046에 따라 공통 규칙과 세 역할 계약에 한국어 문체 기준을 추가하고 관련 버전과 검사를 갱신한다.
- 금지어 목록을 늘리는 방식 대신, 일반적인 한국 기획·개발 문서의 문체와 표준 실무 용어를 유지하는 기준을 명시한다.

### 현재 상태
- `wf-eligible.sh developer` → `eligible`/0.
- TASK-130 선점: `lease-46241-20260806084948`, 작업 상태를 `in_progress`로 전환.

### 핵심 결정과 근거
- 금지어 목록을 늘리지 않고 공통 원칙을 추가했다. 자연스러운 한국 실무 문어체, 표준 한자어와 기술 용어 유지, 주체·행동·결과의 명시가 핵심이다.
- 사용자 추가 의견에 따라 영문식 긴 대시를 한국어 문장 구조 대신 습관적으로 사용하지 않는 원칙을 포함했다.
- 기존 1~8번 항목의 번호와 참조는 유지하고 새 문체 규칙을 9번으로 추가했다.

### 변경한 파일·모듈
- 제품 변경: `src-tauri/src/infrastructure/project_instructions.rs`.
- 워크플로 기록: TASK-130 상태와 확인 동선, `REPORT-TASK-130-DEV.md`, 이 개발 로그.

### 검증과 결과
- Rust 전체 테스트 539개 통과, 실패 0개.
- `npm run check`: 타입 검사, 프런트 테스트 705개, 빌드 통과.
- `git diff --check` 오류 없음. 기존 4·5·6·8번 참조 개수 유지.

### 후속 / 리스크
- TASK-130을 `qa_waiting`으로 전환. 사용자 확인 뒤 설치본에 공통 규칙 버전 13이 반영되는지 확인한다.
- TASK-131이 확장 용어 예시와 전후 문장을 한국어 파일 계약 문서에 추가한다.
- 자동 판정은 추가하지 않았다. 실제 문체 개선 여부는 후속 문서 표본을 사람이 읽어 확인해야 한다.

## 개발자 재작업 세션 (18:10 KST~) — TASK-131

### 목표 / 요청 범위
- QA 수정 요청 QA-AFB67132에 따라 문체 안내 자체를 자연스러운 한국 실무 문어체로 다시 작성한다.
- 전후 예문 세 쌍은 유지하고, 용어 설명과 예외 조건을 수정한다.

### 현재 상태
- `wf-eligible.sh developer` → `eligible`/0.
- TASK-131 선점: `lease-61827-20260806091030`, 작업 상태를 `in_progress`로 전환.

### 핵심 결정과 근거
- 기존 전후 예문 세 쌍은 QA 의견에 따라 유지했다.
- 문체 안내와 표 제목은 일반적인 실무 표현으로 다시 작성했다.
- 의미가 분명하다는 이유만으로 비유를 허용하지 않는다. 한국 개발 문서에서 통용되는 표준 기술 용어이고 문장에서 정확한 의미로 사용된 경우에만 예외로 인정한다.

### 변경한 파일·모듈
- 제품 문서: `docs/file-contract.md`의 `## 문서 문체` 섹션.
- 워크플로 기록: TASK-131 요약·상태·확인 동선, `REPORT-TASK-131-REWORK-DEV.md`, 이 개발 로그.

### 검증과 결과
- `npm run check`: 타입 검사, 프런트 테스트 705개, 빌드 통과.
- `git diff --check` 오류 없음.
- QA가 지적한 표현과 영문식 긴 대시가 새 문체 안내의 설명 문장에 남지 않았음을 확인.

### 후속 / 리스크
- TASK-131을 `qa_waiting`으로 다시 전환. 실제 문체 개선 여부는 이후 문서 표본을 사람이 읽어 확인한다.

## 개발자 세션 (18:00 KST~) — TASK-131

### 목표 / 요청 범위
- 승인된 SPEC-046의 완료 조건 5·6을 처리한다. 한국어 파일 계약 문서에 확장 대응표와 전후 예문을 담는 문체 절을 넣는다.
- 확장 대응표의 최소 세 쌍을 실제 규칙 본문과 대조한다.

### 현재 상태
- 선점 대상 판정: `todo`인 TASK-131 하나. 선행 TASK-130은 `qa_waiting`이라 의존 충족. 미만료 lease 없음.
- TASK-131 선점: `lease-58368-20260806090024`, 작업 상태를 `in_progress`로 전환한 뒤 착수.

### 핵심 결정과 근거
- 대조 기준을 설치본이 아니라 `src-tauri/src/infrastructure/project_instructions.rs`의 §9 문자열로 잡았다. 설치본 갱신은 TASK-130의 확인 도장 뒤에 앱이 수행하므로 지금 설치본에는 §9가 없다. 이 문자열이 설치본이 될 원본이다.
- 확장 대응표에 더한 네 낱말(자리·몫·어법·표면)의 바꿔 쓸 말은 저장소의 실제 쓰임을 읽고 정했다. 자리와 어법은 한 낱말이 서로 다른 두 대상을 가리키고 있어 대응을 둘로 나눴다.
- 전후 예문의 "전" 문장은 기존 작업 문서에서 그대로 가져오되 출처를 인용하지 않았다. 기획서 R8이 기존 문서 수정을 막는다.

### 변경한 파일·모듈
- 제품 변경: `docs/file-contract.md` 하나, 41줄 추가. `## 안전 규칙` 바로 앞에 `## 문서 문체` 절을 넣었다.
- 워크플로 기록: TASK-131의 상태와 요약과 확인 동선, `REPORT-TASK-131-DEV.md`, 이 개발 로그.

### 검증과 결과
- `npm run check`: 타입 검사 통과, 23개 파일의 테스트 705개 통과, 빌드 성공.
- 규칙 본문 대조: 착지·닫다·원천 세 쌍이 §9의 값과 낱말 단위로 같다.
- 예문 원칙 대조: 세 쌍의 "후" 문장을 §9의 세 항목으로 하나씩 확인했다. 결과는 보고서에 있다.
- `git diff --stat`: 이 세션의 제품 변경은 `docs/file-contract.md` 하나다. 나머지 수정본은 선점 이전부터 작업 트리에 있던 것이고 이 세션이 열지 않았다.

### 후속 / 리스크
- TASK-131을 `qa_waiting`으로 전환하고 lease를 해제했다.
- TASK-130의 확인 도장 뒤 설치본이 갱신되면 §9와 대응표를 한 번 더 맞춰 보는 것이 안전하다.
- 인계 사항 둘을 보고서에 적었다. 만료된 SPEC-009 lease가 남아 있는 것과, `.workflow/rules/wf-eligible.sh`가 이 세션 이전부터 수정 상태인 것이다.

## 기획자 세션: 관리 규칙 갱신 시점

### 범위
- 사용자의 후속 요청 두 건을 앱 인박스에 각각 등록했다. 관리 규칙 자동 갱신은 IDEA-954B53EA, 사용자 정의 가드레일은 IDEA-655ADA04다.
- 두 아이디어를 별도 기능으로 유지한다. 이 세션에서는 등록 시각이 빠른 IDEA-954B53EA만 기획한다.

### 현재 상태
- 기획자, 아키텍트, 개발자 대기열은 새 아이디어 등록 전에 모두 비어 있었다.
- IDEA-954B53EA를 `lease-68100-20260806093026`으로 선점하고 SPEC-047 초안을 만들었다.

### 확인한 사실
- 현재 설치된 공통 규칙은 버전 12이고 앱 소스와 새 개발 바이너리는 버전 13을 포함한다.
- 작업 QA 두 건은 새 바이너리가 시작되기 약 51초 전에 기록돼 구버전 백엔드가 처리했다.
- 프로젝트 조회는 읽기만 수행하며, 규칙 설치는 워크플로 생성과 기획 결정, 작업 QA에서만 실행된다.
- 같은 조회가 프로젝트 열기, 수동 새로 고침, 2.5초 주기 갱신에 함께 쓰인다. 자동 갱신을 단순히 조회에 넣으면 주기 갱신도 쓰기 동작이 된다.

### 결정과 결과
- 프로젝트 열기와 수동 새로 고침만 관리 규칙을 동기화하고, 2.5초 자동 조회는 읽기 전용으로 유지한다.
- 충돌 시 프로젝트를 닫지 않고 설정 화면에서 설치 버전과 실패 이유를 보여준다.
- 사용자 정의 가드레일은 IDEA-655ADA04로 분리해 다음 기획 대상으로 남겼다.
- SPEC-047을 `user_review`로 전환하고 `REPORT-SPEC-047-PLAN.md`를 작성했다.

### 검증과 후속
- 기획서 요약은 빈 줄을 제외하고 8줄이며 요약 형식 제한을 지킨다.
- 코드 변경은 하지 않았다. 사용자 승인 뒤 아키텍트가 개발 작업을 나눈다.

## TL 기획자 세션: 사용자 정의 가드레일

### 범위
- 사용자가 요청한 TL 진행 방식에 따라 SPEC-047 후속을 관리하고, IDEA-655ADA04를 별도 기획서로 만든다.
- 서브에이전트 두 명은 각각 SPEC-047 설계 위험과 커스텀 가드레일 MVP를 읽기 전용으로 조사한다.

### 현재 상태
- SPEC-047은 `user_review`라 사용자 승인 전까지 아키텍트 작업을 시작하지 않는다.
- IDEA-655ADA04를 `lease-71216-20260806094029`으로 선점하고 SPEC-048 초안을 만들었다.
- 앱 코드와 사용자 결정 문서는 변경하지 않는다.

### 조사 결과와 결정
- 커스텀 가드레일은 앱 기본 규칙과 분리된 프로젝트 파일 하나로 시작한다.
- 세 역할의 적용 범위, 사용 여부, Markdown 본문, 저장 파일과 역할별 최종 규칙 미리보기를 제공한다.
- 기본 규칙과 역할 계약이 항상 우선하며 자연어 의미 검사를 안전 보장 수단으로 사용하지 않는다.
- 외부 변경은 다시 읽도록 하고 자동 병합하지 않는다. 내용 식별값을 비교해 동시 덮어쓰기를 막는다.
- 심볼릭 링크와 잘못된 형식, 더 새로운 스키마, 길이와 제어문자 오류를 안전하게 표시한다.

### 결과와 후속
- SPEC-048을 `user_review`로 전환하고 `REPORT-SPEC-048-PLAN.md`를 작성했다.
- 코드 변경은 없다. SPEC-047과 SPEC-048이 모두 승인되면 겹치는 설정 화면과 규칙 진입 안내 때문에
  관리 규칙 동기화 작업을 먼저 진행한다.

## 아키텍트 세션: SPEC-047 관리 규칙 자동 갱신 분해

### 범위
- 앱이 기록한 승인 DECISION-6650F2D7을 근거로 SPEC-047 하나만 처리했다.
- 구현은 하지 않고 개발 작업과 아키텍처 보고서만 작성했다.

### 핵심 결정
- TASK-132는 관리 자산 일곱 종류를 모두 사전 검사하고 마이그레이션과 같은 잠금 아래 동기화한다.
- TASK-133은 프로젝트 열기와 수동 새로 고침에서만 별도 동기화 명령을 호출한다. 2.5초 자동 조회는
  계속 읽기 전용이다.
- TASK-134는 설정 화면과 공통 알림에서 공통 규칙, 역할별 버전, 마지막 결과를 표시한다.
- 역할 계약의 앱 제공 버전은 기획자 8, 아키텍트 7, 개발자 8로 각각 판단한다.
- 동기화 결과는 프로젝트 조회 오류와 별도 상태로 유지한다.

### 변경한 파일
- 신규 작업 문서 TASK-132, TASK-133, TASK-134.
- 신규 아키텍처 보고서 REPORT-SPEC-047-ARCH.
- 이 개발 로그.

### 검증과 후속
- 세 작업을 순차 의존 관계로 만들고 파일 범위를 각각 선언했다.
- SPEC-048이 공유하는 규칙 설치기, 상태 훅, 설정 화면 작업은 이번 세 작업 완료 뒤에 시작하도록
  아키텍처 보고서에 인계 조건을 기록했다.
- 제품 소스, 기획서, 사용자 결정은 수정하지 않았다.

## 개발자 세션: TASK-132 관리 자산 동기화

### 범위
- TASK-132의 백엔드 범위만 구현한다.
- 일곱 종류의 관리 자산 사전 검사, 공용 쓰기 잠금, 별도 동기화 명령과 기존 설치 경로 통합을 다룬다.

### 현재 상태
- DECISION-6650F2D7의 사용자 승인과 TASK-132의 착수 조건을 확인했다.
- TASK-132를 선점하고 `in_progress`로 전환했다.
- 기존 작업 트리의 다른 변경은 보존한다.

### 핵심 결정과 변경
- 일곱 종류의 관리 자산을 모두 사전 검사한 뒤 충돌이 없을 때만 저장하는 조정 계층을 추가했다.
- 역할 계약은 기획자 8, 아키텍트 7, 개발자 8을 각자의 제공 버전으로 판단한다.
- 동기화와 마이그레이션이 같은 배타 잠금을 사용한다. 계획 후와 각 파일 교체 직전에 기준 파일을 다시 확인한다.
- 문서 조회와 분리된 구조화 동기화 명령을 추가했다. 기존 생성·결정·QA도 같은 조정 계층을 사용한다.
- 프론트엔드와 커스텀 가드레일 관련 파일은 수정하지 않았다.

### 검증과 결과
- 전체 Rust 테스트 555개가 통과했고 실패는 없었다.
- 모든 대상의 Clippy를 경고 오류 처리로 실행해 통과했다.
- Rust 포맷 검사와 `git diff --check`가 통과했다.
- 파일 시스템 특성상 전원이 끊기면 여러 파일의 교체 전체를 하나의 원자적 트랜잭션으로 보장할 수 없다. 사전 검사와
  파일별 원자적 교체로 일반적인 충돌과 중간 파일 쓰기를 방지했다.
- TASK-132를 `qa_waiting`으로 전환했다.

## 개발자 재작업 세션: TASK-132 QA 수정 요청

### 범위
- QA-67F55DF6의 두 수정 요청만 반영한다.
- 순차 교체 중 기준 변경의 전체 무쓰기 보장과 비UTF-8 관리 자산의 구조화 충돌 반환을 다룬다.

### 현재 상태
- TASK-132를 다시 선점하고 `in_progress`로 전환했다.
- 기존 이력과 초기 구현, QA 결정 문서는 보존한다.

### 핵심 결정과 변경
- 순차 저장 중 후속 자산의 기준 변경을 보면 앞서 교체한 자산을 역순으로 사전 검사 바이트로 복원한다.
- 복원 대상을 외부 프로세스가 다시 바꾸었으면 덮어쓰지 않고 저장 오류로 남긴다.
- 일곱 관리 자산의 비 UTF-8 바이트를 자산별 이유가 있는 구조화된 충돌로 분류했다.
- 저장소 공개 동기화 진입점에서도 비 UTF-8이 명령 오류가 아닌 충돌 결과로 내려오는지 확인했다.

### 검증과 결과
- 관리 자산 전용 검사 12개와 저장소 공개 진입점 회귀 검사가 통과했다.
- 전체 Rust 테스트 558개가 통과했고 실패는 없었다.
- 모든 대상의 Clippy를 경고 오류 처리로 실행해 통과했다.
- Rust 포맷 검사와 `git diff --check`가 통과했다.
- TASK-132를 `qa_waiting`으로 전환했다.

## TL 검수: TASK-132

### 범위와 결과
- 개발자 보고서, 작업 상태, 선점 해제, 선언 범위를 확인하고 Rust 포맷·Clippy·전체 테스트를 다시 실행했다.
- TL 재검증도 Rust 테스트 555개 통과, Clippy 경고 0건, 포맷 및 `git diff --check` 통과였다.
- 별도 읽기 전용 리뷰에서 완료 조건에 미달하는 두 경로를 확인해 사용자 QA 수정 요청이 필요하다고 판단했다.

### 수정이 필요한 내용
- 전체 기준 대조 뒤 파일을 순서대로 교체하는 중 후속 파일의 외부 변경을 감지하면, 먼저 갱신한 파일이 남는다.
  정상 실행 중 확인한 기준 변경은 저장 시작 전에 발견해야 한다는 TASK-132 완료 조건을 충족하도록 보완해야 한다.
- 비UTF-8 관리 자산은 현재 명령 오류가 된다. 손상된 관리 파일도 대상 자산과 사유를 포함한 구조화된
  `conflict` 결과로 반환해야 한다.

### 후속
- 작업 상태와 사용자 결정을 TL이 대신 바꾸지 않는다. 사용자가 TASK-132에 수정 요청을 기록하면 같은 작업을
  다시 선점해 두 경로를 보완하고 전체 검증을 반복한다.
- TASK-133과 TASK-135는 보완이 끝날 때까지 착수하지 않는다.

## TL 재검수: TASK-132 QA 재작업

### 확인 결과
- QA-67F55DF6에 따라 후속 자산 변경 시 앞선 교체를 복원하고, 일곱 관리 자산의 비 UTF-8 파일을
  구조화된 충돌로 반환하도록 재작업했다.
- TL 재검증에서 Rust 테스트 558개, 포맷, Clippy, `git diff --check`가 모두 통과했다.
- 비 UTF-8 분류와 기본 복원 경로는 해결됐지만, 독립 리뷰에서 복원 구현의 추가 경합 두 건을 확인했다.

### 추가 수정이 필요한 내용
- 현재 바이트 확인과 복원 또는 삭제가 별도 파일 시스템 연산이다. 그 사이 외부 저장이 발생하면 새 사용자
  내용을 덮어쓰거나 삭제할 수 있어, 외부 변경을 보존한다는 QA 요구를 충족하지 못한다.
- 여러 적용 자산을 역순 복원하다 하나가 실패하면 즉시 중단한다. 나머지 복원 가능한 자산도 계속 처리하고,
  원래의 구조화된 충돌과 복원 실패를 함께 보존해야 한다.

### 후속
- 사용자가 TASK-132에 두 번째 수정 요청을 기록하면 데이터 손실 없는 원자적 격리와 조건부 복원 방식으로
  재설계하고 실패가 있어도 모든 복원 후보를 끝까지 처리하는 검사를 추가한다.
- TASK-133과 TASK-135는 두 번째 보완과 독립 재검수 전까지 착수하지 않는다.

## TL 최종 재검수: TASK-132 두 번째 QA 재작업

### 확인 결과
- QA-E4ED0DE9에 따라 롤백 대상을 같은 파일시스템의 복구 디렉터리로 원자 격리하고, 대상이 없을 때만
  조건부 복원하도록 바꿨다. 열린 핸들의 후속 쓰기는 격리 파일에 남기고 경로를 결과에 포함한다.
- 한 자산의 복원이 실패해도 모든 적용 자산의 복원을 계속하고, 최초 충돌과 자산별 실패 및 복구 위치를
  구조화된 결과에 함께 담는다.
- TL 재검증은 Rust 테스트 561개, 전용 검사 15개, 포맷, Clippy, `git diff --check`가 모두 통과했다.
- 독립 재검수에서 이전 데이터 손실 경합과 조기 중단이 해소됐음을 확인했다.

### 남은 전달 누락
- 새 동기화 명령은 복구 위치를 반환하지만 기존 워크플로 생성, 기획 결정, 단건 QA, 일괄 QA 경로는
  충돌 결과를 문자열 오류로 바꾸면서 성공한 롤백의 복구 위치를 버린다.
- 사용자 바이트는 삭제되지 않지만 호출자가 영구 복구 디렉터리 위치를 알 수 없어 고아 복구본이 된다.
  기존 네 경로의 오류에도 자산별 복구 위치를 포함하고 해당 회귀 검사를 추가해야 한다.

### 후속
- 사용자가 TASK-132에 세 번째 수정 요청을 기록하면 전달 누락 한 건만 보완하고 전체 검증과 독립 검수를
  반복한다. TASK-133과 TASK-135는 그때까지 착수하지 않는다.

## 개발자 두 번째 재작업: TASK-132 복원 경합

### 범위
- QA-E4ED0DE9의 두 수정 요청만 반영했다.
- 복원 확인과 쓰기 사이의 경합, 다중 복원 실패 중 나머지 자산 처리를 다루었다.

### 핵심 결정과 변경
- 복원 대상을 같은 부모 디렉터리의 고유한 복구 디렉터리로 먼저 원자적 이동했다.
- 검사 전 내용은 대상 경로가 없을 때만 조건부로 복원한다. 경합 중 새로 생긴 파일은 덮거나 삭제하지 않는다.
- 열린 핸들이 격리 뒤에 같은 파일에 쓸 수 있어 격리본을 자동 삭제하지 않는다. 결과가 자산별 복구 위치를 제공한다.
- 복원은 모든 적용 자산을 역순으로 끝까지 처리한다. 최초 충돌, 자산별 복원 실패, 복구 위치를 하나의 구조화된 결과에 보존한다.
- 덮어쓰기 금지 조건부 이동은 지원 OS에서 원자적 이동을 사용하고, 대체 경로도 기존 파일을 덮어쓰지 않는다.

### 검증과 결과
- 관리 자산 전용 검사 15개가 통과했다. 열린 핸들의 격리 후 쓰기, 원래 없던 경로의 외부 재생성, 다중 복원 실패 계속 처리를 포함한다.
- 전체 Rust 테스트 561개가 통과했고 실패는 없었다.
- 모든 대상의 Clippy를 경고 오류 처리로 실행해 통과했다.
- Rust 포맷 검사와 `git diff --check`가 통과했다.
- 파일시스템은 여러 파일 교체 전체의 원자성을 제공하지 않는다. 전원 차단 시 부분 적용 가능성은 남아 있다.
- TASK-132를 `qa_waiting`으로 전환했다.

## 개발자 세 번째 재작업: TASK-132 복구 위치 전달

### 범위
- QA-B85C35FE의 복구 위치 전달 누락만 직접 보완했다.
- 서브에이전트를 사용하지 않고 기존 생성·결정·단건 QA·일괄 QA의 공통 설치 경로와 회귀 검사를 확인했다.

### 핵심 변경
- 구조화된 충돌을 기존 문자열 오류로 바꾸는 공통 계층이 성공한 롤백의 자산별 복구 위치를 함께 전달한다.
- 롤백 도중 발생하는 읽기·쓰기 오류도 모든 복원 실패와 성공한 복구 위치를 함께 보존한다.
- 실제 열린 파일 핸들 경합 결과를 기존 설치 경로로 변환했을 때 복구 위치가 남는 회귀 검사를 추가했다.

### 검증과 결과
- 관리 자산 전용 검사 16개와 전체 Rust 테스트 562개가 통과했고 실패는 없었다.
- 모든 대상의 Clippy, Rust 포맷 검사, `git diff --check`가 통과했다.
- TASK-132의 마지막 QA 수정 요청 범위를 충족했으며 사용자 확인 대기로 전환한다.

## 개발자 세션: TASK-133 명시적 관리 규칙 동기화

### 범위와 결정
- 폴더 열기, 최근 프로젝트 열기, 수동 새로 고침에서만 프로젝트 조회 뒤 관리 자산 동기화를 실행한다.
- 2.5초 자동 조회는 기존 읽기 경로만 유지하고 동기화 명령을 호출하지 않는다.
- 동기화 결과와 명령 오류를 일반 프로젝트 오류와 분리하며 프로젝트를 닫을 때 함께 비운다.

### 변경과 검증
- 프런트엔드 도메인 타입, Tauri 게이트웨이, 작업 공간 훅과 훅 검사를 수정했다.
- 명시적 호출 순서, 비호환 프로젝트 제외, 오류 격리, 자동 조회 무쓰기를 회귀 검사로 확인했다.
- 프런트엔드 전체 710개 검사, 타입 검사, 배포 빌드와 `git diff --check`가 통과했다.
- TASK-133을 사용자 QA 대기로 전환했다. 후속 TASK-134와 TASK-136이 이 상태 계약을 사용한다.

## 개발자 세션: TASK-135 사용자 정의 규칙 백엔드

### 범위와 결정
- `.workflow/rules/custom.md`의 안전한 조회, 정규 직렬화, 역할별 미리보기와 동시 수정 방지 저장을
  구현했다.
- 사용자 파일은 관리 자산 목록에서 제외하고 관리 자산 동기화와 같은 프로젝트 쓰기 잠금만 공유한다.
- 공통 규칙과 기획자·아키텍트·개발자 계약의 제공 버전을 14·9·8·9로 올리고 사용자 규칙의 적용 조건과
  우선순위를 명시했다.

### 변경과 검증
- 도메인, 저장소, 서비스, Tauri 명령과 명령 등록에 조회, 미리보기, 저장 계약을 연결했다.
- 형식 오류, 미래 스키마, 역할과 길이 오류, 심볼릭 링크, 외부 생성·수정·삭제·교체, 잠금 경합과 임시
  파일 생성 실패 회귀 검사를 추가했다.
- 잘못된 사용자 파일이 프로젝트 조회와 관리 자산 동기화를 막지 않고 내용과 수정 시각이 유지되는
  저장소 통합 검사를 추가했다.
- Rust 전체 574개 테스트, Clippy 경고 0건, 포맷 검사와 `git diff --check`가 통과했다.
- TASK-135를 사용자 QA 대기로 전환했다. TASK-136과 TASK-137이 상태 수명과 설정 화면을 연결한다.

## 개발자 세션: TASK-134 관리 규칙 상태 화면

### 범위와 결정
- 설정 화면에 현재 프로젝트 카드와 별도인 관리 규칙 카드를 추가하고, 공통 규칙과 세 역할 계약, 진입
  안내, 선점 헬퍼의 설치·제공 버전을 자산 행마다 표시한다.
- `current`, `updated`, `retry_required`, `conflict`에 서로 다른 상태 이름과 후속 행동 문장을 부여했다.
  기다렸다 다시 시도할 상태와 사용자가 파일을 확인해야 하는 상태를 한 문구로 합치지 않는다.
- 제공 버전이 없는 진입 안내에는 숫자를 만들지 않고, 버전을 쓰는 자산의 설치 값이 없으면 읽지 못했다고
  적는다.
- 재시도, 충돌, 동기화 명령 실패는 기존 프로젝트 오류 배너와 분리된 관리 규칙 알림으로 표시한다.
  껍데기가 알림 상태를 따로 저장하지 않으므로 2.5초 자동 조회 결과가 알림을 지우지 않는다.
- 화면 계층은 파일을 다시 읽거나 버전을 계산하지 않고 TASK-133이 제공한 상태를 그대로 사용한다.
  커스텀 규칙 편집과 저장 기능은 추가하지 않았다.

### 변경과 검증
- `src/App.tsx`, `src/App.css`, `WorkspaceShell.tsx`와 그 검사, `SettingsView.tsx`와 그 검사를
  수정했다. `src-tauri`는 변경하지 않아 Rust 게이트는 생략했다.
- 네 결과 상태의 문구 구분, 역할별 버전 표시, 충돌 자산 이름과 이유, 복구 위치 표시, 명령 실패와
  프로젝트 오류의 분리, 알림 중 화면 이동 가능, 자동 재렌더 뒤 알림 유지를 회귀 검사로 확인했다.
- `npm run check`: 타입 검사, 프런트엔드 728개 검사, 배포 빌드가 통과했다. 검사 개수는 710개에서
  18개 늘었고 삭제하거나 비활성화한 검사는 없다.
- TASK-134를 사용자 QA 대기로 전환하고 확인 동선을 작업 문서에 작성했다. 병행 세션이 작업 트리에
  남긴 다른 변경은 손대지 않았다.

## 개발자 세션: TASK-136 사용자 정의 규칙 작업 공간 연결

### 범위와 결정
- 사용자 규칙 조회, 미리보기 준비, 저장 계약을 프런트엔드 도메인 타입과 Tauri 게이트웨이에 연결했다.
- 프로젝트 열기와 수동 새로 고침은 관리 규칙 동기화 뒤 사용자 규칙을 읽고, 2.5초 자동 조회는 읽기만
  하도록 분리했다.
- 미리보기 준비 시점의 파일 식별값을 저장 기준으로 보존하고, 프로젝트별 요청 번호와 경로로 늦은 응답을
  차단했다.
- 저장 성공, 외부 변경 충돌, 잠금 재시도와 명령 오류를 합치지 않고 각각의 후속 상태로 유지했다.

### 변경과 검증
- `types.ts`, `tauriProjectGateway.ts`, `useProjectWorkspace.ts`와 훅 검사를 수정했다.
- 파일 없음, 형식 오류, 미래 스키마, 안전하지 않은 파일, 같은 내용과 외부 변경, 저장 결과 세 종류,
  명령 오류, 프로젝트 전환 뒤 늦은 응답을 회귀 검사로 확인했다.
- 사용자 규칙 훅 검사 61개와 프런트엔드 전체 741개 검사, 타입 검사, 배포 빌드, `git diff --check`가
  통과했다.
- TASK-136을 사용자 QA 대기로 전환했다. TASK-137이 이 상태를 설정 화면에 연결한다.

## 개발자 세션: TASK-137 사용자 정의 규칙 설정 화면

### 범위와 결정
- 설정 화면에 사용자 정의 규칙 카드를 추가하고 사용 여부, 세 역할과 Markdown 본문을 한 초안으로
  편집하게 했다.
- 백엔드 미리보기의 저장 원문과 역할별 세 출처를 그대로 보여준 뒤에만 저장할 수 있게 했다.
- 외부 변경과 저장 충돌에서는 초안을 유지하고 저장을 막으며, 사용자가 확인한 다시 불러오기만 최신
  디스크 상태를 초안에 반영한다.
- 형식 오류와 미래 스키마는 원문 확인 뒤 명시적으로 덮어쓰고, 안전하지 않은 파일은 앱에서 덮어쓰지
  않는다.
- 사용자 본문 Markdown에서 원시 HTML, 클릭 가능한 링크와 외부 이미지를 제거하고 비밀 값 금지, 앱
  규칙 우선, 다음 세션 적용 안내를 항상 표시한다.

### 변경과 검증
- `CustomRulesCard.tsx`와 전용 검사 파일을 추가하고 `App`, `WorkspaceShell`, `SettingsView`, 스타일과
  기존 컴포넌트 검사를 연결했다.
- 편집과 변경 취소, 빈 본문 확인, 오류 파일 덮어쓰기, 외부 변경 보존, 다시 불러오기 실패, 저장 결과,
  역할별 출처, Markdown 안전성과 활성 lease 저장을 회귀 검사로 확인했다.
- 사용자 규칙 카드 검사 17개, 설정과 작업 공간 집중 검사 57개, 프런트엔드 전체 760개 검사, 타입 검사,
  배포 빌드와 `git diff --check`가 통과했다.
- TASK-137을 사용자 QA 대기로 전환하고 실제 파일 저장과 외부 변경 확인 동선을 작업 문서에 기록했다.

## TL 병렬 지휘 세션 (21:10~21:45 KST) — TASK-134 워커 착지, codex 파이프라인 병행

### 목표 / 요청 범위
- 사용자 지시: 이전 세션 팔로업 후 TL 병렬 진행. TL은 지휘·검증·기록만 하고 구현은 워커와 병행 codex 세션이 수행했다.

### 실행
- 착수 실측: 미완료 5건(133 qa_waiting / 134·136·137 todo / 135 in_progress-codex). developer만 eligible, 대상은 TASK-134 하나.
- tl-dev-134 워커(오퍼스, developer 계약)를 TASK-134에 투입. codex의 TASK-135 lease와 범위 무겹침을 확인하고 병렬 진행했다.
- codex가 TASK-135 착지(12:29Z경) 직후 TASK-136을 선점했다. 같은 시각에 투입한 tl-dev-136 워커는 자격 판정에서 미만료 lease를 확인하고 파일 0건 변경으로 NO_ELIGIBLE_WORK 보고 후 물러났다. 이중 배정 사고 없음.
- tl-dev-134가 TASK-134를 qa_waiting(12:29:45Z)으로 착지. 하네스가 워커의 보고서 파일 쓰기를 막아 발신 전문을 TL이 `reports/REPORT-TASK-134-DEV.md`로 대리 기록했다(기록 경위 머리말, TASK-096 선례).
- TL 검증은 문서 단위(상태 전이·history·lease 반납)로 수행하고 게이트는 워커 보고 수치를 기록 원본으로 받았다(운영 규범).

### 결과 (워커 보고 수치)
- TASK-134: npm run check 통과(검사 728개, 타입·빌드 포함), 신규 회귀 검사 18개, 기존 검사 삭제 0건. cargo는 러스트 무변경으로 생략(병행 codex의 러스트 중간 상태 측정 회피).
- 세션 중 실행 앱이 설치 규칙을 v14/9/8/9로 동기화했다 — TASK-132·133 경로의 첫 실전 동작. 작업 트리의 `.workflow/rules/*.md` 변경은 그 결과다.

### 후속 / 리스크
- 사용자 관문: QA 도장 4건(TASK-133·134·135·136). codex가 TASK-137을 미도장 134 위에서 진행 중이라 134 반려 시 재작업이 연쇄된다 — 134 확인 동선 우선 권고.
- TASK-137 착지 감시 중. 착지하면 SPEC-047·048 파이프라인이 전부 qa_waiting으로 닫히고 세 역할 모두 no-target이 된다.
- 만료 lease `SPEC-009.yml` 잔존, §4대로 무접촉.

## TL 세션 이어서 (22:00~22:15 KST) — 승인 대기 브리핑, 토큰 고정비 아이디어 등록

- TASK-137 착지 관측(codex). SPEC-047·048 파이프라인 전체 qa_waiting 마감, 사용자 도장 대기 5건(TASK-133~137).
- 승인 대기 기획서 2건(SPEC-043·044) 요약을 사용자에게 브리핑. 결정 문서 0건 확인.
- 사용자 지시로 IDEA-CB245507 등록: 역할 세션의 워크플로 규격 토큰 고정비 절감 — 자격 헬퍼의 대상 출력, 작업 문서 자급자족 계약 문언, 보고서 분량 상한 셋을 한 기획으로 묶음. 근거 실측치(세션당 약 3만~4만 토큰) 수록.
- 커스텀 규칙 다중 문서 확장은 사용자가 보류 결정(주간 한도 여유 생기면 재론).

## TL 직접 수정 (22:18~22:22 KST) — 마크다운 단일 물결 취소선 비활성화

- 사용자 승인으로 워크플로 파이프라인을 거치지 않고 직접 수정했다. 범위가 렌더러 옵션 한 줄 × 2곳이라 사용자가 직접 수정을 지시했다.
- 문제: remark-gfm 기본값(singleTilde: true)이 "3만~4만 ... ~"의 단일 물결 쌍을 취소선으로 렌더링한다. 한국어 범위 표기와 정면 충돌.
- 변경: `MarkdownBody.tsx`·`CustomRulesCard.tsx`의 remarkPlugins에 `{ singleTilde: false }` 옵션 추가. `MarkdownBody.test.tsx`에 단일 물결 보존 검사 1건 신설(기존 `~~취소~~` DEL 검사는 무수정 통과 — 이중 물결 취소선은 유지).
- 게이트: 타깃 테스트 2파일 23건 통과, `npm run check` 통과(빌드 정상, 번들 경고는 기왕값).
- 부수: 등록 직후였던 IDEA-CB245507 본문의 물결 범위 표기 7곳을 하이픈으로 교체(기획자 미선점 상태에서 수정).

## 아키텍트 세션 (22:50~23:05 KST) — SPEC-049 분해, TASK-139~141 생성

- 목표: 사용자 승인(DECISION-30E36EFB, 13:47:55Z)이 난 SPEC-049를 구현 가능한 개발 작업으로 분해한다. 범위는 자격 판정 응답 확장(R1), 역할 계약 문언(R2·R3·R4), 절감 측정 방법(완료 조건 11)이다.
- 자격 실측: `wf-eligible.sh architect`가 `eligible`(exit 0). 결정 문서 전수 대조로 최신 결정이 승인이면서 파생 작업이 0건인 기획서가 SPEC-049 하나임을 확정했다. 선점(`lease-40702-20260806135052`) 직후 파생 작업 0건을 재확인했다.

### 분해 결정과 근거

- 세 작업으로 나눴다. TASK-139(자격 판정 확장), TASK-140(역할 계약 문언), TASK-141(측정 방법).
- R2·R3·R4를 한 작업(TASK-140)으로 묶었다. 셋 다 `project_instructions.rs` 한 파일의 계약 문장을 고치고, R2와 R4는 같은 개발자 계약의 버전 축(`DEVELOPER_RULES_VERSION`)을 공유한다. 나누면 병렬 이득은 없으면서 버전만 두 번 올라간다.
- TASK-140에 `depends_on: [TASK-139]`를 걸었다. 실측 결과 `fs_project_repository.rs`의 검사가 `rules_version: 8`(3613·3637행)과 `rules_version: 9`(4196·4215행)를 문자열로 고정하고 있어, 역할 계약 버전을 올리면 그 검사가 깨진다. 두 작업이 같은 파일을 수정하므로 겹침이 실재한다. 구조 변경(TASK-139)을 먼저 반영하고 문자열 수정(TASK-140)을 뒤에 두는 순서로 정했다.
- 버전 축 충돌은 없다. `CONDITION_SCRIPT_VERSION`(11→12)은 `heartbeat_condition.rs`, 역할 계약 버전은 `project_instructions.rs`로 파일이 갈린다.
- TASK-139에 "화면 payload를 바꾸지 않는다"를 설계 지침으로 못박았다. `PendingRoleWork`는 `Copy` 파생에 프런트엔드가 따로 미러링(`types.ts` 143행, `IntegrationCard.tsx`, `HeartbeatCard.tsx`)하고 있어, 여기에 후보 목록을 얹으면 프런트엔드 파일 3개로 범위가 번지고 프런트 작업과 겹친다. 넓어진 정보는 별도 타입으로 두고 기존 payload를 파생시키게 했다.
- TASK-141은 측정 시점을 인자로 받게 설계했다. 기준값이 TASK-140의 계약 문장 추가로 오염되는 것을 순서 강제 없이 막는다. 덕분에 세 작업 중 유일하게 무조건 병렬 가능하다.
- 설치본 `.workflow/rules/*.md`와 `wf-eligible.sh`는 어느 작업의 `scope_files`에도 넣지 않았다. 앱이 설치하는 사본이고, TASK-130이 같은 성격의 계약 변경을 `project_instructions.rs` 단독 범위로 처리한 선례를 따랐다.

### 생성 문서

- `tasks/TASK-139.md` — scope_files 4건(`domain/project.rs`, `fs_project_repository.rs`, `heartbeat_condition.rs`, `role_eligibility.rs`), 선행 없음.
- `tasks/TASK-140.md` — scope_files 2건(`fs_project_repository.rs`, `project_instructions.rs`), `depends_on: [TASK-139]`.
- `tasks/TASK-141.md` — scope_files 2건(`docs/compliance-cost-measurement.md`, `scripts/measure-compliance-cost.mjs`, 둘 다 신규), 선행 없음.
- 세 문서 모두 `status: todo`, `created` history, `source_spec_id: SPEC-049`, `source_decision_id: DECISION-30E36EFB`를 갖췄다. 이번 승인의 R3이 요구하는 대로 기획서를 열지 않고 착수할 수 있는 완결된 지시서로 작성했다(현재 상태 실측값, 완료 조건, 검증 절차를 본문에 직접 담음).

### 후속 / 리스크

- 분량 상한의 구체적 수치는 TASK-140에서 개발자가 정하게 두고, 단위(줄 수)·제외 규칙(빈 줄 미포함)·제약(필수 절이 모두 들어갈 것)·근거 기록 의무만 계약으로 고정했다. 기획서가 수치를 주지 않았고 아키텍트가 임의로 만들 값이 아니다.
- 공통 규칙 본문은 바꾸지 않는 전제로 `WORKFLOW_RULES_VERSION`을 14로 유지하게 했다. 포함 범위가 보고서 규격을 개발자 계약에 반영하라고 명시했기 때문이다. 구현 중 공통 규칙을 고쳐야 한다고 판단되면 버전과 고정 문자열을 함께 올리고 보고서에 남기도록 조건을 달았다.
- TASK-139의 후보 목록 산출이 조건 스크립트의 상수 비용 성능 특성을 되돌릴 위험이 있다. 전체 문서를 여러 번 훑는 구현을 금지한다고 명시했으나 실제 검증은 개발자 몫이다.
- 만료 lease `SPEC-009.yml` 무접촉. decisions/ 미접근. 커밋 없음.

## 아키텍트 세션 이어서 (23:16~23:25 KST) — SPEC-050 분해, TASK-142 생성

- 목표: 사용자 승인(DECISION-877D9096, 14:13:52Z)이 난 SPEC-050(작업 중 표시를 어느 화면에서든 볼 수 있게 한다)을 개발 작업으로 분해한다.
- 자격 실측: `wf-eligible.sh architect`가 `eligible`(exit 0). 결정 전수 대조로 최신 결정이 승인이면서 파생 작업 0건인 기획서가 SPEC-050 하나임을 확정했다. 선점 `lease-58325-20260806141639`.
- 계약 버전 재확인: architect 8, workflow 14로 지난 세션과 동일해 재독하지 않았다. `custom.md` 없음.

### 분해 결정과 근거

- 한 작업(TASK-142)으로 뒀다. R1~R6이 전부 `WorkspaceShell.tsx` 한 구성 요소와 그 스타일(`App.css`), 그 검사 파일에서만 이뤄진다. 나누면 세 파일을 전부 공유해 병렬이 불가능하면서 인수인계 비용만 늘어난다.
- 원 아이디어의 "새 백엔드는 필요하지 않아 보인다"를 기획서가 뒤집지 않았음을 본문에서 확인했다. R5가 새 조회·백엔드 명령 추가를 금지하고 완료 조건 8이 "새로 만든 조회 명령이나 폴링 주기가 없다"를 요구한다. 프런트엔드 전용 작업으로 분해했고, 작업 본문의 `손대지 않는 것`에 갱신 주기 변경 금지를 명시했다.
- 배치는 기획서가 정한 대로 `sidebar-footer`(App.css 114행, WorkspaceShell 279행)로 지정했다. 좌측 메뉴 `<aside>`가 화면 전환 분기 바깥(242행)에 있어 어떤 `view` 값에서도 그려지므로, 여기에 넣으면 R1의 "어느 화면에서도 보인다"가 배치만으로 충족된다. 이 구조 사실을 작업 본문에 실측값으로 실었다.
- 펄스 점 스타일 재사용 불가를 실측해 지침에 적었다. 기존 규칙이 `.agent-activity .pulse`(App.css 157행)로 오늘 화면 카드 안에 한정돼 있어 새 표시에는 적용되지 않는다.
- 접근성 이름 요구를 지침에 넣었다. 축약형이라 아이콘과 숫자만 남으면 읽어주는 이름이 비어버린다.

### 겹침 판정 (TL 지시로 실측)

- TL이 지목한 QA 반려 리스크는 소멸했다. TASK-133~138이 전부 `completed`로 확인돼 되돌아올 여지가 없다. `depends_on`을 걸 근거가 없어 선행 없이 뒀다.
- 진행 중인 세 작업과 무겹침을 확인했다. TASK-139(러스트 4파일)·TASK-140(러스트 2파일)·TASK-141(신규 docs+scripts) 어느 쪽도 `App.css`·`WorkspaceShell.tsx`·`WorkspaceShell.test.tsx`를 선언하지 않는다.
- 선점 시점 활성 lease: TASK-139(developer-claude), TASK-141(developer-fable). 지난 세션이 만든 두 작업을 개발 세션이 실제로 병렬 착수했다.

### 생성 문서

- `tasks/TASK-142.md` — scope_files 3건(`src/App.css`, `WorkspaceShell.test.tsx`, `WorkspaceShell.tsx`), 선행 없음, 완료 조건 9개·검증 절차 8단계.

### 후속 / 리스크

- 축약 표시가 좌측 메뉴 하단에 상시 들어가면서 세로 공간을 쓴다. 기획서가 정보량 축소로 이미 완화했지만 실제 여백은 구현에서 확인해야 한다.
- 완료 조건 8("새 조회·폴링 없음")은 코드를 보지 않으면 확인이 어렵다. 게이트웨이 호출 기록을 쓰는 기존 검사로 확인하도록 검증 절차에 넣었다.
- 만료 lease `SPEC-009.yml` 무접촉. decisions/ 미접근. 커밋 없음.

## 개발자 세션: TASK-141 규격 준수 비용 측정 방법

### 범위와 결정

- 측정 스크립트 `scripts/measure-compliance-cost.mjs`와 측정 문서 `docs/compliance-cost-measurement.md`
  두 파일만 새로 만들었다. 기존 파일은 작업 문서와 개발 로그 외에 고치지 않았다.
- 스크립트는 인자를 주지 않으면 작업 트리를, 커밋 식별자를 주면 그 커밋의 파일을 읽는다. 커밋 모드는
  `git ls-tree -r -l -z`로 크기를 한 번에 읽어 문서 수가 늘어도 호출 횟수가 늘지 않는다.
- 역할별로 착수 문서(등록 문서, 공통 규칙, 역할 계약 하나, 활성 워크플로의 워크플로 문서와 안내
  문서)와 대조 문서(역할별 문서군)를 나눠 집계한다. 나눠야 문서 수와 무관한 고정 비용과 문서 수에
  비례하는 비용을 구분해 읽을 수 있다.
- 출력은 마크다운 표와 JSON을 함께 낸다. JSON에 실행 시각을 넣지 않아 같은 입력이면 출력 전체가
  바이트 단위로 같다.
- 측정값은 후보 문서를 전부 끝까지 읽는다고 가정한 상한이다. 이 가정을 측정 문서의 결과 해석 절에
  명시했다. 변경 전후를 같은 가정으로 비교하는 것이 이 측정의 목적이다.
- 기존 검사 명령에 넣지 않았다. 통과와 실패를 가르는 검사가 아니라 관찰 도구다.

### 검증과 결과

- 인자 없는 실행, 커밋 지정 실행, 같은 입력 두 번 실행(출력 3304바이트 동일), 없는 커밋 식별자
  오류(종료 코드 1), `.workflow`가 없던 오래된 커밋의 오류를 모두 확인했다.
- 변경 전 기준값을 커밋 `c1449d83`에서 측정했다. 개발자 763847토큰, 아키텍트 796562토큰,
  기획자 609387토큰이다. 측정 문서에 적은 표가 같은 명령으로 그대로 재현되는지 확인했다.
- 시점별 추이를 함께 측정했다. 개발자 값이 2026-08-02 `c4ff7da`의 37671토큰에서 나흘 만에 약 20배로
  늘었다. SPEC-049가 인용한 세션당 3만에서 4만 토큰이라는 추정과 초기 시점 값의 크기가 같다.
- `npm run check`: 검사 파일 24개, 검사 761개, 타입 검사와 배포 빌드가 통과했다. 타입 검사 대상은
  `src`, 검사 대상은 `src` 아래 검사 파일이라 이번에 추가한 두 파일은 검사 입력에 들어가지 않는다.
- TASK-141을 사용자 QA 대기로 전환하고 확인 동선을 작업 문서에 적었다. 병행 세션의 미커밋 변경은
  손대지 않았고 커밋도 하지 않았다.

## 개발자 세션: TASK-139 자격 확인 도구가 대상과 제외 사유를 답한다

### 목표 / 요청 범위

SPEC-049 R1(TASK-139). 조건 스크립트와 앱 내부 판정이 역할별로 "일감이 있다·없다" 한 가지만 답하던
것을, 대상 문서 식별자와 후보 목록과 후보별 제외 사유까지 답하도록 넓힌다. 판정 결과와 화면에
전달되는 값의 모양은 바꾸지 않는다.

### 핵심 결정과 근거

- 넓어진 답은 표준 오류로 낸다. 표준 출력 첫 줄은 하트비트 데몬이 옮기는 사유 한 줄이고 SPEC-023이
  그 한 줄을 못박아 두었다. 표준 오류는 본문 주석이 이미 "사람이 읽는 자리"로 정해 둔 채널이라
  세션이 읽는 답이 가기에 맞고, 그 덕분에 기존 검사를 한 줄도 고치지 않고 완료 조건 3을 만족한다.
- 후보 목록은 판정이 실제로 본 후보까지다. 후보 전부를 미리 훑도록 바꾸면 기획자·아키텍트 분기가
  후보마다 선점 파일을 여는 자리에 닿아 후보 하나당 외부 프로세스 넷이 늘고, 그것이 SPEC-041이
  걷어낸 비용의 부활이다. 대상이 없을 때는 어차피 후보 전부를 보므로 사유가 가장 필요한 상황에서
  목록은 완전하다.
- 판정이 후보를 보는 차례를 파일 이름 오름차순으로 맞췄다. 대상이 불리언이던 동안에는 차례가 답을
  바꾸지 않았지만, 대상 하나를 고르는 순간 차례가 답이 된다. 앱의 목록 정렬(`updated_at` 내림차순)을
  그대로 쓰면 두 판정의 대상이 갈리므로, 판정은 파일 이름 순으로 보고 결정 문서 읽기에도 같은
  이유로 정렬을 더했다. 워크플로우를 보는 차례도 디렉터리 이름 순으로 맞췄다.
- 선행 관련 제외는 사유 하나로 묶었다. 선언을 읽을 수 없는 경우와 선행이 안 끝난 경우와 고리가 있는
  경우는 세션이 할 일이 같고, 코드를 늘리면 두 본문과 앱 이식본 셋이 함께 유지해야 할 분기가 는다.
- 넓어진 판정을 조회 결과에 직렬화되지 않는 필드로 실었다. 화면 payload와 프런트엔드 타입을 그대로
  두면서, 대조 검사가 화면이 쓰는 그 배선을 그대로 지나게 하기 위해서다.

### 변경한 파일·모듈

- `src-tauri/src/domain/project.rs`: 넓어진 판정 타입 셋과 제외 사유 코드 상수 추가.
- `src-tauri/src/infrastructure/role_eligibility.rs`: 세 역할 판정을 불리언에서 대상·후보 목록으로
  넓히고 워크플로우·후보 순회 차례를 스크립트에 맞췄다.
- `src-tauri/src/infrastructure/heartbeat_condition.rs`: 셸·PowerShell 두 본문에 대상 줄과 후보 줄
  출력을 넣고 조건 스크립트 버전을 11에서 12로 올렸다.
- `src-tauri/src/infrastructure/fs_project_repository.rs`: 호출부 조정, 결정 문서 읽기 정렬 추가,
  대조 검사 확장.
- `.workflow/도그푸딩--wf_ae6cd700/tasks/TASK-139.md`, `reports/REPORT-TASK-139-DEV.md`.

앱이 설치하는 사본인 `.workflow/rules/wf-eligible.sh`는 편집하지 않았다.

### 검증 단계와 결과

- `cargo test`: 582건 통과. 판정 비용 검사가 역할별 프로세스 예산을 그대로 두고 통과했으므로 외부
  프로세스는 늘지 않았다.
- 두 대조 헬퍼가 종료 코드에 더해 대상과 후보 목록까지 보도록 넓혔고, 그 헬퍼를 지나는 기존 시나리오
  예순 건이 수정 없이 통과했다. 역할별 다중 후보 시나리오 여섯 건과 조건 스크립트 시나리오 표 일곱
  행을 새로 세웠다.
- 임시 저장소에서 손으로도 확인했다. 후보 넷 중 셋이 선점·선행 미충족·겹침으로 빠지고 넷째가
  대상으로 나오며, 같은 실행의 표준 출력은 여전히 `eligible` 한 줄이다.
- `cargo fmt --check`와 `cargo clippy --all-targets` 경고 없음.
- `npm run check`: 타입 검사, 검사 761건, 배포 빌드 통과. 프런트엔드 파일 무변경.

### 후속 / 리스크

- PowerShell 본문은 이 기기에 PowerShell이 없어 실행하지 못했다. 두 시나리오 표가 통합 검사의 윈도우
  러너에서 같은 픽스처를 돌리므로 병합 전 그 러너 결과 확인이 이 위험을 닫는 자리다.
- 저장소에 설치된 조건 스크립트 사본은 아직 버전 11이다. 앱이 관리 자산을 동기화할 때 12로 바뀌며,
  그때까지 이 저장소에서 그 파일을 직접 실행하는 세션은 넓어진 답을 보지 못한다.
- TASK-140이 같은 저장소 파일을 수정하며 이 작업을 선행으로 선언해 두었다. 사용자 QA를 지나면 그
  선행이 충족된다.
- 대상과 제외 사유를 화면에 노출하는 일은 SPEC-049의 이번 범위 밖이다.

## 개발자 세션: TASK-142 좌측 메뉴 활성 세션 축약 표시

### 목표 / 요청 범위

SPEC-050의 R1부터 R6까지(TASK-142). 활성 세션 표시가 오늘 화면에만 있어서 다른 화면에 머무는 동안
세션 진행 여부를 알 수 없던 문제를 해결한다. 좌측 메뉴 하단에 세션 진행 여부와 활성 세션 수만 담은
축약 표시를 상시로 두고, 누르면 활동 화면으로 이동하게 한다.

### 핵심 결정과 근거

- 축약 표시를 `sidebar-footer` 안, 업데이트 조작 요소 위에 두었다. 좌측 메뉴는 화면 전환 분기 바깥에
  있어 이 자리에 넣는 것만으로 "어느 화면에서도 보인다"가 배치로 충족되고, 기획서가 지정한 연동·설정
  버튼 근처라는 조건도 만족한다.
- 노출 조건은 오늘 화면 카드와 같은 `project.activeLeases.length > 0`을 그대로 썼다. 조건이 거짓이면
  요소를 렌더링하지 않으므로 숨김 스타일이 아니라 문서에서 사라진다.
- 접근성 이름을 `aria-label`로 명시했다. 점과 숫자만으로는 읽어주는 이름이 불안정하고, 좌측 메뉴의
  아이콘 전용 버튼이 이미 같은 어법을 쓴다. 값은 `실행 중인 세션 N개, 활동 화면 열기`로 정해, 기존
  검사가 쓰는 정확 이름 `활동`이나 정규식 `/자세히 보기/`에 걸리지 않게 했다. 이름이 겹치면 사이드바
  버튼을 잘못 집어 기존 검사가 광범위하게 깨진다.
- 펄스 점 스타일은 재사용하지 않고 `.sidebar-activity-dot`로 새로 적었다. 기존 규칙
  `.agent-activity .pulse`는 오늘 화면 카드 안으로 한정돼 있어 좌측 메뉴까지 적용되지 않는다.
- 오늘 화면의 기존 상세 카드와 그 스타일은 고치지 않았다. 오늘 화면에서 두 표시가 함께 보이는 것이
  기획서가 정한 결과다.
- 새 조회 함수, 백엔드 명령, 폴링 주기를 만들지 않았다. 기존 `project.activeLeases`를 읽기만 한다.

### 변경한 파일·모듈

- `src/features/projects/components/WorkspaceShell.tsx`: 좌측 메뉴 하단에 축약 표시 버튼 추가.
- `src/App.css`: 축약 표시 전용 스타일 다섯 줄 추가.
- `src/features/projects/components/WorkspaceShell.test.tsx`: `WorkspaceShell 활성 세션 축약 표시`
  묶음 5건 추가. 공용 픽스처는 건드리지 않았다.
- `.workflow/도그푸딩--wf_ae6cd700/tasks/TASK-142.md`: 상태 전환, 이력, 요약 갱신, 확인 동선 신설.

작업 문서가 선언한 `scope_files` 세 개 밖의 소스 파일은 건드리지 않았다. 보고서 파일은 하네스가
서브에이전트의 작성을 차단해 전문을 TL에게 발신하는 것으로 대신했다.

### 검증 단계와 결과

- `npm run check` 통과. 타입 검사 오류 0건, 검사 파일 24개에 검사 766건 통과, 배포 빌드 성공.
- 대상 검사 파일 단독 실행 31건 통과. 변경 전 26건에서 새로 더한 5건이 늘어난 수다.
- 기존 검사 삭제·비활성화 0건. 세 파일 diff의 삭제 줄 둘은 모두 TASK-134·TASK-137에서 온 기존 미커밋
  변경이고 내 변경이 아니다.
- cargo는 실행하지 않았다. 러스트 무변경이고, 같은 시각 TASK-139 세션이 러스트 파일을 수정 중이라
  지금 돌리면 그 세션의 중간 상태를 재게 된다. 이 저장소의 `npm run check`에는 cargo 단계가 없다.
- 선점 시각 기준 `find -newermt` 실측으로 내가 바꾼 소스 파일이 세 개뿐임을 확인했다.

### 후속 / 리스크

- 데스크톱 앱을 띄워 눈으로 보지는 않았다. TASK-139 세션이 러스트 코드를 고치는 중이라 지금 빌드하면
  그 세션의 중간 상태를 컴파일하게 되고, 실패해도 이번 변경과 무관한 실패가 된다. 화면 확인은 사용자
  QA 단계가 정확한 자리다.
- 축약 표시는 활성 세션이 있을 때만 나오므로, QA 시점에 도는 세션이 없으면 이동 동작까지는 볼 수
  없다. 그때는 확인 동선 끝에 적어 둔 검사 명령으로 대신 확인한다.
- 활성 세션 수가 세 자리가 되면 좌측 메뉴 줄이 좁아진다. 문구 영역에 말줄임 처리를 넣어 두었다.

## 개발자 세션: TASK-140 역할 계약의 착수 경로와 보고서 규격

### 목표 / 요청 범위

- SPEC-049의 R2·R3·R4를 구현한다. 개발자 계약에 작업 문서 우선 착수 경로를, 아키텍트 계약에 완결된
  지시서 요구를, 개발자 계약에 보고서 필수 절과 분량 상한을 넣는다.
- 바뀐 계약의 버전 상수와 본문 표기를 올리고, 그 값을 문자열로 고정한 자동 검사를 함께 맞춘다.
- 선점 시각 14:38:31Z, lease `lease-83374-20260806143831`. 선행 TASK-139는 `qa_waiting`이라 조건을
  충족했고, 선점 시점 활성 lease는 만료된 `SPEC-009.yml` 하나뿐이라 겹침이 없었다.

### 핵심 결정과 근거

- 공통 규칙 본문은 바꾸지 않았다. 작업 문서가 개발자 판단으로 남긴 항목이다. 보고서 규격이 개발자
  계약 안에서 완결되므로 `WORKFLOW_RULES_VERSION`은 14 그대로 두었다.
- 보고서 본문 상한은 80줄로 정했다. 구성으로는 요약 10줄에 필수 네 절이 각각 15줄이면 70줄이고 절
  제목과 여유를 더해 80줄이다. 실측으로는 이 워크플로우의 최근 개발 보고서 열 편이 34줄에서 84줄
  사이이고 아홉 편이 80줄 안에 들어온다. 대부분을 자르지 않으면서 가장 긴 쪽에는 제약이 되는 값이다.
- 범위 밖 파일 `managed_project_assets.rs`를 함께 고쳤다. 작업 문서는 버전 문자열을 고정한 검사가
  `fs_project_repository.rs`에만 있다고 적었지만 실제로는 그 파일에도 있었고, 버전을 올리자 검사 네
  개가 실패했다. 검사를 지우는 것은 완료 조건 9번이 금지하고, `blocked`로 두는 것은 값만 맞추면 되는
  일에 뒤따르는 작업을 세우는 선택이라 함께 고치는 쪽을 택했다. 그 파일을 선언한 작업은 이미 완료된
  TASK-132뿐이고 덮는 lease도 없어 겹침 위험은 실측으로 없었다.
- 분량 상한 문장에서 공통 규칙의 표현을 그대로 쓰지 않았다. 세 역할 계약이 결정권자 요약의 세는 법을
  다시 적지 않는지 보는 기존 검사가 있었기 때문이다. 검사를 고치는 대신 문장을 다시 써서 세는 방식은
  공통 규칙 8절을 가리키게 두었다.
- 개발자 계약 `## Allowed`의 읽기 문장은 한 글자도 건드리지 않았다. 완료 조건 3번이 요구하는 자리다.

### 변경한 파일·모듈

- `src-tauri/src/infrastructure/project_instructions.rs`: 개발자 계약에 두 절 신설, 아키텍트 계약
  `## Completion`에 세 줄 추가, 두 버전 상수와 본문 표기 상향, 버전 고정 단언 18곳 갱신.
- `src-tauri/src/infrastructure/fs_project_repository.rs`: 작업 문서가 지목한 네 자리 갱신.
- `src-tauri/src/infrastructure/managed_project_assets.rs`: 버전 값을 고정한 검사 다섯 개 갱신.
- `.workflow/도그푸딩--wf_ae6cd700/tasks/TASK-140.md`: 상태 전환, 이력, 요약 갱신, 확인 동선 신설.

설치본 `.workflow/rules/` 아래 파일과 프런트엔드 파일은 손대지 않았다. 보고서 파일은 하네스가
서브에이전트의 작성을 차단해 전문을 TL에게 발신하는 것으로 대신했다.

### 검증 단계와 결과

- `cargo test --manifest-path src-tauri/Cargo.toml`: 582건 통과, 실패 0. 첫 실행은 577건 통과·5건
  실패였고 실패한 다섯은 모두 옛 버전 값을 고정한 검사였다. 총수가 582로 같아 더하거나 뺀 검사가 없다.
- `cargo fmt -- --check` 통과, `cargo clippy --all-targets -- -D warnings` 경고 0.
- `npm run check`: 검사 파일 24개·검사 766건 통과, 타입 검사와 배포 빌드 성공. 착수 전과 같다.
- 기존 검사 삭제·비활성화 0건. `#[ignore]`를 붙이거나 단언을 약하게 고친 자리가 없다.
- 선점 시각 기준 `find -newermt` 실측으로 이번 세션이 바꾼 파일이 소스 세 개와 작업 문서, lease뿐임을
  확인했다.

### 후속 / 리스크

- 저장소의 설치본은 아직 아키텍트 8, 개발자 9다. 앱이 관리 자산을 동기화할 때 9와 10으로 바뀐다.
- 아키텍트가 `scope_files`를 좁게 선언한 것이 범위 밖 수정의 원인이다. 버전 상수를 올리는 작업을
  앞으로 분해할 때 `managed_project_assets.rs`를 함께 선언해야 한다. 이 파일은 TASK-132가 만든 뒤 아직
  커밋되지 않아 눈에 덜 띄었을 수 있다.
- 80줄 상한이 실제로 적당한지는 다음 몇 세션의 보고서가 답한다. 기존 보고서는 줄이지 않았다.
- 새 계약이 세션의 행동을 실제로 바꾸는지는 다음 개발자 세션이 확인할 자리다. 이번 세션은 옛 계약을
  따라 일했다.

## TL 웨이브 마감 (23:50 KST) — SPEC-049·050 전체 착지, 역할 병렬 활성화

- TASK-139(하트비트)·140(tl-dev-140)·141(tl-dev-134)·142(tl-dev-142) 전부 qa_waiting 착지. 세 역할 no-target, 미만료 lease 0. 보고서 4건 대리 기록 완료.
- 역할 분리 사건: TASK-140을 분해자(tl-dev-136)에게 배정했다가 거절받았다. 근거는 §3(세션당 계약 하나)과 SPEC-049 완료 조건 7(작업 문서 완결성 측정은 분해자가 만들면 거짓 양성). 신규 세션(tl-dev-140)을 얇은 프롬프트로 스폰했고, 그 세션이 기획서·결정 문서를 열지 않고 착지해 분해 품질의 첫 실측치가 나왔다.
- TASK-140의 범위 밖 수정 1건(managed_project_assets.rs 검사 5건)을 TL이 추인. 원인은 분해 시점 미커밋 신규 파일 누락 — 후속 분해 시 scope_files 포함 필요.
- claude-heartbeat 데몬에 group 옵션 추가(직접 수정, 사용자 승인): 잡별 디스패치 그룹, slug 네임스페이스, 하위호환(미선언 시 기존 직렬). 디스패치 테스트 8/8. jobs.d의 workflow-labs 세 잡에 역할별 group 부여. 자식 세션 0 창에서 데몬 재시작(신 PID 82324) — 같은 프로젝트 역할 병렬 활성화. 데몬 저장소 커밋은 사용자 지시 대기.
- 팔로업 비용 절감용 판 상태 스냅샷 `docs/handoff.md` 신설(TL 세션 마감 시 덮어쓰기 운영).
- 사용자 관문: QA 4건(139~142) → 커밋 컷(SPEC-044 규범 첫 적용 — 원격 CI 그린 후 병합).

# 2026-08-12 개발 기록

## 에이전트 자동 배정·작업 대기열 UX 전면 개편

- 범위: 에이전트 관제 화면, 자동 배정 opt-in, 직접 배정 dialog, 고급 설정 drawer, 전체 자격 대기열, watcher 갱신, 연동 화면 폐기, 기존 역할 잡 이전, 런타임 0.9.0 bundle.
- 기본 화면: `작업/설정` 탭과 상시 실행 폼을 제거했다. 헤더에는 작은 실행 환경 상태·`직접 배정`·`고급 설정`만 두고, 본문은 `내 선택 대기`, `확인 필요`, `진행 중`, `배정 대기`, `최근 종료`를 상태가 있을 때만 보여 준다. 자동 배정 기본값은 꺼짐이다.
- 계약: 대상 없음은 실패 행을 만들지 않는 정상 `작업 대기 중`으로 처리한다. 프로젝트 payload는 역할별 전체 자격 후보와 제외 사유를 포함하고, 상세 기술 값과 제어는 dialog/drawer로 숨긴다.
- 갱신: 2.5초 전체 프로젝트 반복 조회를 제거하고 프로젝트 파일 watcher 알림과 활성 화면·활성 실행 중의 가벼운 상태 조회를 분리했다. watcher 저하 시 안전 확인 주기로 계속 동작한다.
- 설정: 역할·provider·모델·인원·안전 확인 주기는 고급 설정으로 이동했다. 기기·프로젝트 한도는 권장값과 다른 프로젝트 합·현재 사용량을 함께 보여 주며 사용자의 상향 선택을 막지 않는다.
- 연동: 사이드바 연동 메뉴와 Dream 관리 UI/API를 제거했다. 런타임 설치·복구와 기존 역할 잡 이전은 `고급 설정`으로 이동했다. 이전은 새 정책을 자동 배정 OFF로 저장한 뒤 앱 소유 역할 잡만 중지하고 서비스 전환을 검증하며, 실패 시 역할 잡을 복구한다.
- 실기 전환: workflow-labs의 기존 기획자·아키텍트·개발자 jobs.d 파일만 제거했다. mech-arena 잡은 유지했고 외부 `com.catze.dream-heartbeat.plist`는 SHA-1 `2c7b580d806341e840ea92cc8079d03bb4092741`로 불변이다. 자동 배정은 실제 DB에서도 꺼짐이다.
- 런타임: 앱 최소 버전을 0.9.0으로 올리고 검증된 macOS universal2 런타임을 bundle했다. stable launcher가 서비스 정의에 전달되며 앱 종료 뒤에도 dispatcher가 running 0.9.0으로 유지된다.
- 시각 QA: 최신 universal debug 앱을 1180px와 940px에서 확인했다. 헤더 개행·가로 스크롤 없이 관제 정보가 유지됐고, 940px 고급 설정은 본문 위 sheet로 렌더됐다. 앱은 940px 에이전트 화면에 열어 뒀다.
- 검증: 프런트 24 files, 446 passed·42 skipped, typecheck, production build. Rust 718 lib + 19 e2e, fmt, Clippy `-D warnings`. bundled runtime contract 0.9.0/API 1/macos-universal과 앱·runtime Mach-O 양 architecture 확인.
- 보호: 사용자와 워크플로 역할이 만든 `.workflow` 변경은 수정하지 않았다. 자동 배정을 켜거나 유료 provider 실행을 시작하지 않았다.
- 남은 위험: Linux·Windows 실제 앱 bundle/service smoke는 target CI 또는 해당 OS 기기에서 확인해야 한다.

## 핫픽스: provider 진단 PATH 보강 + 활동 뷰 안정 정렬

- 목표: 에이전트 뷰의 "claude: 실행 도구 확인이 필요합니다" 오탐 제거, 병렬 실행 시 진행 중 목록이 폴링마다 자리 바꾸는 문제 해소. 사용자 지시로 워크플로 밖 핫픽스.
- 원인 1: GUI 앱의 PATH에 /opt/homebrew/bin이 없어 앱이 띄운 heartbeat 진단이 claude를 못 찾음(executable_missing). 셸·데몬(launchd plist)에서는 정상.
- 원인 2: 런타임 state.read가 runs를 updated_at DESC로 반환하고 앱·프론트가 재정렬 없이 그대로 렌더링 → 병렬 실행이 갱신될 때마다 순서 뒤집힘.
- 변경:
  - src-tauri/src/infrastructure/agent_runtime_process.rs — LauncherCaller가 PATH 뒤에 /opt/homebrew/bin, /usr/local/bin 보강(unix 한정, 중복 제외).
  - src/features/projects/components/agents/AgentRunDashboard.tsx — 진행 중 목록을 startedAt 오름차순(미시작 뒤, 동률 runId)으로 안정 정렬.
  - src/features/projects/components/agents/AgentRuntimeView.test.tsx — 병렬 실행 순서 고정 회귀 테스트 추가.
- 검증: vitest AgentRuntimeView 15개 통과, cargo test agent_runtime_process 11개 통과, tsc -b 통과. 실기기에서 GUI PATH + 보강 경로로 diagnose 실행 → status ready(claude 2.1.228) 확인.
- 후속/리스크: 근본 원인(런타임 updated_at 정렬, plist 밖 설치 경로)은 claude-heartbeat 저장소 몫으로 남음. ~/.local/bin 등 다른 설치 경로는 이번 보강 범위 밖.
- 후속: 핫픽스는 dev에 22b1b49로 커밋·푸시(QA 배치 상태는 00b2e13). 브랜치 스택 정리로 로컬·원격 모두 main과 dev만 남김. 미병합 유실 없음(스쿼시 머지분 트리 대조로 확인).

## 배포 관점 수정: provider 실행 도구 발견 + 사유 코드 노출 ("기획자: diagnostic")

- 목표: mech-arena에서 자동 배정 실패 시 "기획자: diagnostic" 노출 건. 이 기기만이 아니라 배포된 모든 사용자 기기에서 사용자 조치 없이 동작해야 한다는 요구.
- 원인: codex가 ~/.local/bin에 있는데 launchd 데몬 PATH에 없어 executable_missing → 시작 실패. 런타임이 실패 사유에서 진단 상태를 버리고 단계 이름("diagnostic")만 저장, 앱은 모르는 코드를 원문 그대로 화면에 노출.
- claude-heartbeat (PR #24, fix/provider-tool-discovery, 0.9.2):
  - providers/process.py — 자식 PATH에 표준 사용자 설치 위치 보강(augment_path), 탐지·프로브·스폰이 같은 PATH 사용, Windows 대비 명령 절대경로 해석.
  - lifecycle.py — 진단 실패 reason을 provider_<상태>로 보존. 계약 문서 갱신.
  - launchd/systemd 서비스 PATH에 ~/.local/bin 추가.
  - 검증: 336 passed. 실기기 데몬 PATH 재현 → 수정 후 codex ready.
- workflow-labs dev 8bad6e3:
  - 앱 PATH 보강에 ~/.local/bin 추가, 사유 코드(diagnostic·executable_missing·unsupported_version·permission_denied) 한글 문장 번역, 배너도 상태별 문장 사용.
  - 검증: vitest 16 passed, tsc 통과, cargo 모듈 테스트 11 passed.
- 후속: 런타임 0.9.2 릴리스 빌드 후 앱 릴리스에 번들 갱신 필요(다음 릴리스 컷에서). CHANGELOG는 0.8.0에서 중단된 상태라 손대지 않음.
- 추가 조치: 설치 데몬이 아직 0.9.0이라 로컬 증상 지속 → 응급처치로 com.claude-heartbeat plist PATH 맨 앞에 ~/.local/bin 추가 후 데몬 재부트스트랩. 데몬 동일 PATH 재현 진단으로 codex ready 확인. 이 plist는 런타임 0.9.2 설치 시 근본 수정(서비스 템플릿에 이미 반영)으로 대체됨.

## 핫픽스: 레거시 source_idea 참조 하위호환 (아이디어 중복 배정)

- 증상: mech-arena에서 이미 기획된 아이디어들이 미처리로 회귀해 기획자 중복 배정, 보드에서 채택 아이디어가 미할당으로 표시.
- 원인: 옛 계약 기획서는 원천을 source_idea:로 기록. 현행 판정(조건 스크립트·러스트 포트·보드 유도)이 source_idea_id:만 봐서 레거시 참조가 비가시화.
- 경과: 오늘 재배정된 세션들이 중복 산출 없이 누락 필드를 자가보수(리포트 4건). 승인 결정 문서 무변경 확인. 로컬은 세 역할 모두 no-target으로 정상화.
- 수정(dev 859b7a0): 조건 스크립트 v17(sh·PowerShell 템플릿, 저장소 설치본 포함) + collect_nondraft_sources + spec_reference에 source_idea 하위호환. source_idea:는 source_idea_id: 줄과 부분 일치하지 않음을 주석으로 고정.
- 검증: cargo 전체 723 passed. 시나리오 테스트(옛 필드 참조가 아이디어를 닫는다), 아이디어 adopted 유도 테스트 추가. v17 스크립트를 mech-arena 실데이터에 돌려 no-target 확인.
- 선택 근거: 문서 일괄 마이그레이션 대신 판정 하위호환 — 기존 마이그레이션 장치는 매니페스트 스키마 단위라 문서 보수는 새 서브시스템이 필요했고, 하위호환은 문서를 건드리지 않고 모든 레거시 프로젝트를 즉시 무해화. 문서 정본 보수는 세션 자가치유가 지속 수행.

## 핫픽스: 막힘 회복 계약 강화 (미분류 blocked 교착)

- 배경: mech-arena TASK-013 — 완료 조건의 E2E가 에이전트 샌드박스(포트 바인딩 금지)에서 원천 불가한데, 회복 세션이 분류 없이 나가도 계약상 합법이라 개발자 재점검 무한 루프 + 아키텍트 인지 불가. 유저 무개입 원칙(SPEC-053~058 계열, SPEC-058 반려)이라 사용자 통로도 없어 교착.
- 수정(dev 36d67fe): workflow rules v22 + developer rules v16 — (1) blocked로 끝나는 회복은 검증된 사실에 근거한 blocked_kind 기록 의무화, (2) 에이전트 환경이 실행할 수 없는 완료 조건은 definition_error로 명시(아키텍트가 조건 재작성). 검증: cargo 723 passed. 설치본(workflow-labs·mech-arena)에 v22/v16, wf-eligible v17 동기화.
- 실측 진행: mech-arena에 개발자 회복 세션 CLI 배정(run c563da92, TASK-013). 분류→아키텍트 체인 관찰 중. 사용자 개입 0 원칙 준수.
- 참고: 언블록 조언 전 도그푸딩 스펙 전수 훑기 누락 재발(메모리 기록). 막힘 가시성 개선(제외 사유 표시 등)은 별도 아이디어 후보.
- 체인 완주: 개발자 회복 세션이 definition_error 분류 기록 → 판정이 아키텍트 대상(blocked_task)으로 라우팅 → 아키텍트 세션(succeeded)이 정의 오류 확인 후 작업서 교정(성능 개선 범위 부여, TASK-007~012 의존 선언, scope_files 추가) → todo 복귀, 이력 보존. 사용자 개입 0회. 남은 흐름은 선행 완료 후 개발자 자동 재개.

## 핫픽스: 사라진 모델 이름 (model_unavailable)

- 증상: workflow-labs 기획자(codex, model gpt-5.6)가 "선택한 모델을 현재 계정에서 사용할 수 없습니다"로 실행 실패. 코덱스 업데이트로 gpt-5.6이 sol·terra·luna로 갈라져 저장된 이름이 유령이 됨.
- 원흉: 설정 화면이 진단이 받아오는 계정 기준 실시간 모델 목록을 두고 하드코딩 목록을 보여줌 — 사용자가 고른 gpt-5.6 자체가 앱이 내민 낡은 선택지.
- 수정:
  - 런타임(PR #24, 02b0a0c): 저장된 모델이 목록에서 사라지면 실행을 막지 않고 CLI 기본 모델로 대체 실행, requestedModel·modelFallback 기록. 계획도 grant 유지.
  - 앱(dev): 모델 드롭다운을 실시간 카탈로그로 렌더(하드코딩은 카탈로그 없는 응답 대비), 사라진 저장값은 "계정 목록에 없음" 표시 + 기본 모델 실행 안내.
  - 이 기기: 기획자 모델 즉시 비움(config write)으로 바로 복구.
- 검증: 런타임 336 passed, 프론트 449 passed, tsc 통과.

## 핫픽스: 승인 분해 중 결정 중복 노출 (조건 스크립트 v18)

- 사용자 관찰: 아키텍트 작업이 중복 배정처럼 보임. 검수 결과 승인 분해 갈래만 결정 자체의 lease를 확인하지 않았음 — 분해 세션의 lease는 결정 id로 잡히는데 판정은 기획서 lease만 봐서, 세션이 도는 동안 같은 결정이 계속 대상으로 나감(대기열 중복 노출 + 자동 배정 헛 시도). 수정 요청·막힌 작업 갈래는 이미 양쪽을 확인.
- 수정(dev): 조건 스크립트 v18(sh·ps1)과 러스트 포트에 결정 lease 검사 추가(leased 제외). 시나리오·포트 회귀 테스트 추가. 설치본 두 프로젝트 동기화.
- 검증: cargo 724 passed.

## QA 가시성 설계 합의 + 구현 (커밋 대기)

- 문제: 에이전트 뷰 "내 선택 대기"는 qa_waiting 낱개(6건)를 세는데, 개발 뷰는 통째 QA 관문(같은 기획의 작업 전원이 completed/qa_waiting)에 잠긴 세션을 숨겨 두 화면이 어긋남 (mech-arena SPEC-006: TASK-013 진행 중이라 6건이 비가시).
- 합의(사용자와 의논): 세는 건 "지금 누를 수 있는 것만"(사용자 안), 개발 뷰에는 잠긴 묶음을 "준비 중" 카드로 예고(정보 최소화 — 제목 · QA n건 · 남은 작업 m개). 통째 QA 관문 자체는 유지.
- 구현: DevelopmentBoard에 qaGroups/qaBatchReady/actionableQaFileNames/buildUpcomingQa 추가, QaSessionHub에 upcoming 카드, AgentRunDashboard.userDecisions가 actionable만 집계.
- 보류: 도그푸딩 개발자 세션(실행 요약→보고서 열기, TASK-RES-...-04)이 AgentRunDashboard 등 같은 파일을 선점 중이라 테스트·커밋은 세션 종료 후. 파일 백업은 스크래치패드에.
- 참고: 메카 아레나 TASK-013 3차 막힘은 implementation_failure로 정상 분류(복기 비용 절반 절감, 단독 E2E 경계선 미달 — 부하 기기 측정 오염 의심, 세션이 재개 첫 단계로 재측정 명시). 사용자 결정으로 관망.
- 마감: 도그푸딩 세션 산출(실행 요약·보고서 열기, 작업 01~04) 커밋 + QA 가시성 수정(ae326f2) 커밋·푸시. 프론트 481, 러스트 732 전부 통과. 남은 것: 로그 스펙 작업 05(진단 번들) todo — 자동 배정 대기, 완료 시 QA 5건이 예고 카드→선택 대기로 전환됨.

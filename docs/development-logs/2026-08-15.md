
## 품질 확인·개발 보드 전면 개편 (대화 세션, 사샤)

- 목표: 코덱스가 만든 품질 확인 UX 재설계와 개발 보드 정리. 사용자와 스펙 싱크 후 진행.
- 핵심 결정과 근거:
  - 품질 확인: 항목 스테퍼·최종 검토 2단계 폐지 → 통합 QA 플로우 한 페이지 + 결정 한 번(승인/재작업+사유). TestRail식 목록·GitHub 리뷰식 제출 레퍼런스에서 출발했다가 사용자 결정으로 단일 플로우로 확정.
  - QA 작성 주체·시점: 아키텍트가 그룹 생성 시 기획서 기반 통합 플로우 작성. 태스크 미러링 금지. 작성 규격(순서 목록=단계, 인용문=기대 결과)과 전용 렌더링(번호 구슬 카드·기대 결과 하이라이트) 도입.
  - 개발 보드: 태스크 상태 4개(준비/진행 중/막힘/완료)로 통일. "AI 검증" 표현 앱 전체 소멸. 옛 qa_waiting·completed 상태도 완료로 읽어 팔로업 유지. QA 대기 그룹은 레인 헤더에서 품질 확인 바로 진입. 닫힌 그룹은 보드에서 빠지고 기록이 정본.
  - 작업 상세: 결정권자 요약 절만 렌더링("어떤 작업인가" 패널), 내부 명령·검증 절차는 계속 숨김. 상태 라벨 알약 정렬 수정.
  - 사이드메뉴: 오늘 ┃ 아이디어·기획서·개발·품질 확인 ┃ 기록·활동 세 묶음으로 구분선 정리.
  - 세션 중간 사용자 피드백으로 잡은 것: 목록이 자기 칸을 넘어 본문 겹침(긴 제목 min-content, 크로미움에서도 재현된 버그), 중앙정렬 최대폭→왼쪽 정렬+읽기 폭 상한 820px, 세로 화면 고정+내부 스크롤(결정 버튼 상시 노출), 항목 적을 때 목록 간격 벌어짐.
- 규칙 자산: workflow.md 24→25, architect.md 16→18 (통합 QA 플로우 + 작성 규격 두 차례 개정).
- 데모 프로젝트: /Users/catze/project/wf-demo (여행 가계부, 문서 50+개). 그룹 상태 9종·태스크 상태 전부·기획서 4상태 커버, 백엔드 inspect로 상태 파생 실측 확인. GROUP-I의 "구성 중"은 데모용 선점 파일.
- 변경 파일: qa/ 전면 재작성(QaFlowReview 신설, QaSessionView·QaReviewSubmit 삭제), DevelopmentBoard, WorkspaceShell, SpecWorkspace, BlockedTaskPanel, App.css, src-tauri project_instructions.rs, 관련 테스트 전부.
- 검증: 프론트 테스트 482개·타입체크·백엔드 규칙 테스트 통과. 주요 화면 웹킷(실제 엔진) 렌더 실측. 겹침·간격·본문 폭 회귀 테스트 추가.
- 후속/리스크: 문서 내부 상태값 verified 등 프로토콜 명칭은 유지(화면 언어만 통일) — 프로토콜 개명은 별도 마이그레이션 필요. 기존 다항목 QA 그룹은 플로우 페이지에 이어 붙여 표시됨.

### 추가: 기록 화면 타임라인 개편 + 사이드메뉴 그룹화

- 기록: 두 박스 병렬 나열(결정된 기획서/완료된 작업 그룹)을 하나의 시간순 타임라인으로 통합. 월 단위 머리글, 최신 먼저, 행 = 일자·종류 칩(기획 승인/기획 폐기/기능 완료)·제목·메타. 기획서 행은 기존처럼 클릭 시 열람. 상단에 조용한 집계 한 줄. 쌓일수록 달력처럼 읽히는 구조.
- 사이드메뉴: 오늘 ┃ 아이디어·기획서·개발·품질 확인 ┃ 기록·활동 세 묶음, 헤어라인 구분.
- 검증: 타입체크·전체 테스트 482개 통과, 기록 12건 규모 웹킷 렌더 확인.

### 추가: 기록 화면 2차 다듬기 (사용자 피드백 반영)

- 타임라인 행을 헤어라인식에서 앱 공통 카드 규격으로 통일 (대기열·보드 카드와 같은 표면).
- 뱃지 색 분리: 기획 승인=초록, 기획 폐기=적갈, 기능 완료=파랑.
- 오늘 화면 흐름 카드의 "완료"를 구현 보고서 수(태스크 기준)에서 완료된 기능 수(그룹 기준)로 교체.
- 완료된 기능 상세 뷰 신설: 기록에서 기능 완료 행 클릭 → 기능 설명·완료 방식·구성 버전·사용자 확인 플로우(전용 렌더링 재사용)·포함 작업 목록을 한 화면으로. 백엔드 변경 없음(데이터가 이미 요약에 포함).
- 검증: 타입체크·전체 테스트 482개(그룹 상세 열람·복귀 테스트 추가) 통과, 목록·상세 웹킷 렌더 확인.

### 추가: 결정권자 요약 규격 다이어트 + 활동 화면 폐기

- 요약 규격 7항목 → 3항목(제안 / 현재→변경 후 / 비용과 위험 선택). 사용자 결과는 변경 후에 흡수(이득까지 한 호흡), 결정 요청은 폐지(결정은 항상 도장 3개 — 열린 선택지는 승인 요청 전에 확정하라는 규칙 명문화), 영향 범위의 알맹이(유지·되돌리기)는 비용과 위험이 흡수. 공통 규칙 v26. 옛 7항목 문서는 그대로 유효, 보드는 공통 세 값만 읽고 나머지는 원문 보기 몫.
- 결정 보드는 세 덩어리 렌더(제안 헤드라인, 전후 한 쌍, 위험). 파서는 신·구 규격 둘 다 정확 일치로만 수용.
- 활동 메뉴 폐기: 최근 활동 피드는 이사 없이 폐기(문서 전이 정본은 개발 타임라인), 외부 세션 가시성은 에이전트 화면의 "앱 밖 세션" 절로 흡수 — lease 기준으로 앱이 안 돌린 세션만 보여주고 앱 실행이 잡은 대상은 진행 중 목록과 중복 제거. 오늘 배너·사이드바 축약 표시의 목적지는 에이전트로.
- 데모 프로젝트 문서 14건 신규격 반영.
- 검증: 프론트 457개·타입체크·백엔드 규칙 24개 통과, 결정 보드 신규격 웹킷 렌더 확인.

### 추가: 앱 밖 세션 워커카드 부활

- 에이전트 화면의 앱 밖 세션을 옛 활동 화면의 워커카드 형태로 복원·개선: 맥박 점(은은한 애니메이션, 감속 선호 시 정지) + 에이전트 이름 + 역할 칩 + 대상 문서(제목·ID) + 마지막 신호(30초마다 갱신되는 상대시간) + 선점 만료 잔여. 신호가 10분 넘게 끊기면 호박색 점과 "신호 지연" 표기.
- 활동 화면의 죽은 CSS(피드·마크 등) 정리. 검증: 타입체크·전체 테스트 457개 통과, 웹킷 렌더 확인.

### 추가: 앱 밖 세션 다수 대응 (2단 표시)

- 문제는 크게, 정상은 조용히: 신호 지연 세션만 워커카드로 승격, 정상 세션은 한 줄 행(점·이름·역할·대상·신호)으로. 정상 행 8개 초과 시 "외 n개 정상 동작 중 · 전체 보기"로 접힘. 역할 필터 대신 헤더 아래 역할 집계 한 줄. 신호·만료 표기에 일 단위 추가.
- 데모 프로젝트에 세션 13개(정상 12·신호 지연 1) 심어 다수 상태 재현. 검증: 전체 테스트 458개·타입체크 통과, 웹킷 렌더 확인.

### 추가: 앱 밖 세션 구분자 제거 (칩 기반 분리)

- "라벨 · 라벨" 문자 구분을 UI 요소로 교체: 역할 집계·행 역할은 칩, 문서 ID는 옅은 보조 텍스트, 신호 지연은 경고 칩. 행은 5열 그리드(점·이름·역할 칩·문서·신호)로 정렬. 접힘 버튼 문구도 구분자 없이 정리.
- 검증: 전체 테스트 458개·타입체크 통과, 웹킷 렌더 확인(구분 문자 부재를 테스트로 고정).

### 추가: 역할 칩 파트별 색상·정렬 고정

- 역할 칩에 파트별 색(기획자 보라·아키텍트 파랑·개발자 초록)을 집계 칩·행 칩·카드 칩·배정 대기까지 일관 적용. 모르는 역할은 기본 회색.
- 행 그리드의 이름·역할 칸을 고정 폭(150px·64px)으로 바꿔 역할 이름 길이가 달라도 문서 라벨 시작점이 정렬됨. 역할 미기재도 칩 자리를 채워 열이 흔들리지 않음.
- 검증: 전체 테스트 458개·타입체크 통과, 웹킷 렌더로 정렬·색 확인.

## v0.1.17 릴리스 컷 (10:33 KST)

- 목표: 8/14 main 직행 커밋 2건(9eaa2ad, dddb0c8)의 CI 확인 후 배포, dev를 main에 동기화.
- 발견: 두 커밋의 push 검사가 모두 실패 상태였다. 원인은 clippy 위반 2건 —
  `fs_project_repository.rs` 테스트의 `SpecDecisionOutcome`(Copy) `clone` 호출(clone_on_copy),
  `heartbeat_condition.rs` 문서 주석의 `+ 1.` 줄이 목록으로 파싱된 것(doc_lazy_continuation).
- 조치: 두 건 수정(3ce82a5) 후 버전 3종 파일을 0.1.17로 상향(74e4f92), main에 직접 푸시
  (2026-08-13 결정의 핫픽스 차선).
- 검증: 로컬 게이트 4종 통과 — fmt, clippy, cargo test(782+20, 0 실패), npm run check.
  원격 검사 네 갈래(Frontend, Rust ubuntu/windows/macos) 모두 통과 — CI 실행 31856223368,
  대상 커밋 74e4f92. 이 실행을 근거로 태그를 만들었다.
- 태그·병합 시각: v0.1.17 태그를 74e4f92에 생성·푸시(10:32 KST경). 직후 origin/main을 dev에
  병합(3079d69)해 dev behind main 0 확인. 릴리스 워크플로 31856804426 진행 중.
- 후속: 드래프트 릴리스 생성 확인 후 설치 검증과 게시는 사람이 수행(가이드 규정).

## 동의 계약 완성 + 런타임 자체 업데이트 (11:30 KST)

- 목표: v0.1.17 배포 후 발견된 "실행 권한 동의 상태를 확인하지 못했습니다"(agent consent가
  런타임에 없음) 해소 — 사용자 선택은 2번(런타임에 구현) + 하트비트 자체 업데이트 기능 추가.
- 원인 규명: consent 호출은 9eaa2ad(work-group v2)가 추가했으나, 게시된 어떤 런타임에도 해당
  명령이 없음. ~/Git/claude-heartbeat 사이드 체크아웃에 미커밋 구현(+800줄, 테스트 포함)이
  있었고 argparse 배선만 빠져 있었다 — 사용자가 본 usage 오류의 원형.
- 런타임(claude-heartbeat): 사이드 체크아웃의 consent diff를 0.9.4 위로 포팅(무관한 toolName
  훅 제외), cli.py argparse 배선 보강, argparse 경계 테스트 추가, 버전 0.9.5. pytest 359 통과.
  PR #28 — CI 10갈래 전부 통과. 병합·runtime-v0.9.5 태그는 권한 분류기에 막혀 사용자 결정 대기.
- 앱(dev 브랜치, f06d9b2·6a5f62f):
  - infrastructure/agent_runtime_release.rs 신설 — GitHub 릴리스 조회(ReleaseFetcher 갈래),
    zip 내려받기·한 겹 걷기·launcher 실행 비트 복원(릴리스 워크플로 staging과 같은 규약),
    이후는 기존 번들 설치 파이프라인(검증→설치→활성화→서비스 전환) 재사용.
  - 커맨드 3개(check/plan/apply download) + lib.rs 등록 핀. 고급 설정 실행 환경에 조회·받기 UI.
    지원 범위 밖 게시본은 받기 대신 앱 업데이트 안내.
  - release.yml 번들 핀 runtime-v0.9.5, 계약 게이트 REQUIRED_COMMANDS에 consent 3종 추가.
- 검증: cargo fmt/clippy/test(810개) 통과, vitest 462 통과, npm run check 통과, 계약 검증
  스크립트를 포팅된 런타임 소스에 실측 — "0.9.5 / API 1 / macos-universal" 통과.
- 후속: (1) PR #28 병합과 runtime-v0.9.5 태그(사용자), (2) 태그 후 릴리스 자산 게시 확인,
  (3) 앱 v0.1.18 컷 여부는 사용자 결정. macOS quarantine은 앱 프로세스가 직접 쓴 파일이라
  원리상 안 붙지만 첫 실기기 검증 때 확인할 것.

## 직접 작업과 자동 파이프라인 병행 방안 논의, 아이디어 등록 (대화 세션, 사샤)

- 목표: 사용자가 LLM에게 직접 시키는 작업(다이렉트 세션)이 자동 파이프라인과 안전하게 공존하는 방안 논의 후 아이디어 등록.
- 논의 경과와 핵심 결정:
  - 검토한 안 세 가지 — (a) 우선권 게이트: 다이렉트 선언 즉시 신규 개발 세션 차단, 진행 중 태스크 하나만 완주하는 꼬리 대기, (b) 파일 단위 점유 확장, (c) 에이전트 작업 사본 격리(worktree).
  - (b)는 기각 방향으로 정리: 양쪽 다 만질 파일을 사전 예측하기 어렵고, 파일이 안 겹쳐도 의미 충돌과 검증 오염(같은 트리의 상대 WIP가 검증 결과에 섞임)을 못 막는다. 단 계약에 이미 scope_files 겹침 검사가 태스크 간 방어로 존재함을 확인.
  - 사용자 결정: 게이트(a)를 미루고 격리(c)를 우선 아이디어로 등록. "원하는 방향성(오토와 다이렉트의 자연스러운 병행)을 뒤로 미루지 않는다"가 근거.
- 산출물: IDEA-1EAC5145 등록(도그푸딩 워크플로우). 제안 골자는 개발 세션의 태스크별 worktree 격리와 통합 단계 신설. 긴장 5개(verified 의미 분화, 병합 주체, scope_files와의 관계, 사본별 빌드 비용, 세션 기동 결합)와 범위 밖(우선권 게이트는 별도 아이디어)을 명기.
- 검증: workflow.md v26·도그푸딩 README·developer 계약 정독 후 계약 준수 형식(workflow-labs/idea@1, status: inbox)으로 작성. migration.lock 없음 확인.
- 후속 / 리스크: 우선권 게이트 아이디어는 보류 상태로 남음(사용자 재요청 시 등록). 기획자 세션이 이 아이디어를 선점해 기획서로 가공할 예정.

### 추가: runtime-v0.9.5 게시와 dev CI 그린 (12:00 KST경)

- PR #28은 사용자가 직접 병합(스쿼시 145bc96)하고 runtime-v0.9.5 태그를 푸시했다(세션 내 `!` 실행).
  릴리스 워크플로 31860481559 — 3-OS 빌드·publish 전부 성공, 게시본(draft 아님) 자산 3종 확인.
  `releases/latest`가 runtime-v0.9.5를 가리키는 것까지 실측.
- dev CI: 윈도우 갈래 실패는 테스트 픽스처가 launcher 이름을 `heartbeat`로 고정한 탓 —
  플랫폼별 이름으로 수정(dfa5d23) 후 네 갈래 전부 통과.
- 사용자 로컬 확인: 게시 전 조회에서 0.9.4가 설치됨(정상 동작). 0.9.5 게시 후 재조회→설치로
  동의 에러 해소 예정, 사용자 실측 대기.

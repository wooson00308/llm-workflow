# 2026-08-19

## 파이프라인 산출 로직 검수와 후속 아이디어 (대화 세션)

- 검수 범위: v0.1.22 이후 파이프라인 기능 12건(7851줄, 33파일). 다중 에이전트 리뷰가 API 스톨로 2회 실패해 파인더 10기 정리 후 인라인 정독으로 전환. 백엔드 로직(보류 기록·조건 게이트·헬퍼 v7·자모 매칭) 정독 + 프론트는 테스트 커버리지 확인.
- 판정: 병합 저지 결함 없음. 주의 1건 — S071 한도 대응이 독자 완비·작성자 부재(런타임 #37 미구현이면 휴면). #37에 계약 값(reason=usage_limit_reached 정확 일치, usageLimitResetsAt RFC3339) 고정 코멘트.
- 기계 검증: cargo test 911 통과, clippy·fmt·npm check 그린. 타우리 dev 앱 기동해 실사용 확인 병행.
- 후속 아이디어: IDEA-49AB3D06(보류 덮어쓰기 단축 방지 + 대응표 정렬 약속 정합), IDEA-487F3ACF(품질 확인 카드 설명 한두 문장 상한 — 소개 문단 전체도 여전히 김).

## 기획서 결정 보드 가시성 논의와 묶음 아이디어 (대화 세션)

- 사용자와 스크린샷(SPEC-072 결정 보드) 검토: "현재" 상자에 증거 서술(날짜·인용·경위) 예닐곱 줄, "비용과 위험" 전폭 장문 — 요약의 본문화. 뿌리는 구조화 요약이 줄 상한 예외 + "짧은 한 문단"에 숫자가 없는 규칙 공백.
- 방향 합의: 작성 규칙(항목별 문장 상한, 증거는 본문으로) + 표시 조판(글자·행간 상향, 비용과 위험 읽기 폭 상한, 결정 화면 접기 금지)을 "정보 계층" 원칙 하나로 묶음. 접어놓기 단독안은 결정 화면 위험 은닉 문제로 기각. QA 카드 한 줄 요약은 SPEC-075로 별도 진행 중이라 중복 제외 명시.
- 변경한 파일: ideas/IDEA-0470C714.md 등록.

## SPEC-076 패치 완료 확인과 분량 규칙 실전 시험 (대화 세션)

- SPEC-074·075·076 사용자 승인 확인, SPEC-076(요약 분량 상한+결정 보드 조판)은 작업 2개 구현·검증까지 완료.
- 시험 설계: 사연이 긴 실제 필요(IDEA-282CCF57 — 신호 지연을 실행 정보와 겹쳐 정상 장기 작업과 이상을 구분)를 등록해, 새 분량 규칙 아래에서 기획자가 요약을 두세 문장으로 유지하고 증거를 본문으로 내리는지 관찰한다.

## 요약 형식 v3 직접 구현과 곁 영역 리사이즈 아이디어 (대화 세션)

- 사용자 판정으로 결정 보드 재설계를 다이렉트 진행: 작성 규칙 v34(요약은 제안 1문장 + 바뀌는 것 불릿 1~3행 "지금→앞으로" + 비용과 위험 불릿 1~2행, 문단 금지, 구형식 소급 없음) + 파서·보드 불릿형 렌더(행 단위 지금→앞으로, 구형식은 기존 상자 유지). 게이트: 러스트 915·프론트 신규 테스트 포함 전부 그린. SPEC-077 요약을 현행 형식으로 이행해 실물 검증 준비.
- ideas/IDEA-1D88D8F8.md 등록 — 좌우 곁 영역 드래그 리사이즈 + 접기 토글 + 상태 유지 (레퍼런스: VS Code).

## 다듬기 플러그인 검증과 용어 전염 사고 (대화 세션)

- humanize-korean 플러그인 실전 검증(IDEA-1D88D8F8 대상): 정량 층은 유효(쉼표 z+5.84·병렬 틀 반복 검출, 재작성에 반영), 어휘 판정 층은 무효 — 발명어 "곁 영역"을 통과시킴(같은 모델 순환 한계). 결론: 구조 게이트로만 사용, 어휘 최종 감별은 사용자.
- 전염 실측: "곁 영역"이 SPEC-078에 제목 포함 23회 복제. 사용자에게 수정 요청 도장 + 용어 교정 코멘트 권고. 문서 어휘가 파이프라인을 타고 복제되는 구조 확인.
- 메모리 등록: 표준어 두고 신조어 발명 금지(korean-writing-no-coined-terms). §9 과적용 습관 교정.

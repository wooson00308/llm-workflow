# 2026-07-30

## 초기 설계와 구현 기반

- 범위: 로컬 우선 Markdown 워크플로우 앱의 제품 경계, 프로젝트 구조, 스키마·lease·마이그레이션 규약, 크로스 플랫폼 구현 기반.
- 결정: 앱은 외부 LLM을 실행하지 않고 파일 계약으로만 연동한다.
- 결정: 프로젝트 안의 `.workflow/<slug>--<id>/`에 의미 있는 문서를 저장하고 `.workflow/.runtime/`은 Git에서 제외한다.
- 결정: 앱 업데이트와 문서 마이그레이션을 분리하며 활성 lease가 있으면 마이그레이션을 금지한다.
- 결정: Tauri 2 + React/TypeScript + Rust, `main`/`dev`, `v*` 태그 릴리스 구조를 사용한다.
- 참고: Moonshine 저장소의 프런트/Rust 모듈 경계와 Tauri updater/CD 구성을 분석했다. AI 파이프라인 구조는 채택하지 않는다.
- 위험: Tauri 스캐폴드 생성 과정에서 초기 PNG 2개와 선행 문서가 삭제되었고 로컬 복사본을 찾지 못했다. 문서는 대화 내용을 바탕으로 재구성했으며 PNG 원본은 재첨부가 필요하다.
- 변경: 프로젝트 선택·초기화·워크플로우 생성·아이디어 저장 화면과 Tauri IPC를 연결했다.
- 변경: Rust 파일 저장소에 스키마 호환성 판정, 활성 lease 감지, 백업 기반 v0→v1 마이그레이션, 경로 이탈 방어를 구현했다.
- 변경: 명시적 앱 업데이트 확인·다운로드·재시작 UI와 `v*` 태그 기반 Windows/Linux/macOS GitHub Actions를 구성했다.
- 검증: Vitest 5개, Rust 단위 테스트 10개, TypeScript 빌드, `cargo fmt`, `cargo clippy -D warnings`, 릴리스 버전·설정 검증이 모두 통과했다.
- 검증: 실제 macOS 번들에서 임시 프로젝트 선택 → 한국어 워크플로우 생성 → 아이디어 Markdown 저장 → 대시보드 카운트 반영을 확인했다.
- 검증: `Workflow Labs.app`과 11MB `Workflow Labs_0.1.0_aarch64.dmg`를 생성하고 번들 식별자 `com.workflowlabs.desktop`, 버전 `0.1.0`을 확인했다.
- 후속: 실제 GitHub 저장소 연결 후 updater 서명 키와 OS별 코드 서명·notarization 자격 증명을 설정해야 배포할 수 있다.
- 후속: 초기 PNG 목업 원본을 재첨부받으면 현재 UI와 대조해 시각 설계를 보정한다.

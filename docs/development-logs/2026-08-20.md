# 2026-08-20

## 세션 제한 우회의 런타임 수정 다이렉트 구현 (대화 세션)

- 사용자 지시: 디스패처 멈추고 주도 진행, 기획서(SPEC-085)가 이미 나왔으면 구현을 대신하고 폐기는 사용자가 복귀 후 처리.
- 디스패처: 런타임 계약 명령(project.pause)으로 workflow-labs 프로젝트 배정 일시정지. 돌던 세션 4개는 자연 종료 확인(현재 0개). 일시정지는 장부에 저장되어 재기동에도 유지 — 재개는 project.resume.
- 구현(claude-heartbeat, 브랜치 fix/slot-accounting-live-processes, PR #40, Fixes #39): SPEC-085 R1~R8 충족.
  - 기기·프로젝트·역할 한도가 정리 미완 recovery_required 실행(점유)을 함께 집계.
  - tick마다 집계 전에 sweep_dropped_runs가 점유 실행의 남은 정리(이벤트 닫기·lease 반납)를 수행. 생존·판정불가 프로세스는 절대 건드리지 않음(즉살 오판 보호). 정리 완료 시 이벤트 기반 종료 상태로 전환해 슬롯 해제.
  - 계획 응답에 deviceOccupied·projectOccupied, 점유로 막힌 역할은 excluded=slots_held_by_unrecovered_runs, revision 지문에 점유 목록 포함. 계약 문서 갱신.
- 검증: pytest 372 passed 8 skipped(신규 9건). 실기기 장부 드라이런 — 점유 후보 114건 전원 소멸 판정(첫 주기 정리 대상), 남는 점유 0 = 실측 생존 세션 0 일치(검증 6).
- 남은 것: 복귀 후 PR #40 병합 → 런타임 릴리스 컷 → 프로젝트 배정 재개 → SPEC-085 폐기 도장. main 병합 전까지 배포 런타임 0.10.2는 기존 동작 유지(그래서 일시정지를 걸어둠).
